---
phase: 06-live-view-and-ui-polish
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/tests/use-boq-live.test.ts
  - src/tests/totals-panel-render.test.ts
  - src/tests/totals-row-cycle.test.ts
  - src/tests/totals-row-hover.test.ts
  - src/tests/totals-row-context-menu.test.ts
  - src/tests/totals-panel-category-collapse.test.ts
  - src/tests/totals-panel-empty-states.test.ts
  - src/tests/thumbnail-strip-click.test.ts
  - src/tests/thumbnail-lazy-mount.test.ts
  - src/tests/thumbnail-overlay-debounce.test.ts
  - src/tests/use-page-labels.test.ts
  - src/tests/use-ui-panels.test.ts
  - src/tests/pulse-highlight-animation.test.ts
  - src/tests/highlight-overlay-listening.test.ts
  - src/tests/canvas-header-bar.test.ts
autonomous: true
requirements:
  - VIEW-01
  - PDF-05

must_haves:
  truths:
    - "15 Wave 0 RED test files exist and each fails with a meaningful TODO/import-error (not a syntax error)"
    - "npx vitest run -- src/tests/use-boq-live.test.ts exits non-zero with a test failure (not a compile error)"
    - "Every file follows the src/tests/ vitest pattern (no describe/it.skip — uses it.todo or assert.fail)"
  artifacts:
    - path: "src/tests/use-boq-live.test.ts"
      provides: "RED stub for useBoqLive aggregator subscription"
    - path: "src/tests/totals-panel-render.test.ts"
      provides: "RED stub for TotalsPanel BoqStructure rendering"
    - path: "src/tests/pulse-highlight-animation.test.ts"
      provides: "RED stub for PulseHighlight rAF fade"
    - path: "src/tests/highlight-overlay-listening.test.ts"
      provides: "RED stub for Konva listening={false} regression guard"
    - path: "src/tests/canvas-header-bar.test.ts"
      provides: "RED stub for CanvasHeaderBar conditional Set Scale link"
  key_links:
    - from: "src/tests/use-boq-live.test.ts"
      to: "src/renderer/src/lib/boq-aggregator.ts"
      via: "import assert"
      pattern: "boq-aggregator"
---

<objective>
Scaffold all 15 Wave 0 RED test files for Phase 6. Every test file must import the future subject module (causing an import error or assertion failure) so the RED gate is genuine. No implementation is written in this plan.

Purpose: Establish the Nyquist feedback loop before any implementation begins. Per VALIDATION.md, all 15 test files are required before Wave 1 starts. These are the "Automated" verify commands used in every subsequent plan's acceptance criteria.

Output: 15 test files in src/tests/, each failing with import-resolution errors or explicit assert.fail() stubs. npx vitest run exits non-zero.
</objective>

<execution_context>
@C:/Users/franc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/franc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-live-view-and-ui-polish/06-VALIDATION.md
@.planning/phases/06-live-view-and-ui-polish/06-RESEARCH.md

<interfaces>
<!-- Existing test pattern: src/tests/markup-context-menu.test.ts -->
<!-- Existing test pattern: src/tests/boq-aggregator.test.ts -->
<!-- Existing test pattern: src/tests/scale-store.test.ts (vi.useFakeTimers pattern) -->
<!-- Existing test pattern: src/tests/status-bar-scale.test.ts (conditional render) -->
<!-- Existing test pattern: src/tests/page-nav.test.ts (setPage assertions) -->

From src/renderer/src/lib/boq-types.ts:
```typescript
interface BoqStructure {
  metadata: { projectName: string; planFilename: string; exportedDate: string; totalPages: number; totalMarkups: number }
  categories: Array<{ name: string; items: Array<{ label: string; quantity: number; uom: string; color: string | null; type: string }>; subtotals: Array<{ uom: string; total: number }> }>
  grandTotals: Array<{ uom: string; total: number }>
}
```

Future modules (DO NOT exist yet — import will fail RED):
- src/renderer/src/hooks/useBoqLive.ts
- src/renderer/src/components/TotalsPanel.tsx
- src/renderer/src/components/TotalsRow.tsx
- src/renderer/src/components/TotalsRowContextMenu.tsx
- src/renderer/src/components/TotalsCategoryBlock.tsx
- src/renderer/src/components/Thumbnail.tsx
- src/renderer/src/components/ThumbnailStrip.tsx
- src/renderer/src/components/PulseHighlight.tsx
- src/renderer/src/components/HoverRing.tsx
- src/renderer/src/components/CanvasHeaderBar.tsx
- src/renderer/src/hooks/usePageLabels.ts
- src/renderer/src/hooks/useUiPanels.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Wave 0 RED test stubs (batch 1 — totals panel suite, 8 files)</name>
  <files>
    src/tests/use-boq-live.test.ts
    src/tests/totals-panel-render.test.ts
    src/tests/totals-row-cycle.test.ts
    src/tests/totals-row-hover.test.ts
    src/tests/totals-row-context-menu.test.ts
    src/tests/totals-panel-category-collapse.test.ts
    src/tests/totals-panel-empty-states.test.ts
    src/tests/canvas-header-bar.test.ts
  </files>
  <read_first>
    - src/tests/markup-context-menu.test.ts (mount pattern, React.createElement, createRoot+act)
    - src/tests/boq-aggregator.test.ts (BoqStructure fixture builders)
    - src/tests/status-bar-scale.test.ts (conditional-render pattern)
    - .planning/phases/06-live-view-and-ui-polish/06-VALIDATION.md (per-test behavior map rows 1-7, 17-18)
  </read_first>
  <action>
Create 8 RED test stubs following the src/tests/ vitest pattern. Each file:
1. Imports the future module (will fail resolution — this is the RED state).
2. Has one or more `it.todo('behavior description')` entries that match VALIDATION.md rows exactly.
3. Uses `@vitest/globals` or `import { it, expect, describe } from 'vitest'` — mirror the existing test files' import style.

DO NOT write any actual assertions that could accidentally pass. The goal is RED via import failure.

File contents:

**src/tests/use-boq-live.test.ts**
```typescript
import { describe, it } from 'vitest'
// Wave 0 RED stub — useBoqLive does not exist yet
import { useBoqLive } from '../renderer/src/hooks/useBoqLive'

describe('useBoqLive', () => {
  it.todo('returns BoqStructure matching aggregateBoq() snapshot (VIEW-01)')
  it.todo('recomputes when pageMarkups changes')
  it.todo('returns empty categories when no markups exist')
})
```

**src/tests/totals-panel-render.test.ts**
```typescript
import { describe, it } from 'vitest'
// Wave 0 RED stub
import { TotalsPanel } from '../renderer/src/components/TotalsPanel'

describe('TotalsPanel render', () => {
  it.todo('renders category rows from BoqStructure with correct item count (VIEW-01)')
  it.todo('renders item color chip from item.color (D-06)')
  it.todo('renders quantity right-aligned with tabular-nums')
})
```

**src/tests/totals-row-cycle.test.ts**
```typescript
import { describe, it } from 'vitest'
// Wave 0 RED stub
import { TotalsRow } from '../renderer/src/components/TotalsRow'

describe('TotalsRow cycle navigation', () => {
  it.todo('first click calls setPage with first page containing matches (D-10)')
  it.todo('second click advances to N2 (D-10)')
  it.todo('subsequent clicks wrap around to N1 (D-10)')
  it.todo('cycle-index dot appears at leading edge when cycleIndex > 0')
})
```

**src/tests/totals-row-hover.test.ts**
```typescript
import { describe, it } from 'vitest'
// Wave 0 RED stub
import { TotalsRow } from '../renderer/src/components/TotalsRow'

describe('TotalsRow hover', () => {
  it.todo('mouseEnter triggers setHoverMatches with current-page matching markups only (D-11)')
  it.todo('mouseLeave triggers setHoverMatches with empty array')
  it.todo('hover does NOT navigate to a different page')
})
```

**src/tests/totals-row-context-menu.test.ts**
```typescript
import { describe, it } from 'vitest'
// Wave 0 RED stub
import { TotalsRowContextMenu } from '../renderer/src/components/TotalsRowContextMenu'

describe('TotalsRowContextMenu', () => {
  it.todo('right-click opens context menu at cursor position (D-14)')
  it.todo('"Copy as text" calls navigator.clipboard.writeText with tab-separated payload')
  it.todo('count row payload uses integer quantity')
  it.todo('length/area row payload uses 2dp quantity')
  it.todo('Escape key closes the menu')
})
```

**src/tests/totals-panel-category-collapse.test.ts**
```typescript
import { describe, it } from 'vitest'
// Wave 0 RED stub
import { TotalsCategoryBlock } from '../renderer/src/components/TotalsCategoryBlock'

describe('TotalsPanel category collapse', () => {
  it.todo('heading click toggles collapsed state (D-13)')
  it.todo('collapsed state persists to localStorage clmc.ui.collapsedCategories')
  it.todo('chevron glyph shows ▸ when collapsed, ▾ when expanded')
})
```

**src/tests/totals-panel-empty-states.test.ts**
```typescript
import { describe, it } from 'vitest'
// Wave 0 RED stub
import { TotalsPanel } from '../renderer/src/components/TotalsPanel'

describe('TotalsPanel empty states', () => {
  it.todo('shows "Open a PDF to begin." when totalPages === 0 (D-09)')
  it.todo('shows "Place markups to see totals." when totalPages > 0 and zero markups (D-09)')
  it.todo('shows "Place markups on a calibrated page to see length and area totals." when markups exist but only non-count on uncalibrated pages (D-09)')
})
```

**src/tests/canvas-header-bar.test.ts**
```typescript
import { describe, it } from 'vitest'
// Wave 0 RED stub
import { CanvasHeaderBar } from '../renderer/src/components/CanvasHeaderBar'

describe('CanvasHeaderBar', () => {
  it.todo('renders null when totalPages === 0 (D-20)')
  it.todo('shows current page label left-aligned')
  it.todo('shows "Not Set" in textSecondary when uncalibrated but no non-count markups')
  it.todo('shows "Page not calibrated." + Set Scale link when uncalibrated AND has non-count markups (D-20)')
  it.todo('"Set Scale" link click invokes getCalibrationControls().activate() — NOT duplicate code (D-20)')
  it.todo('shows 1:N ratio text when calibrated')
})
```
  </action>
  <verify>
    <automated>npx vitest run -- src/tests/use-boq-live.test.ts 2>&1 | grep -E "(FAIL|Cannot find module|Error)" | head -5</automated>
  </verify>
  <acceptance_criteria>
- All 8 files exist under src/tests/
- Running `npx vitest run -- src/tests/use-boq-live.test.ts` exits non-zero (import resolution fails = RED)
- Running `npx vitest run -- src/tests/totals-panel-render.test.ts` exits non-zero (RED)
- No syntax errors in any stub file (vitest can parse them even if modules don't exist)
- Each file contains it.todo() entries matching the VALIDATION.md behavior descriptions
  </acceptance_criteria>
  <done>8 RED test stubs committed. Each fails on missing module import. it.todo entries match VALIDATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Wave 0 RED test stubs (batch 2 — thumbnail + hooks, 7 files)</name>
  <files>
    src/tests/thumbnail-strip-click.test.ts
    src/tests/thumbnail-lazy-mount.test.ts
    src/tests/thumbnail-overlay-debounce.test.ts
    src/tests/use-page-labels.test.ts
    src/tests/use-ui-panels.test.ts
    src/tests/pulse-highlight-animation.test.ts
    src/tests/highlight-overlay-listening.test.ts
  </files>
  <read_first>
    - src/tests/page-nav.test.ts (setPage assertion pattern)
    - src/tests/scale-store.test.ts (vi.useFakeTimers pattern)
    - src/tests/pdf-loader.test.ts (PDFDocumentProxy mock pattern)
    - .planning/phases/06-live-view-and-ui-polish/06-VALIDATION.md (per-test behavior map rows 8-16)
  </read_first>
  <action>
Create 7 RED test stubs. Same rules as Task 1: imports cause RED via module-not-found, body uses it.todo().

**src/tests/thumbnail-strip-click.test.ts**
```typescript
import { describe, it } from 'vitest'
// Wave 0 RED stub
import { ThumbnailStrip } from '../renderer/src/components/ThumbnailStrip'

describe('ThumbnailStrip', () => {
  it.todo('renders one Thumbnail tile per page (PDF-05)')
  it.todo('clicking a tile calls viewerStore.setPage(N) (PDF-05)')
  it.todo('active page tile has 2px accent outline (D-16)')
  it.todo('idle page tile has 1px border outline (D-16)')
})
```

**src/tests/thumbnail-lazy-mount.test.ts**
```typescript
import { describe, it } from 'vitest'
// Wave 0 RED stub
import { Thumbnail } from '../renderer/src/components/Thumbnail'

describe('Thumbnail lazy mount', () => {
  it.todo('tile does NOT render canvas while NOT intersecting (D-17)')
  it.todo('IntersectionObserver intersection triggers rasterization (D-17)')
  it.todo('non-intersecting tile shows skeleton placeholder')
})
```

**src/tests/thumbnail-overlay-debounce.test.ts**
```typescript
import { describe, it } from 'vitest'
// Wave 0 RED stub — uses vi.useFakeTimers
import { useThumbnailRender } from '../renderer/src/hooks/useThumbnailRender'

describe('useThumbnailRender markup overlay debounce', () => {
  it.todo('markup commit schedules overlay refresh within 200ms±50ms (D-19)')
  it.todo('rapid commits collapse into a single refresh (debounce)')
  it.todo('page raster is NOT re-rendered on markup commit (overlay only)')
})
```

**src/tests/use-page-labels.test.ts**
```typescript
import { describe, it } from 'vitest'
// Wave 0 RED stub
import { usePageLabels } from '../renderer/src/hooks/usePageLabels'

describe('usePageLabels', () => {
  it.todo('returns resolved labels array when pdfDocument.getPageLabels() resolves (D-16)')
  it.todo('returns null when getPageLabels() resolves null — callers use Page N fallback')
  it.todo('resets to null when pdfDocument changes to null')
})
```

**src/tests/use-ui-panels.test.ts**
```typescript
import { describe, it } from 'vitest'
// Wave 0 RED stub
import { useUiPanels } from '../renderer/src/hooks/useUiPanels'

describe('useUiPanels', () => {
  it.todo('reads localStorage clmc.ui on mount; returns stored values (D-03)')
  it.todo('returns DEFAULTS when localStorage has no entry (D-03)')
  it.todo('returns DEFAULTS silently when localStorage entry fails JSON.parse (D-03)')
  it.todo('writes localStorage when setThumbnailsOpen is called')
  it.todo('writes localStorage when setTotalsWidth is called')
  it.todo('toggleCategoryCollapsed adds name when not present')
  it.todo('toggleCategoryCollapsed removes name when already present')
})
```

**src/tests/pulse-highlight-animation.test.ts**
```typescript
import { describe, it } from 'vitest'
// Wave 0 RED stub — will use vi.useFakeTimers + vi.spyOn(window, 'requestAnimationFrame')
import { PulseHighlight } from '../renderer/src/components/PulseHighlight'

describe('PulseHighlight animation', () => {
  it.todo('opacity starts at 0.85 and reaches ~0 after 1500ms (D-12)')
  it.todo('stroke decreases from 6/zoom to 2/zoom linearly over 1500ms (D-12)')
  it.todo('calls onComplete when fade reaches t=1')
  it.todo('cancelAnimationFrame is called on unmount (no state-update-on-unmounted-component) (D-12)')
})
```

**src/tests/highlight-overlay-listening.test.ts**
```typescript
import { describe, it } from 'vitest'
// Wave 0 RED stub — Konva node tree inspection
import { HoverRing } from '../renderer/src/components/HoverRing'
import { PulseHighlight } from '../renderer/src/components/PulseHighlight'

describe('Overlay listening={false} regression guard', () => {
  it.todo('every Konva shape inside HoverRing has listening={false} (D-11)')
  it.todo('every Konva shape inside PulseHighlight has listening={false} (D-12)')
})
```
  </action>
  <verify>
    <automated>npx vitest run -- src/tests/thumbnail-strip-click.test.ts src/tests/use-ui-panels.test.ts src/tests/pulse-highlight-animation.test.ts 2>&1 | grep -E "(FAIL|Cannot find module|Error)" | head -10</automated>
  </verify>
  <acceptance_criteria>
- All 7 files exist under src/tests/
- Each fails on import resolution (module not found) — confirmed by grep output showing "Cannot find module" or "FAIL"
- `npx vitest run` exits non-zero (full suite)
- No TypeScript syntax errors; vitest can parse every stub file
- it.todo entries match VALIDATION.md descriptions exactly
  </acceptance_criteria>
  <done>All 15 Wave 0 RED test stubs created. npx vitest run exits non-zero. VALIDATION.md nyquist_compliant can be set to true after this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test files → source modules | Test stubs import future modules that don't exist — import resolution errors are the intended RED state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-00-01 | Tampering | Test stub content | accept | Stubs are not production code; import-fail is the entire point of Wave 0 |
</threat_model>

<verification>
After both tasks complete:
- `ls src/tests/ | grep -E "(boq-live|totals-panel|totals-row|thumbnail|ui-panels|pulse|highlight|canvas-header)"` shows all 15 new files
- `npx vitest run` exits non-zero
- Zero syntax errors (vitest parses all stubs)
</verification>

<success_criteria>
All 15 Wave 0 test files exist. Every file fails with an import resolution error (module not found) or it.todo entries — never a silent pass. The Nyquist feedback loop is established for Waves 1-6.
</success_criteria>

<output>
After completion, create `.planning/phases/06-live-view-and-ui-polish/06-00-SUMMARY.md`
</output>
