---
phase: 15-boq-pricing-perimeter-simplification
plan: 04
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - src/main/boq-writers.ts
  - .planning/PROJECT.md
  - .planning/phases/15-boq-pricing-perimeter-simplification/15-MANUAL-NOTES.md
autonomous: true
requirements: [SC-1, SC-6]
must_haves:
  truths:
    - "Both writers emit a 5-column BOQ in the locked order: Item · Quantity · UoM · Rate · Cost"
    - "xlsx Rate/Cost cells are NATIVE numbers carrying a ₱ numFmt — SUM() still works"
    - "xlsx has a per-category cost-subtotal row and a grand-total-cost row; the category heading merge spans A:E"
    - "csv Rate/Cost are plain numeric values and the UTF-8 BOM still leads line 1 (₱-safe)"
    - "PROJECT.md no longer lists pricing under Out of Scope; pricing is recorded as delivered by Phase 15"
    - "Old .clmc back-compat, the hardcoded ₱ constant, and priced-BOQ UAT steps are documented in 15-MANUAL-NOTES.md"
  artifacts:
    - path: "src/main/boq-writers.ts"
      provides: "buildBoqXlsx + buildBoqCsv with Rate/Cost columns, ₱ numFmt, cost subtotals, A:E merge"
      contains: "Cost"
    - path: ".planning/phases/15-boq-pricing-perimeter-simplification/15-MANUAL-NOTES.md"
      provides: "Back-compat + currency-constant + priced-BOQ/xlsx UAT documentation"
      contains: "perimeter"
  key_links:
    - from: "src/main/boq-writers.ts (appendItemRow)"
      to: "BoqItemRow.rate / BoqItemRow.cost"
      via: "native-number cell + NUMFMT_PESO"
      pattern: "NUMFMT_PESO"
    - from: "src/main/boq-writers.ts (cost-subtotal / grand-total)"
      to: "BoqCategoryGroup.costSubtotal / BoqStructure.grandTotalCost"
      via: "appendCostSubtotalRow + grand-total-cost row"
      pattern: "costSubtotal|grandTotalCost"
---

<objective>
Wave 3 — turn the BOQ from quantity-only into a **priced** export at the writer boundary, and ship the v1 scope flip. Add `Rate` and `Cost` columns (locked order: Item · Quantity · UoM · Rate · Cost) to BOTH `buildBoqXlsx` and `buildBoqCsv`, with per-category cost subtotals and a grand-total-cost row. Then flip PROJECT.md (pricing moves Out of Scope → Validated) and write the phase manual notes.

Purpose: This is the last writer-side link in the GAP-002 lever — the BOQ that estimators export must carry money, not just quantities. The Wave-0 RED writer tests (15-01) already encode the exact contract; this plan turns them GREEN. The aggregator/types (Wave 1) and store/UI (Wave 2) have already populated `rate`/`cost`/`costSubtotal`/`grandTotalCost` upstream — this plan only renders them at the file-I/O boundary and records the scope reversal.

Output: A 5-column priced xlsx + csv (SUM-safe ₱ cells, cost subtotals, grand-total), a PROJECT.md with pricing reclassified as delivered, and a NEW `15-MANUAL-NOTES.md`. Turns GREEN: `src/tests/boq-writers-xlsx.test.ts`, `src/tests/boq-writers-csv.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-boq-pricing-perimeter-simplification/15-CONTEXT.md
@.planning/phases/15-boq-pricing-perimeter-simplification/15-VALIDATION.md
@.planning/phases/15-boq-pricing-perimeter-simplification/15-PATTERNS.md
@.planning/phases/15-boq-pricing-perimeter-simplification/15-RESEARCH.md

<decisions_locked>
- BOQ column order (CONTEXT line 44): **Item · Quantity · UoM · Rate · Cost** — exactly this order in both writers' title/header rows and every data row.
- xlsx Rate/Cost cells are NATIVE numbers with `numFmt` = `₱#,##0.00` (CONTEXT line 45; RESEARCH line 122). NEVER a pre-formatted `"₱123.45"` string — that breaks `SUM()` (writer comment lines 71-75, 163-169 precedent).
- csv Rate/Cost are plain numeric values (`Number(...toFixed(2))`), NO ₱ glyph in the cell — same discipline as the existing quantity column (CONTEXT line 46). The UTF-8 BOM already on line 1 (boq-writers.ts:247) MUST remain; it already makes ₱ safe (RESEARCH lines 102-109).
- Cost is unit-agnostic: a single ₱ number per category (`BoqCategoryGroup.costSubtotal`) and one project total (`BoqStructure.grandTotalCost`) — NOT bucketed per-UoM like quantity subtotals (CONTEXT line 32; RESEARCH line 96).
- Category-heading merge widens `A:C` → `A:E` (RESEARCH line 136). Every `addRow` that was 3 cells becomes 5 cells; metadata single-column rows are exempt.
- Pricing was "Out of Scope" in PROJECT.md v1 (line 37); GAP-002 reverses that. Move it out of Out-of-Scope and record it as delivered/validated by Phase 15.
</decisions_locked>

<currency_constant_import_path>
**Resolved import path for THIS plan's (main-process) scope:** `boq-writers.ts` runs in the **main** process. RESEARCH §"Shared currency constant" (lines 130-132) + Open Question 3 (line 242) + PATTERNS §"Currency constant" (lines 34-56) are explicit and locked: the writer **CANNOT import a renderer constant** across the process boundary — the codebase's deliberate convention is duplication-with-test-lock (same as the inline `BoqRowType`/`BoqItemRow` mirrors, and the locally re-declared `NUMFMT_DECIMAL` at boq-writers.ts:42). Wave 2's `CURRENCY_SYMBOL` lives renderer-side (likely `src/renderer/src/lib/currency.ts`) for the TotalsPanel; the writer keeps its OWN local copy.

→ **Action for this plan:** add a local `const NUMFMT_PESO = '₱#,##0.00'` (and, if referenced, `const CURRENCY_SYMBOL = '₱'`) directly in `boq-writers.ts` alongside the existing `NUMFMT_DECIMAL`. Do NOT add an import from renderer. Do NOT define a third copy — exactly one local writer copy, mirroring how `NUMFMT_INTEGER`/`NUMFMT_DECIMAL` are local. The ₱ glyph is safe inside the xlsx numFmt literal (UTF-8 XML — RESEARCH line 113).
</currency_constant_import_path>

<interfaces>
<!-- Shapes already created by Wave 1 (aggregator/types) — this plan only READS them in the writer. -->
<!-- BoqItemRow (boq-types.ts + the 4-way mirror incl. boq-writers.ts:19-25): -->
//   rate: number   // ₱ unit rate for this (name,type); 0 when unset
//   cost: number   // rate × quantity
<!-- BoqCategoryGroup gains:  costSubtotal: number   (Σ row costs in the category) -->
<!-- BoqStructure gains:      grandTotalCost: number  (Σ all category costSubtotals) -->
<!-- NOTE: boq-writers.ts inline-duplicates BoqItemRow/BoqCategoryGroup/BoqStructure (lines 19-36). -->
<!-- Wave 1 already added rate/cost/costSubtotal/grandTotalCost to those inline mirrors. If a field is -->
<!-- somehow absent from the writer-side mirror when you read the file, add it (4-way lock; PATTERNS line 28). -->

From src/main/boq-writers.ts (existing local format constants — line 41-48; ADD the peso sibling):
```
const NUMFMT_INTEGER = '0'
const NUMFMT_DECIMAL = '0.00'
const COL_WIDTH_ITEM = 36
const COL_WIDTH_QTY = 12
const COL_WIDTH_UOM = 8
```

From src/main/boq-writers.ts (existing native-number + numFmt discipline — appendItemRow, lines 164-169 — Rate/Cost mirror this):
```
const r = ws.addRow([safeText(item.label), item.quantity, item.uom])
const qtyCell = r.getCell(2)
qtyCell.value = item.quantity
qtyCell.numFmt = numFmtForUom(item.uom)
```
</interfaces>

<read_first>
- src/main/boq-writers.ts (whole file — already in context). The four xlsx touch-points: column defs (111-115), title row (126), `appendItemRow` (164-177), `appendSubtotalRow` (180-187) + Grand Total loop (140-147), `appendCategoryHeading` A:C merge (155-161). The CSV touch-points: header (213), item push (217-223), subtotal push (224-230), Grand Total push (233-239), BOM return (247).
- src/tests/boq-writers-xlsx.test.ts — the RED contract this plan turns GREEN (5-col title row, cost cell `.value` is a Number + `.numFmt` contains ₱, cost-subtotal + grand-total rows, A:E merge). Read to confirm the exact cell indices/assertions you must satisfy. Do NOT edit it.
- src/tests/boq-writers-csv.test.ts — the RED csv contract (header has Rate+Cost, rate/cost numeric not "₱.." strings, line 1 starts with the BOM `﻿`). Do NOT edit it.
- .planning/phases/15-boq-pricing-perimeter-simplification/15-PATTERNS.md §4 (lines 308-373) — the per-touch-point xlsx/csv analog map (appendItemRow → cells 4/5, appendSubtotalRow → cost row, A:C→A:E).
- .planning/phases/15-boq-pricing-perimeter-simplification/15-RESEARCH.md §"ExcelJS ₱ Formatting" (lines 100-137) — the locked `₱#,##0.00` literal + native-number-cell discipline + column-order/merge checklist.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Rate + Cost columns, ₱ numFmt, cost subtotals + grand-total, A:E merge to both writers (proof a / SC-1)</name>
  <files>src/main/boq-writers.ts</files>
  <read_first>
    - src/main/boq-writers.ts (the six xlsx + four csv touch-points listed in `<read_first>` above; the local format-constant block at 41-48 is the analog for NUMFMT_PESO; appendItemRow:164-177 is the analog for native-number Rate/Cost cells; appendSubtotalRow:180-187 + GrandTotal:140-147 are the analog for the cost rows)
    - src/tests/boq-writers-xlsx.test.ts + src/tests/boq-writers-csv.test.ts (the exact RED assertions — cell indices, the ₱-in-numFmt check, the BOM check — to satisfy precisely)
  </read_first>
  <action>
    Add a local format constant in boq-writers.ts next to NUMFMT_DECIMAL (line 42): `const NUMFMT_PESO = '₱#,##0.00'`. Per the locked currency_constant_import_path: this is the writer's OWN copy — do NOT import from renderer; do NOT create a renderer file here. Also add width constants `COL_WIDTH_RATE` and `COL_WIDTH_COST` (~14 each, money columns) next to COL_WIDTH_UOM (line 45). If `CURRENCY_SYMBOL` is referenced anywhere you touch, declare it locally too (`const CURRENCY_SYMBOL = '₱'`), but the numFmt literal already carries ₱ so it is not otherwise required.

    First confirm the writer-side inline mirrors of `BoqItemRow`, `BoqCategoryGroup`, `BoqStructure` (lines 19-36) already carry `rate`/`cost` (on BoqItemRow), `costSubtotal` (on BoqCategoryGroup), and `grandTotalCost` (on BoqStructure) from Wave 1. If any field is missing from the writer-side mirror, add it to keep the 4-way type lock (PATTERNS line 28) — match boq-types.ts field-for-field (the preload mirrors omit `categoryId`; do not change that).

    XLSX (buildBoqXlsx) — apply ALL of these together (RESEARCH line 136: move atomically or columns misalign):
    (1) Column defs (lines 111-115): append `{ key: 'rate', width: COL_WIDTH_RATE }` and `{ key: 'cost', width: COL_WIDTH_COST }` after the `uom` column.
    (2) Title row (line 126): change `['Item', 'Quantity', 'UoM']` → `['Item', 'Quantity', 'UoM', 'Rate', 'Cost']` (locked order).
    (3) `appendItemRow` (lines 164-177): push `item.rate` and `item.cost` into the `addRow([...])` array as cells 4 and 5 (so the array is label, quantity, uom, rate, cost). Then set `r.getCell(4).value = item.rate; r.getCell(4).numFmt = NUMFMT_PESO` and `r.getCell(5).value = item.cost; r.getCell(5).numFmt = NUMFMT_PESO` — NATIVE numbers, mirroring the qtyCell pattern at 167-169. NEVER pass rate/cost through `safeText` (numbers only; writer comment 71-75). Keep the existing item-cell color fill on cell 1 unchanged.
    (4) `appendCategoryHeading` (lines 155-161): widen the merge from `A${r.number}:C${r.number}` to `A${r.number}:E${r.number}`, and pad the heading `addRow` to 5 cells (`[safeText(name), null, null, null, null]`).
    (5) Per-category cost subtotal: after the existing per-UoM quantity-subtotal loop inside the `for (const cat of b.categories)` block (line 136), emit ONE cost-subtotal row for the category. Add a small `appendCostSubtotalRow(ws, costSubtotal: number)` helper mirroring `appendSubtotalRow` (180-187): `ws.addRow(['Subtotal (cost)', null, null, null, costSubtotal])`, `r.font = { bold: true }`, set `r.getCell(5).numFmt = NUMFMT_PESO`, and `r.eachCell((c) => { c.fill = ...SUBTOTAL_FILL_ARGB... })`. Call it with `cat.costSubtotal`.
    (6) Grand-total cost: after the existing per-UoM grand-totals loop (lines 140-147), emit ONE grand-total-cost row: `ws.addRow(['Grand Total (cost)', null, null, null, b.grandTotalCost])`, bold, `getCell(5).numFmt = NUMFMT_PESO`, fill every cell with `GRAND_TOTAL_FILL_ARGB`. Mirror the existing grand-total row shape (141-146) but for the single cost number in column E.

    CSV (buildBoqCsv) — mirror the xlsx column changes as raw numerics (CONTEXT line 46: numeric, no ₱ glyph):
    (7) Header (line 213): `['Item', 'Quantity', 'UoM']` → `['Item', 'Quantity', 'UoM', 'Rate', 'Cost']`.
    (8) Item push (lines 217-223): append two cells — `Number(item.rate.toFixed(2))` and `Number(item.cost.toFixed(2))` — as cols 4/5 (plain numbers, same discipline as the quantity column; do NOT prefix ₱).
    (9) Category heading push (line 216): pad to 5 cells (`[safeText(cat.name), '', '', '', '']`).
    (10) Quantity-subtotal push (lines 224-230): pad to 5 cells (existing Subtotal/total/uom in cols 1-3, `''` in col 4) and add a separate cost-subtotal row per category: `['Subtotal (cost)', '', '', '', Number(cat.costSubtotal.toFixed(2))]`.
    (11) Grand-total push (lines 233-239): pad to 5 cells and add a grand-total-cost row: `['Grand Total (cost)', '', '', '', Number(b.grandTotalCost.toFixed(2))]`.
    (12) BOM (line 247): leave the `return '﻿' + stringify(...)` EXACTLY as-is — the UTF-8 BOM on line 1 is what keeps ₱ (and `m²`) safe; do NOT remove or move it.
  </action>
  <verify>
    <automated>npx vitest run src/tests/boq-writers-xlsx.test.ts src/tests/boq-writers-csv.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 15-VALIDATION.md proof (a) writer columns (SC-1). Both RED writer test files are now GREEN. Specifically:
    - xlsx: the title row is `['Item','Quantity','UoM','Rate','Cost']`; an item row's Cost cell (`getCell(5)`) `.value` is a NUMBER (typeof === 'number', not a string) AND its `.numFmt` contains the ₱ glyph; the Rate cell (`getCell(4)`) is likewise a native number with the ₱ numFmt; a per-category cost-subtotal row and a grand-total-cost row exist; the category-heading merge spans `A:E` (not `A:C`).
    - csv: the header row contains `'Rate'` and `'Cost'`; rate/cost are emitted as numeric values (Number, not a `"₱…"`-prefixed string); the output string's line 1 starts with the UTF-8 BOM `﻿` (U+FEFF).
    - `npx tsc --noEmit` (or `npm run typecheck`) is clean — the writer-side type mirrors carry rate/cost/costSubtotal/grandTotalCost.
  </acceptance_criteria>
  <done>Both writers emit the locked 5-column priced layout; xlsx Rate/Cost are native numbers with the ₱ numFmt and SUM-safe; cost subtotals + grand-total render; A:E merge applied; csv Rate/Cost numeric with the BOM intact; both writer tests GREEN; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Flip PROJECT.md pricing scope (Out of Scope → Validated) + log the v1 reversal (SC-1)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (the Out-of-Scope list line 37 "Unit cost / pricing calculations — BOQ quantity export only; pricing happens in separate tools"; the Validated block lines 13-16; the Key Decisions table lines 57-64)
  </read_first>
  <action>
    Make three edits to PROJECT.md:
    (1) DELETE the Out-of-Scope bullet at line 37 ("Unit cost / pricing calculations — BOQ quantity export only; pricing happens in separate tools"). The string `pricing happens in separate tools` must no longer appear anywhere in the file.
    (2) Add a Validated entry under `### Validated` (lines 13-16): `- [x] Unit cost / pricing — per-(name,type) rates with live cost, category cost subtotals, grand-total cost, and ₱ Rate/Cost columns in xlsx/csv export — Validated in Phase 15: BOQ Pricing & Perimeter Simplification`.
    (3) Add a row to the Key Decisions table (lines 59-64): Decision = "Pricing moved into scope (reverses v1 'Out of Scope')"; Rationale = "GAP-002: pricing is the measuring→estimating lever — quantities alone don't produce a bid"; Outcome = "Delivered in Phase 15 (₱ rates + cost columns; Item Library + currency picker deferred)".
    Do NOT touch any other Out-of-Scope bullet (multi-user, cloud sync, preset item library, custom template stay). Do NOT modify the Active list. Keep ₱ as a hardcoded constant — do NOT claim a currency picker shipped (it is deferred per CONTEXT line 14).
  </action>
  <verify>
    <automated>git grep -c "pricing happens in separate tools" -- .planning/PROJECT.md; git grep -c "Validated in Phase 15" -- .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    `git grep -n "pricing happens in separate tools" -- .planning/PROJECT.md` returns ZERO matches (the first `git grep -c` prints `0` / exits non-zero with no count). A Validated pricing entry referencing Phase 15 AND a Key-Decision row noting the "out of scope" reversal both exist (`git grep -n "Validated in Phase 15" -- .planning/PROJECT.md` and a Key Decisions row containing "Pricing moved into scope" both return a match). No other Out-of-Scope bullet was removed.
  </acceptance_criteria>
  <done>Pricing no longer appears under Out of Scope; a Validated/Phase-15 pricing entry and a Key-Decision reversal row exist; all other scope bullets and the Active list are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Write 15-MANUAL-NOTES.md (back-compat + hardcoded ₱ + priced-BOQ/xlsx UAT) (SC-6)</name>
  <files>.planning/phases/15-boq-pricing-perimeter-simplification/15-MANUAL-NOTES.md</files>
  <read_first>
    - .planning/phases/15-boq-pricing-perimeter-simplification/15-CONTEXT.md (Migration/back-compat lines 62-63; Currency lines 34-36; Deferred lines 109-114)
    - .planning/phases/15-boq-pricing-perimeter-simplification/15-RESEARCH.md (Risks & Back-Compat table lines 183-194 — the "old .clmc loses perimeter-area, NONE/intended" + "no rates key, NONE" rows; the how-to-manual standing requirement is in MEMORY)
  </read_first>
  <action>
    Create `.planning/phases/15-boq-pricing-perimeter-simplification/15-MANUAL-NOTES.md` as a manual-ready reference (the project has a standing how-to-manual requirement — write it document-quality, not as scratch notes). Include exactly these three sections plus a short heading:

    Section 1 — Old `.clmc` perimeter back-compat (no data loss). State plainly: existing projects saved before Phase 15 that contain perimeter markups will, on reload, no longer show the perimeter **area** BOQ row — only the perimeter **length** row remains. This is intended and there is NO data loss: the perimeter area was always computed **live at render time from the polygon geometry, never stored** in the `.clmc` file (it only ever existed as a runtime aggregator row). Perimeter geometry (`points`, `arcs`) is untouched; the length stays arc-aware. No `formatVersion` bump and no migration code is needed (rates is an additive field defaulting to `{}`; perimeter geometry is unchanged — only its BOQ synthesis changed). Old files without a `rates` key load clean with every cost = ₱0.

    Section 2 — ₱ currency is a hardcoded constant (Settings picker deferred). State: the currency symbol is `₱` (Philippine Peso), a hardcoded constant this phase — there is no Settings currency picker yet (deferred). Note the single seam for a future picker: the renderer-side currency constant (Wave 2, e.g. `src/renderer/src/lib/currency.ts`) plus the writer's own local `NUMFMT_PESO = '₱#,##0.00'` in `src/main/boq-writers.ts` (the main process intentionally keeps its own copy — it cannot import renderer constants across the process boundary). A future picker changes both seams. Also note the related deferral: the persistent cross-project Item Library (default rates remembered across projects) is a later phase — this phase prices per project only.

    Section 3 — Manual UAT steps for the priced BOQ + ₱ xlsx rendering. Write a numbered checklist an estimator can follow: (a) open the app, load a PDF, calibrate scale on a page; (b) place a few count/linear markups, name them, set a category; (c) on a totals row, type a rate into the inline ₱ field and confirm the row Cost updates live and the category cost subtotal + grand-total cost update (and that clicking/typing in the rate field does NOT cycle the page or re-arm the draw tool); (d) export to **xlsx**, open it in Excel, and confirm: the columns read Item · Quantity · UoM · Rate · Cost; Rate/Cost render with the ₱ symbol and 2 decimals; a `SUM()` over the Cost column produces the right total (proves cells are native numbers, not text); a per-category cost subtotal row and a grand-total cost row are present; (e) export to **csv**, open in Excel, and confirm Rate/Cost are plain numbers and the ₱ in any text is not mojibaked (the BOM keeps it clean). Cross-reference 15-VALIDATION.md "Manual-Only Verifications" (the inline-field feel, the perimeter unfilled-outline render, and the Excel ₱ render rows) so the UAT lines up with the validation contract.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/15-boq-pricing-perimeter-simplification/15-MANUAL-NOTES.md';const t=fs.readFileSync(p,'utf8');const ok=/perimeter/i.test(t)&&t.includes('₱')&&/xlsx|Excel/i.test(t)&&/Rate/.test(t)&&/Cost/.test(t);if(!ok){console.error('manual notes missing required content');process.exit(1)}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    `15-MANUAL-NOTES.md` exists and contains all three sections: (1) perimeter back-compat explicitly stating area was computed live / never stored → no data loss; (2) ₱ as a hardcoded constant with the Settings-picker deferral noted; (3) a numbered priced-BOQ + ₱ xlsx UAT checklist mentioning Rate, Cost, ₱, and Excel/SUM. The automated guard passes (file mentions perimeter, the ₱ glyph, xlsx/Excel, Rate, and Cost).
  </acceptance_criteria>
  <done>A manual-ready notes file documents the old-project back-compat (no data loss), the hardcoded ₱ constant + deferred picker, and a numbered priced-BOQ/xlsx-in-Excel UAT checklist; the content guard passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only adds two numeric columns to existing export writers and edits planning docs. No new trust boundary is added or crossed; the writers already run in the trusted main process on a `BoqStructure` produced in-process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Tampering | ₱ Rate/Cost written into a spreadsheet cell | accept | ₱ is written as cell **DATA** (a literal in a `numFmt` for xlsx; a plain Number for csv), never as a leading `=`/`+`/`-`/`@` formula. The existing `safeText` formula-injection guard (boq-writers.ts:81-87) is applied ONLY to label strings; Rate/Cost are numbers and bypass it correctly (numbers can't trigger formula execution). No CSV-injection / formula-evaluation risk is introduced. |
| T-15-04-02 | Tampering | xlsx Cost cell stored as a pre-formatted string | mitigate | Cost/Rate cells are kept NATIVE numbers with a display `numFmt` (`getCell(n).value = number; getCell(n).numFmt = NUMFMT_PESO`), so `SUM()` stays correct and the value can't be silently corrupted into text — enforced by the Wave-0 xlsx test asserting `.value` is a Number. |
| T-15-04-03 | Information disclosure | csv ₱ mojibake leaking a wrong currency/value | accept | The UTF-8 BOM already on line 1 (boq-writers.ts:247) is preserved unchanged; ₱ (U+20B1) stays correctly encoded for Excel-on-Windows. No change to the BOM, no new disclosure surface. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — uses the existing `exceljs`/`csv-stringify` already in the repo. No legitimacy gate needed. |

**Block-on-high:** none. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages and no high-severity unmitigated threat — no blocking human checkpoint required.
</threat_model>

<verification>
- `npx vitest run src/tests/boq-writers-xlsx.test.ts src/tests/boq-writers-csv.test.ts` — both GREEN (the Wave-0 RED writer contracts are satisfied: 5-col order, ₱ numFmt on native-number cells, cost subtotal + grand-total rows, A:E merge; csv Rate/Cost numeric + BOM on line 1).
- `npx vitest run` — full suite green; no regression elsewhere (esp. the existing quantity-subtotal and grand-total xlsx/csv assertions, which now must coexist with the new cost rows).
- `npx tsc --noEmit` (or `npm run typecheck`) clean — the writer-side `BoqItemRow`/`BoqCategoryGroup`/`BoqStructure` mirrors carry `rate`/`cost`/`costSubtotal`/`grandTotalCost`.
- `git grep -n "pricing happens in separate tools" -- .planning/PROJECT.md` returns ZERO matches; a Validated/Phase-15 pricing entry + a Key-Decision reversal row exist.
- `15-MANUAL-NOTES.md` exists with the three required sections (back-compat / hardcoded ₱ / priced-BOQ UAT) and passes the content guard.
</verification>

<success_criteria>
- Both writers emit the locked 5-column priced BOQ (Item · Quantity · UoM · Rate · Cost) with per-category cost subtotals and a grand-total-cost row.
- xlsx Rate/Cost are native numbers carrying the ₱ `#,##0.00` numFmt (SUM-safe); the category heading merges A:E.
- csv Rate/Cost are plain numeric values and the UTF-8 BOM still leads line 1.
- `boq-writers-xlsx.test.ts` + `boq-writers-csv.test.ts` are GREEN; full suite green; typecheck clean.
- PROJECT.md no longer lists pricing as Out of Scope and records it as delivered/validated by Phase 15 with a Key-Decision reversal note.
- `15-MANUAL-NOTES.md` documents old-project back-compat (no data loss), the hardcoded ₱ constant + deferred picker, and a priced-BOQ/xlsx-in-Excel UAT checklist.
</success_criteria>

<output>
Create `.planning/phases/15-boq-pricing-perimeter-simplification/15-04-SUMMARY.md` when done.
</output>
