---
phase: 15-boq-pricing-perimeter-simplification
plan: 03
type: execute
wave: 2
depends_on: [15-02]
files_modified:
  - src/renderer/src/lib/currency.ts
  - src/renderer/src/hooks/useBoqLive.ts
  - src/renderer/src/components/TotalsRow.tsx
  - src/renderer/src/components/TotalsCategoryBlock.tsx
  - src/renderer/src/components/TotalsPanel.tsx
  - src/renderer/src/components/TotalsPanelHeader.tsx
  - src/renderer/src/components/markup/PerimeterMarkup.tsx
autonomous: true
requirements: [SC-1, SC-2, SC-4]
must_haves:
  truths:
    - "Editing a row's rate inline recomputes that row's cost live (no other store change needed)"
    - "Typing/clicking in the rate input never cycles pages or arms the draw tool"
    - "The rate map is keyed category-independently by `name|type` (Outlet|count, Skirting|perimeter)"
    - "Each category shows its cost subtotal in ₱; the panel shows a grand-total cost in ₱"
    - "A perimeter markup renders as an unfilled closed outline labeled length-only (P: <len> <unit>)"
  artifacts:
    - path: "src/renderer/src/lib/currency.ts"
      provides: "Single shared CURRENCY_SYMBOL = '₱' seam imported by TotalsRow + the cost-subtotal/grand-total renders"
      contains: "export const CURRENCY_SYMBOL"
    - path: "src/renderer/src/hooks/useBoqLive.ts"
      provides: "rates added to the Zustand selector + useMemo dependency array → live cost recompute on rate edit"
      contains: "rates"
    - path: "src/renderer/src/components/TotalsRow.tsx"
      provides: "Inline ₱ rate input (data-testid=totals-row-rate-input) keyed by rateKey=`${name}|${type}`, dispatching setRate with stopPropagation; ₱ cost display"
      contains: "totals-row-rate-input"
    - path: "src/renderer/src/components/TotalsCategoryBlock.tsx"
      provides: "Net-new per-category ₱ cost-subtotal render (the @135 slot, empty today)"
      contains: "costSubtotal"
    - path: "src/renderer/src/components/markup/PerimeterMarkup.tsx"
      provides: "Unfilled closed outline + length-only label; polygonArea/pixelAreaToReal removed"
      contains: "P: "
  key_links:
    - from: "src/renderer/src/components/TotalsRow.tsx"
      to: "projectStore.setRate"
      via: "input onChange/onBlur/onKeyDown(Enter) → useProjectStore.getState().setRate(rateKey, parsed)"
      pattern: "setRate\\("
    - from: "src/renderer/src/hooks/useBoqLive.ts"
      to: "aggregateBoq rates option"
      via: "rates selector threaded into the aggregateBoq call + useMemo deps"
      pattern: "rates"
    - from: "src/renderer/src/components/TotalsPanel.tsx"
      to: "boq.grandTotalCost"
      via: "net-new grand-total cost bar reading the aggregator's grandTotalCost"
      pattern: "grandTotalCost"
---

<objective>
Wave 2 — the totals-panel pricing UI + live recompute + perimeter render simplification. Turn the Wave 0 RED render/live tests GREEN: add the inline ₱ rate input to TotalsRow (keyed category-independently by `name|type`, dispatching `setRate`, guarded by `stopPropagation` so it never triggers row-click cycle/arm-tool), wire `rates` into `useBoqLive` so an edited rate recomputes costs live, render the net-new per-category cost subtotal + grand-total cost bar, and simplify PerimeterMarkup to an unfilled closed outline with a length-only label.

This plan consumes the data model built by Wave 1 (15-02): `projectStore.rates` + `setRate`, `BoqItemRow.rate`/`cost`, `BoqCategoryGroup.costSubtotal`, `BoqStructure.grandTotalCost`, and the `BoqRowType` rename to `'perimeter'`. It does NOT touch the aggregator, schema, serialize, or writers — those are Wave 1 / Wave 3.

Purpose: this is the user-facing half of pricing — the estimator types a rate on a row and watches the cost (and category/grand totals) update instantly — plus the perimeter visual cleanup. The math/persistence proofs (a, c, d) belong to other waves; this plan owns proof (b) inline-field + live-recompute and the SC-4 perimeter render.

Output: a working priced totals panel (inline ₱ rate edit, live cost, category cost subtotals, grand-total bar) and an unfilled, length-only perimeter render. `15-03-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-boq-pricing-perimeter-simplification/15-CONTEXT.md
@.planning/phases/15-boq-pricing-perimeter-simplification/15-VALIDATION.md

<decisions_locked>
- TYPE RENAME (locked by 15-01 + applied by 15-02): `BoqRowType` member is `'perimeter'` (NOT `'perimeter-length'`; `'perimeter-area'` is deleted). The markup type was already `'perimeter'`, so row type now equals markup type. The rate key for a perimeter row is therefore `Skirting|perimeter`.
- RATE KEY is `` `${name}|${type}` `` — category-INDEPENDENT. This is a SEPARATE string from TotalsRow's existing visibility `itemKey` (`` `${name}|${categoryId}` ``). Build a distinct `rateKey`; reusing `itemKey` mis-scopes the rate (would make the same item priced differently per category, violating the locked "one rate per (name,type)" rule).
- CURRENCY_SYMBOL = '₱' lives in a NEW renderer-side module `src/renderer/src/lib/currency.ts` (chosen location). TotalsRow + the cost-subtotal/grand-total renders import it. (Wave 3's main-process writers get their OWN local copy of the symbol per the codebase's duplication-with-test-lock convention — that is Wave 3 scope, not touched here.)
- Rate-edit commit triggers: onChange (keeps store live) + onBlur + Enter keydown. Parse with `parseFloat`; an empty/NaN input commits `0` (locked: "no rate set behaves as rate = 0"). stopPropagation on the input's pointer + click + keydown (mirror the lightbulb @241-251) so row-level handlers never fire.
- `rate`/`cost` on `BoqItemRow`, `costSubtotal` on `BoqCategoryGroup`, `grandTotalCost` on `BoqStructure` are populated by the Wave 1 aggregator. This plan only READS and RENDERS them — it does not compute cost (no `rate * quantity` arithmetic in the UI; display `item.cost` directly).
- Perimeter render: UNFILLED closed outline (remove `fill`), label `P: <len> <unit>` only (no `A:`), `polygonArea` + `pixelAreaToReal` removed. Keep the arc-aware length path (`buildArcAwareFlatPoints(...true)`, `effectiveArcs`, `polylineLength` on `closedPoints`) — do NOT regress Phase 14.
</decisions_locked>

<interfaces>
<!-- SHAPES this plan reads from the Wave 1 (15-02) source. Use these directly — no exploration needed. -->
<!-- From src/renderer/src/lib/boq-types.ts (after 15-02): -->
//   BoqRowType = 'count' | 'linear' | 'area' | 'perimeter' | 'wall'   // 'perimeter-area' deleted, 'perimeter-length' renamed → 'perimeter'
//   interface BoqItemRow { label; quantity; uom; color; type: BoqRowType; categoryId?; rate: number; cost: number }
//   interface BoqCategoryGroup { name; items: BoqItemRow[]; subtotals: BoqSubtotal[]; costSubtotal: number }
//   interface BoqStructure { metadata; categories: BoqCategoryGroup[]; grandTotals; grandTotalCost: number }
//   AggregateOptions { ...; rates?: Record<string, number> }   // defaults to useProjectStore.getState().rates
<!-- From src/renderer/src/stores/projectStore.ts (after 15-02): -->
//   rates: Record<string, number>                // persisted; plain map (no derived Set)
//   setRate: (key: string, value: number) => void  // set((s)=>({rates:{...s.rates,[key]:value}})) then get().markDirty()
<!-- EXISTING (unchanged) contracts this plan mirrors: -->
//   TotalsRow lightbulb @241-251: onClick={(e)=>{ e.stopPropagation(); useProjectStore.getState().toggleHiddenItem(itemKey) }}
//   ScalePopup numeric input @197-216: <input type="text" inputMode="decimal" value=... onChange=... /> + parseFloat on commit
//   COLORS (src/renderer/src/lib/constants.ts): dominant, secondary, accent, border, textPrimary, textSecondary, hoverSurface
</interfaces>

<read_first>
- src/renderer/src/hooks/useBoqLive.ts (whole file, 55 lines) — the 8-primitive selector block (:30-37) + the `aggregateBoq({...})` call (:41-51) + the `useMemo` dependency array (:52). `rates` must be added to BOTH the selector block (a top-level primitive selector, never `(s)=>s`) AND the dep array, or costs go stale until another store changes.
- src/renderer/src/components/TotalsRow.tsx (whole file, 313 lines) — the lightbulb stopPropagation slot (:241-251), the existing `itemKey` visibility key (:122-125, do NOT reuse for rate), the `isHidden` selector shape (:134), the fixed-width flex slots (cycle-dot :214, lightbulb :241, color chip :254, label :267, quantity :279, uom :292, plus-affordance :305), `handleClick` row cycle/arm (:155-182), and `formatQuantity` (:58-61, the formatter to mirror for ₱ cost).
- src/renderer/src/components/markup/PerimeterMarkup.tsx (whole file, 148 lines) — the area imports (:5-11), `effectiveArcs`/`buildArcAwareFlatPoints` (:75-76, KEEP), the label build with `polygonArea`/`realArea` (:79-88, strip the area half), the `<Line fill=...>` (:113-120, remove fill).
- src/renderer/src/components/TotalsCategoryBlock.tsx — the empty body slot after the items map (:135) where the net-new cost subtotal renders; the heading row style (:79-92) as a styling analog.
- src/renderer/src/components/TotalsPanel.tsx — the category map (:250-262) and the panel shell; the bottom of the categories container is where the grand-total bar goes (or pass into the header).
- src/renderer/src/components/TotalsPanelHeader.tsx — the `rows[]` metadata array (:29-35); an alternate clean seam to surface `Grand Total: ₱X.XX`.
- $HOME/.claude/get-shit-done/references/ui-brand.md — chrome/styling tokens for the net-new cost subtotal + grand-total bar + inline field.

ANALOGS (read once for the pattern, do not modify):
- src/renderer/src/components/ScalePopup.tsx:55-86, 197-216 — the canonical numeric inline input (`type="text" inputMode="decimal"`, `parseFloat`, dark COLORS styling, Enter handler).
- src/tests/use-boq-live.test.ts — the `callHook()` render harness + the "recomputes when pageMarkups changes" test (:129-164); the new "recomputes when rates change" case (added in 15-01) is the proof this plan's useBoqLive edit turns green.
- src/tests/totals-row-rate-edit.test.ts (created by 15-01) — the RED render test asserting the `data-testid="totals-row-rate-input"` dispatch + stopPropagation. Read its assertions; do NOT modify it.
</read_first>

<interface_context>
Run `npx vitest run src/tests/use-boq-live.test.ts src/tests/totals-row-rate-edit.test.ts` BEFORE editing to confirm the Wave 0 RED state (the rate-recompute case + the inline-input render test fail because useBoqLive does not subscribe to `rates` and TotalsRow has no rate input). These two files are the proof this plan turns GREEN. Do NOT edit either test file.
</interface_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CURRENCY_SYMBOL seam + wire `rates` into useBoqLive (live recompute, proof b)</name>
  <files>src/renderer/src/lib/currency.ts, src/renderer/src/hooks/useBoqLive.ts</files>
  <read_first>
    - src/renderer/src/hooks/useBoqLive.ts (whole file — selector block :30-37, aggregateBoq call :41-51, useMemo deps :52)
    - src/renderer/src/stores/projectStore.ts (confirm the `rates` field exists after 15-02; the selector reads `s.rates`)
    - src/tests/use-boq-live.test.ts:129-164 (the recompute-on-change harness; the rates case the edit turns green)
  </read_first>
  <action>
    Create `src/renderer/src/lib/currency.ts` exporting one constant: `export const CURRENCY_SYMBOL = '₱'`. This is the single renderer-side seam (locked location) — Task 2 and Task 3 import it. (The main-process writers in Wave 3 get their own local copy; do not export anything else here.)

    In `useBoqLive.ts`: add a `rates` top-level primitive selector alongside the existing eight — `const rates = useProjectStore((s) => s.rates)` — placed next to the existing `currentFilePath` selector (:37). It MUST be a top-level primitive selector (never `(s) => s`), matching the file's stated discipline so React only re-runs when the `rates` reference changes.

    Thread `rates` into the `aggregateBoq({...})` call object (the aggregator already accepts `rates?` in AggregateOptions per 15-02) and add `rates` to the `useMemo` dependency array (:52). Both edits are required: passing `rates` to `aggregateBoq` WITHOUT adding it to the dep array leaves cost stale until another store changes; adding it to deps WITHOUT passing it does nothing. Update the file's leading docstring count if it still says "eight primitives" (now nine).
  </action>
  <verify>
    <automated>npx vitest run src/tests/use-boq-live.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 15-VALIDATION proof (b) "editing a rate recomputes costs live". The `use-boq-live.test.ts` "recomputes when rates change" case (added in 15-01) goes GREEN — calling `useProjectStore.setState({ rates: {...} })` updates the harness's `current()` cost without any other store change. The pre-existing useBoqLive cases (VIEW-01 snapshot, recompute-on-pageMarkups, empty-categories) stay green. `currency.ts` exists and exports `CURRENCY_SYMBOL === '₱'`.
  </acceptance_criteria>
  <done>currency.ts exports CURRENCY_SYMBOL='₱'; useBoqLive selects `rates`, passes it to aggregateBoq, and lists it in the useMemo deps; the rates-recompute live test is green; no other useBoqLive test regressed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inline ₱ rate input + cost display in TotalsRow (proof b, stopPropagation)</name>
  <files>src/renderer/src/components/TotalsRow.tsx</files>
  <behavior>
    - Typing into the rate input (data-testid="totals-row-rate-input") then committing (change+blur, and separately Enter keydown) calls `useProjectStore.getState().setRate('Outlet|count', <parsed>)` — the category-INDEPENDENT `name|type` key, NOT the visibility `name|categoryId` key.
    - An empty / non-numeric input commits `0` (parseFloat NaN → 0), never throws.
    - Clicking, mouse-down on, or pressing a key inside the input does NOT fire the row's `onArmTool` and does NOT call `setPage` (page navigation) — proven by `e.stopPropagation()` on the input's onClick/onMouseDown/onKeyDown.
    - The row renders the item's cost as `₱<item.cost.toFixed(2)>` using the shared CURRENCY_SYMBOL (read from `item.cost`; no rate×quantity math in the component).
  </behavior>
  <read_first>
    - src/renderer/src/components/TotalsRow.tsx (whole file — lightbulb stopPropagation slot :241-251, itemKey :122-125, isHidden selector :134, fixed-width slots :214-310, handleClick :155-182, formatQuantity :58-61)
    - src/renderer/src/components/ScalePopup.tsx:197-216 (the numeric `<input>` shape to mirror: type="text" inputMode="decimal", parseFloat, dark COLORS styling) + :81-86 (Enter handler shape)
    - src/tests/totals-row-rate-edit.test.ts (the RED render test — read its exact assertions for testid, key string, and the not-called spies; do NOT modify it)
    - src/renderer/src/lib/currency.ts (CURRENCY_SYMBOL — created in Task 1)
  </read_first>
  <action>
    Import `CURRENCY_SYMBOL` from `../lib/currency`. Derive a SEPARATE rate key inside the component: `const rateKey = `${itemName}|${item.type}`` (memoize like `itemKey` at :122-125, deps `[itemName, item.type]`). Do NOT reuse `itemKey` — `itemKey` is `name|categoryId` (category-dependent visibility); `rateKey` is `name|type` (category-independent rate). For a perimeter row `item.type` is `'perimeter'` (post-15-02 rename), so the key is e.g. `Skirting|perimeter`.

    Read the current rate via a top-level selector mirroring `isHidden` (:134): `const rate = useProjectStore((s) => s.rates[rateKey] ?? 0)`. Maintain a local controlled-text state for the input seeded from `rate` (so the field shows the stored value and lets the user type intermediate text); re-seed it when `rateKey`/`rate` change.

    Add a NEW fixed-width flex slot for the rate `<input data-testid="totals-row-rate-input">`, inserted between the quantity span (:279-289) and the UoM span (:292-301) OR between UoM and the plus-affordance (:305-310) — pick one and keep it a fixed-width slot so the row's existing columns never reflow (the row is a flex of fixed slots per the :12-13 layout comment). Mirror the ScalePopup input shape: `type="text"`, `inputMode="decimal"`, a `₱` affordance via CURRENCY_SYMBOL (a small prefix span or placeholder), the dark-theme COLORS tokens (`background: COLORS.dominant`, `border: 1px solid COLORS.border`, `color: COLORS.textPrimary`), shrunk to fit the 28px row height (narrow fixed width, not `width:'100%'`).

    Commit logic: `onChange` updates the local text AND dispatches `useProjectStore.getState().setRate(rateKey, parseFloat(text) || 0)` so costs stay live; `onBlur` and an Enter `onKeyDown` re-commit the parsed value (defensive). `parseFloat` NaN → `0`.

    CRITICAL stopPropagation (mirror lightbulb :244): the input MUST call `e.stopPropagation()` in `onClick`, `onMouseDown`, AND `onKeyDown` so the row-level `handleMouseDown` (:149), `handleClick` (:155 → cycle nav + `onArmTool`), and any key handling never fire while the user edits the rate. Without this, every keystroke/click cycles pages and re-arms the draw tool.

    Add a cost display: a `formatCost` helper returning `` `${CURRENCY_SYMBOL}${item.cost.toFixed(2)}` `` (mirror `formatQuantity` :58-61) and a cost `<span>` (use `fontVariantNumeric: 'tabular-nums'` like the quantity span :282 for column alignment). Read `item.cost` directly — do not compute `rate * quantity` in the component.
  </action>
  <verify>
    <automated>npx vitest run src/tests/totals-row-rate-edit.test.ts src/tests/totals-row-cycle.test.ts src/tests/totals-row-context-menu.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 15-VALIDATION proof (b) inline-field. `totals-row-rate-edit.test.ts` (the 15-01 RED render test) goes GREEN: committing the input calls `setRate` with the `name|type` key (e.g. `'Outlet|count'`) and the parsed number. EXPLICIT stopPropagation proof: the test's spies on `onArmTool` and `setPage` (page navigation) are NOT called after a click/keydown inside the rate input — confirming `e.stopPropagation()`. The already-migrated `totals-row-cycle.test.ts` + `totals-row-context-menu.test.ts` (perimeter→'perimeter' in 15-01) stay green. No rate×quantity arithmetic appears in TotalsRow (`git grep -n "rate \\* \\|\\* quantity\\|\\* item.quantity" src/renderer/src/components/TotalsRow.tsx` returns no matches).
  </acceptance_criteria>
  <done>A ₱ rate input keyed by `name|type` dispatches setRate on change/blur/Enter with stopPropagation on click/mousedown/keydown; the row shows ₱cost from item.cost; the rate-edit render test is green and the two migrated totals-row tests stay green.</done>
</task>

<task type="auto">
  <name>Task 3: Net-new cost subtotal + grand-total renders; simplify PerimeterMarkup (proof b totals, SC-4 render)</name>
  <files>src/renderer/src/components/TotalsCategoryBlock.tsx, src/renderer/src/components/TotalsPanel.tsx, src/renderer/src/components/TotalsPanelHeader.tsx, src/renderer/src/components/markup/PerimeterMarkup.tsx</files>
  <read_first>
    - src/renderer/src/components/TotalsCategoryBlock.tsx (the empty body slot after the items map :135; heading style :79-92 as the bold-row analog)
    - src/renderer/src/components/TotalsPanel.tsx (category map :250-262; the categories container :241-264 — bottom is the grand-total seam)
    - src/renderer/src/components/TotalsPanelHeader.tsx (the rows[] array :29-35 — alternate grand-total seam)
    - src/renderer/src/components/markup/PerimeterMarkup.tsx (imports :5-11; arc-aware length path :75-84 KEEP; area half :81,85,87 DELETE; `<Line fill=...>` :118 REMOVE)
    - src/renderer/src/lib/currency.ts (CURRENCY_SYMBOL — Task 1)
    - $HOME/.claude/get-shit-done/references/ui-brand.md (chrome tokens)
  </read_first>
  <action>
    TotalsCategoryBlock.tsx — render the NET-NEW per-category cost subtotal. The body block (:115-136) currently maps `category.items` then closes with an EMPTY slot at :135 (no subtotal render today). After the items map, render a single cost-subtotal row showing `` `${CURRENCY_SYMBOL}${category.costSubtotal.toFixed(2)}` `` (import CURRENCY_SYMBOL from `../lib/currency`). Style it as a bold/secondary row using COLORS tokens (mirror the heading row chrome :79-92 — `fontWeight: 600`, `color: COLORS.textPrimary`, `padding: '0 16px'`); give it a `data-testid="totals-category-cost-subtotal"` and right-align the ₱ value (tabular-nums) so it lines up under the per-row cost column. Read `category.costSubtotal` directly (populated by the 15-02 aggregator). Also update the stale docstring at :26-27 / :45-47 that mentions "perimeter-length / perimeter-area both map to..." → just "perimeter maps to underlying 'perimeter'".

    TotalsPanel.tsx (or TotalsPanelHeader.tsx) — render the NET-NEW grand-total cost bar. Despite the panel docstring's "pinned grand-total bar", none renders today. Add a bottom bar inside the panel (after the categories container :264, before the context menu :269) showing `` `Grand Total: ${CURRENCY_SYMBOL}${boq.grandTotalCost.toFixed(2)}` `` with a `data-testid="totals-grand-total"`. Use the panel's existing chrome (`background: COLORS.secondary`, `fontWeight: 600`, `color: COLORS.textPrimary`, top border `1px solid COLORS.border`, `flexShrink: 0`) so it matches the Totals header bar (:177-197). Render it even in the empty-state branch only if you choose (`grandTotalCost` is 0 when empty — a `₱0.00` bar is acceptable; if simpler, render it only in the non-empty branch). Read `boq.grandTotalCost` directly. (If you prefer the header seam instead: push one `{ label: 'Grand Total:', value: `${CURRENCY_SYMBOL}${boq.grandTotalCost.toFixed(2)}` }` entry into TotalsPanelHeader's rows[] at :29-35 — pick ONE location, not both.)

    PerimeterMarkup.tsx — simplify the render (SC-4):
    (i) Remove the `fill={`${markup.color}33`}` line from the `<Line>` (:118) so the closed outline is UNFILLED. Keep `closed`, `stroke={markup.color}`, `strokeWidth`, `lineJoin`.
    (ii) Change the label (:79-88) to length only: delete the `polygonArea` call (:81) and the `realArea` line (:85), and change the label string from `` `P: ${realPerim.toFixed(1)} ${u}  A: ${realArea.toFixed(1)} ${u}²` `` (:87) to `` `P: ${realPerim.toFixed(1)} ${u}` ``. Keep the arc-aware length path: `effectiveArcs` (:75), `buildArcAwareFlatPoints(effectivePoints, effectiveArcs, true)` (:76), `closedPoints` + `polylineLength(closedPoints, effectiveArcs)` + `pixelLengthToReal(...)` (:82-84) — do NOT regress Phase 14 arc behavior.
    (iii) Remove the now-unused `polygonArea` and `pixelAreaToReal` from the import (:5-11). Keep `polygonCentroid` (still positions the label), `polylineLength`, `pixelLengthToReal`. Update the component docstring (:43-44) that says label is `"P: 24.6 m  A: 38.2 m²"` → length only.
  </action>
  <verify>
    <automated>npx vitest run src/tests/use-boq-live.test.ts src/tests/totals-row-rate-edit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 15-VALIDATION proof (b) totals render + the SC-4 perimeter manual render. The full automated suite stays green (`npx vitest run` — no regression; the totals-panel render tests that mount TotalsCategoryBlock/TotalsPanel still pass with the net-new ₱ rows present). TypeScript is clean — `category.costSubtotal` and `boq.grandTotalCost` resolve (the 15-02 type additions are in place). PerimeterMarkup imports no longer reference `polygonArea`/`pixelAreaToReal` (`git grep -n "polygonArea\\|pixelAreaToReal" src/renderer/src/components/markup/PerimeterMarkup.tsx` returns no matches) and the `<Line>` has no `fill` prop. MANUAL UAT (SC-4, per 15-VALIDATION Manual-Only table): place a perimeter markup → it renders as an unfilled closed outline with a `P: <len> <unit>` label and NO translucent fill, NO `A:` segment; place markups, set a rate inline → the cost, the category cost subtotal, and the grand-total bar all update live, and clicking/typing in the rate input does NOT cycle pages or arm a draw tool.
  </acceptance_criteria>
  <done>Per-category ₱ cost subtotal renders (net-new slot), a ₱ grand-total bar renders (one location), PerimeterMarkup is an unfilled closed outline with a length-only label and no area imports/math; full suite green; perimeter + live-pricing UX confirmed via manual UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Renderer-only UI. Every task reads existing in-memory store state (`projectStore.rates`, `BoqStructure`) and dispatches the already-defined `setRate` action. No new IPC, no file I/O, no untrusted input crossing a process boundary — `setRate` writes a parsed number into a renderer Zustand map. Persistence of `rates` (the schema/serialize guard) is Wave 1 scope, not this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Tampering | rate input parsing a non-numeric / empty string | mitigate | `parseFloat(text) || 0` commits a numeric 0 on NaN/empty (locked "no rate = 0"); the input never writes a string into `rates` (the map is `Record<string, number>`). |
| T-15-03-02 | Elevation of Privilege (UX) | rate-input events leaking to the row's cycle-nav / arm-tool handlers | mitigate | `e.stopPropagation()` on the input's onClick/onMouseDown/onKeyDown (mirrors lightbulb :244); proven by the render test spies asserting `onArmTool`/`setPage` are NOT called. |
| T-15-03-03 | Tampering | rate scoped to the wrong key (category-dependent) | mitigate | Distinct `rateKey = `${name}|${type}`` (never the visibility `itemKey`); the render test asserts `setRate` is called with the category-independent key. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — uses existing React/Zustand/Konva + Vitest infra (15-VALIDATION: "no new framework install"). No legitimacy gate needed. |
</threat_model>

<verification>
- `npx vitest run src/tests/use-boq-live.test.ts src/tests/totals-row-rate-edit.test.ts` — the two Wave 0 RED proofs this plan owns are GREEN (live recompute on rate change; inline-input setRate dispatch + stopPropagation).
- `npx vitest run` — full suite green (baseline ~586/80); no regression in the totals-panel render tests, the migrated perimeter-type tests, or the WR-07 arc guard (aggregator untouched here).
- `npm run typecheck` (or `npx tsc --noEmit`) clean — `item.cost`, `item.rate`, `category.costSubtotal`, `boq.grandTotalCost`, `projectStore.rates`/`setRate` all resolve against the 15-02 types.
- `git grep -n "polygonArea\|pixelAreaToReal" -- src/renderer/src/components/markup/PerimeterMarkup.tsx` returns ZERO matches; the `<Line>` carries no `fill` prop.
- MANUAL (SC-4, deferred to UAT per 15-VALIDATION Manual-Only table): perimeter renders unfilled + length-only label; inline ₱ rate edit updates cost + category subtotal + grand-total live with no page-cycle/arm-tool side effect.
</verification>

<success_criteria>
- useBoqLive subscribes to `rates` (selector + useMemo deps) → editing a rate recomputes costs live without any other store change.
- TotalsRow has an inline ₱ rate input keyed category-independently by `name|type`, dispatching `setRate` on change/blur/Enter, with stopPropagation on click/mousedown/keydown so the rate field never triggers row cycle-nav or arm-tool.
- TotalsRow displays the row cost in ₱ (read from `item.cost`, no UI arithmetic); each category shows a ₱ cost subtotal; the panel shows a ₱ grand-total bar.
- PerimeterMarkup renders an unfilled closed outline with a length-only `P: <len> <unit>` label; the area imports/math are removed; the arc-aware length path is intact.
- CURRENCY_SYMBOL is defined once in `src/renderer/src/lib/currency.ts` and imported by all renderer consumers in this plan.
- Full Vitest suite green; typecheck clean.
</success_criteria>

<output>
Create `.planning/phases/15-boq-pricing-perimeter-simplification/15-03-SUMMARY.md` when done.
</output>
