---
phase: 15-boq-pricing-perimeter-simplification
plan: 02
type: execute
wave: 1
depends_on: [15-01]
files_modified:
  - src/renderer/src/stores/projectStore.ts
  - src/renderer/src/lib/project-schema.ts
  - src/renderer/src/lib/project-serialize.ts
  - src/renderer/src/lib/boq-types.ts
  - src/renderer/src/lib/boq-aggregator.ts
  - src/preload/index.ts
  - src/preload/index.d.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-5]
must_haves:
  truths:
    - "A perimeter markup emits exactly ONE BOQ row (length), arc-aware, no area row"
    - "Each BOQ row carries rate (₱ for its name|type, 0 when unset) and cost (rate × quantity, 0 when unset)"
    - "Each category exposes costSubtotal (Σ row costs); the structure exposes grandTotalCost (Σ category subtotals)"
    - "A rate edited in the store persists across save/load keyed by `${name}|${type}`, category-independent"
    - "A legacy .clmc with no rates (or a crafted one with bad rates) loads with a sanitized rates map, never throws"
    - "Perimeter is a first-class collision member: plain {name}, suffixed (perimeter) only on a same-name count/linear/area collision in that category"
  artifacts:
    - path: "src/renderer/src/stores/projectStore.ts"
      provides: "rates state + setRate action (mirrors toggleHiddenItem incl. markDirty); reset to {}"
      contains: "setRate"
    - path: "src/renderer/src/lib/boq-aggregator.ts"
      provides: "rate/cost per row, costSubtotal per category, grandTotalCost; perimeter length-only + collision-set membership"
      contains: "grandTotalCost"
    - path: "src/renderer/src/lib/boq-types.ts"
      provides: "BoqItemRow.rate/cost, BoqCategoryGroup.costSubtotal, BoqStructure.grandTotalCost, AggregateOptions.rates; BoqRowType perimeter rename + perimeter-area delete"
      contains: "grandTotalCost"
    - path: "src/renderer/src/lib/project-serialize.ts"
      provides: "snapshot emits rates; hydrate restores rates with finite-≥0 coercion (drop invalid)"
      contains: "rates"
  key_links:
    - from: "src/renderer/src/lib/boq-aggregator.ts"
      to: "useProjectStore rates map"
      via: "opts.rates ?? useProjectStore.getState().rates, lookup by `${name}|${type}`"
      pattern: "rates\\[`\\$\\{name\\}\\|\\$\\{type\\}`\\]"
    - from: "src/renderer/src/lib/project-serialize.ts"
      to: "projectStore.rates"
      via: "snapshot read + hydrate setState with coercion guard"
      pattern: "rates"
    - from: "src/renderer/src/stores/projectStore.ts"
      to: "markDirty"
      via: "setRate calls get().markDirty() like toggleHiddenItem"
      pattern: "setRate"
---

<objective>
Wave 1 — the BOQ data-model + types + aggregator spine. Add the `rates` map to the project store + schema + serialize (mirroring `hiddenItemNames` end-to-end), thread `rate`/`cost`/`costSubtotal`/`grandTotalCost` through `boq-types.ts` and the aggregator, and collapse the perimeter tool to length-only (delete the perimeter-area synthesis, fold perimeter into the D-02 collision set). This turns the aggregator / serialize / schema RED tests from Plan 15-01 GREEN.

Purpose: every downstream wave depends on these shapes. The writers (Wave 2) read `BoqItemRow.rate/cost` + `BoqCategoryGroup.costSubtotal` + `BoqStructure.grandTotalCost`; the totals panel + `useBoqLive` (Wave 2/3) read `rates` + `setRate` from the store and the cost fields from the aggregator. This plan defines the contracts so later waves implement against them — no codebase scavenger hunt.

Output: updated `projectStore.ts`, `project-schema.ts`, `project-serialize.ts`, `boq-types.ts`, `boq-aggregator.ts`, and the two preload mirrors (`preload/index.ts`, `preload/index.d.ts`). After this plan: `boq-aggregator.test.ts`, `project-serialize.test.ts`, `project-schema.test.ts` are GREEN; `use-boq-live`, `boq-writers-*`, and `totals-row-rate-edit` stay RED until Waves 2/3 (expected).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-boq-pricing-perimeter-simplification/15-CONTEXT.md
@.planning/phases/15-boq-pricing-perimeter-simplification/15-PATTERNS.md
@.planning/phases/15-boq-pricing-perimeter-simplification/15-RESEARCH.md
@.planning/phases/15-boq-pricing-perimeter-simplification/15-VALIDATION.md
@.planning/phases/15-boq-pricing-perimeter-simplification/15-01-PLAN.md

<locked_contract>
<!-- Fixed by 15-01 + CONTEXT. Every wave obeys this verbatim. -->
- `rates?: Record<string, number>` — additive optional field on ProjectFileV2, key `` `${name}|${type}` `` (category-INDEPENDENT). NO formatVersion bump. Mirror `hiddenItemNames?` end-to-end.
- projectStore: `rates: Record<string, number>` state + `setRate(key, value)` action mirroring `toggleHiddenItem` (incl. `get().markDirty()`); reset to `{}` on new/loaded project wherever `hiddenItemNames` resets.
- BoqRowType: rename `'perimeter-length'` → `'perimeter'`; DELETE `'perimeter-area'`. (Rename chosen — the markup type is already `'perimeter'`, so row type now equals markup type. Apply atomically across all 4 type-duplication files.)
- BoqItemRow += `rate: number`, `cost: number`. BoqCategoryGroup += `costSubtotal: number`. BoqStructure += `grandTotalCost: number`. AggregateOptions += `rates?: Record<string, number>` (default `useProjectStore.getState().rates`).
- cost = rate × quantity; missing rate key → `rate:0, cost:0` (never throw). costSubtotal = Σ row costs in the category; grandTotalCost = Σ category costSubtotals.
- Perimeter joins the D-02 collision set: plain `{name}`, suffix ` (perimeter)` ONLY on a same-name count/linear/area collision in that category. The type-word helper (rename `nonPerimeterTypeWord` → `typeWord`) must emit `'perimeter'`; `uomFor` must handle `'perimeter'` (→ globalUnit). Perimeter LENGTH stays arc-aware — do NOT touch `polylineLength`/`m.arcs` on the closing-augmented points (Phase 14 must not regress).
- 4 type-duplication files: `boq-types.ts` (canonical), `boq-writers.ts`, `preload/index.d.ts`, `preload/index.ts`. **boq-writers.ts is NOT in this plan's scope — Wave 2 owns it; do NOT touch it.** This plan touches the other three. Preload mirrors OMIT `categoryId` — add only `rate`/`cost` there; apply the perimeter rename/delete in all three in-scope files; do NOT propagate the existing `'wall'` drift in `preload/index.ts`.
</locked_contract>

<interfaces>
<!-- The SHAPES this plan creates. Downstream waves assert/build against these. Extracted from 15-PATTERNS.md + the canonical files. -->

projectStore (src/renderer/src/stores/projectStore.ts) — additive sibling of hiddenItemNames:
//   rates: Record<string, number>                 // persisted; the map IS the O(1) lookup (no derived Set)
//   setRate: (key: string, rate: number) => void  // set({ rates: {...s.rates, [key]: rate} }) then get().markDirty()
//   reset(): rates back to {}                      // alongside hiddenItemNames: [] / hiddenItemSet: new Set()

ProjectFileV2 (src/renderer/src/lib/project-schema.ts) — additive optional, NO formatVersion bump:
//   rates?: Record<string, number>   // absent in pre-Phase-15 files; defaults to {} on load. validateV2 adds NO throw.

boq-types.ts shapes (canonical; mirrored into the two preload files, no categoryId):
//   BoqRowType = 'count' | 'linear' | 'area' | 'perimeter' | 'wall'   // 'perimeter-length'→'perimeter'; 'perimeter-area' DELETED
//   BoqItemRow += rate: number   // ₱ for (name,type); 0 when unset
//   BoqItemRow += cost: number   // rate × quantity
//   BoqCategoryGroup += costSubtotal: number   // Σ row costs in the category
//   BoqStructure += grandTotalCost: number     // Σ all category costSubtotals
//   AggregateOptions += rates?: Record<string, number>   // default useProjectStore.getState().rates

aggregator rate key (src/renderer/src/lib/boq-aggregator.ts:109) — SAME shape as the existing bucket key:
//   const k = `${name}|${type}`        // bucket key (exists)
//   rates[`${name}|${type}`] ?? 0      // rate lookup (new), built where the row is pushed (lines 217-224)
</interfaces>

<read_first>
- src/renderer/src/stores/projectStore.ts — the analog file: `hiddenItemNames`/`hiddenItemSet` state (~12-15), `reset()` (~72-80), `toggleHiddenItem` (~82-91, note the trailing `get().markDirty()`), `setHiddenItemNames` (~93-95). `attachDirtyTracking` (~116-142) does NOT subscribe to projectStore fields — do not add a rates subscription.
- src/renderer/src/lib/project-schema.ts — `hiddenItemNames?` field on `ProjectFileV2` (~83-88), `validateV2` (~92-119, ends `return raw as ProjectFileV2` at ~118, keeps `formatVersion === 2`). No `hiddenItemNames` validation branch — `rates` adds none either.
- src/renderer/src/lib/project-serialize.ts — snapshot `hiddenItemNames` read (~60), hydrate `setState` inside the `suspendDirtyTracking()/resumeDirtyTracking()` try/finally (~74-119), with the `Array.isArray(...)` legacy guard (~115-118).
- src/renderer/src/lib/boq-types.ts — `BoqRowType` (~19-25), `BoqItemRow` (~33-52), `BoqCategoryGroup` (~61-68), `BoqStructure` (~75-81), `AggregateOptions` (~88-98). JSDoc-per-field style to match.
- src/renderer/src/lib/boq-aggregator.ts — `uomFor` (~35-40), `nonPerimeterTypeWord` (~42-46), `RawAccumulator` + `add()` (~20-23, 107-113, bucket key `${name}|${type}` at 109), perimeter synthesis (~144-159: length add at 150-154 KEEP, area add at 155-159 DELETE), collision-map build with the perimeter `continue` skip (~190-200), label loop (~204-216), row-build push (~217-224), per-UoM subtotal loop (~231-239), category push (~245), return (~274). Default-from-store pattern at ~82-90.
- src/preload/index.ts — duplicated `BoqRowType` (~16, ALREADY drifted: omits `'wall'`) + `BoqItemRow` (~16-29, omits `categoryId`) + `BoqCategoryGroup`/`BoqStructure` mirrors.
- src/preload/index.d.ts — duplicated `BoqRowType` (~6, includes `'wall'`) + `BoqItemRow` (~14-20, omits `categoryId`) + `BoqCategoryGroup`/`BoqStructure` mirrors.
- 15-PATTERNS.md §1 (rates persistence end-to-end), §3 (rate/cost on BoqItemRow + aggregator threading), §5a (perimeter-area removal in aggregator) — the line-cited excerpts for each edit above.
- 15-RESEARCH.md "Perimeter-Area Removal Map" rows 1-10 (the disposition table) + "Label change" section (collision-suffix rule).
</read_first>

<interface_context>
Run `npx vitest run src/tests/boq-aggregator.test.ts src/tests/project-serialize.test.ts src/tests/project-schema.test.ts` BEFORE editing to confirm these are currently RED (the 15-01 assertions reference `rate`/`cost`/`costSubtotal`/`grandTotalCost`/`rates` and the one-row perimeter contract that do not exist yet). This plan turns them GREEN. Do NOT run a global suite expecting all-green — `use-boq-live` / `boq-writers-*` / `totals-row-rate-edit` stay RED until Waves 2/3.
</interface_context>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add rates to projectStore + project-schema + project-serialize (mirror hiddenItemNames, with hydrate coercion)</name>
  <files>src/renderer/src/stores/projectStore.ts, src/renderer/src/lib/project-schema.ts, src/renderer/src/lib/project-serialize.ts</files>
  <read_first>
    - src/renderer/src/stores/projectStore.ts (whole file — `hiddenItemNames` state ~12-15, `reset()` ~72-80, `toggleHiddenItem` ~82-91 incl. `get().markDirty()`, `attachDirtyTracking` ~116-142)
    - src/renderer/src/lib/project-schema.ts (`hiddenItemNames?` ~83-88, `validateV2` ~92-119)
    - src/renderer/src/lib/project-serialize.ts (snapshot ~60, hydrate setState + `Array.isArray` guard ~115-118, the `suspendDirtyTracking`/`resumeDirtyTracking` bracket ~74-119)
    - 15-PATTERNS.md §1a/§1b/§1c (the line-by-line additive twins)
  </read_first>
  <behavior>
    - project-serialize round-trip: snapshot reads `useProjectStore.getState().rates`; hydrate restores it; a `rates: { 'Skirting|perimeter': 12 }` survives snapshot→JSON→hydrate deep-equal (project-serialize.test.ts).
    - Legacy default: hydrate of a fixture with NO `rates` key → `rates === {}` (project-serialize.test.ts / project-schema.test.ts).
    - Crafted-input coercion: hydrate of `{ rates: { 'A|count': -5, 'B|count': 'x', 'C|count': 12, 'D|count': NaN } }` → store rates contains ONLY `{ 'C|count': 12 }` (drops negative, non-number, NaN/Infinity) and does not throw (project-serialize.test.ts).
    - validateV2 tolerance: `validateV2` ACCEPTS an object with a `rates` field and one WITHOUT it, no throw, no formatVersion bump (project-schema.test.ts).
  </behavior>
  <action>
    In `projectStore.ts`: add `rates: Record<string, number>` to the state interface and initial state, directly alongside `hiddenItemNames`. The map IS the lookup — do NOT add a derived `Set`. Add a `setRate: (key: string, rate: number) => void` action mirroring `toggleHiddenItem`: `set((s) => ({ rates: { ...s.rates, [key]: rate } }))` then `get().markDirty()` (the trailing markDirty is load-bearing — without it Save won't persist an edited rate). In `reset()`, add `rates: {}` alongside `hiddenItemNames: []` / `hiddenItemSet: new Set()`. Do NOT add any subscription in `attachDirtyTracking` (mirror `hiddenItemNames`, which marks dirty only via its action).

    In `project-schema.ts`: add `rates?: Record<string, number>` to the `ProjectFileV2` interface, copying the JSDoc-with-default style of `hiddenItemNames?` (note: "Additive in Phase 15 — absent in pre-Phase-15 files; defaults to {} on load. Per-(name|type) unit rate in ₱. NO formatVersion bump."). Add NO validation branch in `validateV2` — it tolerates `rates` exactly as it tolerates `hiddenItemNames` (rides the trailing `return raw as ProjectFileV2`). Keep `formatVersion === 2`; do NOT bump.

    In `project-serialize.ts`: in `snapshotProject`, add `rates: useProjectStore.getState().rates` next to the `hiddenItemNames` snapshot line. In `hydrateStores`, inside the SAME `setState` (still inside the `suspendDirtyTracking`/`resumeDirtyTracking` bracket), restore `rates` with a sanitizing coercion guard (this is the threat-model mitigation): build the restored map by iterating `data.rates` only when it is a non-null object, keeping each entry whose value is a finite number ≥ 0 (`typeof v === 'number' && Number.isFinite(v) && v >= 0`), and dropping every other entry; default to `{}` when `data.rates` is absent/non-object. This is the `Record` analog of the `Array.isArray` guard plus a value-level finite-≥0 filter.
  </action>
  <verify>
    <automated>npx vitest run src/tests/project-serialize.test.ts src/tests/project-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 15-VALIDATION proof (b) persistence/round-trip + (d) old-project reload. `npx vitest run src/tests/project-serialize.test.ts src/tests/project-schema.test.ts` is GREEN (the 15-01 rates round-trip, absent-default, and crafted-coercion assertions pass). `git grep -n "setRate" -- src/renderer/src/stores/projectStore.ts` returns ≥1. `git grep -n "rates" -- src/renderer/src/lib/project-serialize.ts` shows BOTH a snapshot read and a hydrate restore. The hydrate restore drops a negative/NaN/string rate (verified by the crafted-input test, not just by grep). `npm run typecheck` is clean.
  </acceptance_criteria>
  <done>projectStore has `rates` + `setRate` (with markDirty) + reset-to-{}; schema has `rates?` with no new throw and no formatVersion bump; serialize snapshots + hydrates rates with a finite-≥0 coercion guard; the two serialize/schema test files are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add rate/cost/costSubtotal/grandTotalCost + perimeter rename to boq-types.ts and the two preload mirrors</name>
  <files>src/renderer/src/lib/boq-types.ts, src/preload/index.ts, src/preload/index.d.ts</files>
  <read_first>
    - src/renderer/src/lib/boq-types.ts (`BoqRowType` ~19-25, `BoqItemRow` ~33-52, `BoqCategoryGroup` ~61-68, `BoqStructure` ~75-81, `AggregateOptions` ~88-98)
    - src/preload/index.ts (`BoqRowType` ~16 — already missing `'wall'`; `BoqItemRow` ~16-29 — omits `categoryId`; `BoqCategoryGroup`/`BoqStructure` mirrors)
    - src/preload/index.d.ts (`BoqRowType` ~6 — includes `'wall'`; `BoqItemRow` ~14-20 — omits `categoryId`; `BoqCategoryGroup`/`BoqStructure` mirrors)
    - 15-PATTERNS.md §3a (the additive fields + the 4-way type lock note) + 15-RESEARCH.md Removal-Map rows 1, 8, 9, 10
  </read_first>
  <behavior>
    - Type-only change: this task adds no runtime behavior; correctness is proven by `npm run typecheck` (clean) plus the aggregator/writer tests that consume these shapes compiling. No `'perimeter-area'` token remains in any in-scope type file.
  </behavior>
  <action>
    In `boq-types.ts`: in `BoqRowType`, rename the member `'perimeter-length'` → `'perimeter'` and DELETE the member `'perimeter-area'` (final union: `'count' | 'linear' | 'area' | 'perimeter' | 'wall'`). Add `rate: number` (JSDoc "Unit rate in ₱ for this (name,type); 0 when unset.") and `cost: number` (JSDoc "rate × quantity, ₱.") to `BoqItemRow`, matching the existing JSDoc-per-field style. Add `costSubtotal: number` (JSDoc "Σ row costs in the category, ₱; unit-agnostic.") to `BoqCategoryGroup`. Add `grandTotalCost: number` (JSDoc "Σ all category costSubtotals, ₱.") to `BoqStructure`. Add `rates?: Record<string, number>` to `AggregateOptions` (JSDoc "Per-(name|type) unit rate map; default useProjectStore.getState().rates.").

    In `preload/index.ts`: apply the SAME perimeter rename + `'perimeter-area'` delete to its duplicated `BoqRowType`, and add `rate: number` + `cost: number` to its duplicated `BoqItemRow`, and `costSubtotal: number` / `grandTotalCost: number` to its `BoqCategoryGroup` / `BoqStructure` mirrors. Do NOT add `categoryId` (the preload mirror deliberately omits it). Do NOT add the missing `'wall'` member — leave that pre-existing drift untouched (out of scope).

    In `preload/index.d.ts`: apply the SAME perimeter rename + `'perimeter-area'` delete + `rate`/`cost` on `BoqItemRow` + `costSubtotal`/`grandTotalCost` on the `BoqCategoryGroup`/`BoqStructure` mirrors. This copy already includes `'wall'` — keep it. Do NOT add `categoryId`.

    Do NOT touch `src/main/boq-writers.ts` — its `BoqRowType`/`BoqItemRow`/`BoqCategoryGroup`/`BoqStructure` duplicates are Wave 2's scope.
  </action>
  <verify>
    <automated>npx vitest run src/tests/project-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 15-VALIDATION proof (a) row/category/grand-total shape + (c) perimeter type rename. `git grep -n "perimeter-area" -- src/renderer/src/lib/boq-types.ts src/preload` returns ZERO. `git grep -n "perimeter-length" -- src/renderer/src/lib/boq-types.ts src/preload` returns ZERO. `git grep -n "rate\b\|cost\b\|costSubtotal\|grandTotalCost" -- src/renderer/src/lib/boq-types.ts` shows all four additions present. `git grep -n "categoryId" -- src/preload` returns ZERO (the omission is preserved). `npm run typecheck` is clean (the structural-lock test `boq-export-ipc.test.ts` still compiles with `STUB_STRUCT`). The verify command is a smoke check that the type changes don't break the schema suite — Task 3 proves the aggregator behavior.
  </acceptance_criteria>
  <done>boq-types.ts has rate/cost on BoqItemRow, costSubtotal on BoqCategoryGroup, grandTotalCost on BoqStructure, rates? on AggregateOptions, and the BoqRowType perimeter rename + perimeter-area delete; both preload mirrors match (no categoryId, no new 'wall', no 'perimeter-area'); typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Thread rate/cost/costSubtotal/grandTotalCost through boq-aggregator + perimeter length-only + collision-set membership</name>
  <files>src/renderer/src/lib/boq-aggregator.ts</files>
  <read_first>
    - src/renderer/src/lib/boq-aggregator.ts (whole file — `uomFor` ~35-40, `nonPerimeterTypeWord` ~42-46, default-from-store ~82-90, `add()` + bucket key ~107-113, perimeter synthesis ~144-159, collision-map `continue` skip ~190-200, label loop ~204-216, row push ~217-224, per-UoM subtotal loop ~231-239, category push ~245, return ~274)
    - 15-PATTERNS.md §3b (row-build + subtotal accumulators) + §5a (perimeter-area removal: synthesis, collision exclusion, label rules, typeWord)
    - 15-RESEARCH.md "Perimeter-Area Removal Map" rows 2-7 + "Label change" section
    - src/tests/boq-aggregator.test.ts (the GREEN target — the rewritten one-row perimeter cases, WR-07 length guard, rate/cost/collision/independence cases from 15-01)
  </read_first>
  <behavior>
    - cost = rate × quantity at row: `rates: { 'Outlet|count': 5 }` + 3 'Outlet' counts → row `{ quantity: 3, rate: 5, cost: 15 }` (boq-aggregator.test.ts).
    - missing rate key → `{ rate: 0, cost: 0 }`, no throw (boq-aggregator.test.ts).
    - category-independence: same `'Outlet|count'` in two categories, one rates entry → both rows read that rate (boq-aggregator.test.ts).
    - costSubtotal/grandTotalCost: `category.costSubtotal === Σ row costs`; `result.grandTotalCost === Σ category costSubtotals` (boq-aggregator.test.ts).
    - perimeter one-row: a lone perimeter named 'Wall' → exactly ONE item, label 'Wall' (plain), type 'perimeter', no '(area)' anywhere (boq-aggregator.test.ts, rewritten :73).
    - perimeter LENGTH arc-aware: WR-07 perimeter with an arc on the closing edge → length ≈ 300 + π·50, found by plain label 'Curved', no area row (boq-aggregator.test.ts, rewritten :95).
    - collision suffix: 'Skirting' perimeter + 'Skirting' linear in one category → `['Skirting (linear)', 'Skirting (perimeter)']` (boq-aggregator.test.ts).
  </behavior>
  <action>
    Read the rates source: add a `rates` option to the aggregator's options destructure with default `useProjectStore.getState().rates`, mirroring how the other inputs default from the store (~82-90). Use `opts.rates ?? useProjectStore.getState().rates` so an injected map wins.

    Per-row rate/cost: at the row-build push (~217-224), before pushing, compute `const rate = rates[`${name}|${type}`] ?? 0` and `const cost = rate * acc.quantity`, and add `rate` + `cost` to the pushed `BoqItemRow`. Do NOT put rate in the `RawAccumulator` (it is not summed). Use the SAME `${name}|${type}` shape as the bucket key.

    Per-category costSubtotal + grandTotalCost: alongside the per-UoM subtotal loop (~231-239), accumulate a scalar `catCost` (`Σ it.cost` over the category's items) and attach `costSubtotal: catCost` to the category push (~245). Maintain a module/function-scope `grandCost` accumulator summed across categories and add `grandTotalCost: grandCost` to the returned structure (~274).

    Perimeter length-only: KEEP the perimeter LENGTH `add()` (~150-154) exactly — it passes `m.arcs` into `polylineLength(closingPts, m.arcs)` on the closing-augmented points (arc-aware; Phase 14 must not regress). Rename the type literal it passes from `'perimeter-length'` → `'perimeter'`. DELETE the perimeter-AREA arm (~155-159): remove the `polygonArea(pts, m.arcs)` call, the `pixelAreaToReal` call, and the `add(catId, m.name, 'perimeter-area', realA)` line entirely. If `polygonArea`/`pixelAreaToReal` become unused in this file, remove them from the import.

    Perimeter joins the collision set: in the collision-map build (~190-200), REMOVE the `if (type === 'perimeter-length' || type === 'perimeter-area') continue` skip so `'perimeter'` participates. Rename the map `nameNonPerimTypes` → `nameTypes` (it now includes perimeter).

    Unified label rule: in the label loop (~204-216), DELETE the hardcoded perimeter branches (`'perimeter-length'`→`(perimeter)` and `'perimeter-area'`→`(area)`). Let perimeter fall through to the same collision check the other types use: plain `{name}`, gaining `${name} (${typeWord(type)})` ONLY when the name has ≥2 colliding types in that category. Rename `nonPerimeterTypeWord` → `typeWord` and add a `'perimeter'` case returning `'perimeter'` (so a collided perimeter row reads `{name} (perimeter)`).

    uomFor: keep the perimeter→globalUnit mapping (~37); rename the literal `'perimeter-length'` → `'perimeter'`.
  </action>
  <verify>
    <automated>npx vitest run src/tests/boq-aggregator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 15-VALIDATION proofs (a) cost math at row/category/grand-total + (c) one-row perimeter + collision-suffix + arc-aware length. `npx vitest run src/tests/boq-aggregator.test.ts` is GREEN (all 15-01 rate/cost/collision/independence cases pass; the rewritten one-row perimeter case and the WR-07 length guard pass; no '(area)' row). `! git grep -n "perimeter-area" -- src/renderer/src/lib src/preload` returns zero (exit-nonzero on any match; the `!` makes a found match FAIL the gate). `git grep -n "perimeter-length" -- src/renderer/src/lib/boq-aggregator.ts` returns ZERO. `git grep -n "grandTotalCost\|costSubtotal" -- src/renderer/src/lib/boq-aggregator.ts` shows both populated. The WR-07 length assertion (≈ 300 + π·50) passes — Phase-14 arc behavior intact. `npm run typecheck` is clean.
  </acceptance_criteria>
  <done>aggregator reads `opts.rates ?? store`, populates `rate`/`cost` per row, `costSubtotal` per category, `grandTotalCost` on the structure; perimeter emits one arc-aware length row and is a first-class collision member with the unified suffix rule; `typeWord` emits 'perimeter'; zero `perimeter-area`/`perimeter-length` tokens remain in src/renderer/src/lib + src/preload; boq-aggregator.test.ts is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk(.clmc) → renderer hydrate | A crafted/corrupt `.clmc` project file is parsed and its `rates` map is loaded into `projectStore` via `hydrateStores`. The map values flow into cost math (`rate × quantity`) and later into ₱ export cells. This is the only untrusted-input surface this plan adds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Tampering | malformed `rates` in a crafted `.clmc` (negative, NaN/Infinity, non-number, non-object) reaching cost math + export cells | mitigate | `hydrateStores` (project-serialize.ts) coerces each `rates` entry: keep only `typeof v === 'number' && Number.isFinite(v) && v >= 0`, drop the rest; non-object/absent `data.rates` → `{}`. This is the `Record` analog of the existing `Array.isArray(hiddenItemNames)` guard, hardened with a finite-≥0 value filter. Proven by the Task-1 crafted-input test (drops `-5`, `'x'`, `NaN`). |
| T-15-02-02 | Tampering | type-mirror divergence — the 3 in-scope `BoqRowType`/`BoqItemRow` duplicates drifting on the perimeter rename | mitigate | All three in-scope mirrors (boq-types.ts, preload/index.ts, preload/index.d.ts) updated in lockstep in Task 2; `npm run typecheck` + the `boq-export-ipc.test.ts` structural lock fail on divergence. (boq-writers.ts is Wave 2's mirror — flagged, not touched here.) |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — modifies existing source against the existing Vitest infra. No new dependency, no legitimacy gate needed. |

No network, auth, or secret surface is added or crossed. No blocking-high threat — the single relevant threat (malformed rates) is fully mitigated by the serialize/hydrate coercion guard.
</threat_model>

<verification>
- `npx vitest run src/tests/boq-aggregator.test.ts src/tests/project-serialize.test.ts src/tests/project-schema.test.ts` — all GREEN (this plan's three target suites). `use-boq-live` / `boq-writers-*` / `totals-row-rate-edit` remain RED until Waves 2/3 — that is expected and NOT a failure of this plan.
- `npm run typecheck` (or `npx tsc --noEmit`) — clean. The structural-lock test `src/tests/boq-export-ipc.test.ts` still compiles after the three `BoqRowType`/`BoqItemRow` mirror edits.
- `! git grep -n "perimeter-area" -- src/renderer/src/lib src/preload` — returns zero matches (gate FAILS if any match is found).
- `git grep -n "perimeter-length" -- src/renderer/src/lib src/preload` — returns zero matches (rename applied everywhere in scope).
</verification>

<success_criteria>
- A perimeter markup emits exactly ONE arc-aware length BOQ row; no area row anywhere; WR-07 length guard intact (no Phase-14 regression).
- Every BOQ row carries `rate` (₱ for its `name|type`, 0 when unset) and `cost` (rate × quantity); each category has `costSubtotal`; the structure has `grandTotalCost`.
- `rates` persists across save/load keyed by `${name}|${type}` (category-independent), via store + schema + serialize mirroring `hiddenItemNames`; legacy and crafted files load with a sanitized `{}`-defaulted map, never throwing.
- Perimeter is a first-class D-02 collision member: plain `{name}`, suffix `(perimeter)` only on a same-name count/linear/area collision; `typeWord` emits `'perimeter'`.
- The three in-scope type-duplication files stay in lockstep on the perimeter rename + the new fields; typecheck clean; zero `perimeter-area`/`perimeter-length` tokens remain in `src/renderer/src/lib` + `src/preload`.
- `boq-aggregator.test.ts`, `project-serialize.test.ts`, `project-schema.test.ts` are GREEN.
</success_criteria>

<output>
Create `.planning/phases/15-boq-pricing-perimeter-simplification/15-02-SUMMARY.md` when done.
</output>
