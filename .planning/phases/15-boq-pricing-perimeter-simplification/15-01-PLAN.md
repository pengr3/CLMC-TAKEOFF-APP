---
phase: 15-boq-pricing-perimeter-simplification
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/tests/boq-aggregator.test.ts
  - src/tests/project-serialize.test.ts
  - src/tests/project-schema.test.ts
  - src/tests/boq-writers-xlsx.test.ts
  - src/tests/boq-writers-csv.test.ts
  - src/tests/use-boq-live.test.ts
  - src/tests/totals-row-cycle.test.ts
  - src/tests/totals-row-context-menu.test.ts
  - src/tests/totals-row-rate-edit.test.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-5, SC-6]
must_haves:
  truths:
    - "Every Wave 1-3 source task has a failing test it must turn green"
    - "The two perimeter two-row assertions are rewritten to expect ONE perimeter row"
    - "The WR-07 arc-length regression guard survives (length assertion kept, area assertion dropped)"
  artifacts:
    - path: "src/tests/totals-row-rate-edit.test.ts"
      provides: "RED render test for the inline rate input dispatch + stopPropagation"
      contains: "setRate"
    - path: "src/tests/boq-aggregator.test.ts"
      provides: "rate/cost cases + rewritten perimeter one-row cases"
      contains: "perimeter"
  key_links:
    - from: "src/tests/boq-aggregator.test.ts"
      to: "aggregateBoq rates option"
      via: "rates: { 'Outlet|count': 5 } injection"
      pattern: "rates:"
---

<objective>
Wave 0 — establish the Nyquist test surface BEFORE any source changes. Extend / rewrite the existing test files so every downstream task in Waves 1-3 has a concrete failing test that proves it on completion. No production source is touched in this plan.

Purpose: GSD Nyquist sampling — each later task maps to one of these proofs (a-d in 15-VALIDATION.md). Writing them first prevents "test-after" drift and makes the perimeter two-row → one-row contract explicit before the aggregator is edited.

Output: Updated `boq-aggregator.test.ts`, `project-serialize.test.ts`, `project-schema.test.ts`, `boq-writers-xlsx.test.ts`, `boq-writers-csv.test.ts`, `use-boq-live.test.ts`, `totals-row-cycle.test.ts`, `totals-row-context-menu.test.ts`, and a NEW `totals-row-rate-edit.test.ts`. All new/changed assertions are RED (fail) until their Wave 1-3 task lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-boq-pricing-perimeter-simplification/15-CONTEXT.md
@.planning/phases/15-boq-pricing-perimeter-simplification/15-VALIDATION.md

<decisions_locked>
- TYPE RENAME (locked this phase): `BoqRowType` member `'perimeter-length'` → `'perimeter'`; member `'perimeter-area'` DELETED. The markup type is already `'perimeter'`, so row type now equals markup type. ALL test fixtures that used `type: 'perimeter-area'` or `type: 'perimeter-length'` become `type: 'perimeter'`.
- Rate map key string = `` `${name}|${type}` `` (category-INDEPENDENT). For a perimeter row the key is `Skirting|perimeter`.
- Cost = rate × quantity. Cost subtotal shape: `costSubtotal: number` on each `BoqCategoryGroup`; `grandTotalCost: number` on `BoqStructure`.
- A row whose key is absent from `rates` → `rate: 0, cost: 0` (never throws).
- Perimeter collision label: plain `{name}`; gains ` (perimeter)` ONLY when a same-named count/linear/area row exists in that category (D-02 shared rule).
</decisions_locked>

<interfaces>
<!-- These are the SHAPES the Wave 1-3 source tasks will create. Tests assert against them. -->
<!-- BoqItemRow gains two numeric fields: -->
//   rate: number   // ₱ unit rate for this (name,type); 0 when unset
//   cost: number   // rate × quantity
<!-- BoqCategoryGroup gains:  costSubtotal: number  (Σ row costs in the category) -->
<!-- BoqStructure gains:      grandTotalCost: number  (Σ all category costSubtotals) -->
<!-- AggregateOptions gains:  rates?: Record<string, number>  (default useProjectStore.getState().rates) -->
<!-- ProjectFileV2 gains:     rates?: Record<string, number>  (additive, NO formatVersion bump) -->
<!-- projectStore gains:      rates: Record<string, number> state + setRate(key, value) action (mirrors toggleHiddenItem incl. markDirty) -->
</interfaces>

<read_first>
- src/tests/boq-aggregator.test.ts — the file being rewritten; existing perimeter two-row cases at lines 73-93 (`'... synthesizes two rows'`) and 95-126 (`WR-07`); fixture `perimeterMarkup()` at lines 30-37 already builds `type: 'perimeter'` markups.
- src/tests/project-schema-hidden.test.ts — THE analog for the rates round-trip + absent-field-default tests. Clone its structure: `snapshotProject emits …`, `hydrateStores accepts …`, `hydrateStores defaults to {} when absent`. Same localStorage polyfill + MINIMAL_V2 fixture shape.
- src/tests/use-boq-live.test.ts — the render harness (`callHook()` at lines 19-52) + `recomputes when pageMarkups changes` test at 129-164. Mirror that harness for a new "recomputes when rates change" test.
- src/tests/boq-export-ipc.test.ts — the cross-process structural lock (STUB_STRUCT has empty categories, so the rename does NOT break it). Do NOT edit this file; just confirm it still compiles after Wave 1.
- src/tests/totals-row-cycle.test.ts and src/tests/totals-row-context-menu.test.ts — contain `'perimeter-area'` / `'perimeter-length'` fixtures to migrate to `'perimeter'`.
- src/renderer/src/lib/boq-aggregator.ts:107-275 — source of truth for the aggregator contract the tests assert against (bucket key `${name}|${type}` at line 109).
</read_first>

<interface_context>
Run `npx vitest run src/tests/boq-aggregator.test.ts` BEFORE editing to confirm the current 9 tests are green (baseline). The rewrite intentionally turns the two perimeter cases RED until Plan 15-03 lands.
</interface_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite aggregator perimeter cases + add rate/cost cases (proofs a, c)</name>
  <files>src/tests/boq-aggregator.test.ts</files>
  <read_first>
    - src/tests/boq-aggregator.test.ts (whole file — the two perimeter cases at :73 and :95, fixtures at :7-37)
    - src/renderer/src/lib/boq-aggregator.ts:107-275 (the contract: `add` key `${name}|${type}`, row build :217-224, subtotal loop :231-239)
  </read_first>
  <action>
    Rewrite the test `'perimeter markup synthesizes two rows: "(perimeter)" and "(area)"'` (currently :73): rename it to assert ONE row. With a single perimeter markup named 'Wall' and NO other 'Wall' markup in the category, expect `result.categories[0].items` length === 1, the single item label === 'Wall' (plain — no suffix, because perimeter now follows the collision rule and there is no collision), `item.type` === 'perimeter', `item.uom` === 'mm', `item.quantity` === 40. Assert NO item label contains '(area)'.

    Rewrite the WR-07 test (currently :95): KEEP the arc-aware LENGTH assertion (`perim.quantity` ≈ 300 + π·50) as the Phase-14 regression guard, but find the row by the plain label 'Curved' (no '(perimeter)' suffix — lone perimeter, no collision). DELETE the area row lookup and the area `toBeCloseTo` assertion entirely. Add `expect(result.categories[0].items).toHaveLength(1)` and assert no '(area)' label exists.

    Add a NEW test for the collision-suffix rule (proof c): one perimeter named 'Skirting' AND one linear named 'Skirting' in the SAME category → expect two rows with labels `['Skirting (linear)', 'Skirting (perimeter)']` (sorted). This proves perimeter joined the D-02 collision set and the type-word helper emits 'perimeter'.

    Add a NEW test for cost math (proof a): inject `rates: { 'Outlet|count': 5 }` into the `aggregateBoq({...})` opts alongside 3 'Outlet' count markups → expect the Outlet row `quantity` === 3, `rate` === 5, `cost` === 15.

    Add a NEW test for category cost subtotal + grand-total (proof a): two priced rows in one category (e.g. rates for two distinct `name|type` keys) → expect `categories[0].costSubtotal` === sum of the two row costs, and `result.grandTotalCost` === that same sum (single category). Add a second category with its own priced row and assert `grandTotalCost` === Σ both categories' `costSubtotal`.

    Add a NEW test for the no-rate-row case (proof a): a count markup whose `name|type` key is absent from `rates` (or pass no `rates` at all) → expect `rate` === 0, `cost` === 0, and the call does not throw.

    Add a NEW test for category-independence (proof b): the same `name|type` (e.g. `'Outlet|count'`) appearing in TWO different categories with one `rates` entry → both rows read `rate` === the single map value.
  </action>
  <verify>
    <automated>npx vitest run src/tests/boq-aggregator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 15-VALIDATION.md proofs (a) + (c). The new rate/cost/collision tests and the rewritten perimeter cases FAIL at runtime now (aggregator has no `rate`/`cost`/`costSubtotal`/`grandTotalCost`, still emits two perimeter rows) — this is the expected RED state. The file MUST still COMPILE (use `as any` casts on `rates`/`rate`/`cost`/`costSubtotal`/`grandTotalCost` if TypeScript complains before the types exist, mirroring how `project-schema-hidden.test.ts` casts to `any`). `git grep -n "Wall (area)\|perimeter-area\|Curved (area)" src/tests/boq-aggregator.test.ts` returns ZERO matches after the rewrite.
  </acceptance_criteria>
  <done>The two perimeter cases assert one row; the WR-07 length guard is intact; 6 new rate/cost/collision/independence cases exist; file compiles; the new and rewritten assertions are RED.</done>
</task>

<task type="auto">
  <name>Task 2: Add rates round-trip + schema-default + live-recompute + writer-column tests (proofs a, b)</name>
  <files>src/tests/project-serialize.test.ts, src/tests/project-schema.test.ts, src/tests/use-boq-live.test.ts, src/tests/boq-writers-xlsx.test.ts, src/tests/boq-writers-csv.test.ts</files>
  <read_first>
    - src/tests/project-schema-hidden.test.ts (whole file — clone its three-test shape for rates)
    - src/tests/use-boq-live.test.ts:19-52 (callHook harness) + :129-164 (recompute-on-change pattern)
    - src/tests/boq-writers-xlsx.test.ts + src/tests/boq-writers-csv.test.ts (existing column/subtotal assertions to extend)
    - src/main/boq-writers.ts:104-153 (xlsx builder) + :203-248 (csv builder) — the contract for the new Rate/Cost columns
  </read_first>
  <action>
    In `project-serialize.test.ts`: add a test that `snapshotProject(...)` emits `rates` read from projectStore (set `useProjectStore.setState({ rates: { 'Skirting|perimeter': 12 } })`, then expect `(snap as any).rates` to deep-equal that map). Add a hydrate round-trip test: `hydrateStores({ ...MINIMAL_V2, rates: { 'Outlet|count': 7 } })` → expect `useProjectStore.getState().rates` deep-equals `{ 'Outlet|count': 7 }`. Add a legacy-default test: `hydrateStores(MINIMAL_V2)` (no `rates` key) → expect `rates` === `{}`. Use `as any` casts mirroring `project-schema-hidden.test.ts`.

    In `project-schema.test.ts`: add a test that `validateV2` ACCEPTS an object containing a `rates` field without throwing, and a test that `validateV2` on a fixture LACKING `rates` passes (mirrors the pre-Phase-8 hiddenItemNames absent-field tolerance). Do NOT assert a thrown error for a malformed rates map at the validateV2 level — coercion is tested via hydrate (the locked decision keeps validateV2 throw-free for additive fields; the guard lives in hydrate/serialize per the threat model).

    In `use-boq-live.test.ts`: add a test `'recomputes when rates change'` using the existing `callHook()` harness: seed one priced count markup + a matching `rates` entry, capture the live `cost`, then `act(() => useProjectStore.setState({ rates: { ...newRate } }))` and assert the harness's `current()` cost updated. This is the live-subscription proof (b) — it FAILS until 15-04 adds `rates` to useBoqLive's selector + deps.

    In `boq-writers-xlsx.test.ts`: add assertions that the title row is `['Item','Quantity','UoM','Rate','Cost']`; that an item row's Cost cell (`getCell(5)`) `.value` is a NUMBER (not a string) and its `.numFmt` contains the ₱ glyph; that a category cost-subtotal row and a grand-total-cost row exist; and that the category-heading merge spans `A:E` (not `A:C`). Build the workbook via `buildBoqXlsx` with a fixture structure that includes `rate`/`cost`/`costSubtotal`/`grandTotalCost` (cast as needed).

    In `boq-writers-csv.test.ts`: add assertions that the header row contains `'Rate'` and `'Cost'`; that rate/cost are emitted as numeric values (Number, not a "₱…"-prefixed string); and that the UTF-8 BOM still leads line 1 (the output string starts with `'﻿'`).
  </action>
  <verify>
    <automated>npx vitest run src/tests/project-serialize.test.ts src/tests/project-schema.test.ts src/tests/use-boq-live.test.ts src/tests/boq-writers-xlsx.test.ts src/tests/boq-writers-csv.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 15-VALIDATION.md proofs (a) writer columns + (b) persistence/live. All newly added assertions FAIL at runtime now (no `rates` in store/schema/serialize; no Rate/Cost columns in writers; useBoqLive does not subscribe to rates) — expected RED. All five files COMPILE (use `as any` where the new fields are referenced before the types exist).
  </acceptance_criteria>
  <done>rates round-trip + absent-default tests exist (cloned from the hiddenItemNames analog); a rates live-recompute test exists; xlsx 5-column + ₱ numFmt + native-number + A:E merge assertions exist; csv Rate/Cost numeric + BOM assertions exist; all RED, all compile.</done>
</task>

<task type="auto">
  <name>Task 3: New inline-rate render test + migrate perimeter-type fixtures (proofs b, c)</name>
  <files>src/tests/totals-row-rate-edit.test.ts, src/tests/totals-row-cycle.test.ts, src/tests/totals-row-context-menu.test.ts</files>
  <read_first>
    - src/tests/totals-row-cycle.test.ts (the React.createElement + jsdom render harness to mirror; the `perimeter-area`/`perimeter-length` fixtures to migrate)
    - src/tests/totals-row-context-menu.test.ts (perimeter-type fixtures + a `.toFixed(2)` assertion)
    - src/renderer/src/components/TotalsRow.tsx:241-251 (lightbulb stopPropagation pattern the rate input mirrors) + :122-125 (the visibility `itemKey` that must NOT be reused for the rate key)
  </read_first>
  <action>
    Create `src/tests/totals-row-rate-edit.test.ts` as a render test mirroring the `totals-row-cycle.test.ts` harness (React.createElement + jsdom + the localStorage polyfill pattern used across the totals-row tests). It must assert: (1) typing into the rate `<input>` (find via a `data-testid="totals-row-rate-input"`) and committing (fire `change` then `blur`, and separately `keydown` Enter) calls `useProjectStore.getState().setRate` with the category-INDEPENDENT key `` `${name}|${type}` `` (e.g. `'Outlet|count'`) and the parsed number; (2) the row's `onArmTool` / page-navigation does NOT fire when interacting with the input (spy on the `onArmTool` prop and on `setPage`; assert neither is called after an input click/keydown — proving `e.stopPropagation()`). Seed `useProjectStore.setState({ rates: {} , setRate: vi.fn(), ... })` or spy on the real `setRate`.

    In `totals-row-cycle.test.ts`: change every `type: 'perimeter-area'` and `type: 'perimeter-length'` fixture to `type: 'perimeter'`. Keep the assertion that a perimeter row resolves to underlying markup type `'perimeter'` via `rowTypeToMarkupType`.

    In `totals-row-context-menu.test.ts`: change `type: 'perimeter-length'` fixtures to `type: 'perimeter'`; keep the `.toFixed(2)` behavior assertion unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/tests/totals-row-rate-edit.test.ts src/tests/totals-row-cycle.test.ts src/tests/totals-row-context-menu.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 15-VALIDATION.md proof (b) inline-field + the perimeter-type migration for proof (c). `totals-row-rate-edit.test.ts` FAILS at runtime (TotalsRow has no rate input yet) but COMPILES. After migration, `git grep -n "perimeter-area\|perimeter-length" src/tests/totals-row-cycle.test.ts src/tests/totals-row-context-menu.test.ts` returns ZERO matches. The two migrated files still PASS where they assert non-perimeter behavior (the type string change is the only edit).
  </acceptance_criteria>
  <done>A RED inline-rate render test exists asserting setRate dispatch + stopPropagation; both totals-row test files use `'perimeter'` (no split types); migrated files compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only edits test files; no runtime trust boundary is added or crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | test fixtures encoding the wrong perimeter contract | mitigate | Tests assert the LOCKED one-row + collision-suffix contract directly, so a regression to two-row synthesis fails CI. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — Wave 0 uses the existing Vitest 4.1.x infra (15-VALIDATION.md: "no new framework install"). No legitimacy gate needed. |
</threat_model>

<verification>
- `npx vitest run src/tests/boq-aggregator.test.ts src/tests/project-serialize.test.ts src/tests/project-schema.test.ts src/tests/use-boq-live.test.ts src/tests/boq-writers-xlsx.test.ts src/tests/boq-writers-csv.test.ts src/tests/totals-row-rate-edit.test.ts src/tests/totals-row-cycle.test.ts src/tests/totals-row-context-menu.test.ts` — RED assertions are present and the suite COMPILES (TypeScript errors === 0; runtime failures are the expected RED).
- `npx tsc --noEmit` (or `npm run typecheck`) is clean — no compile errors introduced by the `as any` casts.
- `git grep -n "perimeter-area\|perimeter-length" src/tests` returns ZERO matches.
</verification>

<success_criteria>
- All Wave 1-3 source tasks have at least one failing test pointing at them.
- The perimeter two-row assertions are rewritten to one-row; the WR-07 arc length guard is intact.
- A new inline-rate render test asserts setRate dispatch + stopPropagation.
- Type-check clean; no `perimeter-area`/`perimeter-length` tokens remain in tests.
</success_criteria>

<output>
Create `.planning/phases/15-boq-pricing-perimeter-simplification/15-01-SUMMARY.md` when done.
</output>
