---
context: phase
phase: 07-canvas-workspace-ux-and-markup-fixes
task: 0
total_tasks: 0
status: pre-planning
last_updated: 2026-05-12T10:20:17.911Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No constraints discovered this session through failure. Section kept for resuming agent awareness._

**Do not proceed until you have read the required reading below.**

<current_state>
Phase 7 UI design contract approved and committed. No plans exist yet.

This session completed /gsd-ui-phase 7 end-to-end:
- gsd-ui-researcher wrote 07-UI-SPEC.md (commit 8efd222)
- gsd-ui-checker returned BLOCKED on Dimension 1: "Cancel" generic CTA in two places
- gsd-ui-researcher revised: "Cancel" → "Discard Changes" (MarkupNamePopup edit mode) and "Discard Scale" (CalibrationDialog) (commit b1a6ce9)
- gsd-ui-checker re-ran: all 6 dimensions PASS
- Frontmatter updated to `status: approved` (commit 39b1e67)

Ready to plan. Next command: `/gsd-plan-phase 7`
</current_state>

<completed_work>
- Task 1: Phase 7 UI-SPEC — DONE (commits 8efd222, b1a6ce9, 39b1e67)
  - All 6 checker dimensions approved on revision 1
  - Key copywriting contract: "Discard Changes" / "Discard Scale" (not "Cancel")
  - Design system: inline-style + COLORS constants (no Tailwind, no shadcn)
  - Zero new color tokens, zero new font sizes, zero new icon imports
</completed_work>

<remaining_work>
- Plan Phase 7 — no PLAN.md exists yet. Run `/gsd-plan-phase 7`.
  The researcher has CONTEXT.md + UI-SPEC.md to draw from — 5 well-specified fixes.
</remaining_work>

<decisions_made>
- MarkupNamePopup edit-mode secondary CTA: "Discard Changes" (not "Cancel") — blocked by UI checker
- CalibrationDialog secondary CTA: "Discard Scale" (not "Cancel") — same rule
- All other decisions carried from 07-CONTEXT.md verbatim (D-01 through D-13)
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)

1. `.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-CONTEXT.md` — all 13 implementation decisions (D-01 through D-13), canonical file references, and code context. Primary source of truth for the plan.
2. `.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-UI-SPEC.md` — approved UI design contract. Planner uses this for spacing/copy/color/interaction contracts.
3. `.planning/STATE.md` §Key Decisions — command pattern, MarkupCommand shape, parent-owned lifecycle. Critical anti-patterns to not regress.

## Critical Anti-Patterns (do NOT repeat these)

- **Generic "Cancel" CTAs**: UI checker blocks any CTA labelled "Cancel", "Submit", "OK", "Save" (bare). Use specific verb+noun: "Discard Changes", "Discard Scale", "Save Changes", "Accept Scale". This was the only BLOCK this session.

## Infrastructure State

- Dev server: not running
- No background processes
- Working tree: clean (all changes committed)

<context>
Session was clean and fast — UI-SPEC only, no code changes. The 5 fixes in Phase 7 are well-defined in CONTEXT.md with precise file paths and line numbers for each affected component. The plan phase should be able to break this into 3–5 plans cleanly:

1. Canvas gutter fix (D-01/D-02/D-03) — structural, low risk, no new components
2. Post-commit markup editing (D-04 through D-07) — MarkupContextMenu + MarkupNamePopup + EditMarkupCommand
3. Totals panel cleanup (D-08/D-09/D-10) — pure deletion, no new code
4. Set Scale dropdown overflow (D-11) — CSS stacking context diagnosis + fix
5. Category deduplication UX (D-12/D-13) — keyboard nav + canonical-name substitution on confirm

The UI-SPEC was approved with the design system as "manual (inline-style + COLORS constants)" — no Tailwind or shadcn in the canvas/panel path.
</context>

<next_action>
Start with: `/clear` then `/gsd-plan-phase 7`
</next_action>
