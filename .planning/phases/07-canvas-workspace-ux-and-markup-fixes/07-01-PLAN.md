---
phase: 07-canvas-workspace-ux-and-markup-fixes
plan: "01"
type: execute
wave: 1
depends_on:
  - "07-00"
files_modified:
  - src/renderer/src/components/CanvasViewport.tsx
  - src/renderer/src/components/TotalsPanel.tsx
  - src/renderer/src/components/TotalsCategoryBlock.tsx
  - src/renderer/src/components/CalibrationDialog.tsx
autonomous: true
requirements:
  - UI-01
  - VIEW-01

must_haves:
  truths:
    - "CanvasViewport root containerRef div uses position: absolute with inset: 0 (not width/height 100%)"
    - "TotalsPanel grand-total bar (data-testid=totals-panel-grand-total) is absent from the DOM"
    - "TotalsCategoryBlock subtotal rows (data-testid=totals-subtotal-row) are absent from the DOM"
    - "CalibrationDialog overlay div has isolation: isolate"
    - "CalibrationDialog secondary button label is exactly 'Discard Scale' (not 'Cancel')"
    - "Vitest suite passes with the Wave 0 RED tests now GREEN for totals assertions"
  artifacts:
    - path: "src/renderer/src/components/CanvasViewport.tsx"
      provides: "inset-0 canvas fill fix"
      contains: "position: 'absolute'"
    - path: "src/renderer/src/components/TotalsPanel.tsx"
      provides: "grand-total bar removed"
    - path: "src/renderer/src/components/TotalsCategoryBlock.tsx"
      provides: "subtotal rows removed"
    - path: "src/renderer/src/components/CalibrationDialog.tsx"
      provides: "isolation: isolate + Discard Scale label"
      contains: "Discard Scale"
  key_links:
    - from: "src/renderer/src/components/CanvasViewport.tsx"
      to: "App.tsx center column div (position: relative)"
      via: "position: absolute; inset: 0 fills positioned ancestor"
      pattern: "position.*absolute.*inset.*0"
    - from: "src/renderer/src/components/TotalsPanel.tsx"
      to: "boq-aggregator.ts grandTotal output"
      via: "aggregator still produces grandTotals field — only render block deleted"
      pattern: "totals-panel-grand-total"
---

<objective>
Deliver three parallel-safe Wave 1 fixes: canvas gutter elimination, totals panel cleanup, and CalibrationDialog dropdown + label fix.

Purpose: These three fixes have zero inter-dependencies and zero file overlap — they can execute in one agent pass. Wave 0 established the RED tests for totals; this wave turns them GREEN. The canvas fix and CalibrationDialog fix have manual UAT verification only.

Output: CanvasViewport root div uses inset:0 (eliminates 800×600 Stage lock); TotalsPanel and TotalsCategoryBlock lose their surplus render blocks; CalibrationDialog gains isolation:isolate on its overlay and renames its abandon button.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-CONTEXT.md
@.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-RESEARCH.md
@.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-PATTERNS.md
@.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-UI-SPEC.md
@.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-00-SUMMARY.md

<interfaces>
<!-- Key contracts extracted from PATTERNS.md and RESEARCH.md -->

From CanvasViewport.tsx (PATTERNS.md §D-02):
Root containerRef div current style (lines 526–538):
  style={{ width: '100%', height: '100%', backgroundColor: '#141414', backgroundImage: '...', backgroundSize: '20px 20px', overflow: 'hidden', cursor: getCursor(), position: 'relative' }}
TARGET after fix:
  style={{ position: 'absolute', inset: 0, backgroundColor: '#141414', backgroundImage: '...', backgroundSize: '20px 20px', overflow: 'hidden', cursor: getCursor() }}
NOTE: Remove position: 'relative' — position: 'absolute' replaces it. The div's absolute children (CalibrationDialog, MarkupNamePopup, etc.) use their parent's padding-box as their containing block, which works correctly because a position:absolute element IS a containing block for its positioned children.

From TotalsPanel.tsx (PATTERNS.md §D-08):
Delete entire block lines 282–323: the `{emptyMsg === null && (<div data-testid="totals-panel-grand-total" ...>...</div>)}` conditional.
The closing </div> at line 324 (outer panel div) must NOT be deleted.

From TotalsCategoryBlock.tsx (PATTERNS.md §D-09):
Delete entire block lines 142–188: the `{category.subtotals.map((sub) => (<div data-testid="totals-subtotal-row" ...>...</div>))}` block.
The </> fragment closing tag at line 190 and outer {!isCollapsed && (...)} structure are preserved.

From CalibrationDialog.tsx (PATTERNS.md §D-11):
Overlay div (lines 52–65) currently:
  style={{ position: 'absolute', inset: 0, background: 'rgba(0,0,0,0.55)', display: 'flex', alignItems: 'center', justifyContent: 'center', zIndex: 10 }}
Add `isolation: 'isolate'` to this style object.
Secondary button (line 175) currently: Cancel
Change to: Discard Scale
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canvas gutter fix (D-02) — CanvasViewport.tsx root div inset:0</name>
  <files>src/renderer/src/components/CanvasViewport.tsx</files>
  <read_first>
    - src/renderer/src/components/CanvasViewport.tsx — read the root containerRef div style object (around lines 526–538 per RESEARCH.md/PATTERNS.md) to confirm the exact current style shape before modifying. Also scan for any other occurrence of the string 'width: \'100%\'' or 'height: \'100%\'' on the containerRef div to ensure you find the right element.
  </read_first>
  <action>
    Locate the root containerRef div in CanvasViewport.tsx — it is the outermost JSX div that has `ref={containerRef}` and currently has `style={{ width: '100%', height: '100%', ... }}`.

    Make exactly two changes to this div's style prop:
    1. Replace `width: '100%'` with `position: 'absolute'`
    2. Replace `height: '100%'` with `inset: 0`
    3. Remove `position: 'relative'` from the style object entirely (it is now superseded by position: 'absolute')

    All other style properties on this div — backgroundColor, backgroundImage, backgroundSize, overflow, cursor — are preserved verbatim. No other lines in CanvasViewport.tsx are changed.

    No changes to the ResizeObserver effect, Stage sizing, containerSize state default, or any other logic.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CanvasViewport.tsx containerRef div style contains `position: 'absolute'` and `inset: 0`
    - CanvasViewport.tsx containerRef div style does NOT contain `width: '100%'` or `height: '100%'`
    - CanvasViewport.tsx containerRef div style does NOT contain `position: 'relative'`
    - All other style properties on that div are unchanged (backgroundColor, backgroundImage, backgroundSize, overflow, cursor all present)
    - npx vitest run exits with the same pass/fail count as after Wave 0 (no new failures, no regressions)
  </acceptance_criteria>
  <done>
    CanvasViewport.tsx root div uses position:absolute + inset:0. Full Vitest suite passes or fails only on the pre-existing Wave 0 RED stubs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Totals panel cleanup (D-08, D-09) — remove grand-total bar and subtotal rows; turn Wave 0 totals RED tests GREEN</name>
  <files>src/renderer/src/components/TotalsPanel.tsx, src/renderer/src/components/TotalsCategoryBlock.tsx</files>
  <read_first>
    - src/renderer/src/components/TotalsPanel.tsx — read lines 275–330 to see the exact grand-total bar block with its opening conditional, the data-testid="totals-panel-grand-total" div, and the closing boundary. Identify the exact line numbers of the block to delete.
    - src/renderer/src/components/TotalsCategoryBlock.tsx — read lines 135–195 to see the subtotals.map() block with data-testid="totals-subtotal-row", the opening map call, and the closing boundary. Identify the exact line numbers of the block to delete.
  </read_first>
  <action>
    In TotalsPanel.tsx: delete the entire grand-total bar conditional block. This block begins with a JSX comment `{/* Pinned grand-total bar */}` or similar and the opening `{emptyMsg === null && (` that wraps the `<div data-testid="totals-panel-grand-total"` element. Delete from the opening comment/conditional through to the closing `)}` of that conditional, inclusive. Do NOT delete the enclosing outer panel div's closing tag (which appears on the line immediately after the conditional block).

    Verify by searching for `data-testid="totals-panel-grand-total"` — it must not appear in the file after the edit.

    In TotalsCategoryBlock.tsx: delete the entire subtotals render block. This block is `{category.subtotals.map((sub) => (<div ... data-testid="totals-subtotal-row" ...>...</div>))}`. Delete from the JSX comment above the block (if present) through to the closing `})}` inclusive. Do NOT delete the `</>` fragment closing tag or the `{!isCollapsed && (...)}` outer conditional structure.

    Verify by searching for `data-testid="totals-subtotal-row"` — it must not appear in the file after the edit.

    Do NOT touch boq-aggregator.ts. The subtotals and grandTotals data fields remain in the aggregator output for the export pipeline.
  </action>
  <verify>
    <automated>npx vitest run src/tests/totals-panel-render.test.ts src/tests/totals-panel-category-collapse.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - TotalsPanel.tsx does NOT contain the string `data-testid="totals-panel-grand-total"` (grep confirms 0 matches)
    - TotalsCategoryBlock.tsx does NOT contain the string `data-testid="totals-subtotal-row"` (grep confirms 0 matches)
    - npx vitest run src/tests/totals-panel-render.test.ts exits 0 — the 'grand-total bar is absent (D-08 cleanup)' test is now GREEN
    - npx vitest run src/tests/totals-panel-category-collapse.test.ts exits 0 — all three inverted subtotal-row assertions are now GREEN
    - boq-aggregator.ts is unchanged (do not read or touch it)
  </acceptance_criteria>
  <done>
    TotalsPanel and TotalsCategoryBlock render no subtotal/grand-total DOM elements. Wave 0 totals RED tests are GREEN. Both test files exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: CalibrationDialog dropdown overflow fix + 'Discard Scale' label (D-11, Landmine 8)</name>
  <files>src/renderer/src/components/CalibrationDialog.tsx</files>
  <read_first>
    - src/renderer/src/components/CalibrationDialog.tsx — read the full file to locate: (a) the overlay div with position:absolute + inset:0 + zIndex:10 (lines 52–65 per PATTERNS.md), (b) the native select element (lines 125–144), and (c) the secondary button with text 'Cancel' (line 175). Confirm exact current style shape of the overlay div before editing.
  </read_first>
  <action>
    Change 1 — overlay div (Option A stacking-context fix, per D-11 priority order):
    Locate the outermost div in CalibrationDialog that has `position: 'absolute'`, `inset: 0`, `background: 'rgba(0,0,0,0.55)'`, and `zIndex: 10`. Add `isolation: 'isolate'` to its style object. This creates a new stacking context so the native select dropdown popout draws above the backdrop.

    Change 2 — secondary button label (Landmine 8, mandatory per D-11 task scope):
    Locate the secondary button that currently renders the text 'Cancel'. Change the text content from 'Cancel' to 'Discard Scale'. This is the abandon/dismiss button for the calibration dialog. The UI-SPEC Copywriting Contract explicitly blocks 'Cancel' and requires 'Discard Scale'.

    No other changes to CalibrationDialog.tsx. Do not change the primary 'Accept Scale' button, the header 'Set Scale', the unit select options, any input fields, or any other styles.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CalibrationDialog.tsx overlay div style includes `isolation: 'isolate'`
    - CalibrationDialog.tsx does NOT contain the string `'Cancel'` as a button text (search for `Cancel` — must find zero matches in button text contexts)
    - CalibrationDialog.tsx secondary button text is exactly `Discard Scale`
    - npx vitest run exits with no new test failures beyond the remaining Wave 0 RED stubs
  </acceptance_criteria>
  <done>
    CalibrationDialog.tsx overlay has isolation:isolate. Secondary button reads 'Discard Scale'. Vitest suite exits with only the pre-existing Wave 0 RED stubs failing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer UI → Zustand store | TotalsPanel reads from boq-aggregator via useBoqLive; deletion of render blocks doesn't touch the store boundary |
| CanvasViewport CSS | CSS change only — no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | TotalsPanel render deletion | accept | boq-aggregator.ts untouched — grandTotals/subtotals data still available for BOQ export; only render blocks removed; no state mutation involved |
| T-07-01-02 | Tampering | CalibrationDialog stacking context | accept | isolation:isolate is a CSS presentation property; no security boundary crossed; affects only visual z-ordering of the native select dropdown |
| T-07-01-03 | Elevation of Privilege | CanvasViewport position change | accept | CSS position:absolute with inset:0 fills the positioned parent; no new event capture or pointer interception; existing overflow:hidden and Konva event handling unchanged |
</threat_model>

<verification>
After Wave 1 completes:
- npx vitest run src/tests/totals-panel-render.test.ts — exits 0 (D-08 GREEN)
- npx vitest run src/tests/totals-panel-category-collapse.test.ts — exits 0 (D-09 GREEN)
- npx vitest run exits non-zero only due to the Wave 0 RED stubs for EditMarkupCommand and MarkupNamePopup mode='edit' (not yet implemented)
- Manual: open app with a PDF loaded, verify canvas fills the window center column end-to-end at 1080p, no dark gutter on right/bottom
- Manual: open Set Scale modal, click unit dropdown, confirm the dropdown list is fully visible and not clipped/hidden
- Manual: confirm CalibrationDialog secondary button reads 'Discard Scale'
</verification>

<success_criteria>
CanvasViewport containerRef div uses position:absolute + inset:0. TotalsPanel and TotalsCategoryBlock contain no grand-total or subtotal DOM elements. CalibrationDialog overlay has isolation:isolate and secondary button reads 'Discard Scale'. Wave 0 totals RED tests are now GREEN. TypeScript builds without error.
</success_criteria>

<output>
After completion, create `.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-01-SUMMARY.md`
</output>
