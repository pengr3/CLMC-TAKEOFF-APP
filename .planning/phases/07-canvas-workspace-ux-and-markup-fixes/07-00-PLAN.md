---
phase: 07-canvas-workspace-ux-and-markup-fixes
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/tests/markup-commands.test.ts
  - src/tests/markup-namepopup.test.ts
  - src/tests/markup-context-menu.test.ts
  - src/tests/totals-panel-render.test.ts
  - src/tests/totals-panel-category-collapse.test.ts
autonomous: true
requirements:
  - MARK-03
  - VIEW-01

must_haves:
  truths:
    - "markup-commands.test.ts has RED failing tests for EditMarkupCommand execute/undo/redo symmetry"
    - "markup-namepopup.test.ts has RED failing tests for mode='edit' labels, pre-fill, and D-13 canonical substitution"
    - "markup-context-menu.test.ts has RED failing tests for Edit as first menuitem and onEdit callback"
    - "totals-panel-render.test.ts asserts grand-total bar IS NULL (flipped from not.toBeNull)"
    - "totals-panel-category-collapse.test.ts asserts subtotal rows ARE NULL (updated assertions)"
    - "Full Vitest suite exits with only the new RED failures (no pre-existing failures introduced)"
  artifacts:
    - path: "src/tests/markup-commands.test.ts"
      provides: "EditMarkupCommand RED test suite"
      contains: "editMarkup/undo symmetry"
    - path: "src/tests/markup-namepopup.test.ts"
      provides: "mode='edit' and D-13 RED test suite"
      contains: "mode='edit' labels"
    - path: "src/tests/markup-context-menu.test.ts"
      provides: "Edit menuitem RED test suite"
      contains: "Edit button as first menuitem"
    - path: "src/tests/totals-panel-render.test.ts"
      provides: "grand-total bar absence assertion"
      contains: "grand-total bar is absent"
    - path: "src/tests/totals-panel-category-collapse.test.ts"
      provides: "subtotal-row absence assertions"
      contains: "toBeNull"
  key_links:
    - from: "src/tests/markup-commands.test.ts"
      to: "src/renderer/src/stores/markupStore.ts"
      via: "useMarkupStore.getState().editMarkup()"
      pattern: "editMarkup"
    - from: "src/tests/markup-namepopup.test.ts"
      to: "src/renderer/src/components/MarkupNamePopup.tsx"
      via: "React.createElement(MarkupNamePopup, { mode: 'edit' })"
      pattern: "mode.*edit"
    - from: "src/tests/markup-context-menu.test.ts"
      to: "src/renderer/src/components/MarkupContextMenu.tsx"
      via: "onEdit prop"
      pattern: "onEdit"
---

<objective>
Establish RED test stubs and update broken assertions for all Phase 7 behavioral changes.

Purpose: Wave 0 creates the failing test baseline that subsequent implementation waves must turn GREEN. This is the TDD anchor for the phase — tests fail now because the code doesn't exist yet; they pass after each implementation wave.

Output: Five modified test files. Three files gain new RED describe blocks (markup-commands, markup-namepopup, markup-context-menu). Two files get assertion inversions that will become GREEN only after the D-08/D-09 implementation in Wave 1.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-CONTEXT.md
@.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-RESEARCH.md
@.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-PATTERNS.md
@.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED stubs for EditMarkupCommand (markup-commands.test.ts) and MarkupContextMenu Edit item (markup-context-menu.test.ts)</name>
  <files>src/tests/markup-commands.test.ts, src/tests/markup-context-menu.test.ts</files>
  <read_first>
    - src/tests/markup-commands.test.ts — read full file to understand existing describe block structure, beforeEach reset shape, makeCount helper, and existing symmetry test pattern (place/undo/redo blocks)
    - src/tests/markup-context-menu.test.ts — read full file to understand the mount() harness, baseProps shape (screenPos, currentColor, onRecolor, onDelete, onClose), and existing Delete button test pattern
    - src/renderer/src/stores/markupStore.ts — read to understand the MarkupStoreState interface and confirm editMarkup action does NOT yet exist (these are RED stubs)
    - src/renderer/src/components/MarkupContextMenu.tsx — read to confirm onEdit prop does NOT yet exist in MarkupContextMenuProps interface
  </read_first>
  <behavior>
    markup-commands.test.ts new describe blocks:
    - "editMarkup/undo symmetry" → it("editMarkup changes name/categoryId/color and pushes cmd to undoStack") — calls useMarkupStore.getState().editMarkup(...) on a placed markup; asserts new name/color visible and undoStack.length === 1. MUST FAIL because editMarkup does not exist.
    - "editMarkup/undo symmetry" → it("undo() reverts to oldName/oldCategory/oldColor; redoStack has one entry") — calls undo(); asserts original values restored and redoStack.length === 1. MUST FAIL.
    - "editMarkup/undo symmetry" → it("redo() re-applies newName/newCategory/newColor") — calls redo(); asserts new values restored and undoStack.length === 1. MUST FAIL.
    - "editMarkup/undo symmetry" → it("editMarkup clears redoStack") — pre-seeds redoStack; calls editMarkup; asserts redoStack.length === 0. MUST FAIL.
    - "editMarkup/undo symmetry" → it("editMarkup on nonexistent markupId is a no-op") — calls editMarkup with unknown id; asserts undoStack.length === 0. MUST FAIL.
    - "editMarkup canonical category (D-13 store side)" → it("edit to existing category name reuses the category (no duplicate created)") — seeds 'Electrical' category, calls editMarkup with 'electrical' (lowercase); asserts Object.keys(categories).length is unchanged. MUST FAIL.
    - "editMarkup canonical category (D-13 store side)" → it("edit to new category name creates the category") — calls editMarkup with brand-new name; asserts category count increases by 1. MUST FAIL.
    markup-context-menu.test.ts new tests inside existing describe:
    - it("renders Edit button as first menuitem before Delete") — querySelectorAll('[role="menuitem"]'), asserts menuitems[0].textContent === 'Edit'. MUST FAIL because onEdit prop does not exist and Edit item is not rendered yet.
    - it("clicking Edit calls onEdit then onClose") — fires click on the Edit button; asserts onEdit called and onClose called. MUST FAIL.
    All existing tests in both files must remain unchanged and must continue to PASS.
    Each new test must use the same imports, beforeEach reset pattern, and mount() harness as the existing tests in the respective file.
    markup-context-menu.test.ts: add onEdit: vi.fn() to baseProps (or a new basePropsWithEdit variant used only by the new tests) so existing tests that don't pass onEdit don't break.
  </behavior>
  <action>
    In markup-commands.test.ts: append two new describe blocks after the last existing describe block. Use the existing makeCount helper and getOrCreateCategory pattern (lines visible after reading the file). The beforeEach reset (pageMarkups: {}, categories: {}, categoryOrder: [], undoStack: [], redoStack: []) must be scoped to each describe or inherited from the file-level beforeEach — mirror the existing pattern exactly.

    For editMarkup call signature, use: useMarkupStore.getState().editMarkup(markupId, page, oldName, oldCategoryName, oldColor, newName, newCategoryName, newColor). This will throw "editMarkup is not a function" making all tests RED as required.

    In markup-context-menu.test.ts: add onEdit: vi.fn() to the existing baseProps const (NOT as a separate var — add the prop directly so all mounts receive it). This keeps existing tests green because MarkupContextMenu currently ignores unknown props via TypeScript's structural typing at JS runtime. Then add the two new it() blocks inside the existing top-level describe. Mount MarkupContextMenu with { ...baseProps } which now includes onEdit.

    Do NOT modify any existing it() blocks in either file. Do NOT add imports that aren't already present except vi from vitest if not already imported.
  </action>
  <verify>
    <automated>npx vitest run src/tests/markup-commands.test.ts src/tests/markup-context-menu.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>
    markup-commands.test.ts: 7 new tests in 2 new describe blocks ALL FAIL with "editMarkup is not a function" or similar. All pre-existing tests PASS.
    markup-context-menu.test.ts: 2 new tests FAIL (Edit button not found in DOM). All pre-existing tests PASS.
    No TypeScript compile errors in the test files themselves (runtime failures only).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED stubs for MarkupNamePopup mode='edit' + D-13 (markup-namepopup.test.ts) and assertion inversions for totals panel (totals-panel-render.test.ts + totals-panel-category-collapse.test.ts)</name>
  <files>src/tests/markup-namepopup.test.ts, src/tests/totals-panel-render.test.ts, src/tests/totals-panel-category-collapse.test.ts</files>
  <read_first>
    - src/tests/markup-namepopup.test.ts — read full file: mount() harness shape, imports, localStorage polyfill pattern in beforeEach, existing describe blocks for mode='count-pre' and 'save-after', the MARKUP_PALETTE import, the React.createElement call structure
    - src/tests/totals-panel-render.test.ts — read lines 237–270 specifically: the 'grand-total bar shows per-UoM totals at the bottom' test that asserts bar is NOT null (Landmine 7 — this assertion must flip)
    - src/tests/totals-panel-category-collapse.test.ts — read lines 105–200 specifically: any test asserting 'totals-subtotal-row' data-testid is present (lines 123, 169, 189 per PATTERNS.md)
    - src/renderer/src/components/MarkupNamePopup.tsx — read to confirm mode union is currently 'count-pre' | 'save-after' only (mode='edit' does not exist yet)
  </read_first>
  <behavior>
    markup-namepopup.test.ts new describe blocks:
    - "MarkupNamePopup — mode='edit' labels (D-06)":
      - it("renders 'Edit markup' aria-label in edit mode") — mounts with mode='edit'; queries [role="dialog"]; asserts getAttribute('aria-label') === 'Edit markup'. MUST FAIL because mode='edit' is not accepted.
      - it("primary button reads 'Save Changes' in edit mode") — asserts button textContent includes 'Save Changes'. MUST FAIL.
      - it("cancel button reads 'Discard Changes' in edit mode") — asserts button textContent includes 'Discard Changes'. MUST FAIL.
      - it("pre-fills name/categoryName/color from initialName/initialCategoryName/initialColor props") — mounts with initialName='Switch', initialCategoryName='Electrical', initialColor=MARKUP_PALETTE[3]; asserts name input value === 'Switch'. MUST FAIL.
    - "MarkupNamePopup — D-13 canonical substitution":
      - it("handleConfirm substitutes canonical name when findCategoryByName returns a match") — seeds store with category 'Electrical'; mounts with initialCategoryName='electrical'; clicks Save; asserts onConfirm was called with categoryName === 'Electrical'. MUST FAIL (findCategoryByName not yet wired into handleConfirm).
      - it("handleConfirm uses typed name verbatim when no case-insensitive match exists") — no seeded categories; asserts onConfirm called with categoryName === 'Plumbing'. This test MUST PASS immediately (existing behavior passes through typed name).

    totals-panel-render.test.ts:
    - In the 'grand-total bar shows per-UoM totals at the bottom' test (line 237), change the it() description to 'grand-total bar is absent (D-08 cleanup)' and change the assertions: replace expect(bar).not.toBeNull() with expect(bar).toBeNull() and remove the follow-on assertions about bar.textContent. This test becomes RED immediately because the grand-total bar still exists in TotalsPanel.tsx before the D-08 wave.

    totals-panel-category-collapse.test.ts:
    - In 'renders heading + items + subtotal expanded by default' (line 105): change expect(subtotal).not.toBeNull() to expect(subtotal).toBeNull() — RED because subtotal rows still render before D-09.
    - In 'heading click toggles collapsed state...' (lines 158–193): change the two expect(...).not.toBeNull() assertions for totals-subtotal-row to expect(...).toBeNull() — both become RED.
    - Keep the fixtureCategory subtotals data intact (do NOT remove subtotals: [{ uom: 'ea', total: 17 }] from the fixture) — the data present but not rendered is the intended test after D-09.
  </behavior>
  <action>
    In markup-namepopup.test.ts: append the two new describe blocks after all existing describes. Use React.createElement(MarkupNamePopup, { mode: 'edit' as const, screenPos: { x: 100, y: 100 }, containerSize: { width: 1000, height: 800 }, initialName: 'Switch', initialCategoryName: 'Electrical', initialColor: MARKUP_PALETTE[3], onConfirm: vi.fn(), onCancel: vi.fn() }) following the existing createElement pattern in the file. For the D-13 canonical test: seed store via useMarkupStore.getState().getOrCreateCategory('Electrical') before mounting. Get the onConfirm spy from the mount props. Trigger confirm by finding the primary button and calling .click() inside act(). Assert spy.mock.calls[0][0].categoryName === 'Electrical'.

    In totals-panel-render.test.ts: change ONLY the it() description string and the two assertion lines (the bar variable query line can remain; just change the assertion from .not.toBeNull() to .toBeNull() and remove the textContent assertions that reference bar!.textContent). Do NOT change any other tests in this file.

    In totals-panel-category-collapse.test.ts: find the three assertion lines that use expect(...).not.toBeNull() on the totals-subtotal-row data-testid and invert them to .toBeNull(). Remove any follow-on lines that call .textContent on the subtotal element after those assertions (since they'd throw on null). Do NOT change the fixture data, imports, or any other tests.
  </action>
  <verify>
    <automated>npx vitest run src/tests/markup-namepopup.test.ts src/tests/totals-panel-render.test.ts src/tests/totals-panel-category-collapse.test.ts --reporter=verbose 2>&1 | tail -50</automated>
  </verify>
  <done>
    markup-namepopup.test.ts: 5 new tests — 4 FAIL (mode='edit' not accepted), 1 PASSES (D-13 verbatim pass-through). All pre-existing tests PASS.
    totals-panel-render.test.ts: the updated 'grand-total bar is absent' test FAILS (bar is still not-null before D-08). All other tests PASS.
    totals-panel-category-collapse.test.ts: 3 updated assertions FAIL (subtotal rows still rendered before D-09). All other tests PASS.
    Running npx vitest run exits non-zero due to the new RED tests and the inverted assertions. Pre-existing pass count is unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → store | Tests call useMarkupStore directly in node env; no IPC boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-00-01 | Tampering | test beforeEach reset | accept | Tests reset store state in beforeEach — no cross-test pollution; isolation is structural |
| T-07-00-02 | Tampering | baseProps mutation in markup-context-menu.test.ts | mitigate | Add onEdit to baseProps const so all existing tests still pass; onEdit is a no-op vi.fn() that doesn't alter behavior of existing assertions |
</threat_model>

<verification>
After Wave 0 completes:
- npx vitest run exits non-zero (expected — RED tests exist)
- Pre-existing test count is unchanged (no regressions introduced)
- New failing tests are ONLY the ones introduced in this plan
- No TypeScript errors in the test files (runtime failures only, not compile errors)
</verification>

<success_criteria>
All five test files modified. New RED tests in markup-commands.test.ts (7), markup-namepopup.test.ts (5 net new, 1 passes), markup-context-menu.test.ts (2). Two assertion inversions in totals-panel-render.test.ts (1 test flipped RED) and totals-panel-category-collapse.test.ts (3 assertions flipped RED). Zero pre-existing test regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-00-SUMMARY.md`
</output>
