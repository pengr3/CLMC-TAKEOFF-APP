---
phase: 07-canvas-workspace-ux-and-markup-fixes
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/renderer/src/components/CategoryAutocomplete.tsx
  - src/renderer/src/components/MarkupNamePopup.tsx
autonomous: true
requirements:
  - MARK-03

must_haves:
  truths:
    - "CategoryAutocomplete accepts highlightedIndex and onHighlightChange props"
    - "Keyboard-highlighted row renders with COLORS.hoverSurface background AND 2px COLORS.accent left border"
    - "Highlighted row auto-scrolls into view via scrollIntoView({ block: 'nearest', behavior: 'auto' })"
    - "highlightedIndex is reset to -1 when the query changes"
    - "ArrowDown/ArrowUp on category input in MarkupNamePopup advances/retreats highlightedIndex"
    - "Enter on highlighted row calls onSelect and stops propagation to the popup wrapper onKeyDown (Landmine 2 mitigated)"
    - "MarkupNamePopup mode union includes 'edit'"
    - "handleConfirm calls findCategoryByName before onConfirm and substitutes canonical name (D-13)"
    - "Wave 0 RED tests for MarkupNamePopup mode='edit' labels and D-13 canonical substitution are now GREEN"
  artifacts:
    - path: "src/renderer/src/components/CategoryAutocomplete.tsx"
      provides: "keyboard navigation with highlightedIndex prop pair"
      contains: "highlightedIndex"
    - path: "src/renderer/src/components/MarkupNamePopup.tsx"
      provides: "mode='edit' support + D-13 canonical substitution + D-12 keyboard wiring"
      contains: "mode === 'edit'"
  key_links:
    - from: "src/renderer/src/components/MarkupNamePopup.tsx"
      to: "src/renderer/src/components/CategoryAutocomplete.tsx"
      via: "highlightedIndex + onHighlightChange props"
      pattern: "highlightedIndex"
    - from: "src/renderer/src/components/MarkupNamePopup.tsx"
      to: "src/renderer/src/stores/markupStore.ts"
      via: "useMarkupStore((s) => s.findCategoryByName)"
      pattern: "findCategoryByName"
---

<objective>
Add keyboard navigation to CategoryAutocomplete and extend MarkupNamePopup with mode='edit', D-13 canonical substitution, and D-12 keyboard wiring.

Purpose: Wave 2 delivers two coordinated changes: CategoryAutocomplete gains controlled highlight state via props, and MarkupNamePopup gains the onKeyDown handler that drives those props. This plan also adds the mode='edit' variant to MarkupNamePopup with correct labels, aria, and no measurement preview — turning the Wave 0 RED tests GREEN.

Output: CategoryAutocomplete with keyboard-navigable suggestion list. MarkupNamePopup with mode='edit' support, D-12 keyboard forwarding, and D-13 canonical-name substitution on confirm.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-CONTEXT.md
@.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-RESEARCH.md
@.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-PATTERNS.md
@.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-UI-SPEC.md
@.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-01-SUMMARY.md

<interfaces>
<!-- Key interfaces from PATTERNS.md — CategoryAutocomplete and MarkupNamePopup -->

Current CategoryAutocompleteProps (PATTERNS.md lines 305–319):
  interface CategoryAutocompleteProps {
    query: string
    onSelect: (categoryName: string) => void
  }

Target CategoryAutocompleteProps after this plan:
  interface CategoryAutocompleteProps {
    query: string
    onSelect: (categoryName: string) => void
    highlightedIndex?: number          // -1 = nothing highlighted; controlled from MarkupNamePopup
    onHighlightChange?: (i: number) => void
  }

Current MarkupNamePopup mode union (PATTERNS.md lines 190–199):
  mode: 'count-pre' | 'save-after'

Target after this plan:
  mode: 'count-pre' | 'save-after' | 'edit'

Current MarkupNamePopup label logic (PATTERNS.md lines 203–212):
  const primaryCta = mode === 'count-pre' ? 'Start Count' : 'Save Markup'
  const cancelLabel = 'Discard'
  aria-label={mode === 'count-pre' ? 'Name count item' : 'Save markup'}

Target after this plan:
  const primaryCta = mode === 'count-pre' ? 'Start Count' : mode === 'edit' ? 'Save Changes' : 'Save Markup'
  const cancelLabel = mode === 'edit' ? 'Discard Changes' : 'Discard'
  aria-label={mode === 'count-pre' ? 'Name count item' : mode === 'edit' ? 'Edit markup' : 'Save markup'}

D-13 canonical substitution addition to handleConfirm (PATTERNS.md lines 228–256):
  // Add subscription:
  const findCategoryByName = useMarkupStore((s) => s.findCategoryByName)
  // In handleConfirm body, before onConfirm call:
  const typed = categoryName.trim()
  const canonical = typed === '' ? '' : (findCategoryByName(typed)?.name ?? typed)
  onConfirm({ name: trimmedName, categoryName: canonical, color: selectedColor })
  // Update useCallback dependency array to include findCategoryByName

CategoryAutocomplete row rendering pattern for highlight (PATTERNS.md lines 322–341):
  data-highlighted={highlightedIndex === idx ? 'true' : undefined}
  background: highlightedIndex === idx ? COLORS.hoverSurface : 'transparent'
  borderLeft: highlightedIndex === idx ? `2px solid ${COLORS.accent}` : 'none'  // D-12 accent indicator

scrollIntoView useEffect pattern (PATTERNS.md lines 354–360):
  useEffect(() => {
    if (clampedIndex < 0) return
    const highlighted = document.querySelector('[data-highlighted="true"]') as HTMLElement | null
    highlighted?.scrollIntoView({ block: 'nearest', behavior: 'auto' })
  }, [clampedIndex])

Wrap-around behavior (UI-SPEC Interaction Contract §D-12):
  ArrowDown at last row → wrap to index 0
  ArrowUp at row 0 → wrap to last row (optionCount - 1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add keyboard navigation to CategoryAutocomplete.tsx (D-12)</name>
  <files>src/renderer/src/components/CategoryAutocomplete.tsx</files>
  <read_first>
    - src/renderer/src/components/CategoryAutocomplete.tsx — read the full file to understand: current props interface, filtered list computation, the rendered row divs with their onMouseDown + e.preventDefault() pattern, the existing itemStyle definition, the COLORS import, and whether a 'Create new: {query}' row exists and how it renders (to count total option slots for clamp/wrap logic)
  </read_first>
  <behavior>
    - New props: highlightedIndex (optional number, default -1) and onHighlightChange (optional callback)
    - clampedIndex computed from highlightedIndex: if highlightedIndex >= optionCount, clamp to optionCount - 1; if undefined, treat as -1
    - Each row div gains: data-highlighted attribute (string 'true' or undefined), background: clampedIndex === idx ? COLORS.hoverSurface : 'transparent' (overrides existing transparent/hoverSurface from mouse events), borderLeft: clampedIndex === idx ? '2px solid ' + COLORS.accent : 'none'
    - useEffect watches clampedIndex: queries document.querySelector('[data-highlighted="true"]') and calls scrollIntoView({ block: 'nearest', behavior: 'auto' })
    - highlightedIndex reset when query changes: useEffect watching query that calls onHighlightChange?.(-1) when query changes
    - onMouseEnter/onMouseLeave row hover behavior is preserved exactly (no changes to mouse hover logic)
    - onMouseDown + e.preventDefault() on each row is preserved exactly
    - When highlightedIndex === idx, mouse hover still applies COLORS.hoverSurface background — the borderLeft is the visual discriminator between keyboard-highlight and mouse-hover
  </behavior>
  <action>
    Extend the CategoryAutocompleteProps interface to add highlightedIndex?: number and onHighlightChange?: (i: number) => void. Both are optional so existing callsites (MarkupNamePopup before this plan's Task 2) compile without changes.

    Compute optionCount = filtered.length + (showCreateOption ? 1 : 0). Compute clampedIndex: if highlightedIndex is undefined or -1, clampedIndex = -1; if highlightedIndex >= optionCount, clampedIndex = optionCount - 1; otherwise clampedIndex = highlightedIndex.

    In the filtered.map() block: add data-highlighted={clampedIndex === idx ? 'true' : undefined}, update background style to `clampedIndex === idx ? COLORS.hoverSurface : 'transparent'` (the onMouseEnter override on top of this is still fine at runtime since it directly mutates e.currentTarget.style), and add borderLeft: `clampedIndex === idx ? '2px solid ${COLORS.accent}' : 'none'` (use template literal with the COLORS.accent value — check what COLORS.accent is from the existing import).

    If a 'Create new' row exists: apply the same data-highlighted and borderLeft treatment using idx = filtered.length (the next index after the category rows).

    Add two useEffect calls: one watching [clampedIndex] that calls scrollIntoView; one watching [query] that calls onHighlightChange?.(-1) to reset on new query. Import useEffect if not already imported.

    Do NOT add any onKeyDown to CategoryAutocomplete itself — keyboard events are handled in MarkupNamePopup (Task 2) and forwarded via the highlightedIndex prop.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CategoryAutocomplete.tsx CategoryAutocompleteProps includes `highlightedIndex?: number` and `onHighlightChange?: (i: number) => void`
    - CategoryAutocomplete.tsx row divs include `data-highlighted` attribute set conditionally on clampedIndex === idx
    - CategoryAutocomplete.tsx row divs include `borderLeft` style set to 2px solid COLORS.accent when highlighted
    - CategoryAutocomplete.tsx has a useEffect watching clampedIndex that calls scrollIntoView with { block: 'nearest', behavior: 'auto' }
    - CategoryAutocomplete.tsx has a useEffect watching query that calls onHighlightChange?.(-1)
    - npx vitest run exits with same failure profile as before this task (no new failures)
  </acceptance_criteria>
  <done>
    CategoryAutocomplete renders highlighted rows with accent border. Keyboard state driven by props. Existing mouse-hover behavior unchanged. Vitest clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add mode='edit' + D-12 keyboard nav + D-13 canonical substitution to MarkupNamePopup.tsx</name>
  <files>src/renderer/src/components/MarkupNamePopup.tsx</files>
  <read_first>
    - src/renderer/src/components/MarkupNamePopup.tsx — read the full file to understand: the MarkupNamePopupProps interface, existing mode handling, handleConfirm useCallback body and its dependency array, the wrapper div onKeyDown (handleKeyDown), the category input element and its current props (line 174 area), the CategoryAutocomplete component usage including current props, the store subscriptions (lines 60–70 area), and existing useState declarations
  </read_first>
  <behavior>
    mode union extension:
    - MarkupNamePopupProps.mode: 'count-pre' | 'save-after' | 'edit'
    Label logic changes (Landmine 5, 6):
    - primaryCta: mode === 'count-pre' ? 'Start Count' : mode === 'edit' ? 'Save Changes' : 'Save Markup'
    - cancelLabel: mode === 'edit' ? 'Discard Changes' : 'Discard'
    - aria-label on [role="dialog"]: mode === 'count-pre' ? 'Name count item' : mode === 'edit' ? 'Edit markup' : 'Save markup'
    D-12 keyboard state:
    - Add useState(-1) for highlightedIndex
    - Category input gets onKeyDown handler (handleCategoryKeyDown): when showCategoryList is true, ArrowDown increments with wrap-around, ArrowUp decrements with wrap-around, Enter with highlightedIndex >= 0 calls onSelect on the highlighted item AND e.stopPropagation() (Landmine 2) AND e.preventDefault()
    - The Enter path: select item by calling the same logic as CategoryAutocomplete onMouseDown — set categoryName to the item at highlightedIndex, close the list, reset highlightedIndex to -1. To get the item: the component knows filtered categories — pass a callback to CategoryAutocomplete OR handle via a shared ref to the filtered list. Simplest approach: expose an onSelectByIndex callback on CategoryAutocomplete OR read the item via the CategoryAutocomplete component's rendered options. The cleanest approach per PATTERNS.md is to lift the Enter handling: in handleCategoryKeyDown, when Enter and highlightedIndex >= 0, call a ref-stable onSelect function that reads the item. Since CategoryAutocomplete already calls onSelect(cat.name) on mousedown, we can wire the same callback: pass an onSelectAtIndex prop OR simply call onSelect with the currently-highlighted name. The highlighted name is the filtered category at highlightedIndex. The simplest wiring: store filteredCategories as a ref in MarkupNamePopup via a prop passed back from CategoryAutocomplete (OR read via the data-highlighted element's textContent). Use the textContent approach: `document.querySelector('[data-highlighted="true"]')?.textContent` inside handleCategoryKeyDown to get the selected name.
    - Pass highlightedIndex and onHighlightChange to CategoryAutocomplete
    - Reset highlightedIndex on list close (setShowCategoryList(false) also resets it)
    D-13 canonical substitution:
    - Add store subscription: const findCategoryByName = useMarkupStore((s) => s.findCategoryByName)
    - In handleConfirm: const typed = categoryName.trim(); const canonical = typed === '' ? '' : (findCategoryByName(typed)?.name ?? typed); pass canonical as categoryName in onConfirm payload
    - Update handleConfirm dependency array to include findCategoryByName
    Header text for edit mode:
    - The popup header (h2-equivalent element) should read 'Edit Markup' when mode === 'edit'. This is the existing header slot in the component — add a ternary to render 'Edit Markup' for mode='edit', leaving existing modes unchanged.
    measurement preview:
    - D-06 states no measurement preview in edit mode. Wrap the existing measurementPreview section (if any) in a condition: `{mode !== 'edit' && <measurement preview JSX>}`. If the measurement preview is already only shown in certain modes, verify it won't show for 'edit'.
  </behavior>
  <action>
    In MarkupNamePopup.tsx, make the following changes in this order:

    1. Extend MarkupNamePopupProps.mode to 'count-pre' | 'save-after' | 'edit'.

    2. Update the three label/aria constants to three-way ternaries per the exact strings above (Landmines 5 and 6).

    3. Update the header text: find the existing JSX element that renders the popup title. Add `mode === 'edit' ? 'Edit Markup' : ` before the existing ternary (or replace with a three-way ternary). The exact text must be 'Edit Markup' (capital M in Markup per UI-SPEC Copywriting Contract).

    4. Add `const [highlightedIndex, setHighlightedIndex] = useState(-1)` alongside the existing useState calls.

    5. Add `const findCategoryByName = useMarkupStore((s) => s.findCategoryByName)` alongside existing store subscriptions.

    6. Update handleConfirm: insert the canonical substitution lines (const typed / const canonical) immediately before the onConfirm({ ... }) call. Update the useCallback dependency array to add findCategoryByName.

    7. Add handleCategoryKeyDown to the category <input> onKeyDown. The handler: if showCategoryList is false, return early. For ArrowDown: e.preventDefault(); const optCount = total options (must be known — use a local variable counting suggestions plus 1 if create-new row is showing; simplest: compute it similarly to CategoryAutocomplete by reading state). For Enter with highlightedIndex >= 0: e.preventDefault(); e.stopPropagation(); get highlighted item text via document.querySelector('[data-highlighted="true"]')?.textContent; if text, call the onSelect handler inline (setCategoryName(text), setShowCategoryList(false), setHighlightedIndex(-1)). For ArrowDown/ArrowUp: compute optionCount and update highlightedIndex with wrap-around.

    8. Pass highlightedIndex={highlightedIndex} onHighlightChange={setHighlightedIndex} to the CategoryAutocomplete component.

    9. Suppress measurement preview for edit mode: wrap the measurement preview section (if present) with {mode !== 'edit' && ...}.

    Do NOT modify any other behavior. The existing onBlur 120ms setTimeout on the category input is preserved (Landmine 3 documentation: Enter in handleCategoryKeyDown stops propagation and does not trigger blur, so the 120ms race is not a concern for keyboard selection).
  </action>
  <verify>
    <automated>npx vitest run src/tests/markup-namepopup.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - MarkupNamePopup.tsx MarkupNamePopupProps.mode includes 'edit' in the union
    - MarkupNamePopup.tsx primaryCta is 'Save Changes' when mode='edit' (three-way ternary with exact string)
    - MarkupNamePopup.tsx cancelLabel is 'Discard Changes' when mode='edit' (conditional per Landmine 5)
    - MarkupNamePopup.tsx aria-label renders 'Edit markup' when mode='edit' (three-way ternary per Landmine 6)
    - MarkupNamePopup.tsx handleConfirm body includes findCategoryByName(typed)?.name ?? typed canonical substitution
    - MarkupNamePopup.tsx handleConfirm useCallback dep array includes findCategoryByName
    - MarkupNamePopup.tsx category input has onKeyDown={handleCategoryKeyDown}
    - MarkupNamePopup.tsx CategoryAutocomplete receives highlightedIndex and onHighlightChange props
    - npx vitest run src/tests/markup-namepopup.test.ts exits 0 — all mode='edit' label tests GREEN, D-13 canonical substitution tests GREEN
  </acceptance_criteria>
  <done>
    MarkupNamePopup accepts mode='edit', renders correct labels and aria. Category input has keyboard nav. D-13 canonical substitution in handleConfirm. Wave 0 markup-namepopup.test.ts RED tests are now GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Category input → store | findCategoryByName reads from Zustand store; result is used for display substitution only; no write occurs in handleConfirm itself (write is in the consumer) |
| Keyboard event → popup submit | Enter on highlighted row must NOT trigger popup submit (Landmine 2 — stopPropagation guard required) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | D-13 canonical substitution bypass via keyboard | mitigate | handleConfirm runs canonical substitution BEFORE calling onConfirm regardless of how confirm was triggered (button click or Enter key); substitution cannot be bypassed |
| T-07-02-02 | Tampering | Double-fire: Enter submits form AND selects autocomplete row | mitigate | Landmine 2 fix: handleCategoryKeyDown calls e.stopPropagation() when Enter is consumed by autocomplete, preventing the wrapper div's handleKeyDown from also firing handleConfirm |
| T-07-02-03 | Denial of Service | Infinite scroll loop in scrollIntoView useEffect | accept | clampedIndex < 0 guard exits early; scrollIntoView is called only when a row is highlighted; no loop risk |
</threat_model>

<verification>
After Wave 2 completes:
- npx vitest run src/tests/markup-namepopup.test.ts — exits 0 (all mode='edit' and D-13 tests GREEN)
- npx vitest run exits non-zero only for the Wave 0 EditMarkupCommand RED stubs (markup-commands.test.ts and markup-context-menu.test.ts Edit item tests — not yet implemented)
- TypeScript compiles without error (npx tsc --noEmit or equivalent)
</verification>

<success_criteria>
CategoryAutocomplete renders keyboard-highlighted rows with accent left border and auto-scroll. MarkupNamePopup accepts mode='edit' with correct labels, aria, header, and no measurement preview. D-13 canonical substitution wired in handleConfirm for all modes. D-12 keyboard nav wired via handleCategoryKeyDown with stopPropagation guard. Wave 0 markup-namepopup RED tests are GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/07-canvas-workspace-ux-and-markup-fixes/07-02-SUMMARY.md`
</output>
