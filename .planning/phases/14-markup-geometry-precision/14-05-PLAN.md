---
phase: 14-markup-geometry-precision
plan: 05
type: execute
wave: 4
depends_on: ["14-01", "14-02", "14-03", "14-04"]
files_modified:
  - src/renderer/src/components/markup/BulgeHandle.tsx
  - src/renderer/src/components/BlockedCommitMessage.tsx
  - src/renderer/src/components/CanvasViewport.tsx
  - src/renderer/src/stores/markupStore.ts
  - src/tests/reshape-arc-command.test.ts
autonomous: true
requirements: [D-08, D-09]
must_haves:
  truths:
    - "Dragging an arc endpoint vertex re-solves the arc through the new endpoint (D-08)"
    - "Dragging the on-arc bulge handle reshapes the arc's curvature (the bulge handle is the curvature edit gesture)"
    - "A sagitta cap stops the bulge drag before the arc would self-intersect, with amber feedback"
    - "All arc/vertex edits are undoable via the existing command pattern and round-trip through save/reload"
    - "Committing an area/perimeter whose boundary self-intersects is BLOCKED, the markup stays in edit mode, the crossing is highlighted red, and a blocked-commit message explains the fix (D-09)"
  artifacts:
    - path: "src/renderer/src/components/markup/BulgeHandle.tsx"
      provides: "interactive zoom-compensated Circle bulge handle (listening=true, hit>visual)"
      min_lines: 30
    - path: "src/renderer/src/components/BlockedCommitMessage.tsx"
      provides: "parent-owned (no setTimeout) blocked-commit message"
      min_lines: 20
  key_links:
    - from: "src/renderer/src/components/CanvasViewport.tsx"
      to: "src/renderer/src/stores/markupStore.ts (reshape-arc)"
      via: "one dispatch per drag session in handleStageMouseUp"
      pattern: "reshape-arc"
    - from: "src/renderer/src/components/CanvasViewport.tsx"
      to: "src/renderer/src/lib/self-intersection.ts"
      via: "findSelfIntersection before area/perimeter commit"
      pattern: "findSelfIntersection"
---

<objective>
Arc/vertex EDITING (D-08) and the self-intersection commit GUARD (D-09). Add the interactive on-arc bulge handle (drag to reshape curvature, with a sagitta cap), endpoint-vertex re-solve, both routed through the existing MarkupCommand undo machinery; and block any area/perimeter commit whose boundary self-intersects, keeping the markup in edit mode, highlighting the crossing red, and surfacing the blocked-commit message.

Purpose: Satisfies ROADMAP success criteria #4 (arc edits measured correctly) and #5 (self-intersection detected + warned, no bogus quantity reaches BOQ).
Output: BulgeHandle.tsx (new), BlockedCommitMessage.tsx (new), markupStore reshape-arc reducers, CanvasViewport edit + guard wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-markup-geometry-precision/14-CONTEXT.md
@.planning/phases/14-markup-geometry-precision/14-PATTERNS.md
@.planning/phases/14-markup-geometry-precision/14-UI-SPEC.md
@.planning/spikes/003-arc-segment-measure/README.md
@.planning/spikes/003b-curved-polygon-area/README.md
@.planning/phases/14-markup-geometry-precision/14-01-SUMMARY.md
@.planning/phases/14-markup-geometry-precision/14-02-SUMMARY.md

<interfaces>
From src/renderer/src/lib/arc-math.ts (14-01): `solveCircle(p1, mid, p2)` — re-solve through a new endpoint or new mid
From src/renderer/src/lib/self-intersection.ts (14-02): `findSelfIntersection(points): { i, j, point } | null`
From src/renderer/src/types/markup.ts (14-01): `arcs?` map + `reshape-arc` MarkupCommand variant (oldArcs/newArcs)
From src/renderer/src/stores/markupStore.ts:
- `moveVertex(markupId, page, vertexIndex, newPoint)` (lines 265-298) — the dispatch shape to mirror
- `pushCommand(stack, cmd)` cap helper (lines 100-103); undo branches lines 441/460/571/590; redo symmetric
- THE ONE dispatch per drag happens in handleStageMouseUp (not in cleanup helpers) — locked anti-pattern
From src/renderer/src/components/markup/VertexHandleOverlay.tsx: the ONLY listening=true handle pattern (Rect, name prefix, hitWidth/hitHeight > visual, e.cancelBubble=true) — copy and change shape to Circle
From src/renderer/src/components/ConfirmationToast.tsx: parent-owned-lifecycle (no setTimeout) — the BlockedCommitMessage pattern
From src/renderer/src/components/CanvasViewport.tsx: handleStageMouseDown (handle name routing), handleStageMouseUp (lines 1196-1318, the 4/zoom dragThreshold lines 1212-1214), finishPolygon (commit path wired at lines ~775/969)
UI-SPEC §"Bulge handle (D-08)" + §"Self-intersection blocked commit (D-09)" are authoritative for sizes/colors/copy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BulgeHandle.tsx + reshape-arc store reducers (undo/redo) with tests</name>
  <read_first>
    - src/renderer/src/components/markup/VertexHandleOverlay.tsx (FULL — the listening=true interactive handle: name prefix, hitWidth/hitHeight larger than visual, e.cancelBubble=true in onMouseDown; copy this and change Rect→Circle)
    - src/renderer/src/stores/markupStore.ts (moveVertex lines 265-298 + pushCommand lines 100-103 + the move-vertex undo branch lines 441/571 — mirror this exact symmetric shape for reshape-arc)
    - src/renderer/src/types/markup.ts (reshape-arc variant from 14-01: oldArcs/newArcs)
    - 14-UI-SPEC.md §"Bulge handle (D-08)" (Circle, resting 9px/zoom accent #0078d4 + 1.5/zoom white stroke; hover 12px/zoom; hit 18px/zoom; sagitta-cap guide turns #e8a838)
  </read_first>
  <action>
    Create src/renderer/src/components/markup/BulgeHandle.tsx mirroring VertexHandleOverlay but as a Konva `Circle`: resting diameter 9/zoom (radius 4.5/zoom), fill COLORS.accent #0078d4, stroke 1.5/zoom white; hover grows to 12/zoom + 2px white stroke; hit target 18/zoom (hitWidth/hitHeight > visual); `listening={true}`; `name` prefix `bulge-${segmentIndex}` so handleStageMouseDown can route the drag; `e.cancelBubble = true` in onMouseDown. Position each handle on its arc edge's on-arc midpoint (from `markup.arcs[segmentIndex]`). Props: markup, currentZoom, onHandleMouseDown(segmentIndex). In src/renderer/src/stores/markupStore.ts add a `reshapeArc(markupId, page, newArcs)` action that builds a `{ type: 'reshape-arc', markupId, page, oldArcs, newArcs }` command, applies it via set(...), `pushCommand(s.undoStack, cmd)`, `redoStack: []` — exactly mirroring moveVertex. Add the `reshape-arc` branch to BOTH undo and redo (swap oldArcs/newArcs symmetrically), placed alongside the move-vertex branches. Create src/tests/reshape-arc-command.test.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json; npx vitest run src/tests/reshape-arc-command.test.ts src/tests/move-vertex-command.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - BulgeHandle uses `Circle`, `listening={true}`, hitWidth/hitHeight 18/zoom (> the 9/zoom visual), `e.cancelBubble = true`, and a `bulge-` name prefix
    - All sizes zoom-compensated (grep `/ currentZoom`); resting fill COLORS.accent, stroke white — no new hexes
    - `reshapeArc` pushes exactly ONE reshape-arc command and clears redoStack (mirrors moveVertex)
    - reshape-arc undo restores oldArcs and redo restores newArcs (round-trip test asserts the arcs map is byte-equal after undo→redo→undo)
    - Test: applying reshapeArc then undo yields the original arcs map; redo re-applies newArcs — 0 drift
    - existing move-vertex-command.test.ts still green
    - tsc clean
  </acceptance_criteria>
  <done>BulgeHandle is an interactive zoom-compensated Circle handle; reshapeArc dispatches a symmetric undoable reshape-arc command; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Bulge-handle drag + endpoint re-solve wired through one dispatch in handleStageMouseUp, with sagitta cap</name>
  <read_first>
    - src/renderer/src/components/CanvasViewport.tsx (handleStageMouseDown handle-name routing; handleStageMouseMove vertex-drag branch lines 1138-1155; handleStageMouseUp lines 1196-1318 + the 4/zoom dragThreshold lines 1212-1214; THE ONE dispatch per drag session happens here, NOT in cleanup — locked anti-pattern from STATE.md/12-RESEARCH)
    - src/renderer/src/lib/arc-math.ts (solveCircle — re-solve the arc when the bulge handle moves to a new mid, or when an endpoint moves: keep the same on-arc mid and re-solve, OR for endpoint re-solve recompute a mid that keeps the curve passing through a sensible point per spike-003)
    - src/renderer/src/components/markup/BulgeHandle.tsx (Task 1 — the handle that initiates the drag)
    - 14-UI-SPEC.md §"Bulge handle" sagitta-cap row (guide turns amber #e8a838 and the handle stops following past the cap) + §"endpoint re-solve" caption
    - .planning/spikes/003b-curved-polygon-area/README.md (sagitta cap prevents most self-intersections — D-09 fallback)
  </read_first>
  <action>
    In CanvasViewport.tsx add a bulge-drag flow mirroring the Phase-12 vertex-drag flow: on mousedown of a `bulge-` handle, record a bulgeDragRef (markupId, segmentIndex, original arcs). In handleStageMouseMove, while bulgeDragRef is set, compute the page-point (inverse transform), CLAMP it to the sagitta cap (max curvature before self-intersection risk — derive the cap from the chord length so the bulge cannot exceed a safe sagitta), update a live preview of the reshaped arc, and turn the preview guide amber (#e8a838) when clamped. In handleStageMouseUp, if the bulge moved beyond the 4/zoom dragThreshold (read liveZoom via getState, lines 1212-1214), dispatch ONE `reshapeArc(markupId, page, newArcs)` with the new `{midX,midY}` for that segment — ONE undo entry per drag. For ENDPOINT re-solve (D-08): when an arc edge's endpoint vertex is dragged (existing vertex-drag path), after computing the new endpoint, re-solve the arc via solveCircle keeping the on-arc mid and write the updated arcs alongside the moveVertex dispatch (or a paired reshape-arc) so the curve follows the new corner — keep it to one undoable gesture. Snapping (14-03) applies to the endpoint drag (D-07) but NOT to the bulge drag (curvature shaping, like the on-arc click).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json; npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "reshapeArc" src/renderer/src/components/CanvasViewport.tsx` dispatch sits inside handleStageMouseUp (one per drag), NOT in a cleanup helper
    - Bulge drag uses the 4/zoom dragThreshold (liveZoom via getState) for click-vs-drag, mirroring lines 1212-1214
    - Sagitta cap: the bulge page-point is clamped to a max sagitta derived from the chord; when clamped the preview guide is amber #e8a838 (grep COLORS.warning in the bulge preview path)
    - Endpoint re-solve: dragging an arc edge's endpoint re-solves the arc (solveCircle called in the vertex-drag commit path for arc edges) and updates arcs in the same undoable gesture
    - Bulge drag does NOT call resolveSnap (curvature shaping is unsnapped); endpoint drag DOES (D-07)
    - `npm run build` succeeds; manual smoke recorded: dragging the bulge deepens/flattens the curve and the reported length changes; one Ctrl+Z reverts the whole reshape
  </acceptance_criteria>
  <done>Bulge drag reshapes curvature (one undoable reshape-arc) with a sagitta cap + amber feedback; endpoint drag re-solves the arc; both round-trip through undo.</done>
</task>

<task type="auto">
  <name>Task 3: BlockedCommitMessage.tsx + self-intersection block-on-commit (D-09)</name>
  <read_first>
    - src/renderer/src/components/ConfirmationToast.tsx (FULL — parent-owned lifecycle, NO setTimeout; copy the surface style: COLORS.secondary bg, 1px COLORS.border, borderRadius 4, boxShadow, 13px text)
    - src/renderer/src/lib/self-intersection.ts (14-02 — findSelfIntersection(points): { i, j, point } | null)
    - src/renderer/src/components/CanvasViewport.tsx (handleStageMouseUp commit path + finishPolygon wired ~775/969 — where area/perimeter commit fires; the centroid+20px popup positioning to anchor the message)
    - 14-UI-SPEC.md §"Self-intersection blocked commit (D-09)" (crossing highlighted #dc2626, stroke (markupStroke+2)/zoom; message lead "Can't finish —" red #dc2626 weight 600) + Copywriting Contract (exact copy)
  </read_first>
  <action>
    Create src/renderer/src/components/BlockedCommitMessage.tsx mirroring ConfirmationToast exactly (parent-owned lifecycle, NO internal setTimeout; CanvasViewport owns dismissal). Props: `anchor: { x: number; y: number }` (screen-space, near the offending markup centroid), `onDismiss?`. Copy from the UI-SPEC Copywriting Contract: lead word "Can't finish —" in red #dc2626 weight 600, body "A self-crossing shape would report a wrong area or perimeter. The crossing is highlighted in red — drag the corners or curve handle apart to fix it, then finish again." In CanvasViewport.tsx, BEFORE committing an area OR perimeter markup (in handleStageMouseUp commit path AND finishPolygon), run `findSelfIntersection(points)` on the closed boundary; if it returns a crossing: BLOCK the commit (do not place/finish the markup), keep the markup in edit/drawing mode, set state to render the offending segment(s) highlighted red (#dc2626, strokeWidth (markupStroke+2)/zoom, listening=false on the transient layer), and set state to render `<BlockedCommitMessage>` anchored at the boundary centroid+20px. Clear both the red highlight and the message when the user moves a vertex/bulge that resolves the crossing and re-commits, or on cancel/Esc. Linear and wall markups (open, not closed) are NOT guarded (only area/perimeter per D-09).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json; npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - BlockedCommitMessage has NO setTimeout (grep returns nothing); parent owns dismissal (mirrors ConfirmationToast)
    - Message lead word "Can't finish —" is #dc2626 weight 600; body copy matches the UI-SPEC Copywriting Contract verbatim
    - `grep -n "findSelfIntersection" src/renderer/src/components/CanvasViewport.tsx` runs before BOTH the area/perimeter commit in handleStageMouseUp AND finishPolygon
    - On a self-intersecting area/perimeter: the commit is blocked (markup stays in edit mode, NOT added to the committed layer), the offending segment renders red #dc2626 at (stroke+2)/zoom listening=false, and the message appears
    - Linear and wall commits are NOT run through findSelfIntersection (open shapes, D-09 scope)
    - After resolving the crossing and re-committing, the red highlight + message clear; the markup commits normally and a valid quantity reaches the BOQ
    - tsc clean; `npm run build` succeeds
  </acceptance_criteria>
  <done>A self-intersecting area/perimeter commit is blocked, the crossing is highlighted red, the parent-owned blocked-commit message explains the fix, and no bogus quantity reaches the BOQ (D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.clmc` arc metadata → bulge/endpoint re-solve | Loaded arcs drive solveCircle during editing |
| User commit → BOQ quantity | The self-intersection guard is the gate that stops a wrong area/perimeter reaching the export |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05-01 | Tampering | self-intersecting boundary → wrong BOQ quantity | mitigate | findSelfIntersection blocks the commit (D-09); markup never reaches the committed layer / BOQ while crossing (Task 3 acceptance) |
| T-14-05-02 | Denial of Service | bulge drag past safe curvature → degenerate arc | mitigate | sagitta cap clamps the bulge before self-intersection risk; solveCircle finite-guarded (14-01) |
| T-14-05-03 | Denial of Service | malformed arcs map from a save file during edit | mitigate | solveCircle returns collinear fallback on degenerate/non-finite input; preview degrades to straight, no crash |
| T-14-05-SC | Tampering | npm package installs | accept | No new packages |
| T-14-05-W | Spoofing/Repudiation/Info-Disclosure/Elevation | (web threats) | accept | N/A — offline single-user desktop app |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds
- `npx vitest run src/tests/reshape-arc-command.test.ts src/tests/move-vertex-command.test.ts` green
- Manual: reshape an arc (one Ctrl+Z reverts); attempt to commit a bowtie area → blocked + red + message; fix → commits
</verification>

<success_criteria>
- Bulge-handle drag reshapes curvature; endpoint drag re-solves; all edits undoable + round-trip (SC #4, D-08).
- Self-intersecting area/perimeter commit blocked with red highlight + message (SC #5, D-09).
</success_criteria>

<output>
Create `.planning/phases/14-markup-geometry-precision/14-05-SUMMARY.md` when done, including the manual-ready bulge-handle + endpoint-re-solve + blocked-commit captions from the UI-SPEC.
</output>
