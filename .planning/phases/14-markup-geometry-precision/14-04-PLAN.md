---
phase: 14-markup-geometry-precision
plan: 04
type: execute
wave: 3
depends_on: ["14-01", "14-03"]
files_modified:
  - src/renderer/src/components/markup/ArcPreview.tsx
  - src/renderer/src/hooks/useMarkupTool.ts
  - src/renderer/src/components/CanvasViewport.tsx
  - src/renderer/src/hooks/useKeyboardShortcuts.ts
  - src/tests/arc-preview.test.ts
autonomous: true
requirements: [D-01, D-02]
must_haves:
  truths:
    - "A linear/perimeter/area/wall edge can be made a circular arc via a 3-point gesture: start / on-arc point / end (D-01)"
    - "The rendered curve passes through the on-arc point and re-solves live as the cursor moves toward the end"
    - "Arc edges and straight edges coexist freely in one markup; default edge is straight"
    - "Hold A makes one edge an arc then reverts to straight; a sticky toggle keeps a run of arc edges on until turned off (D-02)"
    - "Committing an arc edge writes its on-arc midpoint into the markup's arcs map so measurement uses true arc length"
  artifacts:
    - path: "src/renderer/src/components/markup/ArcPreview.tsx"
      provides: "live solved-arc dashed preview, listening=false, zoom-compensated"
      min_lines: 25
    - path: "src/renderer/src/hooks/useMarkupTool.ts"
      provides: "arc-edge drawing state (3-click capture) + arc-mode flag + arcs accumulation into the committed markup"
      contains: "arc"
  key_links:
    - from: "src/renderer/src/components/markup/ArcPreview.tsx"
      to: "src/renderer/src/lib/arc-math.ts"
      via: "solveCircle to sample the curve through start/on-arc/cursor"
      pattern: "solveCircle"
    - from: "src/renderer/src/hooks/useMarkupTool.ts"
      to: "markup.arcs"
      via: "write { midX, midY } keyed by segment start-vertex index on arc-edge commit"
      pattern: "arcs"
---

<objective>
Curved-edge DRAWING (MM-05). Implement the 3-click arc gesture (start / on-arc / end, D-01) inside the existing markup-drawing state machine, the straight↔arc edge mode controls (hold-A one-off + sticky toggle, D-02), and the live solved-arc dashed preview. Committing an arc edge records its on-arc midpoint into the markup's `arcs` map so the 14-01 measurement math reports true arc length/area.

Purpose: Satisfies ROADMAP success criterion #3 (arc edges drawable, pass through the on-arc point, coexist with straight edges).
Output: ArcPreview.tsx (new), useMarkupTool arc state, CanvasViewport click routing for the 3-click gesture, arc-mode keybindings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-markup-geometry-precision/14-CONTEXT.md
@.planning/phases/14-markup-geometry-precision/14-PATTERNS.md
@.planning/phases/14-markup-geometry-precision/14-UI-SPEC.md
@.planning/spikes/003-arc-segment-measure/README.md
@.planning/spikes/003c-arc-input-gesture/README.md
@.planning/phases/14-markup-geometry-precision/14-01-SUMMARY.md
@.planning/phases/14-markup-geometry-precision/14-03-SUMMARY.md

<interfaces>
From src/renderer/src/lib/arc-math.ts (14-01):
- `solveCircle(p1, mid, p2): { cx, cy, r, sweep, collinear }`

From src/renderer/src/types/markup.ts (14-01):
- `arcs?: Record<number, { midX: number; midY: number }>` on Linear/Area/Perimeter/Wall (key = segment start-vertex index)

From src/renderer/src/hooks/useMarkupTool.ts (existing markup state machine):
- holds in-progress `points: StagePoint[]`, `mode: 'idle'|'placing'|'drawing'`, chain state (chainArmed), commitShape; this is where arc capture state lives
From src/renderer/src/components/CanvasViewport.tsx:
- the click handler that calls into useMarkupTool to place points; the dashed straight-segment preview on Layer 1a (lines ~1556-1596, LINE_DASH = [8/zoom, 4/zoom])
From src/renderer/src/hooks/useKeyboardShortcuts.ts:
- isTextInputActive() guard; bare 'A' and 'F3' are FREE (F3 taken by snapping in 14-03 — use bare A for arc hold + another free key for the sticky toggle)
From 14-03 (READ 14-03-SUMMARY.md): the exact snap-state API (snapEnabled / snapSuspended on viewerStore and the resolveSnap call site in handleStageMouseMove) — Task 3 must read it to suppress snapping on the on-arc shaping click consistently with how 14-03 exposed snap control.
UI-SPEC §"Arc drawing — 3-click gesture" + §"Straight ↔ arc edge mode (D-02)" are authoritative.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ArcPreview.tsx — live solved-arc dashed preview</name>
  <read_first>
    - src/renderer/src/components/CanvasViewport.tsx (the in-progress dashed straight-segment preview on Layer 1a lines ~1556-1596; LINE_STROKE_WIDTH = 2/zoom, LINE_DASH = [8/zoom, 4/zoom], listening=false, pending color)
    - src/renderer/src/lib/arc-math.ts (solveCircle — to sample the arc through 3 points)
    - src/renderer/src/components/HoverRing.tsx (zoom-compensation idiom)
    - 14-UI-SPEC.md §"Arc drawing — 3-click gesture" (preview is a Konva Arc/Line-sampled curve, dashed, pending color, listening=false; re-solves every mousemove)
    - src/tests/pulse-highlight-animation.test.ts (the project's react-konva render-test pattern: .test.ts + React.createElement + Konva stub renders to data-attrs read in jsdom) — mirror it for the new ArcPreview test
  </read_first>
  <action>
    Create src/renderer/src/components/markup/ArcPreview.tsx. Props: `start: StagePoint`, `onArc: StagePoint`, `end: StagePoint`, `currentZoom: number`, `color: string`. Call `solveCircle(start, onArc, end)`; when not collinear, render a Konva `Arc` (or a `Line` sampled along the solved circle from start to end through onArc using cx/cy/r/sweep) — dashed with LINE_DASH=[8/zoom, 4/zoom], strokeWidth 2/zoom, stroke=color, `listening={false}`. When collinear, fall back to a straight dashed `Line` start→end (graceful degradation matching the existing straight preview). All sizes zoom-compensated. No fill. Then create src/tests/arc-preview.test.ts following the project's react-konva render-test pattern: assert (a) a non-collinear 3-point input renders a curved sampled preview (a points-bearing Line or an Arc node, NOT a 2-point straight Line) and solveCircle is exercised, and (b) a collinear 3-point input renders a straight 2-point dashed Line with no NaN coordinates.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json; npx vitest run src/tests/arc-preview.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "solveCircle" src/renderer/src/components/markup/ArcPreview.tsx` returns ≥1 (re-solves from the 3 points)
    - `grep -c "listening={false}" src/renderer/src/components/markup/ArcPreview.tsx` ≥ 1 (preview never steals events)
    - Stroke width and dash are divided by currentZoom (grep `/ currentZoom`)
    - Collinear input renders a straight dashed Line (no crash, no Arc with NaN radius)
    - Stroke uses the passed `color` prop (pending markup color), not a hardcoded hex
    - `npx vitest run src/tests/arc-preview.test.ts` is green — the test asserts the curved-vs-collinear branch behavior (W-1: ArcPreview now has behavioral coverage, not tsc-only)
    - tsc clean
  </acceptance_criteria>
  <done>ArcPreview renders a live, zoom-compensated, non-listening dashed curve through start/on-arc/end, degrading to a straight line when collinear, with a vitest test covering both branches.</done>
</task>

<task type="auto">
  <name>Task 2: 3-click arc capture + arc-mode flag + arcs accumulation in useMarkupTool</name>
  <read_first>
    - src/renderer/src/hooks/useMarkupTool.ts (FULL — the in-progress point state, mode machine, chainArmed, commitShape; arc capture state must live alongside points without breaking straight drawing)
    - .planning/spikes/003c-arc-input-gesture/README.md (why 3-click won; the gesture maps start→p1, on-arc→p2/mid, end→p3)
    - .planning/spikes/003-arc-segment-measure/README.md (the on-arc click is the mid point the solver consumes; major/minor disambiguation comes free from the on-arc click)
    - src/renderer/src/types/markup.ts (the arcs map key = segment start-vertex index)
    - 14-UI-SPEC.md §"Straight ↔ arc edge mode (D-02)" (default straight; hold-key one-off; sticky toggle run)
  </read_first>
  <action>
    Extend useMarkupTool.ts with arc-edge state: `arcMode: 'off' | 'sticky'` (sticky toggle, D-02) and a transient `arcHeld: boolean` (hold-key one-off). Add an in-progress arc-capture sub-state: when arc mode is active (sticky OR held) and the next edge is being drawn, capture proceeds as start (already the last committed vertex) → on-arc (first arc click) → end (second arc click) — i.e. an arc edge consumes ONE extra click for the on-arc shaping point versus a straight edge. On completing an arc edge (the END click), append the END vertex to `points` AND write `arcs[startVertexIndex] = { midX: onArc.x, midY: onArc.y }` where startVertexIndex is the index of the edge's start vertex; if `arcHeld` (one-off), revert to straight after this edge; if `arcMode==='sticky'`, keep capturing arc edges. Ensure the accumulated `arcs` map is carried into `commitShape` so the committed Linear/Area/Perimeter/Wall markup persists it. Straight edges leave `arcs` untouched (absent key). Snapping (14-03) applies to the start/end clicks but NOT the on-arc shaping click (UI-SPEC: on-arc is a free point) — expose enough state that CanvasViewport can suppress snapping on the on-arc click. Keep the existing straight-drawing path byte-for-byte when arc mode is off.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json; npx vitest run src/tests/chain-mode.test.ts src/tests/markup-tool-pop-last-point.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - useMarkupTool exposes `arcMode` ('off'|'sticky') + an arc-held flag + a setter to toggle sticky and to set held
    - An arc edge writes `arcs[startVertexIndex] = { midX, midY }` from the on-arc click; the END click is the regular vertex appended to points
    - One-off (held) arc edge reverts arcMode to straight after the edge commits; sticky keeps arc capture across successive edges
    - `commitShape` produces a markup whose `arcs` map contains an entry per arc edge drawn (and none for straight-only shapes)
    - With arc mode OFF, the existing straight-drawing tests (chain-mode, pop-last-point) pass UNCHANGED (no regression)
    - The on-arc shaping click is flagged so snapping can be suppressed for it (a boolean/phase the viewport can read)
    - tsc clean
  </acceptance_criteria>
  <done>useMarkupTool captures the 3-click arc gesture, supports one-off + sticky arc mode, and accumulates the on-arc midpoint into the committed markup's arcs map; straight drawing unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Wire 3-click gesture into CanvasViewport + arc-mode keybindings + ArcPreview render</name>
  <read_first>
    - src/renderer/src/components/CanvasViewport.tsx (the click handler that places markup points via useMarkupTool; handleStageMouseMove placement branch from 14-03 where the live preview updates; Layer 1a transient preview mount; getCursor() lines ~1344-1356 + CROSSHAIR_CURSOR lines ~170-183 for the optional arc-mode cursor)
    - src/renderer/src/hooks/useKeyboardShortcuts.ts (isTextInputActive guard; bare 'A' free for arc hold; pick a second free key for the sticky toggle, e.g. Shift+A or a ribbon affordance — confirm no collision against the documented in-use set Ctrl+O/S/Z/Y/A, Delete, Ctrl+=/-/0, arrows, F3-now-snap)
    - src/renderer/src/components/markup/ArcPreview.tsx (Task 1 — the preview to mount during arc capture)
    - 14-UI-SPEC.md §"Straight ↔ arc edge mode" + Open Q2 (arc-mode cursor badge vs pill-only — pill-only is an acceptable degradation)
  </read_first>
  <action>
    In CanvasViewport.tsx route the 3-click gesture: while arc mode is active and an edge is mid-capture, the placement click handler feeds the on-arc point then the end point into useMarkupTool's arc capture instead of placing a plain straight vertex. During the segment between the on-arc click and the end click, render `<ArcPreview start={…} onArc={…} end={cursorPagePoint} color={pendingColor} currentZoom={…} />` on Layer 1a (the transient preview layer), replacing the straight dashed preview for that edge. Suppress snapping on the on-arc shaping click (read the flag from Task 2). In useKeyboardShortcuts.ts bind: bare `A` keydown → set arcHeld(true) (one-off arc for the edge being drawn), `A` keyup → arcHeld(false); and a sticky-toggle key (e.g. `Shift+A`, confirmed free) → toggle `arcMode` between 'off' and 'sticky'. Guard with isTextInputActive(). Optionally augment getCursor()/CROSSHAIR_CURSOR with an arc tick when arc mode is active; if that proves heavy, rely on a status-pill `Arc` chip in StatusBar (acceptable degradation per UI-SPEC Open Q2) — if adding the chip, append it to the snap segment created in 14-03 (accent #0078d4 background, #ffffff text).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json; npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Bare `A` keydown/keyup toggles the one-off arc-held flag; the sticky-toggle key flips arcMode; both guarded by isTextInputActive(); no collision with the documented in-use bindings (Ctrl+A select-all, F3 snap, etc.)
    - During arc capture (between on-arc click and end click), `<ArcPreview` is rendered on the transient layer and the straight dashed preview is suppressed for that edge (grep shows the conditional)
    - The on-arc shaping click does NOT apply snapping (resolveSnap is skipped for that click — read the diff)
    - Either an arc-mode cursor variant is added to getCursor()/CROSSHAIR_CURSOR, OR an `Arc` chip is appended to the StatusBar snap segment (accent bg / white text) — at least one arc-mode affordance present
    - `npm run build` succeeds
    - Manual smoke (recorded in SUMMARY): draw a linear, hold A and click an on-arc point then an end point → the committed edge is curved and its length exceeds the straight chord
  </acceptance_criteria>
  <done>The 3-click arc gesture is drivable from the canvas with hold-A one-off + sticky toggle, a live arc preview, on-arc-click snapping suppressed, and an arc-mode affordance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pointer/keyboard → arc capture | User clicks/keys drive the gesture; the on-arc point becomes persisted arc metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Denial of Service | ArcPreview solveCircle on degenerate clicks | mitigate | solveCircle (14-01) is finite-guarded + returns collinear=true → ArcPreview falls back to a straight Line, no NaN-radius Arc |
| T-14-04-02 | Tampering | arcs map written with bad coords | mitigate | on-arc point comes from the inverse-transform of a real pointer (finite); 14-01 measurement math also finite-guards on load |
| T-14-04-SC | Tampering | npm package installs | accept | No new packages |
| T-14-04-W | Spoofing/Repudiation/Info-Disclosure/Elevation | (web threats) | accept | N/A — offline single-user desktop app |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds
- `npx vitest run src/tests/chain-mode.test.ts src/tests/markup-tool-pop-last-point.test.ts` green (no straight-drawing regression)
- Manual: a mixed straight+arc linear reports a length greater than its all-straight equivalent
</verification>

<success_criteria>
- 3-click arc gesture draws arc edges that pass through the on-arc point and coexist with straight edges (SC #3).
- Hold-A one-off + sticky toggle both work; default edge is straight (D-02).
- Committed arc edges persist their on-arc midpoint so 14-01 math measures them as true arcs.
</success_criteria>

<output>
Create `.planning/phases/14-markup-geometry-precision/14-04-SUMMARY.md` when done, including the manual-ready arc-mode caption (hold A / sticky toggle) from the UI-SPEC and the final confirmed keybindings.
</output>
