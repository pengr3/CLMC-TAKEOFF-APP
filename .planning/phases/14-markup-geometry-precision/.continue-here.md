---
context: phase
phase: 14-markup-geometry-precision
task: 3
total_tasks: 4
status: in_progress
last_updated: 2026-06-26T10:05:40.219Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered this session. One advisory anti-pattern below (commit hygiene)._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Broad `git add <dir>` sweeps unrelated untracked work | The tree has untracked work that must NOT be committed: `.planning/spikes/004-toolbar-redesign/` (user's separate WIP) and `.planning/spikes/003b-curved-polygon-area/demo.html` (throwaway). A `git add .` / `git add .planning/spikes/` would sweep them in. | advisory | Stage explicit file paths only. This pause committed exactly: the two handoff files, `14-DISCUSS-CHECKPOINT.json`, and `003c-arc-input-gesture/{demo.html,README.md}`. Nothing else. |

<current_state>
Mid **`/gsd-discuss-phase 14`**. This is a DISCUSSION pause — no PLAN.md exists yet.
3 of 4 gray areas are fully locked; Area 4 (Self-intersect & arc editing) is ~90% done with
ONE open sub-decision. All decisions are saved structured in
`14-DISCUSS-CHECKPOINT.json` (re-running discuss-phase will detect it and offer Resume).
The phase itself is fully de-risked by spikes 002/003/003b; spike **003c** (an interactive
arc-gesture demo, built THIS session) settled the arc gesture by letting the user feel it.
</current_state>

<completed_work>

Discussion areas (of 4 the user selected — all 4):
- **Area 1 — Arc drawing gesture: DONE.** Gesture = **3-click** (start / on-arc / end). Straight↔arc switching = **BOTH** momentary hold-key + sticky toggle (default edge straight).
- **Area 2 — Snap on/off & override: DONE.** Activation = **ON by default + hold-key suspend (1 point) + persistent toggle** (keys Alt/F3 TBD vs Phase 9 bindings). Indicator = **per-type glyphs** (square=vertex/endpoint, triangle=on-segment, X reserved for intersection).
- **Area 3 — Which snap targets ship: DONE.** SHIP = vertex/endpoint + nearest-point-on-segment + **close-the-loop (own start vertex)**. DEFER = intersection (X glyph reserved; spike 002 flagged O(k²) cost) + grid. RULE = snap to OTHER markups freely; for the in-progress/edited markup snap ONLY to its own start vertex (never own intermediate / actively-dragged vertex); applies in placement AND editing.
- **Area 4 — Self-intersect & arc editing: IN PROGRESS.** DECIDED: **arc + vertex editing IS IN this phase** — drag an arc endpoint vertex re-solves the arc; drag the on-arc **bulge handle** to reshape (this absorbs drag-to-bulge as the EDIT gesture); snapping + sagitta cap + self-intersection guard all apply on edit; all edits undoable; round-trips through save/BOQ. PROPOSED & not objected: self-intersection on commit = **BLOCK + keep editing** (sagitta cap prevents most).

Cross-cutting (this session):
- **How-To-Manual requirement** captured: every new tool/gesture/shortcut/indicator must be documented **manual-ready as it's built**. Manual itself = its own future phase. Saved to memory `project-howto-manual`.
- Built spike **003c-arc-input-gesture** (`demo.html` + `README.md`) — interactive 3-tab demo (3-click / drag-to-bulge / mixed wall) reusing spike-003 math verbatim.
- Checkpoint written/updated after each area: `14-DISCUSS-CHECKPOINT.json`.
</completed_work>

<remaining_work>

- **Area 4 — ONE open sub-decision (needs the user):** include **post-hoc straight↔arc edge conversion** (right-click a committed edge → "Make arc" / "Make straight"), or DEFER it? Claude recommended DEFER to bound scope. Also confirm self-intersection = BLOCK (vs forgiving warn-but-allow).
- Then `write_context`: produce `14-CONTEXT.md` (+ `14-DISCUSSION-LOG.md`) from the checkpoint.
- Then `git_commit` context, `update_state` (STATE.md), delete the checkpoint.
- Then `confirm_creation` → next step `/gsd-plan-phase 14`.
</remaining_work>

<decisions_made>

- Arc gesture = **3-click**; only gesture that reaches major arcs (>180°) and maps 1:1 to the spike-003 solver (confirmed via the 003c demo).
- Straight↔arc switching = **both** hold-key + sticky toggle.
- Snapping = **on by default**, hold-key suspend, persistent toggle; **per-type glyph** indicator.
- Snap targets = vertex/endpoint + on-segment + close-the-loop; intersection & grid deferred.
- **Arc + vertex editing IN** (bulge handle + endpoint re-solve + guards + undo) — chose this over placement-only after the user asked for it.
- Self-intersection = **block on commit** (proposed; sagitta cap prevents most).
- Documentation = manual-ready capture is a phase deliverable (memory `project-howto-manual`).
</decisions_made>

<blockers>
None. (One human input needed: the Area 4 conversion sub-decision.)
</blockers>

## Required Reading (in order)
1. `.planning/phases/14-markup-geometry-precision/14-DISCUSS-CHECKPOINT.json` — structured record of all locked decisions (Areas 1–3 + Area 4 partial)
2. `.planning/spikes/003c-arc-input-gesture/README.md` — the arc-gesture demo + why 3-click won
3. `.planning/spikes/002-snapping-engine/README.md` — snapping design + perf numbers (MM-06)
4. `.planning/spikes/003-arc-segment-measure/README.md` + `003b-curved-polygon-area/README.md` — arc length + curved area math, sign rule, self-intersection caveat (MM-05)
5. `.planning/ROADMAP.md` — Phase 14 entry (goal + 5 success criteria)
6. Memory `project-howto-manual` — documentation-as-you-build constraint
7. Source to extend: `src/renderer/src/types/markup.ts` (Markup union + arc metadata), `src/renderer/src/lib/markup-math.ts` (polylineLength/polygonArea → arc-aware), `src/renderer/src/components/CanvasViewport.tsx` (`handleStageMouseMove` ~line 1125 = snapping integration point; Phase 12 vertex/body-drag edit machinery)

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: Broad `git add <dir>` with sibling untracked work present → stage explicit paths only. Leave `004-toolbar-redesign/` and `003b/demo.html` untracked.

## Infrastructure State
- Dev app: not running.
- Git: on `master`; this WIP pause is committed and (per user request) pushed to `origin`.
- Untracked (intentionally left): `.planning/spikes/004-toolbar-redesign/`, `.planning/spikes/003b-curved-polygon-area/demo.html`.

<context>
The discussion is nearly complete and entirely recoverable from `14-DISCUSS-CHECKPOINT.json`.
Re-running `/gsd-discuss-phase 14` is the clean resume — it detects the checkpoint and offers
"Resume", dropping you at Area 4's single open question. The user prefers conversational
recommendations over the AskUserQuestion widget (they twice asked to clarify / "what do you
recommend") — lead with a crisp recommendation and confirm in prose.
</context>

<next_action>
Start with: `/gsd-discuss-phase 14` → choose Resume → answer the one open sub-decision
(post-hoc straight↔arc conversion: include or defer; confirm self-intersection = block) →
let it write `14-CONTEXT.md`. Then `/gsd-plan-phase 14`.
</next_action>
