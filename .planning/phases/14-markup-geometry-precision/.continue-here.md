---
context: phase
phase: 14-markup-geometry-precision
task: 0
total_tasks: 0
status: planning_not_started
last_updated: 2026-06-16T10:06:38.797Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints. One advisory anti-pattern below (commit hygiene)._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Broad `git add` swept in unrelated untracked work | `git add .planning/spikes/` staged the user's unrelated, in-progress `004-toolbar-redesign/` spike into a commit; required a `git reset --soft HEAD~1` to unwind. | advisory | Stage explicit file paths, never `git add <dir>`, whenever sibling untracked work exists in the tree. The user's `004-toolbar-redesign/` and a throwaway `demo.html` are STILL untracked — do not commit them. |

<current_state>
v1.0 milestone is 100% complete (17 phases). This session repurposed the empty Phase 14
slot: dropped "Pricing and Item Library" and redefined Phase 14 as
**Markup Geometry Precision — Snapping + Curved-Edge Measurement**, scoped to the
spiked-and-validated work only. ROADMAP.md, STATE.md (Roadmap Evolution), and the phase
directory rename (14-pricing-and-item-library → 14-markup-geometry-precision) are all done
and committed as part of this WIP pause. No PLAN.md exists yet — planning has NOT started.
</current_state>

<completed_work>

This session:
- `/gsd-spike` gap analysis of core measurement/markup → `.planning/BACKLOG.md` (MM-01..MM-12). Builds on prior `GAP-001` product audit (don't repeat it).
- Built + ran 4 feasibility spikes (real numbers in each README):
  - `001-auto-count-symbol-detect` — VALIDATED (offline NCC ~97.5% recall/83% precision/~100ms) but **PARKED** per user. Open follow-up: spike **001b** (detect+review workflow for the >=98% question).
  - `002-snapping-engine` — VALIDATED. → MM-06
  - `003-arc-segment-measure` — VALIDATED. → MM-05
  - `003b-curved-polygon-area` (+ interactive `demo.html`) — VALIDATED. → MM-05
- Spike commits: `62a1025` (gap + 001/002/003), `1422447` (003b).
- Redefined Phase 14 (this pause commit).
</completed_work>

<remaining_work>

- Plan Phase 14: `/gsd-plan-phase 14` (optionally `/gsd-discuss-phase 14` first).
- PARKED: spike `001b` auto-count detect+review.
- OPTIONAL: decide fate of throwaway `.planning/spikes/003b-curved-polygon-area/demo.html` (currently untracked).
</remaining_work>

<decisions_made>

- Dropped Pricing & Item Library entirely; its D-16–D-22 decisions remain in `v1.1-CONTEXT.md` (revivable).
- Phase 14 scope = spiked-and-validated only (snapping MM-06 + curved-edge measurement MM-05). Auto-count deferred.
- Snapping: uniform grid-hash, `cell = zoom-compensated tolerance (screenTolPx / zoom)`, segments indexed by tolerance-padded bbox, intersection-snap deferred.
- Arc: 3-point gesture (start / on-arc / end), per-segment arc metadata carrying the on-arc midpoint; `area = shoelace ± circular-segment` with sign rule **OUTWARD ⟺ sign(cross) ≠ sign(shoelace)**; self-intersection guard on area/perimeter commit.
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/spikes/002-snapping-engine/README.md` — snapping design + real perf numbers
2. `.planning/spikes/003-arc-segment-measure/README.md` — arc length math + data-model recommendation
3. `.planning/spikes/003b-curved-polygon-area/README.md` — curved area sign rule + self-intersection caveat
4. `.planning/BACKLOG.md` — MM-05 / MM-06 context (and the other measurement gaps not in scope)
5. `.planning/ROADMAP.md` — Phase 14 entry (goal + 5 success criteria)
6. Source to extend: `src/renderer/src/lib/markup-math.ts` (polylineLength/polygonArea), `src/renderer/src/types/markup.ts` (Markup union + add arc metadata), `src/renderer/src/components/CanvasViewport.tsx` (pointer→page-space inverse transform = snapping integration point)

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: Broad `git add <dir>` with sibling untracked work present → stage explicit paths only. Leave `004-toolbar-redesign/` and `demo.html` untracked.

## Infrastructure State
- Dev app: not running.
- Git: on `master`; this pause is committed and pushed to `origin`.
- Untracked (intentionally left): `.planning/spikes/004-toolbar-redesign/` (user's separate WIP spike), `.planning/spikes/003b-curved-polygon-area/demo.html` (throwaway).

<context>
Spikes are terminal artifacts — the live thread is PLANNING Phase 14. Everything needed to
plan is de-risked and written down in the three spike READMEs + the decisions above. The
natural first move on resume is `/gsd-plan-phase 14`; run `/gsd-discuss-phase 14` first only
if you want to settle the small open UX choices (snap toggle/keybind, 3-click-arc vs
drag-to-bulge input gesture, which snap types ship in the first plan).
</context>

<next_action>
Start with: `/gsd-plan-phase 14`
</next_action>
