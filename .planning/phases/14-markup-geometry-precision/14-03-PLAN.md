---
phase: 14-markup-geometry-precision
plan: 03
type: execute
wave: 2
depends_on: ["14-02"]
files_modified:
  - src/renderer/src/components/markup/SnapIndicator.tsx
  - src/renderer/src/components/CanvasViewport.tsx
  - src/renderer/src/hooks/useKeyboardShortcuts.ts
  - src/renderer/src/components/StatusBar.tsx
  - src/renderer/src/stores/viewerStore.ts
autonomous: true
requirements: [D-03, D-04, D-05, D-07]
must_haves:
  truths:
    - "While placing or editing a markup, the cursor snaps to nearby existing endpoints/vertices (□) and to the nearest point on existing segments (△), within a tolerance constant in screen pixels at every zoom"
    - "Exactly one snap glyph shows at a time, centered on the snapped page-point the click will use; no glyph when nothing is in tolerance or when snap is off/suspended"
    - "Snapping is ON by default; Alt suspends it for one point; F3 toggles it off/on persistently"
    - "Snapping resolves instantly on a page with thousands of vertices (spatial index, not linear scan)"
    - "For the in-progress/edited markup, snapping targets only its own start vertex (close-the-loop), never its intermediate vertices, never the dragged vertex (D-07)"
  artifacts:
    - path: "src/renderer/src/components/markup/SnapIndicator.tsx"
      provides: "zoom-compensated □/△ snap glyph with two-pass halo, listening=false"
      min_lines: 30
    - path: "src/renderer/src/components/StatusBar.tsx"
      provides: "Snap: ON/OFF/held-off status segment"
      contains: "Snap"
  key_links:
    - from: "src/renderer/src/components/CanvasViewport.tsx"
      to: "src/renderer/src/lib/snapping-engine.ts"
      via: "buildSnapIndex on geometry change + resolveSnap in handleStageMouseMove"
      pattern: "resolveSnap"
    - from: "src/renderer/src/components/CanvasViewport.tsx"
      to: "src/renderer/src/components/markup/SnapIndicator.tsx"
      via: "render active snap candidate on transient layer"
      pattern: "SnapIndicator"
---

<objective>
Wire snapping (MM-06) into the live pointer pipeline: build the spatial index from the current page's geometry, query it in `handleStageMouseMove` to override the cursor's page-point when a candidate is within tolerance, render the per-type □/△ glyph (D-04), and add the ON/suspend(Alt)/toggle(F3) controls (D-03) plus the StatusBar status pill. Snapping applies in placement AND Phase-12 vertex/body-drag editing (D-07).

Purpose: Satisfies ROADMAP success criteria #1 and #2.
Output: SnapIndicator.tsx (new glyph), CanvasViewport snap injection + index rebuild, keybindings, StatusBar segment, viewerStore snap state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-markup-geometry-precision/14-CONTEXT.md
@.planning/phases/14-markup-geometry-precision/14-PATTERNS.md
@.planning/phases/14-markup-geometry-precision/14-UI-SPEC.md
@.planning/spikes/002-snapping-engine/README.md
@.planning/phases/14-markup-geometry-precision/14-02-SUMMARY.md

<interfaces>
From src/renderer/src/lib/snapping-engine.ts (14-02):
- `buildSnapIndex({ vertices, segments, cell }): SnapIndex`
- `resolveSnap(index, cursor, tol, exclude?): SnapCandidate | null` where SnapCandidate = { point: StagePoint; kind: 'vertex'|'segment'; markupId; vertexIndex? }

From src/renderer/src/components/CanvasViewport.tsx:
- `handleStageMouseMove` (lines 1125-1188): every branch converts pointer→page via `stage.getAbsoluteTransform().copy().invert().point(pointer)`; vertex-drag branch 1138-1155, body-drag 1159-1170, placement 1183-1185
- `vertexDragRef`, `bodyDragRef`, `dragPreviewRef` module refs; `currentPage`; `useViewerStore.getState().pageViewports[currentPage]?.zoom` is the live-zoom read pattern (line 1212-1213)

From src/renderer/src/components/HoverRing.tsx: zoom-compensation idiom `const v = PX / currentZoom`, every shape `listening={false}`
From src/renderer/src/components/markup/VertexHandleOverlay.tsx: Konva primitive + screen-px constants pattern (Rect)
From src/renderer/src/lib/color-utils.ts: `getContrastingInk(fillHex): '#000000'|'#ffffff'` (threshold 0.179) for the glyph halo
From src/renderer/src/lib/constants.ts: COLORS.accent '#0078d4', COLORS.warning '#e8a838', COLORS.textSecondary
From src/renderer/src/hooks/useKeyboardShortcuts.ts: central keydown switch; `isTextInputActive()` guard (lines 27-42); Ctrl+A is select-all (line 185) — bare 'A' and 'F3' are FREE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SnapIndicator.tsx — □/△ glyph + snap state in viewerStore</name>
  <read_first>
    - src/renderer/src/components/HoverRing.tsx (zoom-compensation idiom lines 13/61-62; listening=false discipline lines 41-54)
    - src/renderer/src/components/markup/VertexHandleOverlay.tsx (Konva primitive + screen-px module constants, the Rect render shape)
    - src/renderer/src/lib/color-utils.ts (getContrastingInk for the halo color — do NOT invent a second contrast function)
    - src/renderer/src/components/CanvasViewport.tsx (CROSSHAIR_CURSOR two-pass black-then-white technique lines ~170-183 — the halo is the same draw-twice idea)
    - 14-UI-SPEC.md §"Snap indicator glyphs (D-04)" (the authoritative size/stroke/color table)
    - src/renderer/src/stores/viewerStore.ts (existing slice shape to append snap state)
  </read_first>
  <action>
    Create src/renderer/src/components/markup/SnapIndicator.tsx. Props: `candidate: { point: StagePoint; kind: 'vertex' | 'segment' } | null`, `currentZoom: number`, `underlyingColor?: string` (markup color the glyph overlaps, for halo contrast). Declare module-scope screen-pixel constants SQUARE_PX=12, TRIANGLE_PX=14, STROKE_PX=2, HALO_STROKE_PX=3.5; divide each by currentZoom at the top of the body. Render NOTHING when candidate is null. For `kind:'vertex'` render a Konva `Rect` (axis-aligned 12/zoom square, centered on candidate.point, transparent fill, accent #0078d4 stroke); for `kind:'segment'` render a `RegularPolygon sides={3}` (14/zoom circum-diameter, point up). Two-pass halo: draw the wider halo copy first (stroke HALO_STROKE_PX/zoom, color = `getContrastingInk(underlyingColor ?? '#ffffff')`) then the accent copy on top (stroke STROKE_PX/zoom, COLORS.accent). EVERY shape `listening={false}`. Do NOT ship the ✕ glyph (reserved, D-06). In src/renderer/src/stores/viewerStore.ts add `snapEnabled: boolean` (default true) with `setSnapEnabled`, and `snapSuspended: boolean` (default false) with `setSnapSuspended` — transient runtime state, NOT persisted to .clmc.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json; npx vitest run src/tests/highlight-overlay-listening.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/renderer/src/components/markup/SnapIndicator.tsx | grep -c "listening={false}"` ≥ 2 (both glyph passes non-listening)
    - `grep -n "/ currentZoom" src/renderer/src/components/markup/SnapIndicator.tsx` shows every size/stroke divided by currentZoom (square, triangle, both strokes)
    - `grep -n "getContrastingInk" src/renderer/src/components/markup/SnapIndicator.tsx` returns ≥1 (halo uses the existing contrast picker)
    - Uses `RegularPolygon` (sides 3) for △ and `Rect` for □; no ✕/cross glyph rendered (grep finds no cross/X shape)
    - Colors are COLORS.accent #0078d4 only (no new hex literals introduced)
    - viewerStore exposes snapEnabled (default true) + snapSuspended (default false) with setters; neither appears in the serialize path (grep the serialize/persist call site — snap flags absent)
    - tsc clean; existing highlight-overlay-listening.test.ts still green
  </acceptance_criteria>
  <done>SnapIndicator renders exactly one zoom-compensated, haloed, non-listening □ or △ at the snapped point; snap on/suspend state lives in viewerStore (not persisted).</done>
</task>

<task type="auto">
  <name>Task 2: Snap injection in handleStageMouseMove + index rebuild + SnapIndicator render on transient layer</name>
  <read_first>
    - src/renderer/src/components/CanvasViewport.tsx (handleStageMouseMove lines 1125-1188 — the three pointer→page branches that must apply the snap override; the live-zoom getState read lines 1212-1213; the transient/overlay layer where SnapIndicator mounts above committed markups)
    - src/renderer/src/lib/snapping-engine.ts (14-02 — buildSnapIndex + resolveSnap signatures)
    - .planning/spikes/002-snapping-engine/README.md (cell = zoom-compensated tolerance; pageTol = 12 / zoom; rebuild on geometry change is acceptable <120ms at 50k)
    - 14-CONTEXT.md D-07 (in-progress markup snaps only to its own start vertex; never intermediate; never the dragged vertex)
  </read_first>
  <action>
    In CanvasViewport.tsx, build the snap index from the current page's committed markups whenever that geometry changes (page markups for currentPage) — derive vertices (every markup vertex with markupId+vertexIndex) and segments (every edge a→b with markupId+segmentIndex); set `cell = 12 / liveZoom` and `tol = 12 / liveZoom`. Hold the built index in a module ref (mirror vertexDragRef pattern) so handleStageMouseMove reads it without widening its dep list; rebuild the ref via a useEffect keyed on the page's markups + zoom. In handleStageMouseMove, in the placement branch (1183-1185), the vertex-drag branch (1138-1155), and the body-drag branch (1159-1170): after computing `pt = stage.getAbsoluteTransform().copy().invert().point(pointer)`, if `snapEnabled && !snapSuspended` call `resolveSnap(index, pt, tol, exclude)` and, if a candidate is returned, OVERRIDE pt with `candidate.point` before it flows into updateMarkupPreview/setDragPreview; store the candidate (or null) in a `snapCandidate` state for the glyph. Compute `exclude` per D-07: for placement the in-progress markup id with allowVertexIndices=[0] (close-the-loop only); for vertex-drag the edited markup id with blockVertexIndex = the dragged vertex; for body-drag exclude the whole dragged markup. Render `<SnapIndicator candidate={snapCandidate} currentZoom={...} underlyingColor={...} />` on the transient/overlay layer ABOVE committed markups (same layer family as VertexHandleOverlay). Clear snapCandidate to null when snapping is off/suspended or the pointer leaves.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json; npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "resolveSnap" src/renderer/src/components/CanvasViewport.tsx` appears in the placement branch, the vertex-drag branch, AND the body-drag branch (3 call sites, D-07 placement + edit)
    - `grep -n "buildSnapIndex" src/renderer/src/components/CanvasViewport.tsx` is inside a useEffect keyed on page markups + zoom (rebuild-on-change), not inside handleStageMouseMove
    - `cell` and `tol` are both `12 / liveZoom` (zoom-compensated, screen-constant) — grep shows `/ liveZoom` or `/ currentZoom` on both
    - The snap override mutates the local `pt` BEFORE updateMarkupPreview/setDragPreview/moveVertex consume it (read the diff: override precedes the consumer call in each branch)
    - exclude for placement passes allowVertexIndices including 0 and the in-progress markup id; vertex-drag passes blockVertexIndex (D-07 enforced)
    - `<SnapIndicator` is rendered on the transient/overlay layer (above committed markups), passed the live snapCandidate
    - `npm run build` succeeds (renderer compiles)
  </acceptance_criteria>
  <done>The cursor's page-point is overridden to the snapped point during placement and editing per D-07; the index rebuilds on geometry change; the □/△ glyph tracks the active candidate.</done>
</task>

<task type="auto">
  <name>Task 3: Snap keybindings (Alt suspend / F3 toggle) + StatusBar Snap segment</name>
  <read_first>
    - src/renderer/src/hooks/useKeyboardShortcuts.ts (central keydown switch + isTextInputActive guard lines 27-42; Ctrl+A select-all line 185 — confirm bare 'A'/'F3' are free; this hook handles keydown — Alt suspend also needs keyup to restore)
    - src/renderer/src/components/StatusBar.tsx (the Scale segment lines 83-86 + Divider lines 6-18 + aria-live="polite" — append-a-segment pattern)
    - 14-UI-SPEC.md §"Snapping activation & override (D-03)" + §"Feedback Surfaces → Snap status indicator" (ON #cccccc + accent dot; held off dimmed #808080; OFF amber #e8a838)
    - src/renderer/src/stores/viewerStore.ts (snapEnabled/snapSuspended setters from Task 1)
  </read_first>
  <action>
    In useKeyboardShortcuts.ts add: `F3` (bare) → toggle `setSnapEnabled(!snapEnabled)` (persistent on/off, D-03); `Alt` keydown → `setSnapSuspended(true)` and the corresponding `Alt` keyup → `setSnapSuspended(false)` (momentary suspend for one point). Guard both with `isTextInputActive()` exactly as every existing shortcut does. Do NOT collide with existing bindings (Ctrl+A select-all stays; bare A is reserved for arc mode in 14-04 — do not bind bare A here). Register the keyup listener alongside the existing keydown registration and clean it up symmetrically. In StatusBar.tsx, after the Scale segment append a `<Divider />` then a `<span aria-live="polite">` reading `Snap: ON` (text #cccccc with an accent #0078d4 dot), `Snap: held off` (dimmed #808080) when suspended, or `Snap: OFF` (COLORS.warning #e8a838) when disabled — read snapEnabled/snapSuspended from viewerStore. Reuse the existing 13px/400 / 16px-padding chrome verbatim; introduce no new hex outside COLORS.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json; npx vitest run src/tests/markup-shortcuts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "F3" src/renderer/src/hooks/useKeyboardShortcuts.ts` toggles snapEnabled; guarded by isTextInputActive()
    - Alt keydown sets snapSuspended true and Alt keyup sets it false (both listeners present + symmetric cleanup); guarded by isTextInputActive()
    - No bare-'a'/'A' binding is added in this plan (reserved for arc mode 14-04) — grep confirms only Ctrl+A remains
    - StatusBar shows three states: ON (accent dot), held off (dimmed #808080), OFF (amber #e8a838) — driven by snapEnabled/snapSuspended; aria-live="polite" present
    - No new hex literals outside COLORS.* + the existing #cccccc/#808080/#3c3c3c StatusBar tokens
    - tsc clean; existing markup-shortcuts.test.ts green (no regression to existing shortcuts)
  </acceptance_criteria>
  <done>F3 toggles snapping persistently, Alt suspends it momentarily (release restores), and the StatusBar pill shows ON/held-off/OFF; manual-ready snapping caption captured in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.clmc` markups → snap index build | Loaded vertex coords feed buildSnapIndex each page change |
| Keyboard → snap state | Local key events flip transient runtime flags only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Denial of Service | index rebuild on hostile geometry | mitigate | snapping-engine (14-02) skips non-finite vertices on build; rebuild is bounded by page vertex count + debounced to geometry-change effect |
| T-14-03-02 | Tampering | snap flags persisted into .clmc | mitigate | snapEnabled/snapSuspended are runtime-only viewerStore state, explicitly excluded from the serialize path (Task 1 acceptance) |
| T-14-03-SC | Tampering | npm package installs | accept | No new packages |
| T-14-03-W | Spoofing/Repudiation/Info-Disclosure/Elevation | (web threats) | accept | N/A — offline single-user desktop app |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds
- `npx vitest run src/tests/highlight-overlay-listening.test.ts src/tests/markup-shortcuts.test.ts` green
- Manual: on a calibrated page with existing markups, drawing a new linear shows □ on endpoints and △ on segments; Alt held removes the glyph; F3 toggles OFF (amber) and back
</verification>

<success_criteria>
- Cursor snaps to vertices (□) and on-segment points (△) at constant screen tolerance, during placement and editing, honoring D-07.
- Snap ON by default; Alt suspends; F3 toggles; StatusBar reflects state.
- Resolve is index-driven (no linear scan) — instant at thousands of vertices.
</success_criteria>

<output>
Create `.planning/phases/14-markup-geometry-precision/14-03-SUMMARY.md` when done, including the manual-ready snapping + glyph captions (□/△/close-the-loop) from the UI-SPEC.
</output>
