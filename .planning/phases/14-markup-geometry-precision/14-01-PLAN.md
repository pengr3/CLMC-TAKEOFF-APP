---
phase: 14-markup-geometry-precision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/types/markup.ts
  - src/renderer/src/lib/arc-math.ts
  - src/renderer/src/lib/markup-math.ts
  - src/tests/arc-math.test.ts
  - src/tests/markup-math-arc.test.ts
autonomous: true
requirements: [D-01, D-08]
must_haves:
  truths:
    - "A linear/area/perimeter/wall edge can carry optional arc metadata (on-arc midpoint) without breaking pre-Phase-14 .clmc files (they load all-straight)"
    - "Reported length uses true arc length for arc edges (R·sweep), straight chord for straight edges"
    - "Reported area applies the circular-segment correction with the correct OUTWARD/INWARD sign for both bulge directions"
    - "A 90° arc edge reports ~10% more length than its straight chord (under-measurement closed)"
  artifacts:
    - path: "src/renderer/src/lib/arc-math.ts"
      provides: "3-point circle solver, sweep disambiguation, closed-form arc length, circular-segment magnitude"
      exports: ["solveCircle", "arcLength", "circularSegmentMagnitude"]
    - path: "src/renderer/src/lib/markup-math.ts"
      provides: "arc-aware polylineLength + polygonArea (optional arcs arg)"
      contains: "arcs"
    - path: "src/renderer/src/types/markup.ts"
      provides: "optional per-edge arcs field + reshape-arc MarkupCommand variant"
      contains: "reshape-arc"
  key_links:
    - from: "src/renderer/src/lib/markup-math.ts"
      to: "src/renderer/src/lib/arc-math.ts"
      via: "import arcLength + circularSegmentMagnitude"
      pattern: "from './arc-math'"
---

<objective>
Foundation for curved-edge measurement (MM-05). Add additive per-edge arc metadata to the Markup model (no formatVersion bump — old files load all-straight), implement the validated 3-point circular-arc math as a new pure module, and make `polylineLength` / `polygonArea` arc-aware so length and area are measured exactly for curved edges. Cover every numeric behavior with vitest assertions pinned to the spike oracles.

Purpose: Everything downstream (arc drawing, arc editing, BOQ round-trip) depends on this data model + math being correct first.
Output: `arc-math.ts` (new), arc-aware `markup-math.ts`, extended `types/markup.ts`, two new test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-markup-geometry-precision/14-CONTEXT.md
@.planning/phases/14-markup-geometry-precision/14-PATTERNS.md
@.planning/spikes/003-arc-segment-measure/README.md
@.planning/spikes/003b-curved-polygon-area/README.md

<interfaces>
From src/renderer/src/types/markup.ts (current):
- `StagePoint` = { x: number; y: number } (imported from '../hooks/useCalibrationMode')
- `LinearMarkup | AreaMarkup | PerimeterMarkup | WallMarkup` each carry `points: StagePoint[]`
- `WallMarkup.wallHeight` is the precedent additive optional-ish field
- `MarkupCommand` discriminated union (lines 52-112); `move-vertex` variant at lines 79-87 is the shape to mirror
- `isMultiPointMarkup(markup)` type guard exists (line 136)

From src/renderer/src/lib/markup-math.ts (current):
- `polylineLength(points: StagePoint[]): number` (lines 7-14, straight only)
- `polygonArea(points: StagePoint[]): number` (lines 16-26, abs shoelace)
- both throw-validate style established by `pixelLengthToReal` (lines 39-43)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add additive arc metadata + reshape-arc command to the Markup model</name>
  <read_first>
    - src/renderer/src/types/markup.ts (the discriminated union lines 5-43 and MarkupCommand union lines 52-112 — mirror the move-vertex shape lines 79-87 and the WallMarkup.wallHeight additive precedent lines 36-41)
    - src/renderer/src/lib/project-schema.ts (lines 83-119 — the `hiddenItemNames?` additive field precedent that needed NO formatVersion bump; confirm validateV2 cast accepts unknown extra fields silently)
    - .planning/spikes/003-arc-segment-measure/README.md (Data model paragraph — "arcs map keyed by segment index { midX, midY }")
  </read_first>
  <action>
    In src/renderer/src/types/markup.ts add an OPTIONAL field `arcs?: Record<number, { midX: number; midY: number }>` to `LinearMarkup`, `AreaMarkup`, `PerimeterMarkup`, and `WallMarkup` (NOT to `CountMarkup` — single point, no edges). Key = the segment's start-vertex index (edge i→i+1; for closed area/perimeter the closing edge n-1→0 keys on index n-1). Document inline that absence means a straight edge and pre-Phase-14 `.clmc` files omit it and load all-straight (additive, no formatVersion bump — same strategy as `hiddenItemNames?`). Extend the `MarkupCommand` union with a new variant `{ type: 'reshape-arc'; markupId: string; page: number; oldArcs?: Record<number, { midX: number; midY: number }>; newArcs?: Record<number, { midX: number; midY: number }> }` mirroring the move-vertex variant's old/new symmetry so undo/redo is reversible. Do NOT alter project-schema.ts validators — the `arcs` field rides the existing `Markup[]` cast in `validateV2` exactly as `wallHeight` does; confirm this in the SUMMARY rather than adding a validator branch.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "arcs?: Record<number" src/renderer/src/types/markup.ts` returns ≥4 matches (one per multi-point markup type)
    - `grep -n "reshape-arc" src/renderer/src/types/markup.ts` returns ≥1 match
    - `CountMarkup` interface does NOT contain `arcs` (grep the CountMarkup block specifically)
    - `npx tsc --noEmit` passes with zero errors (additive optional field does not break existing call sites)
    - No edit to src/renderer/src/lib/project-schema.ts (additive field rides the existing cast)
  </acceptance_criteria>
  <done>Multi-point markups carry an optional per-edge arc map; MarkupCommand has a symmetric reshape-arc variant; tsc clean; no schema validator change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement arc-math.ts (3-point circle solver + arc length + segment magnitude) with oracle-pinned tests</name>
  <read_first>
    - src/renderer/src/lib/markup-math.ts (the sibling pure-math module — copy its import style for StagePoint, its throw-validate idiom lines 39-43, NO React/Konva imports)
    - .planning/spikes/003-arc-segment-measure/README.md (solveCircle via perpendicular-bisector determinant `d = 2·signedArea(p1,p2,p3)`; collinear fallback when d→0; CCW-sweep major/minor disambiguation; length = R·sweep; accuracy ~1e-10 vs numerical oracle)
    - .planning/spikes/003b-curved-polygon-area/README.md (segMag = (R²/2)·(θ − sinθ); holds for major arcs θ>π too)
  </read_first>
  <action>
    Create src/renderer/src/lib/arc-math.ts as a pure module (import only `StagePoint`; no React/Konva). Export `solveCircle(p1: StagePoint, mid: StagePoint, p2: StagePoint): { cx: number; cy: number; r: number; sweep: number; collinear: boolean }` — implement the perpendicular-bisector determinant solve with `d = 2·signedArea`, return `collinear: true` (and finite chord fallback) when `|d|` is below a scale-aware tolerance. Implement CCW-sweep major/minor disambiguation so the solved arc passes THROUGH `mid`. Export `arcLength(from: StagePoint, mid: StagePoint, to: StagePoint): number` returning `R·sweep`, falling back to straight chord distance `euclideanDistance(from,to)` when collinear. Export `circularSegmentMagnitude(from: StagePoint, mid: StagePoint, to: StagePoint): number` returning `(R²/2)·(sweep − sin(sweep))`, returning 0 when collinear. Reuse `euclideanDistance` from './scale-math' (already imported by markup-math.ts) for the chord fallback. Guard every public function against NaN/Infinity inputs (finite-check; return the straight-chord/zero fallback rather than throwing) — this is the defensive guard the threat model requires for malformed save-file arc metadata. Create src/tests/arc-math.test.ts asserting the spike-003 numbers.
  </action>
  <verify>
    <automated>npx vitest run src/tests/arc-math.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Semicircle case: `arcLength` of a 3-point semicircle of radius 50 equals `Math.PI*50` (157.0796…) within 1e-6
    - Quarter-circle R=2000, 90° sweep: `arcLength` ≈ 3141.593 px and exceeds the straight chord 2828.427 px by ~9.97% (within 1e-3 relative) — the under-measurement regression-direction check
    - Major/reflex arc (sweep > 180°): solver returns sweep > π and `arcLength` matches the spike value 593.41 (R built per spike) within 1e-6
    - Collinear input (3 points on a line): `solveCircle().collinear === true` and `arcLength` falls back to the straight chord distance exactly
    - `circularSegmentMagnitude` for an outward semicircle (R=50) equals `(50²/2)·(π − sin π)` = `1250·π` within 1e-6
    - NaN/Infinity in any input coordinate: functions return a finite fallback (no NaN, no Infinity, no throw) — assert with `Number.isFinite`
    - No `import` of 'react' or 'konva' in arc-math.ts (grep returns nothing)
  </acceptance_criteria>
  <done>arc-math.ts exports a validated 3-point solver, arc length, and segment magnitude matching the spike-003/003b oracles to ≤1e-6, with finite-guarded inputs; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make polylineLength + polygonArea arc-aware (optional arcs arg, sign rule) with oracle-pinned tests</name>
  <read_first>
    - src/renderer/src/lib/markup-math.ts (the EXISTING polylineLength lines 7-14 and polygonArea lines 16-26 — these get the optional second arg; do NOT change the single-arg signature for straight callers)
    - src/renderer/src/lib/boq-aggregator.ts (lines 130-160 — the production callers `polylineLength(points)` and `polygonArea(points)`; they MUST keep compiling unchanged)
    - .planning/spikes/003b-curved-polygon-area/README.md (LOAD-BEARING sign rule: accumulate doubled signed shoelace 2·S; per arc edge subtract `sign(cross)·2·segMag` where `cross = (to−from)×(mid−from)`; OUTWARD ⟺ sign(cross) ≠ sign(2·S); abs at the very end → winding-independent)
  </read_first>
  <action>
    Modify src/renderer/src/lib/markup-math.ts. Import `arcLength` and `circularSegmentMagnitude` from './arc-math'. Add an OPTIONAL second parameter `arcs?: Record<number, { midX: number; midY: number }>` to BOTH `polylineLength` and `polygonArea` (keep the one-arg call valid so boq-aggregator.ts and save-load keep compiling — this is the load-bearing back-compat constraint from 14-PATTERNS.md). In `polylineLength`: for each segment i→i+1, if `arcs[i]` is present use `arcLength(points[i], {x:arcs[i].midX,y:arcs[i].midY}, points[i+1])`, else the existing straight chord. In `polygonArea`: keep the doubled signed shoelace accumulation `2·S` (do NOT pre-abs); then for each edge with an arc midpoint compute `cross = (to−from)×(mid−from)`, `segMag = circularSegmentMagnitude(from, mid, to)`, and subtract `sign(cross)·2·segMag` from the running doubled-signed total; return `abs(total)/2` at the very end. The closed area loops edges i→(i+1)%n so the closing edge keys on `arcs[n-1]`. Add cases to a new src/tests/markup-math-arc.test.ts pinned to spike-003b named cases.
  </action>
  <verify>
    <automated>npx vitest run src/tests/markup-math-arc.test.ts src/tests/markup-math.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Existing src/tests/markup-math.test.ts still passes unchanged (straight callers untouched — back-compat)
    - Square (L=100) + OUTWARD semicircle edge (R=50): `polygonArea` = 13926.9908 within 1e-6 (spike-003b named case)
    - Square (L=100) + INWARD semicircle edge (R=50): `polygonArea` = 6073.0092 within 1e-6 — proves the sign rule fires correctly for BOTH bulge directions
    - Stadium (2 straight + 2 arc, W=200,H=80): `polygonArea` = 21026.5482 within 1e-6
    - Winding-independence: reversing the vertex order of any arc case yields the same area magnitude within 1e-9
    - `polylineLength([a,b], { 0: midOnArc })` for a quarter-circle edge ≈ true arc length (R·sweep), and exceeds the straight chord length by ~10% for a 90° bend
    - `grep -n "polylineLength(" src/renderer/src/lib/boq-aggregator.ts` callers still type-check (`npx tsc --noEmit` clean)
  </acceptance_criteria>
  <done>polylineLength + polygonArea accept optional per-edge arc metadata and apply the validated arc-length + signed-segment correction; straight callers unchanged; oracle cases green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.clmc` file → renderer geometry math | Arc metadata (midX/midY) loaded from a saved project file is untrusted numeric input reaching solveCircle / polygonArea |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Denial of Service | `arc-math.ts` solveCircle/arcLength | mitigate | Finite-check (Number.isFinite) on all input coords; NaN/Infinity → straight-chord/zero fallback, never throw, never loop (Task 2 acceptance) |
| T-14-01-02 | Tampering | `polygonArea` with hostile arcs map | mitigate | circularSegmentMagnitude returns 0 on collinear/degenerate; sign rule operates on finite values only; result clamped via abs |
| T-14-01-SC | Tampering | npm package installs | accept | No new packages installed in this plan (pure TS + existing vitest) — supply-chain surface unchanged |
| T-14-01-W | Spoofing/Repudiation/Info-Disclosure/Elevation | (web threats) | accept | N/A — offline single-user Electron desktop app, no network/auth/multi-tenant surface |
</threat_model>

<verification>
- `npx tsc --noEmit` clean across the whole renderer
- `npx vitest run src/tests/arc-math.test.ts src/tests/markup-math-arc.test.ts src/tests/markup-math.test.ts` all green
- Old `.clmc` round-trip is NOT broken: a markup with no `arcs` field measures identically to pre-Phase-14 (covered by unchanged markup-math.test.ts)
</verification>

<success_criteria>
- Optional `arcs` metadata exists on all four multi-point markups; reshape-arc command added; no formatVersion bump.
- arc-math.ts matches spike-003 to ≤1e-6; markup-math arc area matches spike-003b named cases to ≤1e-6 for both bulge directions.
- A curved edge no longer under-measures (90° arc reports ~10% more than its chord).
</success_criteria>

<output>
Create `.planning/phases/14-markup-geometry-precision/14-01-SUMMARY.md` when done.
</output>
