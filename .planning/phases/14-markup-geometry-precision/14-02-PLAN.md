---
phase: 14-markup-geometry-precision
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/lib/snapping-engine.ts
  - src/renderer/src/lib/self-intersection.ts
  - src/tests/snapping-engine.test.ts
  - src/tests/self-intersection.test.ts
autonomous: true
requirements: [D-05, D-06, D-07, D-09]
must_haves:
  truths:
    - "A spatial index returns the same nearest vertex/segment as a brute-force linear scan over the same point set (0 mismatches)"
    - "Snap resolve stays well under one frame budget at N≈1k–10k vertices"
    - "Nearest-point-on-segment snapping is correct because segments are indexed by their tolerance-padded bounding box (not endpoints only) — this plus vertex/endpoint snap are the shipped targets (D-05)"
    - "Only vertex/endpoint and nearest-point-on-segment targets are built this phase; intersection-snap and grid-snap are deferred (D-06)"
    - "A self-intersecting closed boundary is detected and the offending segment indices are returned (so D-09 can highlight them red)"
  artifacts:
    - path: "src/renderer/src/lib/snapping-engine.ts"
      provides: "uniform grid-hash spatial index; vertex + nearest-point-on-segment resolve"
      exports: ["buildSnapIndex", "resolveSnap"]
    - path: "src/renderer/src/lib/self-intersection.ts"
      provides: "O(n²) closed-boundary self-crossing detector returning offending segment indices"
      exports: ["findSelfIntersection"]
  key_links:
    - from: "src/renderer/src/lib/snapping-engine.ts"
      to: "spike-002 grid-hash design"
      via: "cell = zoom-compensated tolerance, 3×3 vertex scan, bbox-padded segment cells"
      pattern: "cell"
---

<objective>
Foundation for snapping (MM-06) and the self-intersection guard (D-09). Implement the validated spike-002 uniform grid-hash spatial index as a new pure module (no codebase analog exists), and a pure self-intersection detector for closed boundaries. Both are pure functions with brute-force correctness oracles, so both carry exact vitest assertions.

Purpose: The snap resolve and the commit guard both need correct, fast pure geometry primitives before they can be wired into the canvas pipeline (Waves 2 + 4).
Output: `snapping-engine.ts` (new, no analog — built from spike-002), `self-intersection.ts` (new), two test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-markup-geometry-precision/14-CONTEXT.md
@.planning/phases/14-markup-geometry-precision/14-PATTERNS.md
@.planning/spikes/002-snapping-engine/README.md
@.planning/spikes/003b-curved-polygon-area/README.md

<interfaces>
From src/renderer/src/hooks/useCalibrationMode (existing):
- `StagePoint` = { x: number; y: number }

Snap-target rule (D-07) the resolve API must support:
- snap to OTHER markups' vertices + segments freely
- for the in-progress/edited markup: snap ONLY to its own start vertex (close-the-loop); never its intermediate vertices; never the vertex currently being dragged
Tolerance is zoom-compensated: pageTol = 12 / currentZoom (spike-002 recommends cell = tolerance, 1×–2×).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement snapping-engine.ts (uniform grid-hash) with brute-force-oracle tests</name>
  <read_first>
    - .planning/spikes/002-snapping-engine/README.md (FULL — the design oracle: bucket by floor(x/cell)/floor(y/cell); cell = zoom-compensated tolerance ~12 page units; scan 3×3 vertex neighbourhood; segments inserted into EVERY cell their tol-padded bbox overlaps so a single-cell query is exhaustive; intersection snap DEFERRED; vertex query 2–15µs at N=1k–50k; 0 mismatches vs brute force; full rebuild <120ms even at 50k is acceptable)
    - src/renderer/src/lib/markup-math.ts (sibling pure-math style — StagePoint import, no React/Konva, this is where the file lives)
  </read_first>
  <action>
    Create src/renderer/src/lib/snapping-engine.ts as a pure module (import `StagePoint` only; no React/Konva). Define a `SnapCandidate` shape carrying the snapped page-point, a discriminator `kind: 'vertex' | 'segment'` (□ vs △ per D-04), and a provenance id so D-07's exclusion rules can be applied by the caller (e.g. `markupId`, `vertexIndex`). Export `buildSnapIndex(input: { vertices: Array<{ point: StagePoint; markupId: string; vertexIndex: number }>; segments: Array<{ a: StagePoint; b: StagePoint; markupId: string; segmentIndex: number }>; cell: number }): SnapIndex` — build a uniform grid hash keyed by `floor(x/cell),floor(y/cell)`; insert each vertex into its single cell; insert each segment into every cell its `cell`-padded bbox overlaps (the bbox-padded rule — endpoint-only is wrong per spike). Export `resolveSnap(index: SnapIndex, cursor: StagePoint, tol: number, exclude?: { markupId: string; allowVertexIndices?: number[]; blockVertexIndex?: number }): SnapCandidate | null` — scan the 3×3 vertex neighbourhood and the single cursor cell for bbox-indexed segments; among candidates within `tol`, prefer vertices over segments (D-04 □ over △), nearest wins; apply the exclude filter so the in-progress markup contributes ONLY its start vertex (caller passes allowVertexIndices=[0]) and never the dragged vertex (blockVertexIndex). Guard against NaN/Infinity vertices on build (skip non-finite points). Create src/tests/snapping-engine.test.ts with a deterministic seeded point set and a brute-force `nearestWithinTol` reference, asserting parity.
  </action>
  <verify>
    <automated>npx vitest run src/tests/snapping-engine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Over a deterministic seeded set of N=1000 vertices and 2000 random cursor queries, `resolveSnap` returns the SAME nearest-vertex-within-tolerance as a brute-force linear scan for EVERY query: 0 mismatches
    - Repeat the parity check at N=10000: still 0 mismatches
    - Nearest-point-on-segment: for a set of long segments crossing cells with distant endpoints, `resolveSnap` finds on-segment hits that an endpoint-only index would miss (assert ≥1 such hit is found that the naive endpoint-only path misses)
    - Vertex preference: when a vertex and a segment are both within tolerance, the returned candidate has `kind: 'vertex'`
    - D-07 exclusion: with `exclude={markupId:'X', allowVertexIndices:[0]}`, no candidate from markup X has vertexIndex other than 0; with `blockVertexIndex:k`, vertex k of the excluded markup is never returned
    - Build with a vertex containing Infinity does not throw and that vertex is never returned
    - Performance smoke: 10000 resolveSnap calls at N=10000 complete in under 100ms total (well under per-frame budget) — assert wall-clock with a generous ceiling
  </acceptance_criteria>
  <done>snapping-engine.ts returns brute-force-correct nearest vertex/segment via a grid hash with bbox-padded segment cells, honors D-07 exclusion, guards non-finite input, and is fast; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement self-intersection.ts (closed-boundary crossing detector) with tests</name>
  <read_first>
    - src/renderer/src/lib/markup-math.ts (polygonArea shoelace loop lines 16-26 — same "loop the closed point ring, pairwise edge work" shape to mirror)
    - .planning/spikes/003b-curved-polygon-area/README.md (lines 99-103 — "a huge INWARD bulge can dip past the opposite edge and self-intersect … add a validation guard at markup time"; the guard returns the offending segments so D-09 can highlight them)
  </read_first>
  <action>
    Create src/renderer/src/lib/self-intersection.ts as a pure module (import `StagePoint`; no React/Konva). Export `findSelfIntersection(points: StagePoint[]): { i: number; j: number; point: StagePoint } | null` — treat `points` as a CLOSED boundary (edge i→(i+1)%n, including the closing edge), run the classic O(n²) segment-pair crossing test over all non-adjacent edge pairs (adjacent edges share an endpoint and must NOT count as a crossing), and return the first crossing's two edge indices plus the intersection point, or null if the boundary is simple. Use a standard orientation/segment-intersection predicate (sign of cross products); treat collinear-overlap as a crossing. Guard non-finite input (return null rather than throwing). This is a straight-chord boundary guard — the arc sagitta cap (Wave 3) keeps most arcs from self-intersecting, so straight-vertex detection is the load-bearing protection D-09 needs. Create src/tests/self-intersection.test.ts.
  </action>
  <verify>
    <automated>npx vitest run src/tests/self-intersection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A simple convex quad (4 vertices, no crossing) → `findSelfIntersection` returns null
    - A bowtie/figure-eight quad (vertices ordered so two edges cross) → returns the two crossing edge indices and an intersection point within 1e-6 of the analytic crossing
    - Adjacent edges sharing a vertex are NOT reported as a crossing (a normal polygon corner returns null)
    - A self-touching boundary where a vertex lies exactly on a non-adjacent edge is detected (collinear-overlap counts)
    - Non-finite point in the ring → returns null (no throw)
    - Fewer than 4 points → returns null (a triangle cannot self-intersect)
  </acceptance_criteria>
  <done>self-intersection.ts detects closed-boundary self-crossings, ignores shared-vertex corners, returns offending segment indices for the red highlight, and guards bad input; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.clmc` file → snapping/guard math | Markup vertex coordinates loaded from a saved project drive grid-index build and the crossing test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Denial of Service | `buildSnapIndex` with hostile coords | mitigate | Non-finite vertices skipped on build (Task 1 acceptance); cell ≥ tolerance prevents the pathological 25-cell scan blowup the spike flagged |
| T-14-02-02 | Denial of Service | `findSelfIntersection` O(n²) | mitigate | Pure bounded loop over n edges; non-finite input short-circuits to null; n is the markup's own vertex count (estimator-bounded, not attacker-streamed) |
| T-14-02-SC | Tampering | npm package installs | accept | No new packages (pure TS + existing vitest) |
| T-14-02-W | Spoofing/Repudiation/Info-Disclosure/Elevation | (web threats) | accept | N/A — offline single-user desktop app, no network/auth surface |
</threat_model>

<verification>
- `npx vitest run src/tests/snapping-engine.test.ts src/tests/self-intersection.test.ts` green
- `npx tsc --noEmit` clean
- Brute-force parity is the correctness oracle for snapping; analytic crossing points are the oracle for self-intersection
</verification>

<success_criteria>
- Grid-hash snap resolve is brute-force-correct (0 mismatches) and fast at N≈1k–10k.
- D-07 exclusion rules are enforceable through the resolve API.
- Self-intersection detector returns offending segment indices for D-09.
</success_criteria>

<output>
Create `.planning/phases/14-markup-geometry-precision/14-02-SUMMARY.md` when done.
</output>
