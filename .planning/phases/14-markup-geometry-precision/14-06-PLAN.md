---
phase: 14-markup-geometry-precision
plan: 06
type: execute
wave: 5
depends_on: ["14-01", "14-02", "14-03", "14-04", "14-05"]
files_modified:
  - src/renderer/src/lib/boq-aggregator.ts
  - src/renderer/src/components/markup/AreaMarkup.tsx
  - src/renderer/src/components/markup/PerimeterMarkup.tsx
  - src/renderer/src/components/markup/LinearMarkup.tsx
  - src/renderer/src/components/markup/WallMarkup.tsx
  - src/tests/arc-roundtrip.test.ts
  - .planning/phases/14-markup-geometry-precision/14-MANUAL-NOTES.md
autonomous: false
requirements: [D-01, D-04, D-08, D-09]
must_haves:
  truths:
    - "Arc geometry round-trips through save/reload: an arc edge saved to .clmc loads back as the same arc and measures identically (SC #5)"
    - "The BOQ export reports arc-aware length and curved-segment-corrected area for arc edges, not straight-only values (SC #4)"
    - "On-canvas rendering of area/perimeter/linear/wall edges draws the true arc curve where an arc edge exists (not a straight chord)"
    - "Every new tool/gesture/shortcut/indicator has a manual-ready caption + shortcuts table (project-howto-manual constraint)"
    - "A human confirms the five ROADMAP success criteria end-to-end"
  artifacts:
    - path: "src/renderer/src/lib/boq-aggregator.ts"
      provides: "arc-aware length/area in the export pipeline"
      contains: "arcs"
    - path: ".planning/phases/14-markup-geometry-precision/14-MANUAL-NOTES.md"
      provides: "manual-ready captions + shortcuts table + indicator legend for every new feature"
      min_lines: 30
  key_links:
    - from: "src/renderer/src/lib/boq-aggregator.ts"
      to: "src/renderer/src/lib/markup-math.ts (arc-aware)"
      via: "pass markup.arcs into polylineLength / polygonArea"
      pattern: "\\.arcs"
    - from: "src/renderer/src/components/markup/AreaMarkup.tsx"
      to: "src/renderer/src/lib/arc-math.ts"
      via: "sample arc edges for the rendered closed boundary"
      pattern: "arc"
---

<objective>
Close the loop: make the BOQ export and the on-canvas markup renderers arc-aware (so reported quantities and the drawn shapes use the true arc, not the chord), prove arc geometry round-trips through save/reload + export with a test, assemble the manual-ready documentation for every new feature, and run the end-of-phase human UAT against the five ROADMAP success criteria.

Purpose: Satisfies ROADMAP success criteria #4 (no straight-only values for curved edges) and #5 (arc geometry round-trips through save/reload and BOQ export intact), plus the cross-cutting project-howto-manual constraint.
Output: arc-aware boq-aggregator + 4 markup renderers, arc round-trip test, 14-MANUAL-NOTES.md, UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-markup-geometry-precision/14-CONTEXT.md
@.planning/phases/14-markup-geometry-precision/14-PATTERNS.md
@.planning/phases/14-markup-geometry-precision/14-UI-SPEC.md
@.planning/ROADMAP.md
@.planning/phases/14-markup-geometry-precision/14-01-SUMMARY.md
@.planning/phases/14-markup-geometry-precision/14-03-SUMMARY.md
@.planning/phases/14-markup-geometry-precision/14-04-SUMMARY.md
@.planning/phases/14-markup-geometry-precision/14-05-SUMMARY.md

<interfaces>
From src/renderer/src/lib/markup-math.ts (14-01, arc-aware):
- `polylineLength(points, arcs?)` + `polygonArea(points, arcs?)`
From src/renderer/src/lib/boq-aggregator.ts (current):
- linear branch lines 130-133 `polylineLength((m as LinearMarkup).points)`
- area branch lines 134-137 `polygonArea((m as AreaMarkup).points)`
- perimeter branch lines 138-150 (length includes closing segment; area on the closed polygon)
- wall branch lines 151-160 `polylineLength(wallM.points)`
From src/renderer/src/lib/arc-math.ts (14-01): `solveCircle` to sample arc curves for rendering
The markup renderers (AreaMarkup/PerimeterMarkup/LinearMarkup/WallMarkup) currently draw straight Konva Lines between points; an arc edge must instead draw the sampled curve.
.clmc save/load already serializes the full Markup[] (the optional arcs field rides along — confirmed additive in 14-01, no schema change).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make boq-aggregator arc-aware + on-canvas renderers draw arc edges + round-trip test</name>
  <read_first>
    - src/renderer/src/lib/boq-aggregator.ts (linear lines 130-133, area 134-137, perimeter 138-150, wall 151-160 — pass `m.arcs` as the second arg to polylineLength/polygonArea; for perimeter the closing-augmented points need the closing arc key handled)
    - src/renderer/src/components/markup/AreaMarkup.tsx + PerimeterMarkup.tsx + LinearMarkup.tsx + WallMarkup.tsx (how each currently builds its Konva Line points from markup.points — an arc edge must sample the solved circle instead of a straight chord)
    - src/renderer/src/lib/arc-math.ts (solveCircle — sample N points along the arc for the rendered path)
    - .planning/spikes/003b-curved-polygon-area/README.md (perimeter length = Σ straight + Σ arc-length; area uses the original closed polygon with arc correction)
  </read_first>
  <action>
    In boq-aggregator.ts thread `m.arcs` into the math calls: linear → `polylineLength(points, m.arcs)`; area → `polygonArea(points, m.arcs)`; wall → `polylineLength(wallM.points, m.arcs)`; perimeter → length via `polylineLength(closingPts, ...)` (handle the closing-edge arc key consistently with how 14-01 keyed it) and area via `polygonArea(pts, m.arcs)`. In each of the four markup renderers, build the rendered path so that for any segment with an `arcs[i]` entry the path follows the sampled arc (use solveCircle + sample ~24 points along the sweep) instead of a straight chord; segments without an arc entry stay straight. Keep the existing label/length display reading the arc-aware markup-math so the on-canvas label matches the BOQ. Create src/tests/arc-roundtrip.test.ts: build a markup with an arcs map, serialize it through the project serialize path, re-validate via the schema (validateV2), and assert the arcs map survives byte-equal AND that aggregateBoq reports the arc-aware quantity (not the straight chord) for that markup.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json; npx vitest run src/tests/arc-roundtrip.test.ts src/tests/boq-aggregator.test.ts src/tests/boq-aggregator-wall.test.ts src/tests/project-serialize.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\.arcs" src/renderer/src/lib/boq-aggregator.ts` shows arcs passed into linear, area, perimeter, AND wall math calls
    - arc-roundtrip test: a markup with an arcs map serializes → validateV2 → deserializes with the arcs map intact (deep-equal), and aggregateBoq reports a quantity matching the arc-aware math (greater than the straight-chord value for the same shape) within 1e-6
    - Each of the 4 renderers samples the arc curve for arc edges (grep `solveCircle` or an arc-sampling helper in each) and stays straight for non-arc edges
    - Existing boq-aggregator.test.ts + boq-aggregator-wall.test.ts pass UNCHANGED (straight markups unaffected — arcs absent)
    - project-serialize.test.ts passes (no schema regression; additive field rides the existing Markup[] cast)
    - tsc clean
  </acceptance_criteria>
  <done>BOQ length/area and the on-canvas rendering use the true arc for arc edges; arc geometry round-trips through save/reload + export (SC #4, #5); straight markups unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble manual-ready documentation (14-MANUAL-NOTES.md)</name>
  <read_first>
    - 14-UI-SPEC.md (every "Manual-ready caption —" block: vertex snap □, segment snap △, close-the-loop, bulge handle, endpoint re-solve, arc mode, snapping, blocked commit)
    - .planning/phases/14-markup-geometry-precision/14-03-SUMMARY.md + 14-04-SUMMARY.md (the FINAL confirmed keybindings — F3 snap toggle, Alt snap suspend, bare A arc hold, sticky arc toggle key)
    - MEMORY note project-howto-manual (every new tool/gesture/shortcut/indicator documented manual-ready as built: shortcuts table + usage notes + indicator legend)
  </read_first>
  <action>
    Create .planning/phases/14-markup-geometry-precision/14-MANUAL-NOTES.md with three sections: (1) a SHORTCUTS TABLE listing every new key with its action and mode (F3 = toggle snapping on/off; Alt (hold) = suspend snapping for one click; A (hold) = make the next edge an arc; sticky arc toggle = the confirmed key; 3-click arc gesture = start/on-arc/end) — use the FINAL keys confirmed in the 14-03/14-04 SUMMARYs, not the proposed ones; (2) an INDICATOR LEGEND for the on-canvas glyphs/handles (□ blue square = vertex/endpoint/close-the-loop; △ blue triangle = nearest point on a segment; round blue handle = arc bulge / drag to bend; red highlight = self-crossing that blocks commit; ✕ reserved/not shipped) with the exact manual-ready captions copied verbatim from the UI-SPEC; (3) USAGE NOTES paragraphs for snapping, arc drawing, arc editing, and the blocked-commit recovery flow (the UI-SPEC manual-ready captions). This file is the manual-ready handoff; the end-user manual itself is a separate future phase (out of scope).
  </action>
  <verify>
    <automated>test -f .planning/phases/14-markup-geometry-precision/14-MANUAL-NOTES.md && grep -c "□\|△\|Snap\|Arc\|bulge" .planning/phases/14-markup-geometry-precision/14-MANUAL-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with a shortcuts table covering: snap toggle (F3), snap suspend (Alt hold), arc hold (A), arc sticky toggle, and the 3-click arc gesture
    - Indicator legend documents □, △, the round bulge handle, the red self-crossing highlight, and notes ✕ is reserved/not shipped
    - The exact manual-ready captions from the UI-SPEC are present verbatim for every new glyph/handle/gesture/shortcut
    - Keybindings match the FINAL confirmed keys from the 14-03/14-04 SUMMARYs (not the "[OPEN]/proposed" placeholders)
  </acceptance_criteria>
  <done>14-MANUAL-NOTES.md captures the shortcuts table, indicator legend, and usage captions for every Phase-14 feature, ready for the future manual phase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-of-phase human UAT against the five ROADMAP success criteria</name>
  <what-built>
    Snapping (□/△ glyphs, F3/Alt controls, spatial index), 3-click arc drawing (hold-A + sticky), arc editing (bulge handle + endpoint re-solve, undoable), self-intersection blocked commit (red highlight + message), and arc-aware measurement that round-trips through save/reload + BOQ export.
  </what-built>
  <how-to-verify>
    1. Open a calibrated multi-page plan. Start a linear markup near an existing markup's endpoint — confirm a blue □ appears and the point snaps to it; move along an existing segment — confirm a blue △ appears and snaps onto the line. Confirm the glyph size looks constant as you zoom 1x→8x.
    2. Press F3 — confirm the StatusBar shows "Snap: OFF" in amber and no glyph appears; press F3 again to restore. Hold Alt while placing one point — confirm the glyph disappears while held and returns on release.
    3. Draw an area on a page with thousands of existing vertices (or a dense test plan) — confirm snapping stays instant (no lag).
    4. Draw a linear edge, hold A, click an on-arc point then an end point — confirm the edge is a curve passing through the on-arc point; confirm a mixed straight+arc shape coexists. Toggle sticky arc mode and draw a run of arcs.
    5. Confirm the reported length of the curved edge is greater than its straight chord would be; export to BOQ and confirm the exported quantity reflects the arc (not the chord).
    6. Drag the round blue bulge handle — confirm the curve deepens/flattens and the length updates; drag past the safe limit — confirm the guide turns amber and stops; press Ctrl+Z once — confirm the whole reshape reverts. Drag an arc endpoint corner — confirm the arc re-bends to follow it.
    7. Trace an area whose outline crosses itself — confirm the commit is BLOCKED, the crossing is highlighted red, and the "Can't finish —" message appears; drag a corner apart to fix it, then finish — confirm it commits and a valid quantity appears in the totals/BOQ.
    8. Save the project, close, reopen — confirm every arc edge reloads as the same curve with the same measured length (round-trip intact).
  </how-to-verify>
  <verify>
    <human-check>All five ROADMAP success criteria (snapping + indicator; instant at scale; 3-click arc coexisting with straight; true arc length + signed area; self-intersection blocked + arc round-trips save/reload + BOQ) confirmed by the estimator.</human-check>
  </verify>
  <resume-signal>Type "approved" or list the failing scenarios (which become gap-closure plans via /gsd:plan-phase 14 --gaps).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.clmc` arcs field → BOQ math + renderer | Loaded arc metadata drives both the exported quantity and the drawn curve |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06-01 | Tampering | hostile arcs map in a loaded .clmc → wrong BOQ quantity or render crash | mitigate | arc-aware markup-math + solveCircle finite-guard (14-01) clamp degenerate arcs to straight fallback; self-intersection guard (14-05) blocks bad area/perimeter before commit |
| T-14-06-02 | Tampering | additive arcs field breaking old-file load | mitigate | arcs is optional, rides the existing Markup[] cast; validateV2 unchanged; round-trip test asserts old (arc-less) files still load + measure straight |
| T-14-06-SC | Tampering | npm package installs | accept | No new packages |
| T-14-06-W | Spoofing/Repudiation/Info-Disclosure/Elevation | (web threats) | accept | N/A — offline single-user desktop app |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run build` succeeds
- `npx vitest run src/tests/arc-roundtrip.test.ts src/tests/boq-aggregator.test.ts src/tests/boq-aggregator-wall.test.ts src/tests/project-serialize.test.ts` green
- Human UAT (Task 3) confirms all five ROADMAP success criteria
- On phase completion: update ROADMAP.md Phase 14 checkbox + STATE.md
</verification>

<success_criteria>
- BOQ + on-canvas rendering use true arc length / corrected area for arc edges (SC #4).
- Arc geometry round-trips through save/reload + BOQ export intact (SC #5).
- Manual-ready docs assembled; human UAT passes all five success criteria.
</success_criteria>

<output>
Create `.planning/phases/14-markup-geometry-precision/14-06-SUMMARY.md` when done. On UAT approval, mark ROADMAP Phase 14 complete and update STATE.md.
</output>
