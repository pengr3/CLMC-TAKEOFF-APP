---
phase: 09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion
plan: "05"
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03", "09-04"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "All 5 Phase 9 success criteria pass manual verification"
    - "ROADMAP.md marks Phase 9 as complete"
    - "STATE.md records Phase 9 closure"
  artifacts:
    - path: .planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-05-SUMMARY.md
      provides: "UAT results and closure record"
  key_links: []
---

<objective>
Wave 3 UAT and phase closure: manually verify all 5 Phase 9 success criteria, document results, update ROADMAP and STATE.

Purpose: Confirm the five UX improvements are fully functional before closing the phase.
Output: Verified UAT checklist; ROADMAP Phase 9 marked complete; STATE updated.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-00-SUMMARY.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-01-SUMMARY.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-02-SUMMARY.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-03-SUMMARY.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual UAT — verify all 5 Phase 9 success criteria</name>

  <what-built>
    - Plan 00: selectedMarkupIds in viewerStore; deleteGroup in markupStore
    - Plan 01: useDraggable hook; all 9 modals centered + draggable; MarkupNamePopup and ScalePopup converted to overlay-centered
    - Plan 02: Click-to-select on all 5 markup types; selection ring (accent color); Escape/empty-space deselect; Delete key; Ctrl+A
    - Plan 03: Rubber-band drag multi-select; group delete as single undo step; Enter key commits in-progress markup with min-point guard
    - Plan 04: RibbonButton component; RibbonToolbar with 7 tabs (Home/Page/Tools/View/Estimating/Settings/Help); App.tsx updated; Show All / Hide All
  </what-built>

  <how-to-verify>
    Run the app first: npm run dev

    SUCCESS CRITERION 1 — Click-to-select + Delete (single markup):
    A. Open a project (or create new + open PDF + calibrate)
    B. Place at least 3 markups of different types (e.g., one count pin, one linear, one area)
    C. Ensure the Tools tab is active in the ribbon; click the Select button
    D. Click on the count pin — expected: a blue accent ring appears around it
    E. Press Delete — expected: the count pin disappears from canvas and from totals panel
    F. Press Ctrl+Z — expected: the count pin reappears (undo works)
    G. Click on the linear markup, then press Delete — expected: linear removed; Ctrl+Z restores it
    PASS if: selection ring visible on click; Delete removes markup; Ctrl+Z restores it

    SUCCESS CRITERION 2 — Rubber-band multi-select + group delete:
    A. In 'select' mode, hold LMB and drag over at least 2 placed markups
    B. Expected: a blue translucent selection rectangle draws during drag; on release, both markups show selection rings
    C. Press Delete — expected: both markups removed simultaneously
    D. Press Ctrl+Z — expected: BOTH markups restored in a single undo step (one Ctrl+Z, not two)
    E. Try Ctrl+A — expected: all markups on the current page are selected (all show selection rings)
    F. Click on empty canvas — expected: all selection rings disappear (deselect)
    PASS if: rubber-band draws; multi-select works; group delete is one undo step; Ctrl+A selects all; empty click deselects

    SUCCESS CRITERION 3 — All modals centered and draggable:
    A. Click Set Scale in the Tools tab — expected: CalibrationDialog opens at viewport center
    B. Drag the CalibrationDialog body (not the input or button) — expected: modal repositions freely
    C. Cancel (Escape or Cancel button) then open Set Scale again — expected: dialog opens centered again (position reset)
    D. Right-click an existing markup → click Edit from context menu — expected: MarkupNamePopup opens centered (not at mouse position)
    E. Drag the MarkupNamePopup — expected: it repositions freely
    F. Trigger another modal (e.g., open a corrupted file to see an error modal, or try to close with unsaved changes) — expected: centered and draggable
    PASS if: every modal opens centered; dragging repositions; reopening resets to center

    SUCCESS CRITERION 4 — Ribbon toolbar with 7 tabs:
    A. Confirm the flat Toolbar is gone; a ribbon appears at the top with tab labels: Home, Page, Tools, View, Estimating, Settings, Help
    B. Home tab (default): Open, Save, Save As, Replace Plan PDF, Export buttons visible — all functional
    C. Page tab: Previous Page, page indicator, Next Page — clicking navigates pages
    D. Tools tab: Select, Count, Linear, Area, Perimeter, Wall, Set Scale visible — clicking activates each tool; chain badge chip appears when chain mode is armed
    E. View tab: Zoom In, Zoom Out, Fit to Window, Show/Hide Totals, Show All, Hide All — all functional
    F. Settings tab: shows "Coming soon" or equivalent stub message
    G. Help tab: shows "Coming soon" or equivalent stub message
    H. Ribbon panel is visibly taller than the old flat toolbar (buttons are square icon+label, ~60px)
    PASS if: all 7 tabs present and functional; buttons are visibly taller than old toolbar; no feature regressions

    SUCCESS CRITERION 5 — Enter key commits in-progress markup:
    A. Click the Linear tool; click two points on the canvas to draw a segment
    B. Press Enter — expected: the MarkupNamePopup appears (markup committed); do NOT double-click
    C. Name the markup and confirm
    D. Click the Area tool; click only ONE point; press Enter — expected: nothing happens (silent ignore, min-point guard)
    E. Click three points for Area; press Enter — expected: MarkupNamePopup appears
    F. Click the Wall tool; draw two points; press Enter — expected: MarkupNamePopup appears with wall height field
    PASS if: Enter commits Linear and Wall with 2+ points; Enter commits Area and Perimeter with 3+ points; Enter is silent with fewer points

    BONUS — Verify no regressions:
    - Place markups using all 5 tools in each mode (normal and chain)
    - Save and reload project; confirm all markups and visibility state persist
    - Export to Excel; confirm BOQ is correct
    - Right-click context menu (Edit, Delete, Recolor) still works on all markup types
  </how-to-verify>

  <resume-signal>
    Type "approved" if all 5 criteria pass. If any criterion fails, describe the issue(s) — e.g., "Criterion 3 fail: MarkupNamePopup still appears at mouse position instead of centered." Claude will create gap-closure plans.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Close Phase 9 — update ROADMAP and STATE</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>

  <read_first>
    .planning/ROADMAP.md — read Phase 9 entry; update Plans count and mark complete
    .planning/STATE.md — read Current Position section; update to Phase 9 complete
  </read_first>

  <action>
    ROADMAP.md changes:
    1. Find the Phase 9 entry in the phase list ("[ ] Phase 9: ...") — change [ ] to [x].
    2. In the Phase 9 detail section, update:
       - **Plans:** 6 plans
       - Add the Plans list:
           - [x] 09-00-PLAN.md — Type extensions + store additions (Wave 0)
           - [x] 09-01-PLAN.md — useDraggable hook + all 9 modals draggable (Wave 1)
           - [x] 09-02-PLAN.md — Click-to-select + selection ring + Delete/Ctrl+A (Wave 1)
           - [x] 09-03-PLAN.md — Rubber-band multi-select + Enter key commit (Wave 2)
           - [x] 09-04-PLAN.md — Ribbon toolbar (Wave 2)
           - [x] 09-05-PLAN.md — Manual UAT and phase closure (Wave 3)
       - Add the completion date to the Progress table.
    3. In the Progress table, mark Phase 9 as Complete with today's date (2026-05-18 or actual date).

    STATE.md changes:
    1. Update stopped_at to "Phase 9 complete (date)".
    2. Update Current Position: Phase 09 — Complete.
    3. Add Phase 9 to Key Decisions Locked where relevant:
       - "All modals use useDraggable hook — centered on open, draggable by user. Standing rule for all future modals."
       - "RibbonToolbar replaces Toolbar.tsx — 7-tab ribbon (Home/Page/Tools/View/Estimating/Settings/Help)"
       - "selectedMarkupIds: string[] in viewerStore — single-select is [id], multi-select is [id1,id2,...], none is []"
       - "delete-group MarkupCommand variant — group-delete as one undoable undo step"
    4. Update the Next action to reflect Phase 9 complete.
    5. Update progress metrics (phases complete, plans complete).
  </action>

  <verify>
    <automated>npm run test -- --run</automated>
  </verify>

  <done>
    - ROADMAP.md Phase 9 entry is marked [x] and shows 6/6 plans complete
    - STATE.md Current Position shows Phase 9 complete
    - STATE.md Key Decisions Locked includes the useDraggable and RibbonToolbar decisions
    - Full test suite passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT — human verification | Subjective check of visual and functional behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Tampering | Gap-closure plans created without developer approval | accept | Checkpoint gate: developer explicitly types "approved" or describes failures; no automatic gap closure |
</threat_model>

<verification>
- UAT checkpoint approved by developer
- ROADMAP.md Phase 9 marked [x]
- STATE.md Phase 9 in Current Position
- npm run test -- --run exits 0
</verification>

<success_criteria>
All 5 Phase 9 success criteria verified manually:
1. Click-to-select + Delete (single markup, undoable)
2. Rubber-band multi-select + group delete (single undo step)
3. All modals centered on open and draggable
4. Ribbon toolbar with 7 functional tabs
5. Enter key commits in-progress markup with min-point guard
ROADMAP and STATE updated to reflect phase completion.
</success_criteria>

<output>
After completion, create .planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-05-SUMMARY.md
</output>
