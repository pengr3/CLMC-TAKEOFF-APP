---
phase: 09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion
plan: "02"
type: execute
wave: 1
depends_on: ["09-00"]
files_modified:
  - src/renderer/src/components/HoverRing.tsx
  - src/renderer/src/components/markup/CountPinMarkup.tsx
  - src/renderer/src/components/markup/LinearMarkup.tsx
  - src/renderer/src/components/markup/AreaMarkup.tsx
  - src/renderer/src/components/markup/PerimeterMarkup.tsx
  - src/renderer/src/components/WallMarkup.tsx
  - src/renderer/src/components/CanvasViewport.tsx
  - src/renderer/src/hooks/useKeyboardShortcuts.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Clicking a placed markup in 'select' mode shows a visible selection ring (accent color, full opacity)"
    - "Clicking empty canvas in 'select' mode deselects all markups (selection ring disappears)"
    - "Pressing Escape in 'select' mode deselects (clears selectedMarkupIds)"
    - "Pressing Delete when a markup is selected removes it as a single undoable command via deleteMarkup"
    - "Pressing Ctrl+A in 'select' mode selects all markups on the current page"
    - "Clicking a markup while a placement tool is active does NOT select it — placement takes priority (D-03)"
    - "Selection ring coexists with hover ring — they render at different radii without visual overlap"
    - "The selection ring Layer has `listening={false}` — it does NOT intercept click events intended for markup shapes in the layers below"
  artifacts:
    - path: src/renderer/src/components/HoverRing.tsx
      provides: "Optional color and opacity props; defaults unchanged for existing hover usage"
      contains: "color?: string"
    - path: src/renderer/src/components/CanvasViewport.tsx
      provides: "handleMarkupClick, selection ring Layer, empty-space deselect in handleStageClick, Escape-to-deselect"
      contains: "handleMarkupClick"
    - path: src/renderer/src/hooks/useKeyboardShortcuts.ts
      provides: "Delete key and Ctrl+A handlers with isTextInputActive guard"
      contains: "'Delete'"
  key_links:
    - from: "all 5 markup renderers"
      to: src/renderer/src/components/CanvasViewport.tsx
      via: "onClick prop → handleMarkupClick → setSelectedMarkupIds([id])"
      pattern: "onClick"
    - from: src/renderer/src/components/CanvasViewport.tsx
      to: src/renderer/src/components/HoverRing.tsx
      via: "selectedMarkupIds filtered from pageMarkups passed as markups prop with color=COLORS.accent opacity=1.0"
      pattern: "color={COLORS.accent}"
    - from: src/renderer/src/hooks/useKeyboardShortcuts.ts
      to: src/renderer/src/stores/viewerStore.ts
      via: "Delete key reads selectedMarkupIds via getState(); Ctrl+A writes via setSelectedMarkupIds"
      pattern: "selectedMarkupIds"
---

<objective>
Wave 1 parallel track B: implement single click-to-select on all 5 markup renderers, render a selection ring via extended HoverRing, wire empty-space deselect and Escape-to-deselect, and add Delete + Ctrl+A keyboard shortcuts.

Purpose: Users can click a markup to select it and press Delete to remove it — the foundational interaction for the Phase 9 selection model (success criterion 1).
Output: All 5 markup renderers gain onClick; HoverRing gains optional color/opacity props; CanvasViewport renders selection ring; keyboard shortcuts extended.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-CONTEXT.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-RESEARCH.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-PATTERNS.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-00-SUMMARY.md

<interfaces>
<!-- HoverRing extended props (from 09-PATTERNS.md) -->
export interface HoverRingProps {
  markups: Markup[]
  currentZoom: number
  color?: string    // default '#ffffff' (RING_COLOR) — pass COLORS.accent for selection ring
  opacity?: number  // default 0.4 (RING_OPACITY) — pass 1.0 for selection ring
}

<!-- handleMarkupClick shape in CanvasViewport -->
const handleMarkupClick = useCallback((id: string) => {
  if (activeTool !== 'select') return  // D-03: placement always wins
  setSelectedMarkupIds([id])
}, [activeTool, setSelectedMarkupIds])

<!-- Markup renderer onClick prop -->
onClick?: (id: string) => void  // optional; guard is in CanvasViewport, not renderer

<!-- Selection ring Layer in CanvasViewport (after existing Layer 2) -->
<!-- Always mounted; renders HoverRing only when selectedMarkupIds.length > 0 -->
<!-- listening={false} is REQUIRED — this layer must never intercept click events destined for markup shapes -->
<Layer listening={false}>
  {selectedMarkupIds.length > 0 && (
    <HoverRing
      markups={pageMarkups.filter(m => selectedMarkupIds.includes(m.id))}
      currentZoom={currentZoom}
      color={COLORS.accent}  // '#0078d4' — already in COLORS.accent
      opacity={1.0}
    />
  )}
</Layer>

<!-- Hover ring vs. selection ring offsets (from STATE.md + RESEARCH.md) -->
<!-- HoverRing: RING_OFFSET_PX = 4 / currentZoom (4px outer offset) -->
<!-- Selection ring: same component with same offset — they coexist when both active -->
<!-- by rendering in separate layers (hover in Layer 2, selection in new Layer 3) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend HoverRing with optional color/opacity props; add onClick to all 5 markup renderers</name>
  <files>
    src/renderer/src/components/HoverRing.tsx,
    src/renderer/src/components/markup/CountPinMarkup.tsx,
    src/renderer/src/components/markup/LinearMarkup.tsx,
    src/renderer/src/components/markup/AreaMarkup.tsx,
    src/renderer/src/components/markup/PerimeterMarkup.tsx,
    src/renderer/src/components/WallMarkup.tsx
  </files>

  <read_first>
    src/renderer/src/components/HoverRing.tsx — read the full file; extend HoverRingProps with optional color and opacity; replace RING_COLOR with color ?? RING_COLOR and RING_OPACITY with opacity ?? RING_OPACITY throughout the render body
    src/renderer/src/components/markup/CountPinMarkup.tsx — read the full file; note the onContextMenu handler pattern on the Group element (lines 62-67); add onClick identically
    src/renderer/src/components/markup/LinearMarkup.tsx — read the full file; same onContextMenu pattern; add onClick
    src/renderer/src/components/markup/AreaMarkup.tsx — read the full file; add onClick
    src/renderer/src/components/markup/PerimeterMarkup.tsx — read the full file; add onClick
    src/renderer/src/components/WallMarkup.tsx — read the full file; add onClick
  </read_first>

  <action>
    HoverRing.tsx changes:
    1. Extend HoverRingProps: add `color?: string` and `opacity?: number` after currentZoom.
    2. Inside the component body, destructure the new props:
       const strokeColor = color ?? RING_COLOR
       const strokeOpacity = opacity ?? RING_OPACITY
    3. Replace every occurrence of `stroke={RING_COLOR}` with `stroke={strokeColor}`.
    4. Replace every occurrence of `opacity={RING_OPACITY}` with `opacity={strokeOpacity}`.
    5. The existing RING_COLOR and RING_OPACITY constants remain as-is (they are the defaults).
    6. No other changes to HoverRing — listening={false} stays on all shapes.

    For each of the 5 markup renderer files:
    CountPinMarkup.tsx:
    1. Add `onClick?: (id: string) => void` to the CountPinMarkupProps interface (alongside onContextMenu).
    2. Destructure onClick from the props.
    3. On the outer Group element, add: `onClick={() => onClick?.(markup.id)}`
       Place it immediately after the existing onContextMenu handler.

    LinearMarkup.tsx, AreaMarkup.tsx, PerimeterMarkup.tsx, WallMarkup.tsx:
    Apply the identical pattern: add onClick?: (id: string) => void to the props interface, destructure it, add `onClick={() => onClick?.(markup.id)}` on the outer Group.

    IMPORTANT: The D-03 guard (ignore click when a placement tool is active) lives in CanvasViewport's handleMarkupClick, NOT in the renderer. The renderer passes the id and that is all. This keeps renderers clean and policy in one place.

    No new hex literals — all existing RING_COLOR (#ffffff) and RING_OPACITY values are preserved as constants; the new color prop defaults to RING_COLOR.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npm run test -- --run</automated>
  </verify>

  <done>
    - HoverRingProps contains color?: string and opacity?: number
    - All 5 markup renderer Props interfaces contain onClick?: (id: string) => void
    - All 5 renderers pass onClick?.(markup.id) on their outer Group element
    - All existing tests pass
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire selection in CanvasViewport; add Delete and Ctrl+A keyboard shortcuts</name>
  <files>
    src/renderer/src/components/CanvasViewport.tsx,
    src/renderer/src/hooks/useKeyboardShortcuts.ts
  </files>

  <read_first>
    src/renderer/src/components/CanvasViewport.tsx — read the full file; focus on: handleStageClick (line 459), the Escape key useEffect (line 312), Layer structure (line 627+), how props are passed to markup renderers (lines 727+), how viewerStore selectors are consumed
    src/renderer/src/hooks/useKeyboardShortcuts.ts — read the full file; understand the existing Ctrl+Z/Ctrl+Y handler structure (lines 92-100); the placement location for new Delete and Ctrl+A handlers
    src/renderer/src/stores/viewerStore.ts — note that selectedMarkupIds, setSelectedMarkupIds, clearSelection are now available (added in 09-00)
    src/renderer/src/lib/constants.ts — confirm COLORS.accent is '#0078d4' (it is, from the file read)
  </read_first>

  <action>
    CanvasViewport.tsx changes:

    1. Add viewerStore selectors near the top of the component (alongside existing activeTool selector):
       const selectedMarkupIds = useViewerStore((s) => s.selectedMarkupIds)
       const setSelectedMarkupIds = useViewerStore((s) => s.setSelectedMarkupIds)
       const clearSelection = useViewerStore((s) => s.clearSelection)

    2. Add handleMarkupClick callback (after the existing handleContextMenu callback):
       const handleMarkupClick = useCallback((id: string) => {
         if (activeTool !== 'select') return  // D-03: placement takes priority
         setSelectedMarkupIds([id])
       }, [activeTool, setSelectedMarkupIds])

    3. In handleStageClick: at the END of the handler body (after all existing early returns), add:
       if (activeTool === 'select' && e.target === stageRef.current) {
         clearSelection()  // D-05: empty-space click deselects
       }
       Add clearSelection to the useCallback deps array.

    4. In the Escape key useEffect (line 312): extend the existing handleKeyDown to also handle 'select' mode deselect:
       Inside the handleKeyDown function, after the existing Escape block, there is already logic for returning to 'select' mode. Also ensure: when Escape is pressed and activeTool === 'select', call clearSelection(). This matches the "Escape deselects in select mode" requirement. Add clearSelection to the useEffect deps array.

    5. Pass onClick={handleMarkupClick} to all 5 markup renderer usages in the Layer 1b render section:
       - CountPinMarkup: add onClick={handleMarkupClick}
       - LinearMarkup: add onClick={handleMarkupClick}
       - AreaMarkup: add onClick={handleMarkupClick}
       - PerimeterMarkup: add onClick={handleMarkupClick}
       - WallMarkup: add onClick={handleMarkupClick}

    6. Add a new always-mounted selection ring Layer AFTER the existing Layer 2 (hover/pulse layer).
       CRITICAL (review concern — Layer 3 listening={false}): The selection ring Layer MUST have listening={false}.
       Without it, the selection ring layer would sit on top of the markup layers and intercept click events
       meant for markup shapes — clicks would hit the selection ring Konva shapes, not the Group nodes in Layer 2.
       listening={false} makes the layer completely transparent to mouse/touch events:
       <Layer listening={false}>
         {selectedMarkupIds.length > 0 && (
           <HoverRing
             markups={pageMarkups.filter((m) => selectedMarkupIds.includes(m.id))}
             currentZoom={currentZoom}
             color={COLORS.accent}
             opacity={1.0}
           />
         )}
       </Layer>
       Import COLORS from '../lib/constants' if not already imported (it already is from the existing toolbar work).

    useKeyboardShortcuts.ts changes:

    7. Add Delete key handler. Read the existing handler structure carefully. Insert the Delete handler BEFORE any existing guard that would block it (note: isTextInputActive must come first). Location: insert after the existing Ctrl+Z/Ctrl+Y blocks but before any return guard. Pattern mirrors the RESEARCH.md Feature 1.9 spec:
       if (e.key === 'Delete') {
         if (isTextInputActive()) return
         const selectedIds = useViewerStore.getState().selectedMarkupIds
         if (selectedIds.length === 0) return
         const currentPage = useViewerStore.getState().currentPage
         if (selectedIds.length === 1) {
           useMarkupStore.getState().deleteMarkup(currentPage, selectedIds[0])
         } else {
           const allMarkups = Object.values(useMarkupStore.getState().pageMarkups).flat()
           const toDelete = allMarkups.filter((m) => selectedIds.includes(m.id))
           useMarkupStore.getState().deleteGroup(toDelete)
         }

       IMPORTANT (review concern — stale selection): After calling deleteMarkup or deleteGroup, ALWAYS
       call clearSelection() to prevent stale selectedMarkupIds from pointing to deleted markup IDs.
       The call below is mandatory — do not remove it or move it after a return statement:
         useViewerStore.getState().clearSelection()
         return
       }

    8. Add Ctrl+A handler. Insert after the Delete handler:
       if (e.ctrlKey && e.key === 'a') {
         if (isTextInputActive()) return
         e.preventDefault()
         const activeTool = useViewerStore.getState().activeTool
         if (activeTool !== 'select') return
         const currentPage = useViewerStore.getState().currentPage
         const allIds = useMarkupStore.getState().getMarkups(currentPage).map((m) => m.id)
         useViewerStore.getState().setSelectedMarkupIds(allIds)
         return
       }
       Note: totalPages guard is not strictly needed for Ctrl+A (if no PDF is open, getMarkups returns [], and setSelectedMarkupIds([]) is a no-op). Add the guard `if (totalPages === 0) return` if it makes the intent clearer.

    Import useViewerStore and useMarkupStore at the top of useKeyboardShortcuts.ts if not already imported (check the file — they may already be there for existing shortcuts).

    No new hex literals anywhere. COLORS.accent is the selection ring color.
  </action>

  <verify>
    <automated>npm run test -- --run && npx tsc --noEmit</automated>
  </verify>

  <done>
    - CanvasViewport.tsx contains handleMarkupClick callback
    - CanvasViewport.tsx passes onClick={handleMarkupClick} to all 5 markup renderers
    - CanvasViewport.tsx renders a selection ring Layer with listening={false} and HoverRing using color={COLORS.accent} and opacity={1.0}
    - handleStageClick clears selection when e.target === stageRef.current and activeTool === 'select'
    - Escape key useEffect clears selection when activeTool === 'select'
    - useKeyboardShortcuts.ts contains the Delete key handler with isTextInputActive() guard AND clearSelection() called after every delete operation
    - useKeyboardShortcuts.ts contains the Ctrl+A handler with isTextInputActive() guard
    - All existing tests pass; npx tsc --noEmit exits 0
    - Manual smoke: clicking a count pin in 'select' mode shows the blue accent selection ring; clicking empty canvas deselects; pressing Delete removes the markup (Ctrl+Z restores it)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard handler (Delete / Ctrl+A) | Reads selectedMarkupIds from store; guard isTextInputActive() prevents fires inside text inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Tampering | Delete key fires in text input | mitigate | isTextInputActive() guard as first check in Delete handler (D-04 + D-27 pattern) |
| T-09-02-02 | Tampering | Ctrl+A selects in wrong tool mode | mitigate | activeTool !== 'select' guard inside Ctrl+A handler (D-08) |
| T-09-02-03 | Spoofing | Placement click hits existing markup → triggers selection | mitigate | D-03 guard: handleMarkupClick returns early when activeTool !== 'select' |
</threat_model>

<verification>
- npm run test -- --run exits 0
- npx tsc --noEmit exits 0
- HoverRing.tsx contains `color?: string` in HoverRingProps
- All 5 markup renderer files contain onClick in their Props interface
- CanvasViewport.tsx contains handleMarkupClick and selection ring Layer with COLORS.accent
- CanvasViewport.tsx selection ring `<Layer>` element has `listening={false}` attribute
- useKeyboardShortcuts.ts contains 'Delete' key handler with clearSelection() call after every deleteMarkup/deleteGroup
</verification>

<success_criteria>
- Click-to-select works for all 5 markup types
- Selection ring renders in accent color at full opacity
- Selection ring Layer has listening={false} — no click event interception
- Empty canvas click and Escape deselect
- Delete removes selected markup (undoable); clearSelection() is called after every deletion — no stale IDs
- Ctrl+A selects all on current page
- Placement priority maintained: clicking while a tool is active does not select
</success_criteria>

<output>
After completion, create .planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-02-SUMMARY.md
</output>
