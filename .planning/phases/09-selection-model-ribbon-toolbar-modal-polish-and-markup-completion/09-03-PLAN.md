---
phase: 09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion
plan: "03"
type: execute
wave: 2
depends_on: ["09-00", "09-01", "09-02"]
files_modified:
  - src/renderer/src/components/CanvasViewport.tsx
  - src/renderer/src/hooks/useViewportControls.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "In 'select' mode, holding LMB and dragging on empty canvas draws a visible rubber-band selection rectangle"
    - "Releasing LMB after rubber-band selects all markups whose full bounding box is inside the rectangle"
    - "The rubber-band does not activate the Stage pan behavior — pan is disabled for LMB in 'select' mode"
    - "Pressing Delete after rubber-band multi-select removes all selected markups as a single undoable command (deleteGroup)"
    - "Pressing Enter commits an in-progress Linear, Area, Perimeter, or Wall markup (guarded by min-point count)"
    - "Pressing Enter with fewer than 2 points (linear/wall) or fewer than 3 points (area/perimeter) is silently ignored"
    - "Pressing Enter while a text input is focused does NOT commit markup (isTextInputActive guard)"
    - "Middle-mouse pan still works in 'select' mode (unaffected)"
    - "Spacebar+drag pan still works in 'select' mode (unaffected)"
  artifacts:
    - path: src/renderer/src/components/CanvasViewport.tsx
      provides: "rubber-band state; onMouseDown/onMouseMove/onMouseUp handlers on Stage; AABB containment check; rubber-band Rect in Layer 1a; Enter key in Escape useEffect"
      contains: "rubberBand"
    - path: src/renderer/src/hooks/useViewportControls.ts
      provides: "Konva.dragButtons useEffect reads activeTool from useViewerStore; LMB drag is suppressed in 'select' mode unless spacebar is held; isDraggable stays true"
      contains: "activeTool"
  key_links:
    - from: src/renderer/src/hooks/useViewportControls.ts
      to: src/renderer/src/components/CanvasViewport.tsx
      via: "Konva.dragButtons controls which mouse buttons can pan the Stage — in 'select' mode without spacebar, only middle-mouse pans; LMB is freed for rubber-band"
      pattern: "Konva.dragButtons"
    - from: src/renderer/src/components/CanvasViewport.tsx
      to: src/renderer/src/stores/viewerStore.ts
      via: "onMouseUp containment result calls setSelectedMarkupIds(matchedIds)"
      pattern: "setSelectedMarkupIds"
    - from: src/renderer/src/components/CanvasViewport.tsx
      to: src/renderer/src/stores/markupStore.ts
      via: "Enter key calls finishLinear or finishPolygon from useMarkupTool"
      pattern: "finishLinear"
---

<objective>
Wave 2: rubber-band multi-select (D-06, D-07, D-08, D-09) and Enter-key markup completion (D-26, D-27). Both features modify CanvasViewport.tsx; they are in the same plan to prevent merge conflicts.

Purpose: Users can drag to select multiple markups and delete them as one undo step; users can press Enter to commit in-progress markup without double-clicking.
Output: Rubber-band selection rectangle on CanvasViewport; Konva.dragButtons gated on activeTool to suppress LMB pan in 'select' mode; Enter key wired in existing Escape useEffect.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-CONTEXT.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-RESEARCH.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-PATTERNS.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-00-SUMMARY.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-02-SUMMARY.md

<interfaces>
<!-- Rubber-band state shape in CanvasViewport -->
type RubberBandState = { startX: number; startY: number; endX: number; endY: number } | null
// useState<RubberBandState>(null)

<!-- Stage-space coordinate conversion (established canonical pattern from STATE.md) -->
// stage.getAbsoluteTransform().copy().invert().point(pointer)
// where pointer = stage.getPointerPosition()

<!-- AABB bounding box per markup type (from RESEARCH.md Feature 1.6) -->
// CountMarkup: { minX: p.x-10, maxX: p.x+10, minY: p.y-10, maxY: p.y+10 } (PIN_RADIUS_WORLD=10)
// LinearMarkup/AreaMarkup/PerimeterMarkup/WallMarkup: min/max over points array

<!-- Containment check (D-07: full bbox must be INSIDE rubber-band) -->
function isFullyInside(markup, band): boolean
// bx1=min(band.startX,band.endX), bx2=max(...), by1=min(...), by2=max(...)
// minX >= bx1 && maxX <= bx2 && minY >= by1 && maxY <= by2

<!-- Enter key logic in existing Escape useEffect (from RESEARCH.md Feature 4.2) -->
// if (e.key === 'Enter') {
//   if (isTextInputActive()) return
//   if (markupState.mode !== 'drawing') return
//   if (toolType === 'linear' || toolType === 'wall') {
//     if (points.length >= 2) { e.preventDefault(); finishLinear() }
//   } else if (toolType === 'area' || toolType === 'perimeter') {
//     if (points.length >= 3) { e.preventDefault(); finishPolygon() }
//   }
// }

<!-- useViewportControls actual Konva.dragButtons mechanism (read from codebase) -->
// Current useEffect (lines 77-79):
//   useEffect(() => {
//     Konva.dragButtons = spaceHeld ? [0, 1] : [1]
//   }, [spaceHeld])
// isDraggable is hardcoded to true (line 168). Pan is NOT controlled by the draggable prop.
// Pan is controlled exclusively by Konva.dragButtons: [0,1] means LMB+MMB pan; [1] means MMB only.
// New formula: Konva.dragButtons = (spaceHeld || activeTool !== 'select') ? [0, 1] : [1]
// This means:
//   - In any non-'select' mode: [0,1] — LMB drag pans as before
//   - In 'select' mode without spacebar: [1] — only MMB pans; LMB drag freed for rubber-band
//   - In 'select' mode with spacebar: [0,1] — spacebar override restores LMB pan
//   isDraggable stays true (unchanged). No change to the draggable prop or its return.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate Konva.dragButtons on activeTool; add rubber-band selection to CanvasViewport</name>
  <files>
    src/renderer/src/hooks/useViewportControls.ts,
    src/renderer/src/components/CanvasViewport.tsx
  </files>

  <read_first>
    src/renderer/src/hooks/useViewportControls.ts — read the full file; focus on the Konva.dragButtons useEffect (lines 77-79) and the spaceHeld state; this is the mechanism to modify for 'select' mode pan suppression. Note: isDraggable is hardcoded true and must NOT be changed.
    src/renderer/src/components/CanvasViewport.tsx — read the full file; focus on: the existing Stage props (onMouseDown, onMouseMove, onMouseUp if any), Layer 1a render section, the Escape useEffect deps array, how rubberBand state would be integrated into the Layer 1a render, the markupState and its toolType/mode/points fields, finishLinear and finishPolygon callsites
    src/renderer/src/stores/viewerStore.ts — confirm selectedMarkupIds, setSelectedMarkupIds are available (added in 09-00); also confirm activeTool selector shape
    src/renderer/src/lib/constants.ts — confirm COLORS.accent value for rubber-band rect stroke
  </read_first>

  <action>
    PART A — useViewportControls.ts: gate Konva.dragButtons for 'select' mode

    The hook already uses Konva.dragButtons to control which mouse buttons trigger Stage pan. isDraggable is hardcoded to true and must stay true — do NOT modify it. The existing useEffect at lines 77-79 is the correct extension point.

    1. Add a reactive selector for activeTool at the top of the hook body (alongside the existing currentPage and setViewport selectors):
       const activeTool = useViewerStore((s) => s.activeTool)

    2. Modify the existing Konva.dragButtons useEffect:
       - Add activeTool to the dependency array
       - Change the formula from:
           Konva.dragButtons = spaceHeld ? [0, 1] : [1]
         to:
           Konva.dragButtons = (spaceHeld || activeTool !== 'select') ? [0, 1] : [1]
       This means:
         - In any non-'select' mode: LMB + MMB both pan (existing behavior preserved)
         - In 'select' mode, no spacebar: only MMB pans; LMB drag is freed for rubber-band
         - In 'select' mode with spacebar held: LMB + MMB pan (spacebar override, D-06)

    3. isDraggable remains hardcoded true. The return object is unchanged. No wiring change needed in CanvasViewport for this part.

    PART B — CanvasViewport.tsx: rubber-band state and handlers

    1. Define a module-level constant at the top of the file (after imports, before the component):
       const RUBBER_BAND_FILL = 'rgba(0,120,212,0.1)'
       This removes the raw rgba literal from JSX per the COLORS tokens rule. (The opacity fill variant is not a COLORS token — a named constant at module scope is the correct approach here.)

    2. Add rubber-band state near the other local state:
       const [rubberBand, setRubberBand] = useState<{ startX: number; startY: number; endX: number; endY: number } | null>(null)

    3. Add AABB bounding box helper function at module scope (before the component):
       function getMarkupBBox(markup: Markup): { minX: number; maxX: number; minY: number; maxY: number }
       - For CountMarkup: minX = p.x - 10, maxX = p.x + 10, minY = p.y - 10, maxY = p.y + 10 (PIN_RADIUS_WORLD = 10)
       - For LinearMarkup, AreaMarkup, PerimeterMarkup, WallMarkup: minX = Math.min(...points.map(p=>p.x)), maxX = Math.max(...), minY/maxY same
       Import Markup type if not already imported (it is, from existing code).

    4. Add isFullyInside helper at module scope:
       function isFullyInside(markup: Markup, band: { startX: number; startY: number; endX: number; endY: number }): boolean
       Normalise band corners: bx1 = Math.min(band.startX, band.endX), bx2 = Math.max(...), by1/by2 same.
       Get bbox via getMarkupBBox(markup).
       Return minX >= bx1 && maxX <= bx2 && minY >= by1 && maxY <= by2.

    5. Add Stage onMouseDown handler (or extend existing if one exists):
       When activeTool === 'select' AND e.evt.button === 0 AND not spaceHeld:
       - Get stage-space point via stage.getAbsoluteTransform().copy().invert().point(stage.getPointerPosition())
       - setRubberBand({ startX: pt.x, startY: pt.y, endX: pt.x, endY: pt.y })
       When activeTool !== 'select': do nothing in this handler.

    6. Add Stage onMouseMove handler:
       If rubberBand !== null:
       - Get stage-space point (same transform as above)
       - setRubberBand(prev => prev ? { ...prev, endX: pt.x, endY: pt.y } : null)

    7. Add Stage onMouseUp handler:
       If rubberBand !== null:
       - Get pageMarkups for the current page (use the existing pageMarkups local variable or read from store)
       - Filter: const matched = pageMarkups.filter(m => isFullyInside(m, rubberBand))
       - If matched.length > 0: setSelectedMarkupIds(matched.map(m => m.id))
       - setRubberBand(null)

    8. In Layer 1a (the listening={false} layer that holds calibration and in-progress shapes), add the rubber-band Rect:
       {rubberBand && (
         <Rect
           x={Math.min(rubberBand.startX, rubberBand.endX)}
           y={Math.min(rubberBand.startY, rubberBand.endY)}
           width={Math.abs(rubberBand.endX - rubberBand.startX)}
           height={Math.abs(rubberBand.endY - rubberBand.startY)}
           stroke={COLORS.accent}
           strokeWidth={1 / currentZoom}
           fill={RUBBER_BAND_FILL}
           listening={false}
         />
       )}
       Import Rect from 'react-konva' if not already imported.

    PART C — CanvasViewport.tsx: Enter key commit (D-26, D-27)

    9. In the existing Escape key useEffect (the one that calls cancelMarkup and setActiveTool('select')), extend the handleKeyDown function to also handle 'Enter':
       Add inside handleKeyDown, after the Escape block:
       if (e.key === 'Enter') {
         if (isTextInputActive()) return
         if (markupState.mode !== 'drawing') return
         if (markupState.toolType === 'linear' || markupState.toolType === 'wall') {
           if (markupState.points.length >= 2) {
             e.preventDefault()
             finishLinear()
           }
           // else: silent ignore per D-26
         } else if (markupState.toolType === 'area' || markupState.toolType === 'perimeter') {
           if (markupState.points.length >= 3) {
             e.preventDefault()
             finishPolygon()
           }
           // else: silent ignore per D-26
         }
       }

    10. Extend the useEffect's deps array to include: markupState.toolType, markupState.points.length, finishLinear, finishPolygon (alongside the existing deps).

    11. Verify that isTextInputActive is available in CanvasViewport.tsx. It is imported from useKeyboardShortcuts or a shared utility — check the existing import. If it is only in useKeyboardShortcuts.ts, check if it is exported from there or defined as a standalone function. Read the file. If necessary, duplicate the simple check inline: const isTextInputActive = () => { const el = document.activeElement; return el instanceof HTMLInputElement || el instanceof HTMLTextAreaElement || (el instanceof HTMLElement && el.isContentEditable) }

    Cross-cutting: COLORS tokens only; no raw hex; RUBBER_BAND_FILL module-level constant for the rgba fill; zoom-compensated strokeWidth on rubber-band Rect (1/currentZoom); listening={false} on Rect.
  </action>

  <verify>
    <automated>npm run test -- --run && npx tsc --noEmit</automated>
  </verify>

  <done>
    - useViewportControls.ts: Konva.dragButtons useEffect reads activeTool and uses formula (spaceHeld || activeTool !== 'select') ? [0,1] : [1]; isDraggable remains true (unchanged)
    - CanvasViewport.tsx contains RUBBER_BAND_FILL module-level constant (no raw rgba in JSX)
    - CanvasViewport.tsx contains rubberBand state and getMarkupBBox + isFullyInside helpers
    - CanvasViewport.tsx Stage has onMouseDown, onMouseMove, onMouseUp handlers for rubber-band
    - Rubber-band Rect renders in Layer 1a with COLORS.accent stroke, RUBBER_BAND_FILL fill, and zoom-compensated strokeWidth
    - Enter key is handled in the Escape useEffect with min-point guards and isTextInputActive guard
    - All existing tests pass; npx tsc --noEmit exits 0
    - Manual smoke: drag over markups → they are highlighted with selection ring; Delete → deleteGroup fires (Ctrl+Z restores all); Enter while drawing linear with 2 points → popup appears; Enter with 1 point → silent ignore; middle-mouse pan works in 'select' mode; spacebar+LMB drag pans in 'select' mode
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stage-space coordinates | rubber-band uses inverse stage transform — not raw pointer coords; canonical pattern from STATE.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Denial of Service | rubber-band flicker from 60fps setState | accept | useState for rubberBand is acceptable per RESEARCH.md Pitfall 9 — single Rect, within React 19 budget |
| T-09-03-02 | Tampering | Enter key commits markup during popup input | mitigate | isTextInputActive() guard fires first; popup's auto-focused input returns true from guard |
| T-09-03-03 | Tampering | rubber-band activates while pan is expected | mitigate | Konva.dragButtons formula gates LMB pan; spacebar overrides to pan even in 'select' mode; middle-mouse always pans |
</threat_model>

<verification>
- npm run test -- --run exits 0
- npx tsc --noEmit exits 0
- CanvasViewport.tsx contains rubberBand state and isFullyInside function
- CanvasViewport.tsx contains RUBBER_BAND_FILL module-level constant (grep: no raw rgba in JSX)
- CanvasViewport.tsx contains 'Enter' in the keyboard useEffect
- useViewportControls.ts Konva.dragButtons useEffect deps array includes activeTool
- useViewportControls.ts isDraggable is still hardcoded true (not modified)
</verification>

<success_criteria>
- Rubber-band drag in 'select' mode selects multiple markups (full-containment rule D-07)
- Group Delete removes all selected as single undo step
- Middle-mouse and spacebar+drag pan unaffected in 'select' mode
- Enter key commits in-progress markup with minimum-point guard; silent ignore on degenerate shape
</success_criteria>

<output>
After completion, create .planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-03-SUMMARY.md
</output>
