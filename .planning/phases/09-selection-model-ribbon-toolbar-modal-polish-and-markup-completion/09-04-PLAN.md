---
phase: 09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion
plan: "04"
type: execute
wave: 2
depends_on: ["09-00"]
files_modified:
  - src/renderer/src/components/RibbonButton.tsx
  - src/renderer/src/components/RibbonToolbar.tsx
  - src/renderer/src/App.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "The app renders a tabbed ribbon instead of the flat toolbar"
    - "Home tab is active by default and shows: Open, Save, Save As, Replace Plan PDF, Export buttons"
    - "Page tab shows Previous Page, page indicator (Page N of M), Next Page"
    - "Tools tab shows: Select, Count, Linear, Area, Perimeter, Wall, Set Scale — all functional with chain badge chips and chevron"
    - "View tab shows: Zoom In, Zoom Out, Fit to Window, Show Totals toggle, Show All, Hide All"
    - "Settings and Help tabs are stubbed ('Coming soon')"
    - "Estimating tab is present (stub or Export shortcut)"
    - "All existing toolbar functionality remains accessible — no feature regression"
    - "Show All calls setHiddenItemNames([]) + markDirty(); Hide All collects all name|categoryId keys + markDirty()"
    - "RibbonButton is ~60px square with icon above label — visually distinct from IconButton"
  artifacts:
    - path: src/renderer/src/components/RibbonButton.tsx
      provides: "60px square flex-column icon-above-label button component"
      exports: ["RibbonButton"]
    - path: src/renderer/src/components/RibbonToolbar.tsx
      provides: "Full ribbon with 7 tabs; replaces Toolbar.tsx; same prop interface"
      exports: ["RibbonToolbar", "RibbonToolbarProps"]
    - path: src/renderer/src/App.tsx
      provides: "Updated import: RibbonToolbar instead of Toolbar"
      contains: "RibbonToolbar"
  key_links:
    - from: src/renderer/src/components/RibbonToolbar.tsx
      to: src/renderer/src/components/CanvasViewport.tsx
      via: "getCanvasControls(), getCalibrationControls(), getChainArmedItem() module-level refs — NOT duplicated, called directly per STATE.md rule"
      pattern: "getCanvasControls"
    - from: src/renderer/src/components/RibbonToolbar.tsx
      to: src/renderer/src/stores/projectStore.ts
      via: "Show All: setHiddenItemNames([]); Hide All: setHiddenItemNames(allKeys) — both followed by markDirty()"
      pattern: "setHiddenItemNames"
    - from: src/renderer/src/App.tsx
      to: src/renderer/src/components/RibbonToolbar.tsx
      via: "onOpenClick, onReplaceClick, onExportClick props preserved identically"
      pattern: "RibbonToolbar"
---

<objective>
Wave 2 parallel track: create RibbonButton.tsx and RibbonToolbar.tsx; update App.tsx to use RibbonToolbar instead of Toolbar. Toolbar.tsx is NOT deleted in this plan — it stays alongside until the UAT plan confirms no regressions (the executor may delete it at the end of this plan after App.tsx is updated and tests pass).

Purpose: Replace the flat action bar with the tabbed ribbon toolbar (D-15 through D-24), adding the Select tool button and Show All/Hide All visibility controls.
Output: RibbonButton component; RibbonToolbar with 7 working tabs; App.tsx updated.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-CONTEXT.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-RESEARCH.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-PATTERNS.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-00-SUMMARY.md

<interfaces>
<!-- RibbonToolbarProps — identical to ToolbarProps for zero App.tsx friction -->
export interface RibbonToolbarProps {
  onOpenClick: () => void | Promise<void>
  onReplaceClick: () => void | Promise<void>
  onExportClick: () => void | Promise<void>
}

<!-- Module-level refs from CanvasViewport — reuse, do not duplicate -->
import { getCanvasControls, getCalibrationControls, getChainArmedItem } from './CanvasViewport'

<!-- Show All / Hide All handlers (from RESEARCH.md Feature 3.6 + PATTERNS.md) -->
handleShowAll: setHiddenItemNames([]) then markDirty()
handleHideAll: collect all name|categoryId keys across pageMarkups then setHiddenItemNames(allKeys) then markDirty()
Note: setHiddenItemNames is designed for hydration and does NOT call markDirty internally (Pitfall 7)

<!-- COLORS tokens used in RibbonButton and RibbonToolbar -->
COLORS.accent, COLORS.secondary, COLORS.border, COLORS.textPrimary, COLORS.textSecondary, COLORS.activeSurface, COLORS.hoverSurface, COLORS.dominant

<!-- Chain badge chip — read from getChainArmedItem() during render — not a stored selector -->
<!-- Renders only when activeTool === 'count' (or respective tool) AND getChainArmedItem() !== null -->
<!-- Same shape as Toolbar.tsx lines 368-380 -->

<!-- Select tool icon -->
import { MousePointer } from 'lucide-react'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RibbonButton.tsx component</name>
  <files>src/renderer/src/components/RibbonButton.tsx</files>

  <read_first>
    src/renderer/src/components/Toolbar.tsx — read the IconButton function (lines 29-96); RibbonButton is a taller variant of the same button shape. Copy the hover/active inline style pattern, COLORS usage, aria attributes, and children slot verbatim; change only the dimensions and layout direction.
    src/renderer/src/lib/constants.ts — confirm COLORS token names available for use.
  </read_first>

  <action>
    Create src/renderer/src/components/RibbonButton.tsx.

    Props interface (named RibbonButtonProps):
    - icon: React.ComponentType<{ size?: number; color?: string }> — the Lucide icon component
    - label: string — always rendered below the icon (unlike IconButton where label is optional)
    - onClick: () => void
    - active?: boolean — default false
    - disabled?: boolean — default false
    - title?: string — tooltip
    - ariaLabel?: string
    - onContextMenu?: (e: React.MouseEvent<HTMLButtonElement>) => void
    - children?: React.ReactNode — slot for badge chips and chevron elements

    Button dimensions (D-16): width: 60, height: 60 (square)
    Layout: flexDirection: 'column', alignItems: 'center', justifyContent: 'center', gap: 4
    Icon size: 20 (larger than IconButton's 16)
    Label fontSize: 11, fontWeight: 600 (below icon)
    Active indicator: borderBottom: `2px solid ${COLORS.accent}` when active; '2px solid transparent' otherwise
    Background when active: COLORS.activeSurface
    Hover/mousedown inline style updates: same pattern as IconButton (onMouseEnter/onMouseLeave/onMouseDown/onMouseUp)
    Disabled: opacity 0.4, cursor 'not-allowed', pointerEvents 'none'
    position: 'relative' — allows badge chip children to use absolute positioning if needed
    aria-pressed={active}, aria-disabled={disabled}, tabIndex={disabled ? -1 : 0}

    Import: COLORS from '../lib/constants'; React from 'react'.
    Export: export function RibbonButton(...): React.JSX.Element

    No Tailwind. No raw hex literals. Inline styles only.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <done>
    - src/renderer/src/components/RibbonButton.tsx exists and exports RibbonButton
    - Props interface matches spec above
    - npx tsc --noEmit exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Create RibbonToolbar.tsx and update App.tsx</name>
  <files>src/renderer/src/components/RibbonToolbar.tsx, src/renderer/src/App.tsx</files>

  <read_first>
    src/renderer/src/components/Toolbar.tsx — read the entire file (lines 1-525). This is the reference for all imports, store selectors, handleMarkupToolClick, handleChevronClick, ScaleContextMenu usage, chain badge chip render, and the full list of buttons. RibbonToolbar must replicate all functional behavior.
    src/renderer/src/App.tsx — read the Toolbar import (line 4) and usage (lines 244-248 approx); also read any height/layout calc that might reference Toolbar. Update import and JSX in this file.
    src/renderer/src/stores/projectStore.ts — read setHiddenItemNames (line 93) and markDirty — confirm the markDirty function exists and its location.
    src/renderer/src/stores/markupStore.ts — confirm pageMarkups is accessible via useMarkupStore.getState().pageMarkups for the Hide All handler.
    src/renderer/src/lib/constants.ts — read LAYOUT.toolbarHeight consumers. If nothing reads LAYOUT.toolbarHeight to compute layout geometry, the ribbon can have its own height without updating the constant.
    src/renderer/src/stores/viewerStore.ts — confirm selectedMarkupIds and setSelectedMarkupIds are available (added in 09-00); the Select tool button sets activeTool to 'select'.
  </read_first>

  <action>
    Create src/renderer/src/components/RibbonToolbar.tsx:

    IMPORTS: Copy from Toolbar.tsx lines 1-27 verbatim, then:
    - Change import source from Toolbar to RibbonToolbar (self).
    - Add MousePointer from 'lucide-react' for the Select tool button.
    - Add import { RibbonButton } from './RibbonButton'.
    - Add import { useMarkupStore } from '../stores/markupStore' (needed for Hide All and hasMarkups).
    - Keep all existing Toolbar imports: useViewerStore, useScaleStore, useProjectStore, useProject, getCanvasControls, getCalibrationControls, getChainArmedItem, ScaleContextMenu, MIN_ZOOM, MAX_ZOOM, COLORS.

    PROPS INTERFACE: Named RibbonToolbarProps — identical to ToolbarProps:
    - onOpenClick: () => void | Promise<void>
    - onReplaceClick: () => void | Promise<void>
    - onExportClick: () => void | Promise<void>

    COMPONENT: export function RibbonToolbar({ onOpenClick, onReplaceClick, onExportClick }: RibbonToolbarProps): React.JSX.Element

    STORE SELECTORS: Copy verbatim from Toolbar.tsx lines 121-149. Add:
    - const setSelectedMarkupIds = useViewerStore((s) => s.setSelectedMarkupIds) — for Select button click
    - For Show/Hide All, read from store imperatively in the handlers (useMarkupStore.getState())

    LOCAL STATE: const [activeTab, setActiveTab] = useState<string>('home')  // D-24: default 'home'
    Import useState from 'react'.

    HANDLERS:
    - handleMarkupToolClick: copy from Toolbar.tsx lines 190-196 (toggle or activate)
    - handleChevronClick: copy from Toolbar.tsx (opens ScaleContextMenu)
    - All existing Toolbar handlers for Save, SaveAs, etc: copy verbatim
    - handleSelectTool: setActiveTool('select') (or if activeTool === 'select', keep it — no toggle needed, 'select' is the idle state)
    - handleShowAll: useProjectStore.getState().setHiddenItemNames([]); useProjectStore.getState().markDirty()
    - handleHideAll:
        const { pageMarkups } = useMarkupStore.getState()
        const allKeys = Array.from(new Set(Object.values(pageMarkups).flat().map((m) => `${m.name}|${m.categoryId}`)))
        useProjectStore.getState().setHiddenItemNames(allKeys)
        useProjectStore.getState().markDirty()

    RENDER STRUCTURE:
    Outer wrapper div: position: 'relative', background: COLORS.secondary, borderBottom: `1px solid ${COLORS.border}`, userSelect: 'none'

    Tab strip (row of tabs): display: 'flex', flexDirection: 'row', paddingLeft: 8, gap: 0
    Each tab is a button: ~80px wide, height ~28px, fontSize: 12, fontWeight: 600
    Active tab: borderBottom: `2px solid ${COLORS.accent}`, color: COLORS.textPrimary, background: COLORS.dominant
    Inactive tab: borderBottom: '2px solid transparent', color: COLORS.textSecondary, background: 'transparent'
    Tabs: 'home', 'page', 'tools', 'view', 'estimating', 'settings', 'help' — labels capitalized

    Ribbon panel (below tab strip): height: ~68px (at least 2× the tab strip height per D-16 goal), display: 'flex', flexDirection: 'row', alignItems: 'center', padding: '0 8px', gap: 4
    Content switches on activeTab:

    HOME TAB (D-17): Open (FileUp icon), Save (Save icon, disabled if isSaving), Save As (SaveAll icon, disabled if isSaving), Replace Plan PDF (Replace icon, disabled if isSaving or no project open), Export (Download icon, disabled if isExporting or !hasMarkups)

    PAGE TAB (D-18): Prev Page button (ChevronLeft), page indicator text ("Page {currentPage} of {totalPages}", centered, color COLORS.textSecondary), Next Page button (ChevronRight)

    TOOLS TAB (D-19): Select (MousePointer, active when activeTool==='select'), Count (MapPin), Linear (Minus), Area (Square), Perimeter (Hexagon), Wall (BrickWall), Set Scale (Ruler, with chevron child when pageScale !== null). Each markup tool uses handleMarkupToolClick; Select uses setActiveTool('select'). Chain badge chips: for each tool button, render the chip when activeTool === tool AND getChainArmedItem() !== null (copy exact chip JSX from Toolbar.tsx lines 368-380).

    VIEW TAB (D-20): Zoom In (ZoomIn, calls getCanvasControls()?.zoomIn()), Zoom Out (ZoomOut, calls getCanvasControls()?.zoomOut()), Fit to Window (Maximize, calls getCanvasControls()?.fitToWindow()), Show Totals (toggle — read showTotals from useUiPanels, calls toggleTotals()), Show All (Eye icon — calls handleShowAll), Hide All (EyeOff icon — calls handleHideAll)
    Import Eye, EyeOff from 'lucide-react'. Read useUiPanels from '../hooks/useUiPanels' — check what it exports (showTotals, toggleTotals). If useUiPanels is not already imported in Toolbar.tsx, check how it's accessed; the Toolbar may have a Show Totals button already — copy that pattern.

    ESTIMATING TAB (D-21, implementer discretion): Show the Export button again as a quick shortcut. Same as Home tab's Export button. Label this section "Quick Export".

    SETTINGS TAB (D-22, stubbed): Render a centered text "Settings — Coming soon" in COLORS.textSecondary.

    HELP TAB (D-23, stubbed): Render a centered text "Help — Coming soon" in COLORS.textSecondary.

    ScaleContextMenu: copy from Toolbar.tsx exactly; it conditionally renders when contextMenu.open === true (or however Toolbar manages it — read the Toolbar file for the exact contextMenu state and ScaleContextMenu integration).

    App.tsx changes:
    1. Change `import { Toolbar } from './components/Toolbar'` to `import { RibbonToolbar } from './components/RibbonToolbar'`.
    2. Change `<Toolbar .../>` to `<RibbonToolbar .../>` — props are identical (onOpenClick, onReplaceClick, onExportClick).
    3. Do NOT delete Toolbar.tsx yet — keep it alongside in case tests reference it. The executor may delete it after confirming npm run test -- --run passes.

    CRITICAL: All calls to getChainArmedItem(), getCanvasControls(), getCalibrationControls() in RibbonToolbar are module-level ref reads — not duplicated trigger logic. Per STATE.md: "Module-level ref pattern for canvas controls — simpler than React context or Zustand function refs for cross-component communication."

    CRITICAL for Show/Hide All (Pitfall 7 from RESEARCH.md): setHiddenItemNames does NOT call markDirty internally. handleShowAll and handleHideAll must BOTH call markDirty() after setHiddenItemNames.

    No raw hex literals. No Tailwind. Inline styles with COLORS tokens only.
  </action>

  <verify>
    <automated>npm run test -- --run && npx tsc --noEmit</automated>
  </verify>

  <done>
    - src/renderer/src/components/RibbonToolbar.tsx exists and exports RibbonToolbar and RibbonToolbarProps
    - RibbonToolbarProps matches ToolbarProps exactly (3 callback props)
    - App.tsx imports RibbonToolbar and renders it instead of Toolbar
    - All 7 tab panels render correct content per D-17 through D-24
    - Show All and Hide All handlers call markDirty() after setHiddenItemNames
    - Chain badge chips render on Tools tab active tool button when chain is armed
    - Set Scale chevron renders on the Set Scale RibbonButton
    - All existing tests pass (especially toolbar-related tests: toolbar-replace-pdf.test.ts, toolbar-saving-disabled.test.ts)
    - npx tsc --noEmit exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer UI → projectStore | Hide All writes hiddenItemNames to project state; markDirty ensures persistence on save |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Tampering | Hide All does not persist because markDirty not called | mitigate | handleHideAll explicitly calls markDirty() after setHiddenItemNames (Pitfall 7 from RESEARCH.md) |
| T-09-04-02 | Spoofing | Chain badge chip shows on wrong tool button | mitigate | Chip render is gated on activeTool === 'count' (or tool name) AND getChainArmedItem() !== null — same logic as Toolbar.tsx |
| T-09-04-03 | Denial of Service | Duplicate Set Scale trigger code | mitigate | getCalibrationControls()?.activate() is the sole entry point; RibbonToolbar calls it via module ref, not a duplicate |
</threat_model>

<verification>
- npm run test -- --run exits 0
- npx tsc --noEmit exits 0
- App.tsx contains RibbonToolbar (not Toolbar) in its import and JSX
- RibbonToolbar.tsx contains all 7 tab names: home, page, tools, view, estimating, settings, help
- RibbonToolbar.tsx contains handleShowAll and handleHideAll with markDirty()
- RibbonToolbar.tsx contains getChainArmedItem() reads (not duplicated chain logic)
</verification>

<success_criteria>
- Tabbed ribbon renders; all 7 tabs are clickable and show correct content
- All existing toolbar features remain accessible via the ribbon
- Select tool appears in Tools tab and sets activeTool='select'
- Show All/Hide All in View tab work and mark project dirty
- Chain badge chips and Set Scale chevron work identically to old Toolbar
- No test regressions
</success_criteria>

<output>
After completion, create .planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-04-SUMMARY.md
</output>
