---
phase: 09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/renderer/src/types/viewer.ts
  - src/renderer/src/types/markup.ts
  - src/renderer/src/stores/viewerStore.ts
  - src/renderer/src/stores/markupStore.ts
  - src/tests/delete-group-command.test.ts
  - src/tests/viewer-store-selection.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "viewerStore.selectedMarkupIds initialises to [] and can be set or cleared via actions"
    - "viewerStore clears selectedMarkupIds when the page changes, when resetViewer is called, and when hydrate is called"
    - "markupStore.deleteGroup removes all supplied markups across all pages in a single undoable command"
    - "Undoing a delete-group re-inserts every markup to its original page; redoing it removes them again"
    - "TypeScript compiles cleanly — no reference to cmd.markup on a 'delete-group' command"
    - "After deleteMarkup or deleteGroup is called, the keyboard handler in Plan 02 calls clearSelection() — no stale selectedMarkupIds remain after deletion"
  artifacts:
    - path: src/renderer/src/types/viewer.ts
      provides: "selectedMarkupIds: string[], setSelectedMarkupIds, clearSelection on ViewerState"
      contains: "selectedMarkupIds: string[]"
    - path: src/renderer/src/types/markup.ts
      provides: "delete-group variant on MarkupCommand union"
      contains: "'delete-group'"
    - path: src/renderer/src/stores/viewerStore.ts
      provides: "implementation of selectedMarkupIds field and actions; clear on navigation"
      contains: "selectedMarkupIds: []"
    - path: src/renderer/src/stores/markupStore.ts
      provides: "deleteGroup action; delete-group branches in undo() and redo()"
      contains: "deleteGroup"
    - path: src/tests/delete-group-command.test.ts
      provides: "RED test stubs for deleteGroup action, undo, redo"
    - path: src/tests/viewer-store-selection.test.ts
      provides: "RED test stubs for selectedMarkupIds init, set, clear, page-change clear"
  key_links:
    - from: src/renderer/src/types/markup.ts
      to: src/renderer/src/stores/markupStore.ts
      via: "MarkupCommand 'delete-group' variant drives new undo/redo branches"
      pattern: "'delete-group'"
    - from: src/renderer/src/stores/viewerStore.ts
      to: src/renderer/src/types/viewer.ts
      via: "ViewerState interface consumed by viewerStore create()"
      pattern: "selectedMarkupIds"
---

<objective>
Wave 0 prerequisite: extend types and stores to support the Phase 9 selection model and group-delete feature. Every subsequent plan (Wave 1+) depends on these types compiling cleanly and the actions existing.

Purpose: Establish the data contracts before any interactive code is written. Prevents TypeScript crashes at the `undo()` switch fallthrough and enables parallel implementation in Wave 1.
Output: Two updated type files, two updated store files, two RED test stubs.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-CONTEXT.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-RESEARCH.md
@.planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ViewerState type + viewerStore with selectedMarkupIds</name>
  <files>src/renderer/src/types/viewer.ts, src/renderer/src/stores/viewerStore.ts, src/tests/viewer-store-selection.test.ts</files>

  <read_first>
    src/renderer/src/types/viewer.ts — read the full ViewerState interface; add three new fields after setActiveTool (line 51)
    src/renderer/src/stores/viewerStore.ts — read the full file; extend setPage, nextPage, prevPage, resetViewer, hydrate to include selectedMarkupIds: []
    src/tests/viewer-store.test.ts — read to understand existing test patterns; mirror them for the RED stubs
  </read_first>

  <behavior>
    - Test 1: viewerStore initialises with selectedMarkupIds equal to []
    - Test 2: setSelectedMarkupIds(['id-a', 'id-b']) → getState().selectedMarkupIds deep-equals ['id-a', 'id-b']
    - Test 3: clearSelection() after setSelectedMarkupIds → selectedMarkupIds equals []
    - Test 4: setPage() clears selectedMarkupIds → after setSelectedMarkupIds then setPage, selectedMarkupIds equals []
    - Test 5: nextPage() clears selectedMarkupIds
    - Test 6: prevPage() clears selectedMarkupIds
    - Test 7: resetViewer() clears selectedMarkupIds
    - Test 8: hydrate() clears selectedMarkupIds
  </behavior>

  <action>
    Step 1 — Create RED test file src/tests/viewer-store-selection.test.ts. Import useViewerStore. Write 8 test stubs (describe block "viewerStore — selectedMarkupIds") that import and call the new actions. All tests must FAIL at this point because the actions do not exist yet. Do NOT import anything that doesn't exist — reference the store's getState() shape only.

    Step 2 — Extend src/renderer/src/types/viewer.ts: after the setActiveTool declaration (line 51), add three new declarations to the ViewerState interface:
      selectedMarkupIds: string[]
      setSelectedMarkupIds: (ids: string[]) => void
      clearSelection: () => void

    Step 3 — Extend src/renderer/src/stores/viewerStore.ts:
    - Add selectedMarkupIds: [] to the initial state object (alongside activeTool on line 15).
    - Add two new actions in the create() body (after setActiveTool):
        setSelectedMarkupIds: (ids) => set({ selectedMarkupIds: ids }),
        clearSelection: () => set({ selectedMarkupIds: [] }),
    - In setPage: change `set({ currentPage: page })` to `set({ currentPage: page, selectedMarkupIds: [] })` (per D-01 — selection is page-scoped).
    - In nextPage: change `set({ currentPage: currentPage + 1 })` to `set({ currentPage: currentPage + 1, selectedMarkupIds: [] })`.
    - In prevPage: change `set({ currentPage: currentPage - 1 })` to `set({ currentPage: currentPage - 1, selectedMarkupIds: [] })`.
    - In resetViewer: add selectedMarkupIds: [] to the set() call alongside activeTool.
    - In hydrate: add selectedMarkupIds: [] to the set() call alongside activeTool.
    - In setFile: add selectedMarkupIds: [] to the set() call (opening a new file deselects everything).

    TypeScript must compile after this step. Run: npx tsc --noEmit
  </action>

  <verify>
    <automated>npm run test -- --run viewer-store-selection</automated>
  </verify>

  <done>
    - src/renderer/src/types/viewer.ts contains selectedMarkupIds: string[] in ViewerState
    - src/renderer/src/stores/viewerStore.ts contains setSelectedMarkupIds and clearSelection actions
    - All 8 viewer-store-selection tests pass (GREEN)
    - npx tsc --noEmit exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend MarkupCommand union + markupStore deleteGroup action with undo/redo</name>
  <files>src/renderer/src/types/markup.ts, src/renderer/src/stores/markupStore.ts, src/tests/delete-group-command.test.ts</files>

  <read_first>
    src/renderer/src/types/markup.ts — read lines 52-77 (MarkupCommand union); insert new variant after the 'delete' variant on line 54
    src/renderer/src/stores/markupStore.ts — read lines 1-55 (interface + helpers), lines 113-123 (deleteMarkup pattern), lines 214-271 (undo switch — critical: insert BEFORE line 259 const page = cmd.markup.page), lines 274-332 (redo switch — insert BEFORE line 319)
    src/tests/markup-store.test.ts — read to understand existing test patterns; mirror for RED stubs
    src/tests/markup-commands.test.ts — read existing command test patterns
  </read_first>

  <behavior>
    - Test 1: deleteGroup([markupA, markupB]) removes both markups from their pages; pageMarkups[page] no longer contains either ID
    - Test 2: deleteGroup pushes a single 'delete-group' command onto undoStack (length increases by 1, not 2)
    - Test 3: after deleteGroup, undo() re-inserts both markups to their original pages
    - Test 4: after deleteGroup then undo(), redo() removes both markups again
    - Test 5: deleteGroup across two pages (markupA on page 1, markupB on page 2) removes both and undo re-inserts each to its correct page
    - Test 6: deleteGroup with empty array is a no-op (undoStack length unchanged)
  </behavior>

  <action>
    Step 1 — Create RED test file src/tests/delete-group-command.test.ts. Import useMarkupStore. Write 6 test stubs (describe block "markupStore — deleteGroup command") that call deleteGroup and exercise undo/redo. Tests must FAIL because deleteGroup does not exist yet.

    Step 2 — Extend src/renderer/src/types/markup.ts: in the MarkupCommand union (lines 52-77), insert a new variant after the '{ type: "delete"; markup: Markup }' line:
      | { type: 'delete-group'; markups: Markup[] }

    Step 3 — Extend src/renderer/src/stores/markupStore.ts:
    a) In the MarkupStoreState interface (after line 22 deleteMarkup declaration), add:
         deleteGroup: (markups: Markup[]) => void

    b) In the store create() body, add the deleteGroup action (after deleteMarkup):
       deleteGroup: (markups) =>
         set((s) => {
           if (markups.length === 0) return s
           const idSet = new Set(markups.map((m) => m.id))
           const nextPageMarkups = { ...s.pageMarkups }
           for (const p of Object.keys(nextPageMarkups).map(Number)) {
             nextPageMarkups[p] = (nextPageMarkups[p] ?? []).filter((m) => !idSet.has(m.id))
           }
           return {
             pageMarkups: nextPageMarkups,
             undoStack: pushCommand(s.undoStack, { type: 'delete-group', markups }),
             redoStack: []
           }
         }),

    c) In the undo() function: insert the delete-group branch IMMEDIATELY BEFORE the line `const page = cmd.markup.page` (currently line 259). Insert:
         if (cmd.type === 'delete-group') {
           const nextPageMarkups = { ...s.pageMarkups }
           for (const m of cmd.markups) {
             nextPageMarkups[m.page] = [...(nextPageMarkups[m.page] ?? []), m]
           }
           return {
             pageMarkups: nextPageMarkups,
             undoStack: s.undoStack.slice(0, -1),
             redoStack: [...s.redoStack, cmd]
           }
         }
       The const page = cmd.markup.page line must remain immediately after this new block.

    d) In the redo() function: insert the delete-group branch IMMEDIATELY BEFORE the line `const page = cmd.markup.page` (currently line 319). Insert:
         if (cmd.type === 'delete-group') {
           const idSet = new Set(cmd.markups.map((m) => m.id))
           const nextPageMarkups = { ...s.pageMarkups }
           for (const p of Object.keys(nextPageMarkups).map(Number)) {
             nextPageMarkups[p] = (nextPageMarkups[p] ?? []).filter((m) => !idSet.has(m.id))
           }
           return {
             pageMarkups: nextPageMarkups,
             undoStack: pushCommand(s.undoStack, cmd),
             redoStack: s.redoStack.slice(0, -1)
           }
         }

    CRITICAL: If the new branches are not placed BEFORE const page = cmd.markup.page, TypeScript will error because 'delete-group' commands have markups (plural) not markup (singular). This is Pitfall 3 from RESEARCH.md.

    Run: npx tsc --noEmit — must exit 0 before proceeding.
  </action>

  <verify>
    <automated>npm run test -- --run delete-group-command && npx tsc --noEmit</automated>
  </verify>

  <done>
    - src/renderer/src/types/markup.ts MarkupCommand union contains '{ type: "delete-group"; markups: Markup[] }'
    - src/renderer/src/stores/markupStore.ts MarkupStoreState interface contains deleteGroup: (markups: Markup[]) => void
    - src/renderer/src/stores/markupStore.ts contains the delete-group branch in BOTH undo() and redo() BEFORE const page = cmd.markup.page
    - All 6 delete-group-command tests pass (GREEN)
    - Full test suite passes: npm run test -- --run exits 0
    - npx tsc --noEmit exits 0
    - NOTE: clearSelection() is called in the keyboard handler (Plan 02) after every deleteMarkup/deleteGroup call — selection is cleared at the handler level, not inside the store action itself (cross-store calls from markupStore into viewerStore would violate separation of concerns).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer store mutation | All store actions run in renderer — no IPC, no external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-00-01 | Tampering | deleteGroup undo re-insert order | accept | Markups re-inserted in cmd.markups array order; no order guarantee needed for undo correctness |
| T-09-00-02 | Denial of Service | deleteGroup with very large Markup[] | accept | All markups are already in memory; idSet is O(n); no new risk beyond existing store size |
</threat_model>

<verification>
After both tasks complete:
- npm run test -- --run exits 0 (all existing tests still pass, 14 new tests pass)
- npx tsc --noEmit exits 0
- grep confirms: src/renderer/src/types/viewer.ts contains selectedMarkupIds: string[]
- grep confirms: src/renderer/src/types/markup.ts contains 'delete-group'
- grep confirms: src/renderer/src/stores/markupStore.ts contains deleteGroup and the delete-group branches in both undo() and redo()
</verification>

<success_criteria>
- ViewerState interface extended with selectedMarkupIds, setSelectedMarkupIds, clearSelection
- viewerStore clears selectedMarkupIds on setPage, nextPage, prevPage, resetViewer, hydrate, setFile
- MarkupCommand union extended with delete-group variant
- markupStore exports deleteGroup action; undo/redo handle delete-group correctly
- All tests pass; TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create .planning/phases/09-selection-model-ribbon-toolbar-modal-polish-and-markup-completion/09-00-SUMMARY.md
</output>
