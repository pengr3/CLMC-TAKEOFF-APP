---
phase: 08-markup-workflow-and-wall-tool
plan: "06"
type: execute
wave: 3
depends_on: ["08-02", "08-04"]
files_modified:
  - src/renderer/src/components/TotalsRow.tsx
  - src/renderer/src/components/markup/CountPinMarkup.tsx
  - src/renderer/src/components/markup/LinearMarkup.tsx
  - src/renderer/src/components/markup/AreaMarkup.tsx
  - src/renderer/src/components/markup/PerimeterMarkup.tsx
  - src/renderer/src/components/HoverRing.tsx
  - src/renderer/src/components/PulseHighlight.tsx
autonomous: true
requirements: []
tags: [visibility, totals-row, renderers, hover, pulse]

must_haves:
  truths:
    - "TotalsRow renders a 16px slot before the color chip; Lightbulb when item is visible, LightbulbOff when item.name ∈ hiddenItemNames"
    - "Lightbulb click toggles hiddenItemNames via useProjectStore.getState().toggleHiddenItem(itemName) AND e.stopPropagation() prevents row cycle navigation (Pitfall 9)"
    - "CountPinMarkup returns null when markup.name ∈ projectStore.hiddenItemNames"
    - "LinearMarkup returns null when hidden"
    - "AreaMarkup returns null when hidden"
    - "PerimeterMarkup returns null when hidden"
    - "HoverRing has a 'wall' branch mirroring the 'linear' open-polyline render"
    - "PulseHighlight has a 'wall' branch mirroring the 'linear' open-polyline render"
    - "src/tests/totals-row-visibility.test.ts GREEN"
    - "src/tests/markup-visibility.test.ts GREEN"
  artifacts:
    - path: "src/renderer/src/components/TotalsRow.tsx"
      provides: "Lightbulb/LightbulbOff slot with toggleHiddenItem on click"
      contains: "Lightbulb"
    - path: "src/renderer/src/components/markup/CountPinMarkup.tsx"
      provides: "Hidden-item skip-render"
      contains: "hiddenItemNames"
    - path: "src/renderer/src/components/markup/LinearMarkup.tsx"
      provides: "Hidden-item skip-render"
      contains: "hiddenItemNames"
    - path: "src/renderer/src/components/markup/AreaMarkup.tsx"
      provides: "Hidden-item skip-render"
      contains: "hiddenItemNames"
    - path: "src/renderer/src/components/markup/PerimeterMarkup.tsx"
      provides: "Hidden-item skip-render"
      contains: "hiddenItemNames"
    - path: "src/renderer/src/components/HoverRing.tsx"
      provides: "'wall' branch for open-polyline hover ring"
      contains: "'wall'"
    - path: "src/renderer/src/components/PulseHighlight.tsx"
      provides: "'wall' branch for open-polyline pulse"
      contains: "'wall'"
  key_links:
    - from: "src/renderer/src/components/TotalsRow.tsx"
      to: "src/renderer/src/stores/projectStore.ts"
      via: "useProjectStore(s => s.hiddenItemNames.includes(itemName)) + useProjectStore.getState().toggleHiddenItem(itemName)"
      pattern: "hiddenItemNames|toggleHiddenItem"
    - from: "src/renderer/src/components/markup/*.tsx"
      to: "src/renderer/src/stores/projectStore.ts"
      via: "useProjectStore(s => s.hiddenItemNames.includes(markup.name)) — primitive boolean selector"
      pattern: "hiddenItemNames"
---

<objective>
Wave 3B: deliver the show/hide UI surface and add the wall branch to the two transient overlay renderers (HoverRing + PulseHighlight).

Purpose: The persistence + toggle action exist (Wave 1B 08-02); this plan adds the lightbulb UI in TotalsRow and the skip-render check in every markup renderer so hidden items actually disappear from the canvas. Hidden markups still aggregate (D-15) and still cycle/click correctly in the totals panel. HoverRing + PulseHighlight gain a 'wall' branch so wall markups participate in hover and pulse like the other markup types (without these branches walls would never highlight on hover).

Output: 7 files touched (1 TotalsRow + 4 markup renderers + 2 overlays).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-markup-workflow-and-wall-tool/08-CONTEXT.md
@.planning/phases/08-markup-workflow-and-wall-tool/08-RESEARCH.md
@.planning/phases/08-markup-workflow-and-wall-tool/08-PATTERNS.md
@.planning/phases/08-markup-workflow-and-wall-tool/08-02-PLAN.md

<interfaces>
TotalsRow.tsx current layout (lines 9-11 comment):
  [cycle-dot 6px slot][color chip 10x10][gap 4px][label flex:1][quantity right][gap 4px][uom 40px]

Target layout (insert lightbulb between cycle-dot and color-chip):
  [cycle-dot 6px slot][lightbulb 16px][color chip 10x10][gap 4px][label flex:1][quantity right][gap 4px][uom 40px]

Lightbulb slot pattern (PATTERNS.md §TotalsRow):
  - 16px width, flexShrink: 0, display: flex, alignItems: center, cursor: pointer
  - Click handler: e.stopPropagation() then useProjectStore.getState().toggleHiddenItem(itemName)  // Pitfall 9
  - Title attribute: isHidden ? 'Show on canvas' : 'Hide from canvas'
  - Icon: 12px Lightbulb (visible state) or LightbulbOff (hidden state); color = COLORS.textSecondary

Markup renderer skip-render pattern (uniform across CountPinMarkup, LinearMarkup, AreaMarkup, PerimeterMarkup):
  Place at the TOP of the function body, BEFORE any computed values:
    const isHidden = useProjectStore((s) => s.hiddenItemNames.includes(markup.name))
    if (isHidden) return null
  Requires: import { useProjectStore } from '../../stores/projectStore' (or '../stores/projectStore' depending on file depth)

Note: WallMarkup.tsx already has this skip-render from 08-04 — do NOT duplicate.

HoverRing.tsx current type dispatch (PATTERNS.md lines 50-98 of the file):
  if (m.type === 'count') { return <Circle ... /> }
  if (m.type === 'linear') { return <Line ... /> }  // open polyline
  // area + perimeter: closed polygon
  const closing = [...m.points, m.points[0]]
  return <Line ... points={closing.flatMap(...)} ... />

Target: Add a 'wall' branch BEFORE the closing-polygon fallback, mirroring 'linear' (open polyline):
  if (m.type === 'wall') {
    return <Line key={m.id} points={m.points.flatMap((p) => [p.x, p.y])}
      stroke={RING_COLOR} strokeWidth={stroke + offset * 2} opacity={RING_OPACITY}
      lineCap="round" lineJoin="round" listening={false} />
  }

PulseHighlight.tsx — same 'wall' branch insertion, same open-polyline shape.

Hidden-item handling for HoverRing/PulseHighlight: NO skip check needed inside these overlays. Hidden markups skip-render at the LinearMarkup/AreaMarkup/PerimeterMarkup/CountPinMarkup/WallMarkup level; HoverRing receives the SAME markups array but those markups simply have no underlying renderer drawn. Hovering an invisible markup does technically produce a hover-ring with nothing under it — but the trigger for hover comes from MOUSE position over a Konva Group, and Groups for hidden markups are not rendered (they returned null), so no mouseEnter fires. Pulse is triggered by TotalsRow click; clicking a hidden row WILL fire a pulse with nothing visible underneath — D-15 explicitly documents this as acceptable (the pulse just produces nothing visible).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TotalsRow lightbulb slot</name>
  <files>src/renderer/src/components/TotalsRow.tsx</files>
  <read_first>
    - src/renderer/src/components/TotalsRow.tsx — read full file: layout comment (lines 9-11), imports, labelToName helper (lines 54-57), handleClick + handleContextMenu (around line 140-150), the cycle-dot slot (lines 178-198), the color chip (lines 200-212), the row root div with onClick
    - .planning/phases/08-markup-workflow-and-wall-tool/08-PATTERNS.md §"src/renderer/src/components/TotalsRow.tsx" — exact pattern with the e.stopPropagation() click handler
    - .planning/phases/08-markup-workflow-and-wall-tool/08-RESEARCH.md §"Pitfall 9" — e.stopPropagation() MANDATORY to prevent row cycle navigation from also firing
    - src/renderer/src/stores/projectStore.ts — confirm hiddenItemNames + toggleHiddenItem (from 08-02 Task 2) exist
    - src/tests/totals-row-visibility.test.ts — re-read the RED stubs to confirm exact expected DOM structure (title attribute, click behavior)
  </read_first>
  <action>
    Three edits to TotalsRow.tsx:

    1. Imports: Add `Lightbulb` and `LightbulbOff` to the lucide-react import (verified installed in lucide-react 1.6.0 per RESEARCH §Environment Availability). Add `import { useProjectStore } from '../stores/projectStore'` if not already imported.

    2. Inside the component body, BEFORE the JSX return:
       - Read the item name: re-use the existing `itemName` computation (line 101 area: `useMemo(() => labelToName(item.label), [item.label])`). If `itemName` is named differently, derive it from item.label following the existing pattern.
       - Add: `const isHidden = useProjectStore((s) => s.hiddenItemNames.includes(itemName))` — primitive boolean selector (Object.is safe per RESEARCH §"Zustand selector identity").

    3. JSX insertion: between the cycle-dot slot (lines 178-198) and the color chip (lines 200-212), insert a new <div> element following the pattern below. Use inline styles + COLORS tokens (no Tailwind per CLAUDE.md):
       - Width 16px, flexShrink: 0, display: flex, alignItems: center, cursor: pointer
       - onClick handler: `(e) => { e.stopPropagation(); useProjectStore.getState().toggleHiddenItem(itemName) }` — the e.stopPropagation() is MANDATORY (Pitfall 9) to prevent the row's onClick (cycle navigation) from firing simultaneously.
       - title attribute: `isHidden ? 'Show on canvas' : 'Hide from canvas'`
       - data-testid: 'totals-row-lightbulb' (so the test from 08-00 can find it via that selector; if the test uses a different selector verify and add the matching attribute)
       - Inside: conditionally render `{isHidden ? <LightbulbOff size={12} color={COLORS.textSecondary} /> : <Lightbulb size={12} color={COLORS.textSecondary} />}` — use the COLORS.textSecondary token (or equivalent muted-text token used elsewhere in this file)

    Do NOT change the existing handleClick logic for the row. Do NOT change labelToName, the cycle-dot, the color chip, the label, the quantity column, or the uom column. The lightbulb slot is purely additive.

    Edge case to handle: if `itemName` derivation depends on a useMemo or callback that runs once per render, the isHidden selector should follow the same pattern — call useProjectStore inside the component body (a React hook), not inside a useMemo or callback.
  </action>
  <verify>
    <automated>npx vitest run src/tests/totals-row-visibility.test.ts --reporter=verbose 2>&1 | tail -30 && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/tests/totals-row-visibility.test.ts` exits 0 (all 3 tests GREEN: Lightbulb visible state, LightbulbOff hidden state, click toggles AND does not cycle).
    - `npx tsc --noEmit` exits 0.
    - Source assertion: `grep -n "import.*Lightbulb.*LightbulbOff\|import.*LightbulbOff.*Lightbulb" src/renderer/src/components/TotalsRow.tsx` returns at least 1 line (or two separate import lines for the two icons).
    - Source assertion: `grep -n "hiddenItemNames.includes" src/renderer/src/components/TotalsRow.tsx` returns one match.
    - Source assertion: `grep -n "toggleHiddenItem" src/renderer/src/components/TotalsRow.tsx` returns one match (the click handler).
    - Source assertion: `grep -n "e.stopPropagation()" src/renderer/src/components/TotalsRow.tsx` returns at least 1 match in the lightbulb click handler (Pitfall 9).
    - Source assertion: `grep -n "Show on canvas\|Hide from canvas" src/renderer/src/components/TotalsRow.tsx` returns at least 1 line (the title attribute).
    - Source assertion: `grep -n "<Lightbulb\|<LightbulbOff" src/renderer/src/components/TotalsRow.tsx` returns at least 2 lines (both icons referenced).
    - Regression assertion: `npx vitest run src/tests/totals-panel-render.test.ts src/tests/totals-panel-category-collapse.test.ts` exits 0 (Phase 6/7 totals tests not broken by the added slot).
  </acceptance_criteria>
  <done>
    TotalsRow has the lightbulb slot. Click toggles visibility AND does not cycle. totals-row-visibility.test.ts GREEN. Phase 6/7 totals regression tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hidden-item skip-render in 4 markup renderers (CountPinMarkup, LinearMarkup, AreaMarkup, PerimeterMarkup)</name>
  <files>src/renderer/src/components/markup/CountPinMarkup.tsx, src/renderer/src/components/markup/LinearMarkup.tsx, src/renderer/src/components/markup/AreaMarkup.tsx, src/renderer/src/components/markup/PerimeterMarkup.tsx</files>
  <read_first>
    - src/renderer/src/components/markup/CountPinMarkup.tsx — read full file: imports, component function signature, first computed value (so we know where to insert the skip check)
    - src/renderer/src/components/markup/LinearMarkup.tsx — read lines 35-50 (component body start; the `const strokeWidth` computation is around line 51)
    - src/renderer/src/components/markup/AreaMarkup.tsx — read lines 35-50 (find first computed value)
    - src/renderer/src/components/markup/PerimeterMarkup.tsx — read lines 35-50 (find first computed value)
    - .planning/phases/08-markup-workflow-and-wall-tool/08-PATTERNS.md §"Hidden-Item Skip-Render (renderer components)" — uniform pattern across all five renderers (the 4 here + WallMarkup already done in 08-04)
    - src/renderer/src/components/WallMarkup.tsx — confirm the exact skip-render pattern from 08-04 Task 1 (so all 5 renderers use IDENTICAL code shape)
    - src/tests/markup-visibility.test.ts — re-read the RED stubs (4 tests, one per renderer) to confirm expectations
  </read_first>
  <action>
    Apply the IDENTICAL two-line skip-render to all 4 files. Use the EXACT same code shape that WallMarkup.tsx uses (from 08-04). This uniformity is intentional — it makes the pattern grep-discoverable and prevents per-file drift.

    For each of the 4 files:

    1. Imports: Add `import { useProjectStore } from '../../stores/projectStore'` (note: depth is ../../ because these files are nested inside src/renderer/src/components/markup/). If the import is already present (e.g., from another use), do not duplicate.

    2. Component body — insert as the FIRST TWO LINES of the function body, before any other computed value (before `const strokeWidth`, before `const fill = markup.color`, before any useState/useCallback):
       ```
       const isHidden = useProjectStore((s) => s.hiddenItemNames.includes(markup.name))
       if (isHidden) return null
       ```

    Do NOT change any other code in these files. Do NOT change props. Do NOT change render logic. Do NOT add any conditional rendering outside the early return.

    React hook ordering: useProjectStore is a hook — placing it at the TOP of the function body ensures it runs before any conditional return (React Rules of Hooks). The early `return null` happens AFTER the hook call, which is rule-compliant.
  </action>
  <verify>
    <automated>npx vitest run src/tests/markup-visibility.test.ts --reporter=verbose 2>&1 | tail -30 && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/tests/markup-visibility.test.ts` exits 0 (4 tests GREEN: CountPinMarkup, LinearMarkup, AreaMarkup, PerimeterMarkup all return null when hidden).
    - `npx tsc --noEmit` exits 0.
    - Source assertion: `grep -n "useProjectStore" src/renderer/src/components/markup/CountPinMarkup.tsx` returns at least 2 (import + hook call).
    - Source assertion: `grep -n "useProjectStore" src/renderer/src/components/markup/LinearMarkup.tsx` returns at least 2.
    - Source assertion: `grep -n "useProjectStore" src/renderer/src/components/markup/AreaMarkup.tsx` returns at least 2.
    - Source assertion: `grep -n "useProjectStore" src/renderer/src/components/markup/PerimeterMarkup.tsx` returns at least 2.
    - Source assertion: `grep -n "if (isHidden) return null" src/renderer/src/components/markup/CountPinMarkup.tsx` returns one match.
    - Source assertion: `grep -n "if (isHidden) return null" src/renderer/src/components/markup/LinearMarkup.tsx` returns one match.
    - Source assertion: `grep -n "if (isHidden) return null" src/renderer/src/components/markup/AreaMarkup.tsx` returns one match.
    - Source assertion: `grep -n "if (isHidden) return null" src/renderer/src/components/markup/PerimeterMarkup.tsx` returns one match.
    - Uniformity check: `grep -h "useProjectStore((s) => s.hiddenItemNames" src/renderer/src/components/markup/*.tsx src/renderer/src/components/WallMarkup.tsx | sort -u | wc -l` returns 1 (all 5 renderers use the IDENTICAL selector expression).
    - Regression assertion: `npx vitest run --reporter=verbose 2>&1 | tail -5` shows no pre-existing renderer tests regressing.
  </acceptance_criteria>
  <done>
    All 4 renderers have the skip-render check at the top. markup-visibility.test.ts GREEN. TypeScript clean. No regressions in existing renderer tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: HoverRing + PulseHighlight wall branch</name>
  <files>src/renderer/src/components/HoverRing.tsx, src/renderer/src/components/PulseHighlight.tsx</files>
  <read_first>
    - src/renderer/src/components/HoverRing.tsx — read full file: the markup type dispatch (around lines 50-98 per PATTERNS.md) showing the count → linear → (area+perimeter closing-polygon) cascade; identify the exact insertion point for the 'wall' branch (between 'linear' and the closing-polygon fallback)
    - src/renderer/src/components/PulseHighlight.tsx — read full file: same structure as HoverRing; same insertion point
    - .planning/phases/08-markup-workflow-and-wall-tool/08-PATTERNS.md §"src/renderer/src/components/HoverRing.tsx" — exact 'wall' branch shape (open polyline like 'linear')
    - .planning/phases/08-markup-workflow-and-wall-tool/08-PATTERNS.md §"src/renderer/src/components/PulseHighlight.tsx" — same dispatch pattern
    - .planning/phases/08-markup-workflow-and-wall-tool/08-RESEARCH.md §"Markup Renderer Skip-Render Pattern" final paragraph — confirms HoverRing/PulseHighlight need NO hidden-item check (hidden markups never trigger hover; click-pulse on hidden item is intentionally a no-op visual)
  </read_first>
  <action>
    Add an identical 'wall' branch to both HoverRing.tsx and PulseHighlight.tsx. The wall branch mirrors the 'linear' branch verbatim (open polyline; no closing segment).

    For BOTH files:

    1. Locate the type dispatch in the .map() / .forEach() callback that handles each markup. The current structure is:
       - count → Circle
       - linear → Line (open polyline)
       - default (area + perimeter) → Line with closing segment

    2. Insert a new branch after the 'linear' branch and BEFORE the closing-polygon default:
       ```
       if (m.type === 'wall') {
         return <Line
           key={m.id}
           points={m.points.flatMap((p) => [p.x, p.y])}
           stroke={RING_COLOR}                  // re-use the existing color constant
           strokeWidth={stroke + offset * 2}    // re-use existing stroke + offset constants from the 'linear' branch
           opacity={RING_OPACITY}               // re-use existing opacity constant
           lineCap="round"
           lineJoin="round"
           listening={false}
         />
       }
       ```
       Use the EXACT constant names already in scope in each file — read them while implementing (they may be RING_COLOR / RING_OPACITY for HoverRing and different names for PulseHighlight; mirror what the 'linear' branch uses verbatim).

    3. For PulseHighlight, the props are slightly different — read the existing 'linear' branch and copy its prop shape verbatim into the new 'wall' branch (which may include opacity computed from progress, strokeWidth from the pulse animation state, etc.).

    Do NOT add any hidden-item skip check here — the parent layer's filtering on `currentPageMatches` (or the renderer-level skip-render in Task 2) means HoverRing/PulseHighlight naturally receive no hidden walls in their markups array. RESEARCH §"Markup Renderer Skip-Render Pattern" confirms this.

    Do NOT modify the count, linear, area, or perimeter branches.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10 && npx vitest run src/tests/hover-ring.test.ts src/tests/pulse-highlight-animation.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - Source assertion: `grep -n "m.type === 'wall'" src/renderer/src/components/HoverRing.tsx` returns one match.
    - Source assertion: `grep -n "m.type === 'wall'" src/renderer/src/components/PulseHighlight.tsx` returns one match.
    - Source assertion: in BOTH files, the 'wall' branch returns a <Line> (not a <Circle>) — `grep -A 2 "m.type === 'wall'" src/renderer/src/components/HoverRing.tsx | grep -c "<Line"` returns 1.
    - Source assertion: the wall branch uses `m.points.flatMap` — `grep -A 5 "m.type === 'wall'" src/renderer/src/components/HoverRing.tsx | grep -c "flatMap"` returns 1.
    - Regression assertion: `npx vitest run src/tests/hover-ring.test.ts src/tests/pulse-highlight-animation.test.ts` exits 0 (Phase 6 transient-overlay tests still pass).
  </acceptance_criteria>
  <done>
    HoverRing and PulseHighlight both have a 'wall' branch that renders an open polyline mirroring the 'linear' branch. TypeScript clean. Phase 6 overlay tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TotalsRow click → toggleHiddenItem | In-process Zustand call; markDirty path covered |
| Renderer hidden check → useProjectStore selector | Primitive boolean selector; safe re-render boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Tampering | Lightbulb click also triggers row cycle navigation (Pitfall 9) | mitigate | e.stopPropagation() MANDATORY in the click handler; test "click does not cycle" enforces this |
| T-08-06-02 | DoS | hiddenItemNames.includes() called on every render of every markup in a 100-wall project = O(n²) | accept | n bounded by ~100 markup names per RESEARCH §"hiddenItemNames bloat" threat; modern CPUs handle 10000 string compares per frame easily |
| T-08-06-03 | Information Disclosure | LightbulbOff icon reveals "this item is hidden" — but click target is always visible | accept | Intended affordance — D-14 requires always-visible icon |
</threat_model>

<verification>
- `npx vitest run src/tests/totals-row-visibility.test.ts src/tests/markup-visibility.test.ts` exits 0
- `npx tsc --noEmit` exits 0
- Hidden markup name disappears from canvas immediately; TotalsRow stays interactive
- HoverRing / PulseHighlight render correctly for wall markups (manual UAT covers this)
</verification>

<success_criteria>
TotalsRow has Lightbulb/LightbulbOff slot. All 5 markup renderers (4 in this plan + WallMarkup from 08-04) skip-render when hidden. HoverRing + PulseHighlight handle wall as open polyline. Both Wave 0 visibility tests GREEN. No Phase 6/7 regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-markup-workflow-and-wall-tool/08-06-SUMMARY.md`
</output>
