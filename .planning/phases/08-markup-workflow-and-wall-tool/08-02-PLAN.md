---
phase: 08-markup-workflow-and-wall-tool
plan: "02"
type: execute
wave: 1
depends_on: ["08-00"]
files_modified:
  - src/renderer/src/lib/project-schema.ts
  - src/renderer/src/lib/project-serialize.ts
  - src/renderer/src/stores/projectStore.ts
autonomous: true
requirements: []
tags: [persistence, store, schema, visibility]

must_haves:
  truths:
    - "ProjectFileV2 interface has an additive `hiddenItemNames?: string[]` field — no formatVersion bump"
    - "snapshotProject emits hiddenItemNames from projectStore on every save"
    - "hydrateStores accepts hiddenItemNames from loaded ProjectFileV2 and writes to projectStore; defaults to [] when absent"
    - "projectStore has hiddenItemNames: string[] field initialized to []"
    - "projectStore has hiddenItemSet: Set<string> derived field initialized as new Set([]) and kept in sync with hiddenItemNames by toggleHiddenItem and hydrateStores"
    - "projectStore has toggleHiddenItem(name) action that flips name in/out of the array and calls markDirty() last"
    - "reset() clears hiddenItemNames to [] and hiddenItemSet to new Set()"
    - "src/tests/project-schema-hidden.test.ts GREEN (all 3 tests)"
  artifacts:
    - path: "src/renderer/src/lib/project-schema.ts"
      provides: "Additive optional hiddenItemNames field on ProjectFileV2"
      contains: "hiddenItemNames"
    - path: "src/renderer/src/lib/project-serialize.ts"
      provides: "Serialize hiddenItemNames into snapshot; hydrate it on load with [] default"
      contains: "hiddenItemNames"
    - path: "src/renderer/src/stores/projectStore.ts"
      provides: "hiddenItemNames state + hiddenItemSet derived state + toggleHiddenItem action"
      contains: "hiddenItemSet"
  key_links:
    - from: "src/renderer/src/stores/projectStore.ts"
      to: "src/renderer/src/stores/projectStore.ts"
      via: "toggleHiddenItem calls get().markDirty() last"
      pattern: "markDirty"
    - from: "src/renderer/src/lib/project-serialize.ts"
      to: "src/renderer/src/stores/projectStore.ts"
      via: "snapshotProject reads useProjectStore.getState().hiddenItemNames; hydrateStores writes useProjectStore.setState({hiddenItemNames, hiddenItemSet})"
      pattern: "hiddenItemNames"
---

<objective>
Wave 1B: add visibility state to projectStore and wire it through the persistence path.

Purpose: hiddenItemNames is takeoff state (D-13) — it travels with the .clmc file. This plan extends the project schema additively (no formatVersion bump per RESEARCH §Feature 3), serializes/hydrates the field, and adds the toggleHiddenItem store action that the TotalsRow lightbulb will call. Visibility is NOT undoable (D-16) — it goes through markDirty(), not the command pattern. A derived hiddenItemSet: Set<string> is maintained in sync for O(1) renderer lookups.

Output: project-schema.ts, project-serialize.ts, projectStore.ts extended additively. No existing behavior changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-markup-workflow-and-wall-tool/08-CONTEXT.md
@.planning/phases/08-markup-workflow-and-wall-tool/08-RESEARCH.md
@.planning/phases/08-markup-workflow-and-wall-tool/08-PATTERNS.md
@.planning/phases/08-markup-workflow-and-wall-tool/08-00-PLAN.md

<interfaces>
Current ProjectFileV2 (project-schema.ts lines 63-83):
  formatVersion: 2, createdAt, updatedAt, pdf, globalUnit, categories,
  categoryOrder, currentPage, pages

Target additive field:
  hiddenItemNames?: string[]   // additive; absent in pre-Phase 8 files; default [] on load

Current ProjectStoreState (projectStore.ts lines 6-19):
  currentFilePath, isDirty, isSaving, isExporting, lastSavedAt,
  setSaved, setSaving, setExporting, setCurrentFilePath, markDirty, reset

Target additive fields/actions:
  hiddenItemNames: string[]
  hiddenItemSet: Set<string>        // derived O(1) lookup; kept in sync with hiddenItemNames
  toggleHiddenItem: (name: string) => void
  setHiddenItemNames: (names: string[]) => void  // used by hydrateStores

markDirty pattern (projectStore.ts lines 53-57):
  if (_hydrating) return
  if (get().isDirty) return
  set({ isDirty: true })

snapshotProject return object (project-serialize.ts lines 46-60):
  Build a ProjectFileV2 literal with formatVersion: 2, ..., currentPage, pages
  ADD: hiddenItemNames: useProjectStore.getState().hiddenItemNames
  NOTE: hiddenItemSet is NOT persisted (it is derived/in-memory only)

hydrateStores wrapper (project-serialize.ts lines 72-116):
  Bracketed by suspendDirtyTracking() / resumeDirtyTracking()
  Calls reset() then hydrate() on each store
  ADD: useProjectStore.setState({
         hiddenItemNames: Array.isArray(data.hiddenItemNames) ? data.hiddenItemNames : [],
         hiddenItemSet: new Set(Array.isArray(data.hiddenItemNames) ? data.hiddenItemNames : [])
       })
       — placed INSIDE the suspend/resume bracket so markDirty stays suppressed

validateV2 (project-schema.ts lines 87-113):
  Cast at line 113 (`return raw as ProjectFileV2`) — NOT strict; accepts the additive optional field silently. No change needed (Pitfall 11 documented).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema additive field + serialize/hydrate wiring</name>
  <files>src/renderer/src/lib/project-schema.ts, src/renderer/src/lib/project-serialize.ts</files>
  <read_first>
    - src/renderer/src/lib/project-schema.ts — read full file: ProjectFileV2 interface (lines 63-83), validateV2 (lines 87-113), migration helpers (lines 125-163)
    - src/renderer/src/lib/project-serialize.ts — read full file: SnapshotParams (lines 14-20), snapshotProject (lines 28-61), hydrateStores (lines 72-116)
    - .planning/phases/08-markup-workflow-and-wall-tool/08-RESEARCH.md §"Feature 3: Show/Hide Visibility" → "Schema Migration Policy (D-13)" — confirms NO formatVersion bump
    - .planning/phases/08-markup-workflow-and-wall-tool/08-RESEARCH.md §"Pitfall 11" — validateV2 cast tolerates the new field
    - src/tests/project-schema-hidden.test.ts — read the RED stubs from 08-00 to confirm exact behavior expected
  </read_first>
  <action>
    Edit two files together so serialize and hydrate land atomically.

    File 1 — src/renderer/src/lib/project-schema.ts:
    - In the ProjectFileV2 interface (lines 63-83), add a new optional field after the `pages` array property: `hiddenItemNames?: string[]` with a JSDoc comment: "Additive in Phase 8 — absent in pre-Phase 8 files; defaults to [] on load. Per-project visibility filter for canvas markup rendering (D-13)."
    - Do NOT bump formatVersion (still `2`).
    - Do NOT modify validateV2 (cast at line 113 accepts the additive field silently — Pitfall 11).
    - Do NOT modify migrateV1ToV2 (the v1→v2 migration produces a Phase 8-compatible v2 without hiddenItemNames; the hydrate path defaults to []).

    File 2 — src/renderer/src/lib/project-serialize.ts:
    - Add `import { useProjectStore } from '../stores/projectStore'` if not already present (it is — line 6).
    - In snapshotProject (after the existing pages array, inside the returned object on line 46-60): add a new property `hiddenItemNames: useProjectStore.getState().hiddenItemNames`. This always emits the array (empty or populated) — never undefined — so re-saves don't drop the field. Do NOT persist hiddenItemSet (it is a derived in-memory field).
    - In hydrateStores (inside the try block, after the three store hydrate() calls and BEFORE the finally block, so it runs while suspendDirtyTracking is active): add a single setState call that writes BOTH hiddenItemNames AND hiddenItemSet into projectStore atomically: `useProjectStore.setState({ hiddenItemNames: Array.isArray(data.hiddenItemNames) ? data.hiddenItemNames : [], hiddenItemSet: new Set(Array.isArray(data.hiddenItemNames) ? data.hiddenItemNames : []) })`. The Array.isArray guard tolerates pre-Phase 8 files where the field is absent (defaults to []).

    Do NOT touch existing serialize fields. Do NOT touch the v1 migration path. Do NOT touch validateV2.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - Source assertion: `grep -n "hiddenItemNames" src/renderer/src/lib/project-schema.ts` returns at least 1 line (the optional field on ProjectFileV2).
    - Source assertion: `grep -n "formatVersion: 2" src/renderer/src/lib/project-schema.ts` still present (no bump to 3).
    - Source assertion: `grep -n "hiddenItemNames" src/renderer/src/lib/project-serialize.ts` returns at least 2 lines (snapshotProject emit + hydrateStores set).
    - Source assertion: `grep -n "Array.isArray(data.hiddenItemNames)" src/renderer/src/lib/project-serialize.ts` returns at least 1 line (the defaulting guard in hydrateStores).
    - Source assertion: `grep -n "hiddenItemSet" src/renderer/src/lib/project-serialize.ts` returns at least 1 line (the hydrateStores setState that also sets the Set).
    - Source assertion: the new setState call appears INSIDE the try block (before the finally) in hydrateStores — verify with `grep -B 2 "hiddenItemNames" src/renderer/src/lib/project-serialize.ts | grep -c "useViewerStore.getState().hydrate"` confirms the new line sits AFTER the existing hydrate calls.
  </acceptance_criteria>
  <done>
    ProjectFileV2 gains optional hiddenItemNames field. snapshotProject always emits it from projectStore. hydrateStores writes hiddenItemNames AND derived hiddenItemSet back to projectStore with [] / new Set() defaults for absent/corrupted values. No formatVersion bump. TypeScript clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: projectStore.hiddenItemNames + hiddenItemSet + toggleHiddenItem + reset extension</name>
  <files>src/renderer/src/stores/projectStore.ts</files>
  <read_first>
    - src/renderer/src/stores/projectStore.ts — read full file (113 lines): ProjectStoreState interface (lines 6-19), markDirty pattern (lines 53-57), reset (lines 59-66), attachDirtyTracking (lines 86-112)
    - .planning/phases/08-markup-workflow-and-wall-tool/08-PATTERNS.md §"src/renderer/src/stores/projectStore.ts" — exact toggleHiddenItem implementation pattern with markDirty() call as final step (Pitfall 4)
    - .planning/phases/08-markup-workflow-and-wall-tool/08-RESEARCH.md §"Pitfall 4" — toggleHiddenItem MUST call markDirty() explicitly because attachDirtyTracking only watches markupStore/scaleStore/viewerStore (NOT projectStore itself)
    - src/tests/project-schema-hidden.test.ts — re-read to confirm test expectations
    - src/tests/totals-row-visibility.test.ts — confirm the toggle behavior the lightbulb will trigger in Wave 3
  </read_first>
  <action>
    Extend projectStore.ts with the hiddenItemNames slice, the derived hiddenItemSet, and the toggle action. All additive — no behavioral changes to existing actions.

    1. ProjectStoreState interface (lines 6-19): Add new properties after lastSavedAt:
       - `hiddenItemNames: string[]`
       - `hiddenItemSet: Set<string>`  // derived in-memory O(1) lookup; NOT persisted in .clmc
       - `toggleHiddenItem: (name: string) => void`
       - `setHiddenItemNames: (names: string[]) => void`  // used by hydrateStores in Task 1

    2. Initial state literal in create<ProjectStoreState>((set, get) => ({ ... })) (around line 37): Add:
       - `hiddenItemNames: []`
       - `hiddenItemSet: new Set<string>()`

    3. Implement toggleHiddenItem action — follow the exact pattern from PATTERNS.md but update BOTH fields atomically:
       - Compute next via checking s.hiddenItemNames.indexOf(name): if idx >= 0 filter the name out, else append.
       - Use a single set((s) => ({ hiddenItemNames: <next>, hiddenItemSet: new Set(<next>) })) updater that updates BOTH fields in the same set call.
       - After the set call, call get().markDirty() as the LAST line (Pitfall 4 — required because attachDirtyTracking does not subscribe to projectStore itself; the explicit markDirty call closes the dirty-tracking gap).
       - JSDoc: "Flip a name in/out of hiddenItemNames and syncs hiddenItemSet for O(1) renderer lookups. Marks the project dirty so Save persists the change. D-16: visibility toggle is NOT undoable — direct projectStore action, no MarkupCommand."

    4. Implement setHiddenItemNames action: single set call that updates BOTH: `set({ hiddenItemNames: names, hiddenItemSet: new Set(names) })`. No markDirty call (this is used by hydrateStores during load — dirty tracking is already suspended via suspendDirtyTracking).

    5. reset action (lines 59-66): Add `hiddenItemNames: []` and `hiddenItemSet: new Set()` to the reset literal so opening a second project in the same session clears stale visibility state (matches the existing "Clean slate first" pattern from project-serialize.ts:87).

    Do NOT touch attachDirtyTracking — hiddenItemNames is in projectStore itself, NOT in markupStore/scaleStore/viewerStore, so it does NOT belong in the subscribe-watched fields. The toggleHiddenItem's explicit markDirty() call IS the dirty-tracking integration.
  </action>
  <verify>
    <automated>npx vitest run src/tests/project-schema-hidden.test.ts --reporter=verbose 2>&1 | tail -30 && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/tests/project-schema-hidden.test.ts` exits 0 (all 3 tests GREEN: snapshot emits, hydrate writes, hydrate defaults to []).
    - `npx tsc --noEmit` exits 0.
    - Source assertion: `grep -n "hiddenItemNames: string\[\]" src/renderer/src/stores/projectStore.ts` returns at least 1 line (interface decl).
    - Source assertion: `grep -n "hiddenItemSet" src/renderer/src/stores/projectStore.ts` returns at least 4 matches (interface decl, initial state, toggleHiddenItem update, reset).
    - Source assertion: `grep -n "toggleHiddenItem" src/renderer/src/stores/projectStore.ts` returns at least 2 lines (interface + implementation).
    - Source assertion: `grep -n "setHiddenItemNames" src/renderer/src/stores/projectStore.ts` returns at least 2 lines.
    - Source assertion: `grep -A 8 "toggleHiddenItem:" src/renderer/src/stores/projectStore.ts | grep -c "get().markDirty()"` returns 1 (Pitfall 4 satisfied — markDirty is called as the last line of toggleHiddenItem).
    - Source assertion: `grep -A 8 "reset: () => set" src/renderer/src/stores/projectStore.ts | grep -c "hiddenItemNames: \[\]"` returns 1 (reset clears the field).
    - Source assertion: `grep -A 8 "reset: () => set" src/renderer/src/stores/projectStore.ts | grep -c "hiddenItemSet: new Set"` returns 1 (reset clears the Set).
    - Regression assertion: `npx vitest run src/tests/project-store.test.ts 2>&1 | tail -10` (if file exists) shows no regressions. Otherwise, the full project-serialize tests still pass: `npx vitest run src/tests/project-serialize.test.ts`.
  </acceptance_criteria>
  <done>
    projectStore exposes hiddenItemNames + hiddenItemSet + toggleHiddenItem + setHiddenItemNames. Toggle marks dirty and keeps both fields in sync. Reset clears both. project-schema-hidden.test.ts GREEN. No regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .clmc file → hydrate path | Untrusted disk content crosses here; validateV2 cast accepts any shape, deserializer's Array.isArray() guard rejects non-array hiddenItemNames |
| projectStore → snapshot path | In-process Zustand read; no boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | hiddenItemNames array bloated in crafted .clmc file (DoS through huge array) | accept | Array.isArray check on load; hiddenItemSet.has() in renderers is O(1); no allocation explosion possible because the Set is built once per project load from the names array |
| T-08-02-02 | Tampering | hiddenItemNames contains non-string entries (e.g., crafted file with [{}, null, 42]) | accept | Renderer hiddenItemSet.has(markup.name) where markup.name is always string — non-string entries in the Set simply never match and produce no visibility effect; documented behavior (Pitfall 11 from RESEARCH) |
| T-08-02-03 | Information Disclosure | hiddenItemNames in .clmc reveals which items the estimator chose to hide (could be commercially sensitive) | accept | hiddenItemNames is takeoff state per user decision D-13 — the user explicitly chose to persist it; .clmc files are already user-private project files containing all takeoff data |
</threat_model>

<verification>
- `npx vitest run src/tests/project-schema-hidden.test.ts` exits 0 (3 tests GREEN)
- `npx tsc --noEmit` exits 0
- A .clmc file round-trip preserves hiddenItemNames
- An old (pre-Phase 8) .clmc file loads with hiddenItemNames defaulting to [] and hiddenItemSet defaulting to new Set()
- toggleHiddenItem marks the project dirty and updates both hiddenItemNames and hiddenItemSet atomically
</verification>

<success_criteria>
ProjectFileV2 has additive hiddenItemNames field (no formatVersion bump). snapshotProject emits, hydrateStores writes back. projectStore has hiddenItemNames + hiddenItemSet + toggleHiddenItem + setHiddenItemNames. project-schema-hidden.test.ts GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/08-markup-workflow-and-wall-tool/08-02-SUMMARY.md`
</output>
