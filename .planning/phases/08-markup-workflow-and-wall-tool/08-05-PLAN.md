---
phase: 08-markup-workflow-and-wall-tool
plan: "05"
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03", "08-04"]
files_modified:
  - src/renderer/src/components/Toolbar.tsx
  - src/renderer/src/components/CanvasViewport.tsx
autonomous: true
requirements: []
tags: [toolbar, canvas, wall, chain-badge, crosshair]

must_haves:
  truths:
    - "Toolbar shows a fifth IconButton with BrickWall icon next to Count/Linear/Area/Perimeter"
    - "handleMarkupToolClick accepts 'wall' as an argument; re-clicking the active tool toggles to 'select' (existing chain-break path via cancelMarkup)"
    - "Each markup tool's IconButton renders an inline chain badge chip (colored dot + truncated name) when activeTool matches AND getChainArmedItem() returns non-null"
    - "CanvasViewport exports module-level getChainArmedItem() following the established _canvasControls/_calibrationControls pattern"
    - "CanvasViewport _chainArmedItem is populated via useEffect watching markupState.chainArmed + pendingName + pendingColor"
    - "CROSSHAIR_CURSOR module-scope constant computed once (SVG data-URL with URI-encoded SVG, hotspot '12 12')"
    - "Container div cursor becomes CROSSHAIR_CURSOR when isMarkupTool(activeTool) || calibMode !== 'idle'; preserves existing pan/grab logic otherwise"
    - "WallMarkup renderer is mounted in Layer 1b alongside LinearMarkup/AreaMarkup/PerimeterMarkup, filtered by m.type === 'wall'"
    - "MarkupNamePopup save-after wiring passes toolType and initialWallHeight from markupState"
    - "Linear-preview path (Layer 1a + dblclick finish) extended to cover toolType === 'wall' (walls draw as polylines with the same dblclick finish)"
  artifacts:
    - path: "src/renderer/src/components/Toolbar.tsx"
      provides: "Wall button + chain badge chips on all 5 markup tools"
      contains: "BrickWall"
    - path: "src/renderer/src/components/CanvasViewport.tsx"
      provides: "getChainArmedItem + CROSSHAIR_CURSOR + WallMarkup mount + popup wiring"
      contains: "getChainArmedItem"
  key_links:
    - from: "src/renderer/src/components/Toolbar.tsx"
      to: "src/renderer/src/components/CanvasViewport.tsx"
      via: "getChainArmedItem() imported module-level getter"
      pattern: "getChainArmedItem"
    - from: "src/renderer/src/components/CanvasViewport.tsx"
      to: "src/renderer/src/components/WallMarkup.tsx"
      via: "<WallMarkup ... /> mount in Layer 1b filtered by m.type === 'wall'"
      pattern: "WallMarkup"
    - from: "src/renderer/src/components/CanvasViewport.tsx"
      to: "src/renderer/src/components/MarkupNamePopup.tsx"
      via: "toolType + initialWallHeight props passed when markupState.toolType === 'wall'"
      pattern: "initialWallHeight"
---

<objective>
Wave 3A: integrate the wall tool into the toolbar and the canvas viewport. Wire the chain badge cross-component channel via a new module-level ref. Apply the crosshair cursor over the canvas when a markup or scale tool is active.

Purpose: This plan closes the loop on three features at once because they all touch the toolbar and the canvas viewport — splitting them across plans would create file-ownership conflicts in Wave 3. After this plan, the user can click the Wall button, draw a wall polyline, see the chain badge update as they place successive walls, and see the crosshair cursor over the canvas.

Output: Toolbar.tsx (BrickWall button + chain chip on all 5 markup buttons + handleMarkupToolClick extended). CanvasViewport.tsx (getChainArmedItem module-ref + CROSSHAIR_CURSOR constant + WallMarkup mount + popup toolType/initialWallHeight props + linear-preview path extended to 'wall').
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-markup-workflow-and-wall-tool/08-CONTEXT.md
@.planning/phases/08-markup-workflow-and-wall-tool/08-RESEARCH.md
@.planning/phases/08-markup-workflow-and-wall-tool/08-PATTERNS.md
@.planning/phases/08-markup-workflow-and-wall-tool/08-01-PLAN.md
@.planning/phases/08-markup-workflow-and-wall-tool/08-04-PLAN.md

<interfaces>
Toolbar.tsx existing markup tools section (lines 357-392):
  Four IconButtons: Count (MapPin), Linear (Minus), Area (Square), Perimeter (Hexagon)
  Each uses handleMarkupToolClick(tool) which toggles tool↔'select'.

Toolbar.tsx handleMarkupToolClick (line 189):
  if (activeTool === tool) setActiveTool('select') else setActiveTool(tool)
  Extend signature to add 'wall' — no body change (existing toggle handles chain-break via CanvasViewport effect calling cancelMarkup).

Toolbar.tsx IconButton children pattern (Set Scale chevron, lines 334-351):
  <IconButton ...>
    {pageScale !== null && <span ...>{'▾'}</span>}
  </IconButton>
  Chain chip uses the same children mechanism.

CanvasViewport.tsx existing module-level refs (lines 43-61):
  let _canvasControls: { zoomIn, zoomOut, fitToWindow } | null = null
  export function getCanvasControls() { return _canvasControls }
  let _calibrationControls: { activate, activateVerify, cancel } | null = null
  export function getCalibrationControls() { return _calibrationControls }

  ADD:
  let _chainArmedItem: { name: string; color: string } | null = null
  export function getChainArmedItem() { return _chainArmedItem }

CanvasViewport.tsx existing activeTool→tool effect (lines 193-199):
  if (isMarkupTool(activeTool) && markupState.toolType !== activeTool) activateMarkup(activeTool)
  else if (!isMarkupTool(activeTool) && markupState.mode !== 'idle') cancelMarkup()
  NO CHANGE — isMarkupTool already includes 'wall' (from 08-00); activateMarkup accepts 'wall' (from 08-01)

CanvasViewport.tsx existing linear preview path (around lines 616, 453):
  Line 616:  {markupState.toolType === 'linear' && markupState.mode === 'drawing' && ...}
  Line 453:  if (markupState.toolType === 'linear' && markupState.mode === 'drawing') finishLinear()
  EXTEND both to: (markupState.toolType === 'linear' || markupState.toolType === 'wall')

CanvasViewport.tsx existing Layer 1b markup map (lines 663-734):
  pageMarkups.filter((m) => m.type === 'linear').map((m) => <LinearMarkup ... />)
  pageMarkups.filter((m) => m.type === 'area').map((m) => <AreaMarkup ... />)
  pageMarkups.filter((m) => m.type === 'perimeter').map((m) => <PerimeterMarkup ... />)
  ADD: pageMarkups.filter((m) => m.type === 'wall').map((m) => <WallMarkup ... />)

CanvasViewport.tsx existing MarkupNamePopup save-after wiring (lines 861-872):
  <MarkupNamePopup mode="save-after" screenPos={...} containerSize={...}
    onConfirm={commitShape} onCancel={() => { cancelMarkup(); setActiveTool('select') }} />
  EXTEND with: toolType={markupState.toolType ?? undefined}
              initialWallHeight={markupState.toolType === 'wall' ? markupState.pendingWallHeight : undefined}

getCursor() (lines 464-478):
  Returns string ('grab', 'crosshair', 'pointer', 'default')
  Replace the four `return 'crosshair'` calls with `return CROSSHAIR_CURSOR` where CROSSHAIR_CURSOR is the module-scope SVG data-URL string.

CROSSHAIR_CURSOR construction (module-scope, computed ONCE):
  SVG: 24×24 viewport, 8 lines total (4 black outline at width 3, 4 white foreground at width 1.5)
  Geometry: arms from (0,12)→(10,12) and (14,12)→(24,12) horizontally; (12,0)→(12,10) and (12,14)→(12,24) vertically — leaves a 4px gap at center (12,12) per D-17
  Encoded: `data:image/svg+xml,${encodeURIComponent(svg)}` (URI-encoded — Pitfall 8 says NOT raw double quotes inside double-quoted url())
  Final: `url("data:image/svg+xml,...") 12 12, crosshair`  // hotspot at intersection; fallback to standard crosshair if data-URL fails
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Toolbar wall button + handleMarkupToolClick + chain badge chips on all 5 markup tools</name>
  <files>src/renderer/src/components/Toolbar.tsx</files>
  <read_first>
    - src/renderer/src/components/Toolbar.tsx — read full file: existing imports (lucide-react icons), IconButton component (lines 28-95), handleMarkupToolClick (line 189), Set Scale chevron pattern (lines 334-351), markup tools section (lines 357-392)
    - .planning/phases/08-markup-workflow-and-wall-tool/08-PATTERNS.md §"src/renderer/src/components/Toolbar.tsx" — exact patterns for chain badge chip + fifth IconButton placement
    - .planning/phases/08-markup-workflow-and-wall-tool/08-RESEARCH.md §"Wall Toolbar Icon (D-11)" — confirms BrickWall is the chosen icon (verified installed in lucide-react 1.6.0)
    - .planning/phases/08-markup-workflow-and-wall-tool/08-RESEARCH.md §"Chain badge placement" — chip lives inside the active tool's IconButton via the children prop (mirrors Set Scale chevron)
    - src/renderer/src/lib/constants.ts (or wherever COLORS is defined) — confirm COLORS.activeSurface or equivalent token for the chip background
  </read_first>
  <action>
    Three edits to Toolbar.tsx:

    1. Imports: Add BrickWall to the lucide-react import. Add `import { getChainArmedItem } from './CanvasViewport'` (the module-level getter exported in Task 2; Task 2 must land in the same wave for the type to resolve — even though they are written in parallel, Wave 3 dependency means both files MUST be saved before compile).

    2. handleMarkupToolClick (line 189): Extend the type signature to include 'wall': `(tool: 'count' | 'linear' | 'area' | 'perimeter' | 'wall'): void`. NO body change — existing toggle to 'select' triggers cancelMarkup() via the CanvasViewport effect, which resets chainArmed: false via INITIAL_STATE (chain-break path).

    3. Markup tools section (lines 357-392):

       3a. Insert a fifth IconButton AFTER the Perimeter button (which uses Hexagon icon). The new button:
       - icon={BrickWall}
       - label="Wall"
       - active={activeTool === 'wall'}
       - disabled={setScaleDisabled}  (same uncalibrated guard as the other markup tools)
       - onClick={() => handleMarkupToolClick('wall')}
       - title="Wall tool — measure wall area (length × height) in m²"

       3b. Add the chain badge chip as a child inside EACH of the 5 markup IconButtons (Count, Linear, Area, Perimeter, Wall). The chip renders ONLY when this tool is the active tool AND getChainArmedItem() returns non-null. Pattern (per PATTERNS.md §Set Scale chevron, line 343):

       Inside each IconButton, add `children` block:
       ```
       {activeTool === '<thisTool>' && getChainArmedItem() !== null && (
         <span style={{
           display: 'inline-flex', alignItems: 'center', gap: 3,
           marginLeft: 6, fontSize: 11, fontWeight: 600,
           background: COLORS.activeSurface, borderRadius: 3,
           padding: '1px 4px', maxWidth: 80, overflow: 'hidden',
           textOverflow: 'ellipsis', whiteSpace: 'nowrap'
         }}>
           <span style={{ width: 8, height: 8, borderRadius: '50%',
             background: getChainArmedItem()!.color, flexShrink: 0 }} />
           {getChainArmedItem()!.name}
         </span>
       )}
       ```

       Use the COLORS token that matches the existing toolbar accent style — read constants.ts to pick the right one (COLORS.activeSurface, COLORS.surfaceMuted, or similar; if no exact match exists, use a neutral light background like '#f0f0f0' inline). The chip MUST remain readable against both the active-underline accent and the default IconButton background.

       To avoid five copy-paste blocks, optionally extract a small `<ChainBadgeChip />` local component (top of file) that takes no props and renders the conditional chip — each IconButton then includes `{activeTool === 'count' && <ChainBadgeChip />}` etc. This keeps the diff readable. Either approach satisfies the acceptance criteria.

    Do NOT change the existing button order (Count → Linear → Area → Perimeter → Wall). Do NOT modify the Set Scale chevron block. Do NOT use Tailwind — this file uses inline styles + COLORS tokens (CLAUDE.md cross-cutting constraint).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - Source assertion: `grep -n "import.*BrickWall" src/renderer/src/components/Toolbar.tsx` returns one match.
    - Source assertion: `grep -n "getChainArmedItem" src/renderer/src/components/Toolbar.tsx` returns at least 2 matches (import + usage; at least 5 if inlined per-button).
    - Source assertion: `grep -n "handleMarkupToolClick('wall')" src/renderer/src/components/Toolbar.tsx` returns one match (the new button's onClick).
    - Source assertion: `grep -n "'count' | 'linear' | 'area' | 'perimeter' | 'wall'" src/renderer/src/components/Toolbar.tsx` returns at least 1 match (handleMarkupToolClick signature).
    - Source assertion: `grep -c "icon={BrickWall}" src/renderer/src/components/Toolbar.tsx` returns 1.
    - Source assertion: `grep -n "Wall tool — measure wall area" src/renderer/src/components/Toolbar.tsx` returns one match (the title attribute).
    - Source assertion: `grep -n "activeTool === 'wall'" src/renderer/src/components/Toolbar.tsx` returns at least 1 match (active prop or chip conditional).
    - Behavior assertion: a Vitest render test of Toolbar with totalPages>0 finds 5 markup IconButtons (Count, Linear, Area, Perimeter, Wall) — covered by existing Phase 7 toolbar tests being non-regressive; no new test required.
  </acceptance_criteria>
  <done>
    Toolbar.tsx has 5 markup buttons. handleMarkupToolClick accepts 'wall'. Each markup IconButton conditionally renders the chain badge chip when active AND chain is armed. TypeScript clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CanvasViewport — getChainArmedItem ref, CROSSHAIR_CURSOR, WallMarkup mount, popup wiring, linear-preview extension</name>
  <files>src/renderer/src/components/CanvasViewport.tsx</files>
  <read_first>
    - src/renderer/src/components/CanvasViewport.tsx — read in chunks: imports + module-level refs (lines 1-65), activeTool→markup tool effect (lines 193-199), linear/preview drawing path (lines 440-480 for the dblclick/finishLinear handler near line 453), getCursor (lines 464-478), container div with cursor (lines 539-552), Layer 1a in-progress preview (lines 616-659), Layer 1b committed markup map (lines 663-734), MarkupNamePopup save-after wiring (lines 861-872)
    - .planning/phases/08-markup-workflow-and-wall-tool/08-PATTERNS.md §"src/renderer/src/components/CanvasViewport.tsx" — exact extension patterns for getChainArmedItem ref, CROSSHAIR_CURSOR construction, Layer 1b wall map, popup props extension
    - .planning/phases/08-markup-workflow-and-wall-tool/08-RESEARCH.md §"Feature 4: Crosshair Cursor" — CSS data-URL constraints, SVG geometry, Pitfall 8 (URI encoding)
    - .planning/phases/08-markup-workflow-and-wall-tool/08-RESEARCH.md §"Pitfall 1" — isMarkupTool MUST include 'wall' (already done in 08-00)
    - src/renderer/src/components/WallMarkup.tsx — confirm the component exists (from 08-04 Task 1) and its prop shape (markup, category, currentZoom, pageScale, onHoverEnter, onHoverLeave, onContextMenu)
    - src/renderer/src/types/markup.ts — confirm WallMarkup type import path
  </read_first>
  <action>
    Five additive edits to CanvasViewport.tsx. All must land in one task because they share the file and several share the same import block.

    1. Imports: Add `import { WallMarkup } from './WallMarkup'` (WallMarkup.tsx from 08-04). Add type import for WallMarkup type: `import type { WallMarkup as WallMarkupType } from '../types/markup'` (alias to avoid collision with the component import).

    2. Module-level _chainArmedItem ref (insert after _calibrationControls around line 61):
       ```
       let _chainArmedItem: { name: string; color: string } | null = null
       export function getChainArmedItem(): { name: string; color: string } | null { return _chainArmedItem }
       ```
       Mirrors the _canvasControls / _calibrationControls pattern verbatim (PATTERNS.md §"Module-Level Ref Cross-Component Channel").

    3. CROSSHAIR_CURSOR module-scope constant (insert after the module-level refs, before the component function declaration):
       Build the SVG string in TypeScript using string concatenation (per Pitfall 8 — use single quotes inside the SVG so the outer double-quoted url() value doesn't conflict). Build 8 line elements: 4 black outline at stroke-width 3, then 4 white foreground at stroke-width 1.5. Geometry per D-17: horizontal arms (0,12)→(10,12) and (14,12)→(24,12); vertical arms (12,0)→(12,10) and (12,14)→(12,24). Wrap in `<svg xmlns='http://www.w3.org/2000/svg' width='24' height='24'>...</svg>`. Build the final cursor value using IIFE: `const CROSSHAIR_CURSOR: string = (() => { const svg = ...; return \`url("data:image/svg+xml,\${encodeURIComponent(svg)}") 12 12, crosshair\`; })();`
       The encodeURIComponent call is mandatory (Pitfall 8) — it transforms the embedded single quotes, < > characters, etc., into valid URL-encoded form.
       The hotspot coordinates `12 12` place the cursor hot spot at the geometric center of the 24×24 SVG (the cross intersection point). This is the standard Chromium CSS cursor hotspot syntax: `url("...") <x> <y>, <fallback>`.

    4. Populate _chainArmedItem via useEffect inside the component (place near the existing effects, e.g., after the activeTool→markup tool effect):
       ```
       useEffect(() => {
         _chainArmedItem = (markupState.chainArmed && markupState.pendingName)
           ? { name: markupState.pendingName, color: markupState.pendingColor }
           : null
       }, [markupState.chainArmed, markupState.pendingName, markupState.pendingColor])
       ```
       (Mirror the existing useEffect pattern that populates _calibrationControls.)

    5. getCursor extension: Replace EVERY `return 'crosshair'` in the existing getCursor function (lines 464-478) with `return CROSSHAIR_CURSOR`. There are typically 2-3 such returns (calibMode !== 'idle', markupState.mode === 'drawing', count tool placing). Container div cursor style (line 552-ish) reads `cursor: getCursor()` — no change needed; it picks up the new return value automatically.

    6. WallMarkup mount in Layer 1b — insert after the perimeter map block (around line 720-734):
       ```
       {pageMarkups.filter((m) => m.type === 'wall').map((m) => {
         const category = getCategory(m.categoryId)
         if (!category) return null
         return <WallMarkup key={m.id} markup={m as WallMarkupType} category={category}
           currentZoom={currentZoom} pageScale={pageScale}
           onHoverEnter={handleHoverEnter} onHoverLeave={handleHoverLeave}
           onContextMenu={handleContextMenu} />
       })}
       ```
       Use the EXACT same prop destructure as the LinearMarkup mount block (which is a few lines above) — same handlers, same currentZoom, same pageScale.

    7. Extend the linear-preview path to cover wall:
       - Line ~453 (dblclick finishLinear gate): change `if (markupState.toolType === 'linear' && markupState.mode === 'drawing')` to `if ((markupState.toolType === 'linear' || markupState.toolType === 'wall') && markupState.mode === 'drawing')`. Wall uses the SAME finish gesture as linear (dblclick to commit polyline) because both are open polylines.
       - Line ~616 (Layer 1a in-progress preview): change the equivalent `markupState.toolType === 'linear'` check to include `|| markupState.toolType === 'wall'`. Wall preview reuses the linear preview rendering — no new component needed because both are open polylines drawn with the same temporary <Line>.

    8. MarkupNamePopup save-after wiring (lines 861-872): Pass two new props:
       - `toolType={markupState.toolType ?? undefined}` — tells the popup which tool variant to render (wall conditional row, etc.)
       - `initialWallHeight={markupState.toolType === 'wall' ? markupState.pendingWallHeight : undefined}` — supplies the chain-inherited or default wall height to the popup

       The `commitShape` callback already accepts the wallHeight payload (extended in 08-01 Task 1) — no change to the onConfirm wiring beyond the prop additions.

    Constraints:
    - Pitfall 1: isMarkupTool already includes 'wall' from 08-00; the activeTool effect (lines 193-199) needs NO change.
    - Pitfall 8: CROSSHAIR_CURSOR construction uses encodeURIComponent on a single-quote-using SVG string.
    - Module-scope CROSSHAIR_CURSOR computed ONCE — never inline the SVG inside the component (would compute on every render).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10 && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run` exits 0 with no pre-existing CanvasViewport tests regressing (any pre-existing test failures must be the ones explicitly created in 08-00 as RED — those that are now GREEN: chain-mode, project-schema-hidden, wall-math, boq-aggregator-wall).
    - Source assertion: `grep -n "import { WallMarkup }" src/renderer/src/components/CanvasViewport.tsx` returns one line.
    - Source assertion: `grep -n "let _chainArmedItem" src/renderer/src/components/CanvasViewport.tsx` returns one line.
    - Source assertion: `grep -n "export function getChainArmedItem" src/renderer/src/components/CanvasViewport.tsx` returns one line.
    - Source assertion: `grep -n "CROSSHAIR_CURSOR" src/renderer/src/components/CanvasViewport.tsx` returns at least 3 matches (declaration + at least 2 usages in getCursor).
    - Source assertion: `grep -n "encodeURIComponent" src/renderer/src/components/CanvasViewport.tsx` returns at least 1 match (Pitfall 8 satisfied).
    - Source assertion: `grep -n 'data:image/svg+xml' src/renderer/src/components/CanvasViewport.tsx` returns at least 1 match.
    - Source assertion: `grep -c "return 'crosshair'" src/renderer/src/components/CanvasViewport.tsx | head -1` returns 0 (all string-literal returns replaced with CROSSHAIR_CURSOR).
    - Source assertion: `grep -n "m.type === 'wall'" src/renderer/src/components/CanvasViewport.tsx` returns at least 1 match (Layer 1b filter).
    - Source assertion: `grep -n "<WallMarkup" src/renderer/src/components/CanvasViewport.tsx` returns one match (the JSX mount).
    - Source assertion: `grep -n "markupState.toolType === 'wall'" src/renderer/src/components/CanvasViewport.tsx` returns at least 2 matches (popup props guard + linear-preview extension).
    - Source assertion: `grep -n "initialWallHeight" src/renderer/src/components/CanvasViewport.tsx` returns one match (popup prop passthrough).
    - Source assertion: `grep -n "markupState.toolType === 'linear' || markupState.toolType === 'wall'" src/renderer/src/components/CanvasViewport.tsx` returns at least 2 matches (dblclick gate + Layer 1a preview gate).
    - Source assertion: `grep -n 'url("data:image/svg' src/renderer/src/components/CanvasViewport.tsx | grep -c '"12 12"'` returns 1 (hotspot coordinates explicitly set to 12 12 — center of 24×24 SVG).
    - Behavior assertion: chain badge chip appears in Toolbar when a markup is placed with chain mode active — verified manually in Wave 4 UAT (Scenario 2). Crosshair hotspot alignment — verified manually in Wave 4 UAT (Scenario 9).
  </acceptance_criteria>
  <done>
    CanvasViewport.tsx has: getChainArmedItem ref + populator effect, CROSSHAIR_CURSOR constant with hotspot 12 12 + getCursor swap, WallMarkup Layer 1b mount, popup toolType/initialWallHeight props wired, linear-preview path covers wall. TypeScript clean. All Wave 0-2 tests still GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markupState (renderer hook) → _chainArmedItem ref | In-process React effect; no external boundary |
| activeTool string → CROSSHAIR_CURSOR cursor | Single-process CSS; data-URL is hard-coded at module scope (no user input) |
| WallMarkup render → Konva Stage | All wall geometry comes from markupStore (validated upstream); no Konva injection vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Tampering | CROSSHAIR_CURSOR SVG data-URL contains unescaped quotes (Pitfall 8) → URL rejected by browser, cursor reverts to fallback | mitigate | encodeURIComponent() applied to the SVG string; single quotes inside SVG (not double); fallback `, crosshair` keyword in the CSS value ensures a usable cursor even if the data-URL fails |
| T-08-05-02 | DoS | Module-scope CROSSHAIR_CURSOR string is enormous (~500 bytes) — but computed once and re-used; no per-frame cost | accept | Module-scope IIFE runs once at file load; React renders only re-read the constant string |
| T-08-05-03 | Information Disclosure | Chain badge chip displays armed item name in toolbar (visible to anyone with screen access) | accept | The user explicitly typed the item name to arm the chain; visibility in toolbar matches intent (D-03 visual indicator) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run` exits 0 — all Wave 0-3 tests GREEN, no regressions
- Manual smoke test (Wave 4 UAT will exhaustively cover):
  - Click Wall button → toolbar shows BrickWall icon highlighted; cursor over canvas becomes rifle-scope crosshair
  - Place a wall polyline (click-click-dblclick) → popup appears with wall-height row
  - Confirm → markup appears as polyline with parallel hairline + m² label
  - Place a second wall → chain badge chip appears in Wall IconButton; no re-prompt for name
  - Press Esc → chain badge disappears; next wall re-prompts for name + height
</verification>

<success_criteria>
Toolbar has 5 markup buttons + chain badge chips on all. CanvasViewport exports getChainArmedItem(), applies CROSSHAIR_CURSOR over canvas (with hotspot 12 12), mounts WallMarkup in Layer 1b, passes wall popup props, extends linear-preview gates to cover wall. All Wave 0-3 tests GREEN. TypeScript clean.
</success_criteria>

<output>
After completion, create `.planning/phases/08-markup-workflow-and-wall-tool/08-05-SUMMARY.md`
</output>
