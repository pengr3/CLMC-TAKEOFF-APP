---
phase: 08-markup-workflow-and-wall-tool
plan: "01"
type: execute
wave: 1
depends_on: ["08-00"]
files_modified:
  - src/renderer/src/hooks/useMarkupTool.ts
autonomous: true
requirements: []
tags: [chain-mode, hook, state-machine]

must_haves:
  truths:
    - "MarkupDrawState.chainArmed defaults to false and becomes true after the first commitShape/commitCountName"
    - "MarkupDrawState.pendingWallHeight defaults to 2400 and inherits across chain commits"
    - "commitShape/commitCountName preserve pendingName/pendingCategoryName/pendingColor/toolType when chainArmed is true; reset to drawing mode (not idle)"
    - "cancel() resets chainArmed to false via INITIAL_STATE"
    - "Wall branch in commitShape constructs WallMarkup with payload.wallHeight ?? prev.pendingWallHeight"
    - "src/tests/chain-mode.test.ts GREEN"
  artifacts:
    - path: "src/renderer/src/hooks/useMarkupTool.ts"
      provides: "Generalized chain markup state machine + wall tool support"
      contains: "chainArmed"
  key_links:
    - from: "src/renderer/src/hooks/useMarkupTool.ts"
      to: "src/renderer/src/types/markup.ts"
      via: "WallMarkup interface for wall branch in commitShape"
      pattern: "WallMarkup"
    - from: "src/renderer/src/hooks/useMarkupTool.ts"
      to: "src/renderer/src/stores/markupStore.ts"
      via: "store.placeMarkup dispatch for wall (mirrors linear/area/perimeter branches)"
      pattern: "placeMarkup"
---

<objective>
Wave 1A: refactor useMarkupTool to support chain markup mode (D-01, D-02, D-04, D-05) and wall tool placement.

Purpose: Generalize the count-tool "stay armed" pattern (lines 106-153) to all five markup tools. After commitShape/commitCountName, when chain is armed, preserve name/category/color/toolType so the user can place successive markups of the same item without re-prompting. Wall is wired here so commitShape's discriminated-tool dispatch produces WallMarkup objects.

Output: useMarkupTool.ts extended in place with chainArmed boolean, pendingWallHeight number, chain-aware post-commit reset, and a wall branch in commitShape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-markup-workflow-and-wall-tool/08-CONTEXT.md
@.planning/phases/08-markup-workflow-and-wall-tool/08-RESEARCH.md
@.planning/phases/08-markup-workflow-and-wall-tool/08-PATTERNS.md
@.planning/phases/08-markup-workflow-and-wall-tool/08-00-PLAN.md

<interfaces>
<!-- The exact MarkupDrawState extension form. Source: 08-PATTERNS.md §useMarkupTool. -->

Current MarkupDrawState (useMarkupTool.ts lines 13-24):
  mode: 'idle' | 'naming' | 'drawing' | 'confirming' | 'placing'
  toolType: 'count' | 'linear' | 'area' | 'perimeter' | null
  points, previewPoint, popupScreenPos, pendingName, pendingCategoryName,
  pendingColor, pendingPage, errorToast

Target additive fields:
  toolType: 'count' | 'linear' | 'area' | 'perimeter' | 'wall' | null
  chainArmed: boolean          // false in INITIAL_STATE; true after first commit
  pendingWallHeight: number    // 2400 in INITIAL_STATE; carried across chain commits

Current commitShape signature:
  commitShape: (payload: { name: string; categoryName: string; color: string }) => void

Target signature:
  commitShape: (payload: { name: string; categoryName: string; color: string; wallHeight?: number }) => void

Current activate signature:
  activate: (tool: 'count' | 'linear' | 'area' | 'perimeter') => void

Target signature:
  activate: (tool: 'count' | 'linear' | 'area' | 'perimeter' | 'wall') => void

stateRef snapshot pattern (Pitfall 3 — must follow exactly):
  Inside commitShape, read `const prev = stateRef.current` BEFORE any setState updater.
  Dispatch store.placeMarkup(m) OUTSIDE of setState (not inside the updater function).
  Call setState(...) AFTER the store dispatch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend MarkupDrawState + INITIAL_STATE + activate signature</name>
  <files>src/renderer/src/hooks/useMarkupTool.ts</files>
  <read_first>
    - src/renderer/src/hooks/useMarkupTool.ts — read full file (entire ~330 lines): MarkupDrawState interface (lines 13-24), INITIAL_STATE (lines 26-37), UseMarkupToolReturn interface (lines 55-72), activate (~line 75), cancel (~line 102), commitCountName (lines 106-153), commitShape (lines 270-325)
    - .planning/phases/08-markup-workflow-and-wall-tool/08-PATTERNS.md §useMarkupTool — exact extension forms with line numbers
    - src/tests/chain-mode.test.ts — read the RED stubs from 08-00 to confirm what fields/behavior the tests expect
  </read_first>
  <action>
    Apply additive extensions to MarkupDrawState (line 13), INITIAL_STATE (line 26), and the UseMarkupToolReturn interface (line 55). DO NOT modify behavior in this task — Task 2 handles the chain-aware commit logic.

    1. MarkupDrawState (line 13-24): Extend the toolType union to add `| 'wall'`. Add two new fields after errorToast: `chainArmed: boolean` and `pendingWallHeight: number` (with JSDoc comments noting "true after first commitShape; reset to false by cancel()" and "millimetres; inherited across chain wall commits; default 2400").

    2. INITIAL_STATE (line 26-37): Add `chainArmed: false` and `pendingWallHeight: 2400` to the literal. The 2400 value is the canonical default from D-08 (Pitfall 7 — INITIAL_STATE must always re-establish this).

    3. UseMarkupToolReturn interface (line 55-72): Extend the activate parameter type to add `| 'wall'`. Extend the commitShape parameter type to add `wallHeight?: number` as an optional property.

    4. Inside activate implementation: extend the runtime check / pendingColor seed switch to handle 'wall' (mirrors the linear/area/perimeter branches — wall has no special activate-time logic beyond setting toolType='wall' and entering 'naming' or 'drawing' mode following the existing pattern). Concretely: any place that currently switches on toolType to choose initial mode must accept 'wall' and follow the same path as linear (linear and wall are both polyline tools).

    Do NOT touch commitShape or commitCountName logic in this task. Task 2 owns those.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - Source assertion: `grep -n "chainArmed:" src/renderer/src/hooks/useMarkupTool.ts` returns at least 2 lines (interface decl + INITIAL_STATE).
    - Source assertion: `grep -n "pendingWallHeight:" src/renderer/src/hooks/useMarkupTool.ts` returns at least 2 lines.
    - Source assertion: `grep -n "INITIAL_STATE" src/renderer/src/hooks/useMarkupTool.ts | head -1` precedes a block where `grep -A 20 "INITIAL_STATE: MarkupDrawState"` shows both `chainArmed: false` and `pendingWallHeight: 2400`.
    - Source assertion: `grep -n "'wall'" src/renderer/src/hooks/useMarkupTool.ts` returns at least 2 hits (toolType union member + activate signature member).
  </acceptance_criteria>
  <done>
    MarkupDrawState has chainArmed + pendingWallHeight + 'wall' in toolType. INITIAL_STATE seeds the correct defaults. UseMarkupToolReturn surface accepts 'wall' in activate and wallHeight? in commitShape. `npx tsc --noEmit` exits 0. The chain-mode.test.ts test "INITIAL_STATE has chainArmed: false and pendingWallHeight: 2400" passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chain-aware post-commit reset + wall branch in commitShape + commitCountName</name>
  <files>src/renderer/src/hooks/useMarkupTool.ts</files>
  <read_first>
    - src/renderer/src/hooks/useMarkupTool.ts — re-read commitShape (lines 270-325) and commitCountName (lines 106-153) with the changes from Task 1 in mind
    - .planning/phases/08-markup-workflow-and-wall-tool/08-PATTERNS.md §useMarkupTool — the "Chain mode prototype — count 'stay armed' pattern" block and the "Wall branch inside commitShape" block — these are the exact target patterns
    - .planning/phases/08-markup-workflow-and-wall-tool/08-RESEARCH.md §"Pitfall 3" — StrictMode double-firing prevention: dispatch store.placeMarkup OUTSIDE setState updaters; read `const prev = stateRef.current` BEFORE
    - .planning/phases/08-markup-workflow-and-wall-tool/08-RESEARCH.md §"Pitfall 7" — INITIAL_STATE must carry pendingWallHeight: 2400 (already done in Task 1)
    - src/renderer/src/types/markup.ts — confirm WallMarkup interface from 08-00 is importable
    - src/tests/chain-mode.test.ts — the test "first commitShape sets chainArmed to true" and "cancel() resets chainArmed to false" must turn GREEN after this task
  </read_first>
  <action>
    Modify commitShape (around line 270-325) and commitCountName (around line 106-153) to implement chain-aware behavior. Add the wall branch to commitShape's tool-type dispatch.

    1. Wall branch in commitShape (after the perimeter branch around line 314): Add an `else if (prev.toolType === 'wall')` branch that constructs a WallMarkup object using `prev.points` and `payload.wallHeight ?? prev.pendingWallHeight`. Use the same id/page/createdAt/categoryId derivation as the linear/area/perimeter branches. Dispatch via `store.placeMarkup(m)` outside any setState updater (Pitfall 3). Import the WallMarkup type from '../types/markup' if not already imported.

    2. Replace the post-commit reset in commitShape (currently `setState(INITIAL_STATE)` at ~line 324) with a chain-aware reset. Read prev from stateRef.current (already done at the top of commitShape — re-use that snapshot). The new reset logic:
       - First-commit case: if prev.chainArmed was false, set chainArmed to true on the next state. Either way (first or subsequent), preserve pendingName/pendingCategoryName/pendingColor/toolType and reset mode to 'drawing' (linear/area/perimeter/wall) — same as the prototype count "stay armed" pattern but adapted for shape tools (which start in 'drawing' mode, not 'placing').
       - Preserve pendingWallHeight: `payload.wallHeight ?? prev.pendingWallHeight` (so wall tool carries the height forward; non-wall payload leaves it at 2400 or previous).
       - Reset transient geometry only: points: [], previewPoint: null, popupScreenPos: null, pendingPage: null, errorToast: null.
       - The reset target shape: `{ ...INITIAL_STATE, toolType: prev.toolType, mode: 'drawing', pendingName: payload.name, pendingCategoryName: payload.categoryName, pendingColor: payload.color, pendingWallHeight: payload.wallHeight ?? prev.pendingWallHeight, chainArmed: true }`.

    3. commitCountName (around line 106-153): The count tool already implements "stay armed" via mode='placing'. Add chainArmed=true to its post-commit state (the existing setState updater that sets mode: 'placing'). Preserve pendingWallHeight via spread of prev (it stays at 2400 — wall and count tools don't share height state but the field must not be dropped).

    4. cancel() (around line 102): No body change required — it already calls `setState(INITIAL_STATE)` which now includes `chainArmed: false` and `pendingWallHeight: 2400` from Task 1. Add a comment line noting "INITIAL_STATE resets chainArmed and pendingWallHeight per Pitfall 7".

    Constraints (re-asserting from research):
    - Pitfall 3: Read `const prev = stateRef.current` once at the top of commitShape; do NOT call store.placeMarkup inside a setState updater.
    - Pitfall 7: All chain resets must derive from INITIAL_STATE as the base; never partial-build a state object that omits chainArmed or pendingWallHeight.
    - The wall branch in commitShape mirrors the linear branch verbatim except for the markup-object shape (which gains wallHeight).
  </action>
  <verify>
    <automated>npx vitest run src/tests/chain-mode.test.ts --reporter=verbose 2>&1 | tail -30 && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/tests/chain-mode.test.ts` exits 0 (all 3 tests pass — INITIAL_STATE defaults, first commit arms chain, cancel resets).
    - `npx tsc --noEmit` exits 0.
    - Source assertion: `grep -n "prev.toolType === 'wall'" src/renderer/src/hooks/useMarkupTool.ts` returns one match inside commitShape.
    - Source assertion: `grep -n "chainArmed: true" src/renderer/src/hooks/useMarkupTool.ts` returns at least 2 matches (one in commitShape reset, one in commitCountName).
    - Source assertion: `grep -n "stateRef.current" src/renderer/src/hooks/useMarkupTool.ts` shows usage at the top of commitShape (Pitfall 3 invariant intact).
    - Behavior assertion: a wall-branch dispatch produces a WallMarkup with the payload.wallHeight value when provided, else with prev.pendingWallHeight (verified by the chain-mode test that walks `pendingWallHeight` through a chain).
    - Regression assertion: `npx vitest run src/tests/markup-tool-pop-last-point.test.ts src/tests/markup-tool-strictmode.test.ts` both exit 0 (no StrictMode regression).
  </acceptance_criteria>
  <done>
    chain-mode.test.ts GREEN. Wall placement path produces WallMarkup objects via store.placeMarkup. Chain state survives commitShape; cancel() resets it. StrictMode-safety pattern preserved. No regressions in existing markup-tool tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| useMarkupTool hook → markupStore | Hook dispatches placeMarkup via store getter; no IPC; single process |
| commitShape payload → WallMarkup wallHeight field | payload.wallHeight is `number` (validated upstream in MarkupNamePopup); no string→number coercion needed here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | commitShape wall branch reads payload.wallHeight | mitigate | Payload type is `wallHeight?: number` — TypeScript rejects strings; defaults to prev.pendingWallHeight (always 2400 or a previously-validated number) when undefined |
| T-08-01-02 | DoS | chainArmed remains true indefinitely if no cancel/tool-switch | accept | Chain breaks naturally via Esc, re-clicking active tool, or tool-switch (existing CanvasViewport effect calls cancelMarkup); no runaway state possible |
</threat_model>

<verification>
- `npx vitest run src/tests/chain-mode.test.ts` exits 0
- `npx tsc --noEmit` exits 0
- Pre-existing useMarkupTool tests still pass
- Wall markup dispatch produces WallMarkup objects via the existing placeMarkup path
</verification>

<success_criteria>
useMarkupTool.ts state machine generalized to support chain mode for all five tools. Wall tool dispatch produces WallMarkup objects. chain-mode.test.ts GREEN. No regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-markup-workflow-and-wall-tool/08-01-SUMMARY.md`
</output>
