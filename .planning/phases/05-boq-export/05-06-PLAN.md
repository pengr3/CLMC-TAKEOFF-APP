---
phase: 05-boq-export
plan: 06
type: execute
wave: 5
depends_on:
  - "05-05"
files_modified:
  - .planning/phases/05-boq-export/05-UAT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements:
  - EXPRT-01
  - EXPRT-02
must_haves:
  truths:
    - "User has clicked the Export button (or pressed Ctrl+Shift+E) on a project with markups across ≥2 pages and ≥2 categories, chosen .xlsx, and the file opens in Microsoft Excel without errors"
    - "User has typed =SUM(B8:B20) (or equivalent across the quantity column) in the opened .xlsx and seen a non-zero numeric result (NOT #VALUE! — proves D-03 native-number cells work)"
    - "User has confirmed the metadata header rows (Project / Plan / Exported / Pages / Markups), bold + frozen title row, bold category heading rows, item-cell colors matching canvas markup colors, light-gray Subtotal + Grand Total rows are all rendered correctly in Excel"
    - "User has exported the same project to .csv (via dialog filter switch), opened it in Excel, and confirmed quantity columns import as numbers + structural rows align with the .xlsx"
    - "User has triggered the D-06 path: a project with one calibrated page + one uncalibrated page (with linear/area/perimeter markups on the uncalibrated page), and confirmed the warning modal lists the affected page, [Continue] proceeds (excluding the uncalibrated page's measurements but keeping its counts), [Cancel] aborts"
    - "User has triggered the D-21 error path: target a .xlsx file already open in Excel (file lock), clicked Export, saved with overwrite, and confirmed the error modal surfaces the OS reason without crashing"
    - "REQUIREMENTS.md EXPRT-01 + EXPRT-02 marked checked with phase-completion annotation"
    - "ROADMAP.md Phase 5 marked complete with completion date"
    - "STATE.md updated: completed_phases incremented; current focus shifted to Phase 6"
  artifacts:
    - path: ".planning/phases/05-boq-export/05-UAT.md"
      provides: "Human UAT result — outcome of 6 manual test scenarios"
      contains: "EXPRT-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "EXPRT-01 + EXPRT-02 marked Complete"
      contains: "[x] **EXPRT-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 marked complete"
      contains: "Phase 5: BOQ Export"
    - path: ".planning/STATE.md"
      provides: "Phase 5 closure metrics"
      contains: "Phase 5"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "Both reference EXPRT-01 + EXPRT-02 as Complete"
      pattern: "EXPRT-0[12]"
---

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This is a manual verification + documentation closure plan; no production code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | Tester checks the wrong file (e.g., a stale BOQ from a prior dev iteration) and falsely reports "looks fine" | mitigate | UAT.md template includes setup steps that re-create a known-good test project from scratch (place markups on at least 2 pages, 2 categories, including one uncalibrated page) — eliminates ambiguity. |
| T-05-06-02 | Information Disclosure | Test artifacts (sample BOQ files) committed to repo by accident | mitigate | UAT.md instructs test files saved OUTSIDE the repo (e.g., user's Desktop or temp folder); .gitignore already excludes common temp paths; explicit reminder in UAT.md not to commit. |
</threat_model>

<objective>
Phase closure plan: human UAT against the criteria EXPRT-01 (XLSX with item/qty/UoM grouped by category) and EXPRT-02 (CSV with same structure), plus four cross-cutting Phase 5 manual checks (uncalibrated warning, error surfacing, keyboard shortcut, Save dialog filter switch). Documents the outcome in `05-UAT.md`. On full pass, marks REQUIREMENTS.md / ROADMAP.md / STATE.md.

This is the only checkpoint plan in Phase 5 because the prior 5 plans together produce a fully automated-tested system: every measurable behavior (aggregation correctness, Excel cell types/formats, CSV byte structure, IPC discriminated unions, modal accessibility, keyboard shortcut guard, Toolbar disabled state) is verified by Vitest before this plan runs. The human checks here cover what Vitest CANNOT exercise: real Excel paint behavior (not just the bytes), Sheets/Numbers compatibility, the OS Save dialog UX, and end-to-end perceived flow.

Per the GSD UAT pattern (Phase 4 / Phase 4.1 ran the same shape): the human runs through the test plan, marks each scenario PASS / FAIL / GAP, and any GAP triggers a separate gap-closure phase (e.g., 05.1) before phase closure. If all scenarios PASS, this plan completes and the phase is closed.

NO production code changes in this plan. The single executable activity is Task 1 (a `checkpoint:human-verify` task running the UAT template); Task 2 closes the docs after the user signals approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-boq-export/05-CONTEXT.md
@.planning/phases/05-boq-export/05-VALIDATION.md
@.planning/phases/05-boq-export/05-00-PLAN.md
@.planning/phases/05-boq-export/05-01-PLAN.md
@.planning/phases/05-boq-export/05-02-PLAN.md
@.planning/phases/05-boq-export/05-03-PLAN.md
@.planning/phases/05-boq-export/05-04-PLAN.md
@.planning/phases/05-boq-export/05-05-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual UAT — six scenarios covering EXPRT-01 / EXPRT-02 / D-06 / D-18 / D-21</name>
  <what-built>
Plans 00-05 collectively delivered:
- BoqStructure type tree + isExporting projectStore field (Plan 00)
- aggregateBoq + findUncalibratedMarkupPages pure aggregator (Plan 01)
- buildBoqXlsx + buildBoqCsv main-process writers + extension enforcers + safeText formula-injection guard (Plan 02)
- Three new IPC channels (dialog:saveExport / file:writeBoqXlsx / file:writeBoqCsv) + preload triad (Plan 03)
- useExport hook + UncalibratedExportWarningModal (Plan 04)
- Toolbar Export IconButton + Ctrl+Shift+E shortcut + App.tsx orchestration with toast/modal/error routing (Plan 05)

Full automated suite is green (`npx vitest run` passes). This UAT covers behaviors Vitest cannot reach: real Excel rendering, Save dialog filter UX, perceived flow, cross-tool CSV fidelity (Sheets/Numbers), file-lock error surface.

The user runs the 6 scenarios below. Each must end in PASS for this plan to complete; any FAIL becomes a gap that triggers Phase 05.1 (gap-closure plan).
  </what-built>
  <how-to-verify>

Create `.planning/phases/05-boq-export/05-UAT.md` with the template below BEFORE the user begins testing. Then walk the user through each scenario one by one, recording PASS / FAIL / GAP outcome.

```markdown
# Phase 5 BOQ Export — User Acceptance Test

**Tested:** {date filled by tester}
**Build:** {commit SHA filled at start}
**Tester:** {user identity}

## Setup (do this once before scenarios)

1. Run `npm run dev` to launch the app in dev mode.
2. Click "Open" → choose any multi-page construction PDF (or use the fixture at `src/tests/fixtures/sample-1page.pdf` — but for full coverage use a 2+ page PDF you have lying around).
3. Calibrate page 1 (click "Set Scale", draw a line, type real-world distance). Leave page 2 UNCALIBRATED for scenario 4.
4. Place markups:
   - Page 1: 2-3 count markups named "Outlet" (Electrical category), 1-2 linear markups named "Wire" (Electrical), 1 area markup named "Floor" (Civil category).
   - Page 2: 2 count markups named "Switch" (Electrical), 1 linear markup named "Conduit" (Electrical) — this one will be excluded from export when uncalibrated.
5. Save the project (Ctrl+S → choose a path → name it `phase5-uat.clmc`). This populates `currentFilePath` so the Default Save Path uses the .clmc basename.

## Scenario 1 — XLSX export full flow (EXPRT-01)

**Steps:**
1. Click the **Export** icon in the Toolbar (Download icon, after "Replace Plan PDF").
2. Save dialog opens with `phase5-uat-BOQ.xlsx` pre-filled, "Excel Workbook (.xlsx)" filter selected by default.
3. Confirm the save (use the proposed filename). The dialog closes.
4. A success toast appears at the bottom: `Exported: phase5-uat-BOQ.xlsx`.
5. Open the saved .xlsx in Microsoft Excel.

**Expected (all must pass):**
- [ ] Sheet name is `BOQ`.
- [ ] Rows 1-5 contain `Project: phase5-uat`, `Plan: {original-pdf-name}.pdf`, `Exported: 2026-MM-DD`, `Pages: N`, `Markups: M`.
- [ ] Row 6 is blank.
- [ ] Row 7 is bold: `Item | Quantity | UoM`.
- [ ] Row 7 stays visible when scrolling (frozen pane test).
- [ ] Bold category heading rows: `Electrical` (merged across A:C), then later `Civil` — no fill color on these rows.
- [ ] Under Electrical, rows for `Outlet`, `Wire` (and possibly `Switch`, `Conduit` from page 2 if calibrated). Item-cell fill color matches the canvas marker color.
- [ ] Quantity cells are RIGHT-aligned (Excel default for numbers — proves they're not strings).
- [ ] Type `=SUM(B8:B30)` in any empty cell. Result is a non-zero NUMBER (not #VALUE!).
- [ ] Subtotal rows: `Subtotal` in column A, total in column B, UoM in column C; light-gray fill (`#EFEFEF`) + bold.
- [ ] One Subtotal row per UoM in each category (e.g., Electrical has one for `ea` and one for the linear unit).
- [ ] Grand Total row(s) at the bottom, slightly darker gray fill (`#CCCCCC`) + bold.
- [ ] Column widths look readable — Item ~36 chars, Quantity ~12, UoM ~8.

**Outcome:** PASS / FAIL / GAP — _________
**Notes:** _____________________________________

## Scenario 2 — CSV export structural mirror (EXPRT-02)

**Steps:**
1. Click Export again.
2. In the Save dialog, switch the filter dropdown to "CSV (.csv)".
3. Confirm — observe the filename auto-changes to `phase5-uat-BOQ.csv`.
4. Save. Toast: `Exported: phase5-uat-BOQ.csv`.
5. Open the .csv in Microsoft Excel.

**Expected:**
- [ ] Same row structure as the .xlsx (metadata block → blank → title → categories → items → subtotals → grand totals).
- [ ] Quantity column imports as numbers (not text). `=SUM(B:B)` returns a non-zero number.
- [ ] Counts are integers (no `.00`); lengths/areas show 2 decimal places.
- [ ] Open the same .csv in Notepad — line endings are `\r\n` (no spurious blank lines), no UTF-8 BOM at the start (file should not start with `ï»¿`).

**Outcome:** PASS / FAIL / GAP — _________
**Notes:** _____________________________________

## Scenario 3 — Cross-tool CSV fidelity (Sheets / Numbers — EXPRT-02 stretch)

**Steps:**
1. Open the same .csv in Google Sheets (drag-drop into a new sheet) and/or macOS Numbers if available.

**Expected:**
- [ ] Rows align identically to Excel.
- [ ] Quantity columns import as numbers in each tool.
- [ ] Apostrophe-prefixed labels (none expected in this fixture, but if any item name happens to start with `=`, `+`, `-`, `@`, the leading apostrophe is invisible; the cell renders as text).

**Outcome:** PASS / FAIL / GAP / SKIPPED — _________ (skip if no Sheets/Numbers access)
**Notes:** _____________________________________

## Scenario 4 — D-06 uncalibrated-pages warning

**Steps:**
1. Confirm page 2 is UNCALIBRATED and has the linear markup `Conduit` from setup.
2. Click Export.

**Expected (BEFORE save dialog opens):**
- [ ] Modal appears with title "Pages without scale".
- [ ] Body text reads: "Page 2 have markups but no scale set. Their length, area, and perimeter measurements will be excluded from the export. Counts on those page export normally." (singular/plural may vary; "Page 2" matches.)
- [ ] Continue button is focused (visible focus ring or tab order starts there).
- [ ] Press Escape → modal closes; no save dialog opens; no toast.
- [ ] Click Export again → modal reappears.
- [ ] Click Cancel → modal closes; no save dialog; no toast.
- [ ] Click Export a third time → modal reappears.
- [ ] Click Continue → modal closes; save dialog opens.
- [ ] Confirm save → toast appears.
- [ ] Open the .xlsx — `Conduit` row is NOT in the BOQ (length excluded). `Switch` (count) IS in the BOQ (counts have no scale dependency).

**Outcome:** PASS / FAIL / GAP — _________
**Notes:** _____________________________________

## Scenario 5 — D-18 keyboard shortcut + isTextInputActive guard

**Steps:**
1. Click anywhere on the canvas (no input focused).
2. Press **Ctrl+Shift+E**.

**Expected:**
- [ ] Save dialog opens.
- [ ] Cancel the dialog.
- [ ] Click on the canvas to start placing a count markup → MarkupNamePopup appears with text input focused.
- [ ] Type the letter sequence "Test E" — the letter `E` types into the input normally.
- [ ] While typing, press **Ctrl+Shift+E**.

**Expected:**
- [ ] Save dialog does NOT open. The Ctrl+Shift+E is suppressed by `isTextInputActive`.

**Outcome:** PASS / FAIL / GAP — _________
**Notes:** _____________________________________

## Scenario 6 — D-21 error surface (file-lock)

**Steps:**
1. Open `phase5-uat-BOQ.xlsx` in Microsoft Excel and KEEP IT OPEN.
2. Back in the takeoff app, click Export.
3. Save dialog opens — choose the SAME filename `phase5-uat-BOQ.xlsx`. (Excel will hold a lock on it.)
4. Click Save (overwrite confirmation may appear; accept it).

**Expected:**
- [ ] App does NOT crash.
- [ ] An error modal appears with text starting "Export failed:" followed by the OS error reason (e.g., `EBUSY: resource busy or locked, rename ...` on Windows, or similar).
- [ ] Click Close on the modal — modal dismisses; toast does NOT appear.
- [ ] Close Excel (releasing the lock).
- [ ] Click Export → save → confirm overwrite.
- [ ] Toast appears: `Exported: phase5-uat-BOQ.xlsx`.

**Outcome:** PASS / FAIL / GAP — _________
**Notes:** _____________________________________

---

## Summary

| Scenario | Outcome |
|----------|---------|
| 1. XLSX full flow | _____ |
| 2. CSV structural mirror | _____ |
| 3. Cross-tool CSV fidelity | _____ |
| 4. D-06 uncalibrated warning | _____ |
| 5. D-18 keyboard + guard | _____ |
| 6. D-21 error surface | _____ |

**Phase 5 verdict:** PASS / GAP / FAIL — _________

If any scenario is GAP or FAIL, the phase is NOT closed. The next plan (e.g., `05-07-PLAN.md` gap-closure) addresses each failing case before REQUIREMENTS / ROADMAP / STATE are updated.
```

Walk the user through each scenario in turn. After each, record PASS / FAIL / GAP. After all 6 are complete, present the summary and ASK:

> All 6 scenarios complete. Total: {N pass / N fail / N gap}.
> 
> If all PASS → reply "approved" and I'll proceed to Task 2 (close docs).
> If any FAIL or GAP → reply "gaps: <description>" and I'll create a gap-closure plan (05-07) instead of closing.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 scenarios passed, or "gaps: <description>" if any scenario failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Close docs — REQUIREMENTS / ROADMAP / STATE updates after UAT approval</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (locate EXPRT-01 + EXPRT-02 entries; both currently `[ ]`)
    - .planning/ROADMAP.md (locate Phase 5 entry — currently `[ ]` with empty Plans count)
    - .planning/STATE.md (locate progress block + completed_phases counter)
    - .planning/phases/05-boq-export/05-UAT.md (Task 1 created — confirm all 6 scenarios marked PASS before doing this Task)
  </read_first>
  <action>
This task ONLY runs if Task 1 ended with "approved" (all 6 scenarios PASS). If any scenario is FAIL or GAP, do NOT run this task — instead, the next plan in the phase (05-07-PLAN.md gap-closure) addresses the gap.

Open `.planning/REQUIREMENTS.md`. Update the EXPRT-01 and EXPRT-02 lines:

- Find: `- [ ] **EXPRT-01**: User can export the takeoff sheet to Excel (.xlsx) with columns: item name, quantity, unit of measure — rows grouped by category`
- Replace with: `- [x] **EXPRT-01**: User can export the takeoff sheet to Excel (.xlsx) with columns: item name, quantity, unit of measure — rows grouped by category — **completed Phase 5 (2026-MM-DD), human UAT scenarios 1-3 passed**`

- Find: `- [ ] **EXPRT-02**: User can export the takeoff sheet to CSV with the same structure as the Excel export`
- Replace with: `- [x] **EXPRT-02**: User can export the takeoff sheet to CSV with the same structure as the Excel export — **completed Phase 5 (2026-MM-DD), human UAT scenario 2-3 passed**`

Update the bottom Traceability table — change `EXPRT-01 | Phase 5 | Pending` to `EXPRT-01 | Phase 5 | Complete`, same for EXPRT-02.

Open `.planning/ROADMAP.md`. Update the Phase 5 entry:

- Find: `- [ ] **Phase 5: BOQ Export** - Export takeoff sheet to Excel and CSV, grouped by category`
- Replace with: `- [x] **Phase 5: BOQ Export** - Export takeoff sheet to Excel and CSV, grouped by category (completed 2026-MM-DD)`

- In the Phase 5 details section, update `**Plans**: TBD` to `**Plans**: 7 plans (00 Wave 0 RED tests + types + isExporting; 01 aggregator; 02 main writers + extension enforcers; 03 IPC triad; 04 useExport hook + UncalibratedExportWarningModal; 05 Toolbar/Keyboard/App.tsx wiring; 06 UAT closure)`

- After the existing `**Plans**: 7 plans...` line, replace `**Plans**: TBD` if it appears elsewhere with the actual list:
  ```
  Plans:
  - [x] 05-00-PLAN.md — Wave 0 RED tests + boq-types.ts + projectStore.isExporting
  - [x] 05-01-PLAN.md — boq-aggregator.ts (renderer pure)
  - [x] 05-02-PLAN.md — boq-writers.ts (main pure) + enforceXlsxExtension/enforceCsvExtension
  - [x] 05-03-PLAN.md — IPC triad: dialog:saveExport + file:writeBoqXlsx + file:writeBoqCsv
  - [x] 05-04-PLAN.md — useExport hook + UncalibratedExportWarningModal
  - [x] 05-05-PLAN.md — Toolbar Export button + Ctrl+Shift+E + App.tsx wiring
  - [x] 05-06-PLAN.md — Manual UAT + roadmap closure
  ```

In the Progress table at the bottom of ROADMAP.md, update the Phase 5 row:
- Find: `| 5. BOQ Export | 0/0 | Not started | - |`
- Replace with: `| 5. BOQ Export | 7/7 | Complete | 2026-MM-DD |`

Open `.planning/STATE.md`. Update:

- `progress.completed_phases`: increment from current value (5 was the last) to 6
- `progress.total_plans`: increment by 7 (from 40 to 47)
- `progress.completed_plans`: increment by 7 (from 26 to 33; or whatever the current count is)
- `progress.percent`: recompute (completed_phases / total_phases × 100)
- `last_updated`: today's ISO timestamp
- `last_activity`: today
- `stopped_at`: "Phase 5 complete — BOQ Export shipped (XLSX + CSV with grouped categories, item colors, native-number SUM-ready cells, atomic write, Ctrl+Shift+E shortcut, uncalibrated-page warning, error modal)"

In the "Current Position" section:
- Phase: change to `06 (live-view-ui-polish) — NOT STARTED`
- Plan: `n/a — phase planning pending`

In the "Performance Metrics" table:
- Phases complete: `6 / 6` (or 7/8 depending on whether 03.1/04.1 count)
- Plans complete: increment by 7
- Requirements delivered: increment by 2 (now 25/25 if EXPRT-01 + EXPRT-02 close out the v1 set)

In the "Plans" rolling table at the bottom of the metrics block, append:
```
| Phase 05 P00 | (executor fills) | 2 tasks | (executor fills) files |
| Phase 05 P01 | (executor fills) | 1 task | 1 file |
| Phase 05 P02 | (executor fills) | 2 tasks | 2 files |
| Phase 05 P03 | (executor fills) | 2 tasks | 3 files |
| Phase 05 P04 | (executor fills) | 2 tasks | 2 files |
| Phase 05 P05 | (executor fills) | 3 tasks | (executor fills) files |
| Phase 05 P06 | (this plan) | 2 tasks | 4 files |
```
(Per-plan execution times are filled by the executor at commit time from the SUMMARY.md durations; for now insert "—" if unknown.)

In "Session Continuity":
- Last activity: today's ISO
- Last session: today's ISO
- Stopped at: "Phase 5 closed — BOQ Export complete. Next: Phase 6 — Live View and UI Polish."
- Next action: "Plan Phase 6 — Live View and UI Polish (`/gsd-plan-phase 6`)"

After the three doc updates, commit with message:
```
docs(05): close Phase 5 — BOQ Export complete (EXPRT-01, EXPRT-02)
```
  </action>
  <verify>
    <automated>grep -c "\\[x\\] \\*\\*EXPRT-01\\|\\[x\\] \\*\\*EXPRT-02" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\\[x\\] \\*\\*EXPRT-01" .planning/REQUIREMENTS.md` is `1`
    - `grep -c "\\[x\\] \\*\\*EXPRT-02" .planning/REQUIREMENTS.md` is `1`
    - `grep -c "EXPRT-01 | Phase 5 | Complete" .planning/REQUIREMENTS.md` is `1`
    - `grep -c "EXPRT-02 | Phase 5 | Complete" .planning/REQUIREMENTS.md` is `1`
    - `grep -c "\\[x\\] \\*\\*Phase 5: BOQ Export\\*\\*" .planning/ROADMAP.md` is `1`
    - `grep -c "5. BOQ Export | 7/7 | Complete" .planning/ROADMAP.md` is `1`
    - `grep -c "completed_phases: 6\\|completed_phases: 7" .planning/STATE.md` is at least `1` (post-increment)
    - `grep -c "Phase 5 closed\\|Phase 5 complete\\|BOQ Export shipped" .planning/STATE.md` is at least `1`
    - `cat .planning/phases/05-boq-export/05-UAT.md` exists and contains "PASS" outcomes for all 6 scenarios
    - `git log -1 --pretty=%B` shows the commit message starting with `docs(05): close Phase 5`
  </acceptance_criteria>
  <done>REQUIREMENTS / ROADMAP / STATE all reflect Phase 5 closure; UAT.md committed; v1 requirement set is now 25/25 complete (or 23/25 + the 2 v1 export reqs); Phase 6 ready to plan.</done>
</task>

</tasks>

<verification>
- All 6 UAT scenarios PASS (per Task 1)
- Doc updates committed (per Task 2)
- Repo invariant: `npx vitest run` still GREEN end-to-end
- ROADMAP "Coverage" table at bottom shows v1 mapped + completed: EXPRT-01 + EXPRT-02 both Complete
</verification>

<success_criteria>
- 6 manual scenarios run, outcomes recorded in 05-UAT.md
- If all PASS: REQUIREMENTS / ROADMAP / STATE updated; commit made
- If any FAIL/GAP: stop and create 05-07-PLAN.md gap-closure (NOT this plan's responsibility — orchestrator handles)
- Phase 5 declared complete; project advances toward Phase 6
</success_criteria>

<output>
After completion, create `.planning/phases/05-boq-export/05-06-SUMMARY.md` documenting:
- 6 UAT scenarios + their outcomes (PASS counts)
- Phase 5 closure: which requirements moved to Complete, which roadmap rows updated, which STATE fields changed
- Phase 6 prep: next-action prompt for the orchestrator
- Total Phase 5 plan count + total file additions (boq-types.ts, boq-aggregator.ts, boq-writers.ts, useExport.ts, UncalibratedExportWarningModal.tsx) + total file modifications (Toolbar.tsx, useKeyboardShortcuts.ts, App.tsx, projectStore.ts, ipc-handlers.ts, project-io.ts, preload index.ts/d.ts) + 8 new test files + 3 existing test files updated
</output>
