---
phase: 03.1-markup-gap-closure-and-visual-redesign
plan: 06
status: complete
checkpoint_result: approved
closes_phase: 3
closes_phase_alias: 03.1
updated: 2026-04-21
---

# Plan 03.1-06 — Human-verify checkpoint + Phase 3/3.1 closure

## Outcome

**Task 1 — Pre-verification: PASS**
- `npm run typecheck` exits 0
- `npx vitest run` exits 0 (211 tests across 20 files)
- Dev server started without error

**Task 2 — Human verification checkpoint: APPROVED**
- 43/45 steps approved on first pass
- 2 steps initially failed (label illegibility + non-stamp behavior on measurement labels) → follow-up fix landed in commit `7cb86fb` — then all 45 steps approved
- Plus two mid-session gap fixes that landed during the checkpoint window:
  - `f22bd41` — eliminated StrictMode-induced double-click in count-tool placement
  - `0f62d39` — Ctrl+Z during a linear/area/perimeter draw now pops the last vertex instead of undoing a committed markup

**Task 3 — Docs closure: COMPLETE**
- `.planning/REQUIREMENTS.md` MARK-08 entry annotated as revised by Phase 03.1
- Traceability row for MARK-08 updated to `Phase 3 (revised by 03.1)`
- `.planning/ROADMAP.md` Phase 3 checkbox flipped to `[x]` with `(completed 2026-04-21 via Phase 03.1 gap-closure)` suffix
- All 6 Phase 03.1 plan entries flipped to `[x]`
- Plan 03-05 entry flipped to `[x]` (checkpoint ultimately closed via Phase 03.1)
- Progress table updated: Phase 3 = `5/5 Complete 2026-04-21`, Phase 3.1 = `6/6 Complete 2026-04-21`

## Verbatim user signal

> approved woww that's better
> — after `7cb86fb` label-stamp fix

The initial "approved" (on 43/45) ran through most of the protocol cleanly. The two failing steps were step 26 ("label still not readable") and steps 29-30 ("I fucking hate this. Make it like the pins that is anchored like a stamp" / "I hate this kindly revise"). Those flagged that Plan 02's measurement-label implementation had regressed from D-34's "pure world-anchored" lock back to zoom-compensated sizing (divide font by currentZoom). Commit `7cb86fb` restored the stamp behavior and added a dark rounded chip background (D-35 fallback) for legibility. Re-verification on those three steps produced the final "approved woww that's better" signal covering the full 45-step protocol.

## Fixes landed during Plan 06 execution

| Commit | What |
|--------|------|
| `f22bd41` | Eliminate double-click registration — `useMarkupTool.recordClick` / `commitShape` were running `store.placeMarkup` inside a `setState` updater, which React 19 StrictMode double-invokes in dev. Moved side effects outside the updater; added StrictMode regression test. |
| `0f62d39` | Ctrl+Z during an in-progress draw now pops the last vertex (via new `popLastPoint` handler and module-level `markup-undo-ref.ts`). When not drawing or stack empty, falls through to committed-markup undo as before. 8 new regression tests. |
| `7cb86fb` | Measurement labels (Linear/Area/Perimeter) are now pure world-anchored (`LABEL_FONT_WORLD = 16`, no `/ currentZoom`) with a dark rounded chip background. Labels now scale with zoom like count pins — matches the stamp metaphor the user locked in during discuss-phase. |

## 45-step protocol — final status

All 45 steps approved (see `C:\Users\franc\.claude\image-cache\...` screenshots from user's verification session for reference). Notable callouts:

- **Step 7** (single click = "1" not "2"): PASS — confirms `f22bd41` double-click fix.
- **Step 16** (mid-draw Ctrl+Z pops vertex): PASS — confirms `0f62d39`.
- **Step 20** (one commit per shape): PASS — confirms `commitShape` double-commit also fixed.
- **Step 26** (area label readable): PASS after `7cb86fb`.
- **Step 29** (max-zoom label scales like stamp): PASS after `7cb86fb`.
- **Step 30** (fit-zoom no snap): PASS.
- **Step 44** (Ctrl+Z fallback after emptying in-progress vertex stack): PASS.

## One-line summary

**Phase 3 + 3.1 closed.** Markup tool set delivers MARK-01 through MARK-10 end-to-end, MARK-08 revised to per-name-group color semantic. Project advances to Phase 4 (Project Persistence).
