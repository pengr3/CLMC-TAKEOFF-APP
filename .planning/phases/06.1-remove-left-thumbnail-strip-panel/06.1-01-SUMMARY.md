---
phase: "06.1"
plan: "01"
subsystem: "ui-shell"
tags: ["cleanup", "panel-layout", "thumbnail-removal"]
dependency_graph:
  requires: []
  provides: ["two-column-app-shell", "trimmed-useUiPanels"]
  affects: ["src/renderer/src/App.tsx", "src/renderer/src/hooks/useUiPanels.ts"]
tech_stack:
  added: []
  patterns: ["two-column flex shell (canvas + TotalsPanel)"]
key_files:
  created: []
  modified:
    - "src/renderer/src/hooks/useUiPanels.ts"
    - "src/renderer/src/App.tsx"
    - "src/tests/use-ui-panels.test.ts"
  deleted:
    - "src/renderer/src/components/ThumbnailStrip.tsx"
    - "src/renderer/src/components/Thumbnail.tsx"
    - "src/renderer/src/hooks/useThumbnailRender.ts"
    - "src/tests/thumbnail-strip-click.test.ts"
    - "src/tests/thumbnail-lazy-mount.test.ts"
    - "src/tests/thumbnail-overlay-debounce.test.ts"
decisions:
  - "Stale thumbnails key in localStorage clmc.ui is silently ignored — new shape check only validates totals + collapsedCategories (T-06.1-01 accept disposition)"
metrics:
  duration: "~8 minutes"
  completed_date: "2026-05-12"
  tasks: 2
  files_changed: 9
---

# Phase 6.1 Plan 01: Remove Left Thumbnail Strip Panel Summary

**One-liner:** Deleted six thumbnail files (ThumbnailStrip, Thumbnail, useThumbnailRender + three test files) and trimmed useUiPanels + App.tsx to a two-column flex shell (canvas + TotalsPanel) with zero failing tests.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Delete thumbnail test files; trim use-ui-panels.test.ts to 4 tests | 347c15b | src/tests/* (4 files) |
| 2 | Delete production source files; edit useUiPanels + App.tsx | f993b89 | src/renderer/src/* (5 files) |

## What Was Built

**Task 1 — Test cleanup:**
- Deleted `src/tests/thumbnail-strip-click.test.ts`, `thumbnail-lazy-mount.test.ts`, `thumbnail-overlay-debounce.test.ts`
- Edited `src/tests/use-ui-panels.test.ts`: removed 4 thumbnail-asserting tests, updated DEFAULTS test body to assert `totals` + `collapsedCategories` only, updated localStorage fixture in last test to drop the `thumbnails` key

**Task 2 — Production cleanup:**
- Deleted `src/renderer/src/components/ThumbnailStrip.tsx`, `Thumbnail.tsx`, `src/renderer/src/hooks/useThumbnailRender.ts`
- Edited `useUiPanels.ts`: removed `thumbnails` from `UiState`, `DEFAULTS`, `readStorage()` shape check, `UseUiPanelsApi`, hook body return — exposes only `totals`, `collapsedCategories`, `setTotalsOpen`, `setTotalsWidth`, `toggleCategoryCollapsed`
- Edited `App.tsx`: removed ThumbnailStrip import, thumbnail destructure, `thumbnailsDragWidth` local state, `effectiveThumbnailWidth`, ThumbnailStrip JSX block, left Splitter JSX block — two-column shell retained

## Verification

- `npx vitest run`: 57 test files, 409 tests — all passing
- `npx tsc --noEmit -p tsconfig.web.json`: zero errors
- All 6 thumbnail files absent from disk
- `containerWidth` and `effectiveTotalsWidth` retained in App.tsx — right Splitter + TotalsPanel intact

## Deviations from Plan

### Auto-fixed Issues

None — plan executed exactly as written.

**Note on task ordering:** Task 1 edited the test fixture for "toggleCategoryCollapsed removes name" to remove the `thumbnails` key. The test could only pass once Task 2 updated the hook's shape check to no longer require `thumbnails`. Both tasks were completed before committing to keep the test suite green. This is consistent with the plan's Wave 0 / Wave 1 structure — the plan intended both waves to complete before the checkpoint.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. The stale `thumbnails` key in any existing `clmc.ui` localStorage entry is harmlessly ignored by the updated shape check (T-06.1-01 accept disposition — per plan threat model).

## Known Stubs

None.

## Human Verification: APPROVED (2026-05-12)

Visual verification passed:
1. Left thumbnail strip is absent from the UI
2. TotalsPanel right Splitter resizes correctly
3. Page navigation via CanvasHeaderBar arrows works normally

## Self-Check: PASSED

- `src/renderer/src/hooks/useUiPanels.ts` — exists, no "thumbnails" string
- `src/renderer/src/App.tsx` — exists, no ThumbnailStrip/setThumbnailsOpen/effectiveThumbnailWidth; contains containerWidth + effectiveTotalsWidth
- `src/tests/use-ui-panels.test.ts` — exists, 4 `it(` blocks
- Commit 347c15b — exists (Task 1)
- Commit f993b89 — exists (Task 2)
