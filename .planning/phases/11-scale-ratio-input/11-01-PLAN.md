---
phase: "11"
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/tests/scale-ratio-math.test.ts
  - src/renderer/src/lib/scale-math.ts
autonomous: true
requirements:
  - SCAL-01

must_haves:
  truths:
    - "computePixelsPerMmFromRatio produces numerically identical pixelsPerMm as computePixelsPerMm for the same ratio"
    - "isoSheetLabel returns 'W × H mm — SizeName' for A0–A4 dimensions within ±5 mm tolerance"
    - "isoSheetLabel returns 'W × H mm' (no size name) for non-standard dimensions"
    - "computePixelsPerMmFromRatio throws for non-positive pageWidthPx, pageViewWidthPt, or denominator"
  artifacts:
    - path: "src/tests/scale-ratio-math.test.ts"
      provides: "Unit tests for computePixelsPerMmFromRatio and isoSheetLabel"
      exports: []
    - path: "src/renderer/src/lib/scale-math.ts"
      provides: "computePixelsPerMmFromRatio and isoSheetLabel exported functions"
      exports: ["computePixelsPerMmFromRatio", "isoSheetLabel"]
  key_links:
    - from: "src/tests/scale-ratio-math.test.ts"
      to: "src/renderer/src/lib/scale-math.ts"
      via: "import { computePixelsPerMmFromRatio, isoSheetLabel } from '@renderer/lib/scale-math'"
      pattern: "computePixelsPerMmFromRatio|isoSheetLabel"
---

<objective>
Add computePixelsPerMmFromRatio and isoSheetLabel as exported functions to scale-math.ts, verified by a TDD test file that proves numerical equivalence with the existing draw-line path and correct ISO sheet labelling.

Purpose: The ratio tab in ScalePopup requires a pure math function to convert a 1:N drawing scale into pixelsPerMm. The formula must be numerically identical to the draw-line path (computePixelsPerMm) for the same ratio. The isoSheetLabel helper produces the human-readable sheet size confirmation display (D-03).
Output: scale-ratio-math.test.ts (all tests pass GREEN), scale-math.ts extended with two new exports.
</objective>

<execution_context>
@C:/Users/Admin/Roaming/CLMC TAKEOFF APP/.planning/phases/11-scale-ratio-input/11-RESEARCH.md
@C:/Users/Admin/Roaming/CLMC TAKEOFF APP/.planning/phases/11-scale-ratio-input/11-PATTERNS.md
</execution_context>

<context>
@C:/Users/Admin/Roaming/CLMC TAKEOFF APP/.planning/PROJECT.md
@C:/Users/Admin/Roaming/CLMC TAKEOFF APP/.planning/ROADMAP.md
</context>

<interfaces>
From src/renderer/src/lib/scale-math.ts (current exports — append new functions after line 156):

```typescript
export function computePixelsPerMm(
  linePixelLength: number,
  realWorldDistance: number,
  unit: ScaleUnit   // ScaleUnit = 'mm' | 'cm' | 'm' | 'in' | 'ft'
): number  // throws for non-positive inputs

export function toMm(value: number, unit: ScaleUnit): number
export function fromMm(mm: number, unit: ScaleUnit): number
export function formatScaleRatio(pixelsPerMmOrUnit: number, unit?: MeasurementUnit): string
export const MIN_CALIBRATION_PIXELS: number  // = 10
```

From src/tests/scale-math.test.ts (import + describe/it pattern to replicate):

```typescript
import { describe, it, expect } from 'vitest'
import { computePixelsPerUnit, ... } from '@renderer/lib/scale-math'
// @renderer alias maps to src/renderer/src/ per vitest.config.ts

describe('functionName', () => {
  it('description', () => {
    expect(fn(input)).toBeCloseTo(expected, 10)
    expect(() => fn(bad)).toThrow('exact error message')
  })
})
```

Test file location: src/tests/ (not co-located with source — matches existing scale-math.test.ts placement).
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1 RED: Write failing tests for computePixelsPerMmFromRatio and isoSheetLabel</name>
  <files>src/tests/scale-ratio-math.test.ts</files>
  <read_first>
    - src/tests/scale-math.test.ts — import pattern, describe/it structure, throw assertion pattern, .toBeCloseTo(n, 10) precision pattern
    - src/renderer/src/lib/scale-math.ts — existing computePixelsPerMm signature and error messages to cross-check against
  </read_first>
  <behavior>
    computePixelsPerMmFromRatio:
    - A1 page at 1:100: pageWidthPx=1000, pageViewWidthPt=2384 (≈841mm), denominator=100 → result ≈ 0.01189 px/mm. Must match computePixelsPerMm(1000, 841.067*100, 'mm') within 10 decimal places.
    - throws 'pageWidthPx must be positive' when pageWidthPx=0
    - throws 'pageViewWidthPt must be positive' when pageViewWidthPt=0
    - throws 'denominator must be positive' when denominator=0
    - throws 'denominator must be positive' when denominator=-1

    isoSheetLabel:
    - (841, 594) → '841 × 594 mm — A1'
    - (594, 841) → '594 × 841 mm — A1' (landscape match)
    - (1189, 841) → '1189 × 841 mm — A0'
    - (297, 210) → '297 × 210 mm — A4'
    - (500, 300) → '500 × 300 mm' (no size name)
    - (844, 596) → '844 × 596 mm — A1' (within ±5 mm tolerance)
  </behavior>
  <action>
    Create src/tests/scale-ratio-math.test.ts importing computePixelsPerMmFromRatio and isoSheetLabel from '@renderer/lib/scale-math'. These functions do not yet exist so the import compiles but tests fail at runtime with "not a function" or TypeScript compile errors. Follow the exact describe/it nesting from scale-math.test.ts.

    For the numerical cross-check test: compute pageWidthMm from pageViewWidthPt via `pageViewWidthPt * 25.4 / 72`, then call computePixelsPerMm(pageWidthPx, pageWidthMm * denominator, 'mm') inline and assert .toBeCloseTo with the result from computePixelsPerMmFromRatio. Import both functions at the top of the file.

    Run `npx vitest run src/tests/scale-ratio-math.test.ts` — tests MUST be RED (fail or error) at this stage.
  </action>
  <verify>
    <automated>npx vitest run src/tests/scale-ratio-math.test.ts 2>&amp;1 | findstr /i "FAIL\|error\|cannot find"</automated>
  </verify>
  <done>scale-ratio-math.test.ts exists; npx vitest run src/tests/scale-ratio-math.test.ts reports at least 1 test failure or import error — RED confirmed.</done>
</task>

<task type="tdd">
  <name>Task 2 GREEN: Implement computePixelsPerMmFromRatio and isoSheetLabel in scale-math.ts</name>
  <files>src/renderer/src/lib/scale-math.ts</files>
  <read_first>
    - src/renderer/src/lib/scale-math.ts — read the full file to find the correct append point (after line 156, after computePixelsPerMm). Match existing error throw message style exactly.
    - src/tests/scale-ratio-math.test.ts — read the failing tests to confirm expected behavior before implementing
    - C:/Users/Admin/Roaming/CLMC TAKEOFF APP/.planning/phases/11-scale-ratio-input/11-PATTERNS.md — Pattern assignments section for scale-math.ts: exact function signatures and ISO_SIZES array
  </read_first>
  <action>
    Append two new exported functions to src/renderer/src/lib/scale-math.ts after the existing computePixelsPerMm function (after line 156).

    computePixelsPerMmFromRatio(pageWidthPx: number, pageViewWidthPt: number, denominator: number): number
    - Guard: throw new Error('pageWidthPx must be positive') if pageWidthPx <= 0
    - Guard: throw new Error('pageViewWidthPt must be positive') if pageViewWidthPt <= 0
    - Guard: throw new Error('denominator must be positive') if denominator <= 0
    - pageWidthMm = pageViewWidthPt * 25.4 / 72
    - realWorldWidthMm = pageWidthMm * denominator
    - return pageWidthPx / realWorldWidthMm
    This formula is algebraically equivalent to computePixelsPerMm(pageWidthPx, pageWidthMm * denominator, 'mm').

    isoSheetLabel(widthMm: number, heightMm: number): string
    - Module-scope constant ISO_SIZES: Array<{ name: string; wMm: number; hMm: number }> with entries for A0 (1189×841), A1 (841×594), A2 (594×420), A3 (420×297), A4 (297×210)
    - TOL = 5 (mm tolerance)
    - w = Math.round(widthMm), h = Math.round(heightMm)
    - Iterate ISO_SIZES; check portrait match (|w-s.wMm|<=TOL && |h-s.hMm|<=TOL) and landscape match (|w-s.hMm|<=TOL && |h-s.wMm|<=TOL)
    - Return '${w} × ${h} mm — ${s.name}' on match, or '${w} × ${h} mm' if no match
    - Use the × character (U+00D7, multiplication sign), not letter x

    No other changes to scale-math.ts. Do NOT modify existing functions.

    Run `npx vitest run src/tests/scale-ratio-math.test.ts` — all tests MUST be GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/tests/scale-ratio-math.test.ts</automated>
  </verify>
  <done>
    All tests in scale-ratio-math.test.ts pass GREEN.
    scale-math.ts contains: export function computePixelsPerMmFromRatio(
    scale-math.ts contains: export function isoSheetLabel(
    npx vitest run exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scale-math.ts → callers | Pure functions; callers pass numeric inputs. Invalid inputs throw. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | computePixelsPerMmFromRatio inputs | mitigate | Guard clauses throw for non-positive pageWidthPx, pageViewWidthPt, denominator — prevents division-by-zero and silent negative scale |
</threat_model>

<verification>
npx vitest run src/tests/scale-ratio-math.test.ts — all tests GREEN.
npx vitest run — full suite GREEN (no regressions).
scale-math.ts exports computePixelsPerMmFromRatio and isoSheetLabel confirmed by: grep -c "export function computePixelsPerMmFromRatio" src/renderer/src/lib/scale-math.ts (returns 1).
</verification>

<success_criteria>
- scale-ratio-math.test.ts exists with at least 9 test cases covering numerical cross-check, throw guards, and ISO label cases
- All tests pass GREEN: npx vitest run exits 0
- scale-math.ts exports computePixelsPerMmFromRatio and isoSheetLabel
- computePixelsPerMmFromRatio(1000, 2384, 100) ≈ computePixelsPerMm(1000, 841.067*100, 'mm') within floating point precision
</success_criteria>

<output>
After completion, create .planning/phases/11-scale-ratio-input/11-01-SUMMARY.md
</output>
