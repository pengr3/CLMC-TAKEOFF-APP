---
phase: "10"
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/renderer/src/hooks/useMarkupTool.ts
  - src/renderer/src/lib/markup-undo-ref.ts
  - src/renderer/src/hooks/useKeyboardShortcuts.ts
  - src/renderer/src/components/CanvasViewport.tsx
autonomous: true
requirements:
  - MARK-09

must_haves:
  truths:
    - "While drawing, Ctrl+Z removes only the last placed point and keeps the tool active"
    - "After popping one or more points with Ctrl+Z during drawing, Ctrl+Y re-adds the most recently popped point"
    - "Ctrl+Z on the first (only) in-progress point cancels the whole markup and returns to idle"
    - "After a markup is fully committed, Ctrl+Z and Ctrl+Y continue to work at whole-markup granularity — unchanged"
    - "All five multi-point tools (linear, area, perimeter, wall, future) share the same pop/repush logic"
    - "All 12 tests in markup-tool-point-redo.test.ts pass (GREEN)"
    - "All 66 existing test files continue to pass"
  artifacts:
    - path: "src/renderer/src/hooks/useMarkupTool.ts"
      provides: "redoPoints field in MarkupDrawState + INITIAL_STATE; repushLastPoint useCallback; extended popLastPoint with SC3 auto-cancel + redoPoints push; recordClick clears redoPoints on new placement; repushLastPoint in UseMarkupToolReturn and return object"
      exports: ["UseMarkupToolReturn", "MarkupDrawState", "useMarkupTool"]
    - path: "src/renderer/src/lib/markup-undo-ref.ts"
      provides: "setMarkupRedoHandler and getMarkupRedoHandler exports mirroring the existing undo pair"
      exports: ["setMarkupRedoHandler", "getMarkupRedoHandler"]
    - path: "src/renderer/src/hooks/useKeyboardShortcuts.ts"
      provides: "Ctrl+Y dispatch prefers getMarkupRedoHandler() before falling through to markupStore.redo()"
    - path: "src/renderer/src/components/CanvasViewport.tsx"
      provides: "setMarkupRedoHandler(repushLastPoint) useEffect with null cleanup on unmount"
  key_links:
    - from: "CanvasViewport.tsx (mount useEffect)"
      to: "markup-undo-ref.ts setMarkupRedoHandler"
      via: "setMarkupRedoHandler(repushLastPoint)"
      pattern: "setMarkupRedoHandler"
    - from: "useKeyboardShortcuts.ts (Ctrl+Y branch)"
      to: "markup-undo-ref.ts getMarkupRedoHandler"
      via: "getMarkupRedoHandler()?.() ?? false"
      pattern: "getMarkupRedoHandler"
    - from: "useMarkupTool.ts popLastPoint"
      to: "cancel()"
      via: "direct call when points.length === 1 before entering setState"
      pattern: "cancel\\(\\)"
---

<objective>
Implement all production code changes for Phase 10: add redoPoints to the draw state machine, implement repushLastPoint, wire the redo handler ref, and extend the Ctrl+Y dispatch. Makes all 12 RED tests from Plan 01 turn GREEN.

Purpose: Deliver MARK-09 SC1-SC5 — step-level in-progress undo/redo as the foundation for all future undo behavior in the markup toolset.
Output: Four modified production files; all MARK-09 success criteria verifiable via automated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-granular-undo-foundation/10-RESEARCH.md
@.planning/phases/10-granular-undo-foundation/10-PATTERNS.md
@.planning/phases/10-granular-undo-foundation/10-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor needs — extracted from codebase. -->

From src/renderer/src/hooks/useMarkupTool.ts (current state):
  MarkupDrawState interface (lines 16-31) — add redoPoints: StagePoint[] field
  INITIAL_STATE (lines 33-46) — add redoPoints: []
  UseMarkupToolReturn (lines 64-81) — add repushLastPoint: () => boolean
  popLastPoint implementation (lines 356-365) — extend with SC3 + redoPoints push
  recordClick setState updater (lines 184-188) — add redoPoints: []
  return object (lines 367-379) — add repushLastPoint

From src/renderer/src/lib/markup-undo-ref.ts (full file, 23 lines):
  Existing exports: setMarkupUndoHandler, getMarkupUndoHandler
  Add symmetric: setMarkupRedoHandler, getMarkupRedoHandler

From src/renderer/src/hooks/useKeyboardShortcuts.ts:
  Ctrl+Y branch (lines 114-119) — extend to prefer getMarkupRedoHandler() before redo()
  Import line — add getMarkupRedoHandler to the markup-undo-ref import

From src/renderer/src/components/CanvasViewport.tsx:
  Undo useEffect (lines 273-281) — add parallel redo useEffect after it
  useMarkupTool destructure (line ~270) — add repushLastPoint
  Import line — add setMarkupRedoHandler to the markup-undo-ref import
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend markup-undo-ref.ts with redo handler pair</name>
  <files>src/renderer/src/lib/markup-undo-ref.ts</files>

  <read_first>
    - src/renderer/src/lib/markup-undo-ref.ts — read the full 23-line file; the addition mirrors the existing undo pair exactly
  </read_first>

  <behavior>
    - setMarkupRedoHandler(fn) stores fn in _markupRedoHandler; getMarkupRedoHandler() returns it
    - setMarkupRedoHandler(null) clears _markupRedoHandler to null
    - getMarkupRedoHandler() returns null initially
    - The two new functions are exported alongside the existing undo pair
  </behavior>

  <action>
    Append four items to markup-undo-ref.ts after the last line of the file (after the existing getMarkupUndoHandler closing brace):

    1. A blank line separator
    2. A module-level nullable variable: let _markupRedoHandler: (() => boolean) | null = null
    3. setMarkupRedoHandler function — same JSDoc style as setMarkupUndoHandler, same signature shape: (handler: (() => boolean) | null): void — assigns _markupRedoHandler = handler
    4. getMarkupRedoHandler function — returns _markupRedoHandler; return type (() => boolean) | null

    Keep the JSDoc comment style, whitespace, and export keyword placement identical to the undo pair. Substitute "Undo" → "Redo" and "_markupUndoHandler" → "_markupRedoHandler" throughout.
  </action>

  <verify>
    <automated>npx vitest run src/tests/markup-tool-point-redo.test.ts 2>&1 | grep "markup-undo-ref module"</automated>
  </verify>

  <acceptance_criteria>
    - src/renderer/src/lib/markup-undo-ref.ts exports setMarkupRedoHandler and getMarkupRedoHandler (grep -c "export function setMarkupRedoHandler" src/renderer/src/lib/markup-undo-ref.ts returns 1)
    - The redo-ref describe block in markup-tool-point-redo.test.ts passes (npx vitest run src/tests/markup-tool-point-redo.test.ts 2>&1 shows "markup-undo-ref module" describe block green)
    - npx vitest run src/tests/markup-tool-pop-last-point.test.ts exits 0 — undo-ref tests unaffected
  </acceptance_criteria>

  <done>markup-undo-ref.ts exports both undo and redo handler pairs; redo-ref test turns GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend useMarkupTool.ts with redoPoints state and repushLastPoint</name>
  <files>src/renderer/src/hooks/useMarkupTool.ts</files>

  <read_first>
    - src/renderer/src/hooks/useMarkupTool.ts — read the full file (381 lines) to see the complete current state before any edits
    - src/renderer/src/hooks/useCalibrationMode.ts — import of StagePoint type used in the redoPoints field declaration
    - .planning/phases/10-granular-undo-foundation/10-PATTERNS.md — Section "useMarkupTool.ts" contains the exact before/after code for all 6 extensions in this file
  </read_first>

  <behavior>
    - MarkupDrawState.redoPoints: StagePoint[] — present in interface and INITIAL_STATE
    - popLastPoint: when points.length === 1, calls cancel() directly (NOT inside setState) then returns true; mode becomes 'idle' (SC3)
    - popLastPoint: when points.length > 1, pushes popped point to front of redoPoints before slicing — redoPoints: [popped, ...prev.redoPoints]
    - popLastPoint dependency array changes from [] to [cancel] because it now calls cancel()
    - repushLastPoint: stateRef outer guard (mode !== 'drawing' → false; redoPoints.length === 0 → false); setState inner guard; moves first element of redoPoints back to end of points; previewPoint: null; returns true on success
    - recordClick setState updater (linear/area/perimeter/wall branch): adds redoPoints: [] to the return object so new placements clear the in-progress redo history
    - UseMarkupToolReturn includes repushLastPoint: () => boolean
    - Return object includes repushLastPoint
  </behavior>

  <action>
    Make six targeted edits to useMarkupTool.ts. Do not rewrite the file. Each edit is a minimal addition or replacement:

    Edit 1 — MarkupDrawState interface: After the pendingWallHeight field (last field before closing brace), add one new field: redoPoints: StagePoint[] with JSDoc comment "Points popped by Ctrl+Z during an in-progress draw; cleared on new recordClick".

    Edit 2 — INITIAL_STATE object: After pendingWallHeight: 2400, add redoPoints: [].

    Edit 3 — UseMarkupToolReturn interface: After the popLastPoint: () => boolean declaration, add repushLastPoint: () => boolean.

    Edit 4 — popLastPoint useCallback body (lines 356-365): Replace the entire useCallback with the following logic:
      - Outer guard: stateRef.current.mode !== 'drawing' → return false
      - Outer guard: stateRef.current.points.length === 0 → return false
      - SC3 branch: if stateRef.current.points.length === 1, call cancel() directly (not inside setState), return true
      - setState updater: inner guard (prev.mode !== 'drawing' || prev.points.length === 0 → return prev); slice the last point off as `popped`; return spread prev with points sliced to -1, previewPoint: null, redoPoints: [popped, ...prev.redoPoints]
      - Change the dependency array from [] to [cancel]

    Edit 5 — recordClick setState updater (the linear/area/perimeter/wall branch return at lines 184-188): Add redoPoints: [] as a new key alongside pendingPage so new placements clear the in-progress redo history.

    Edit 6 — repushLastPoint useCallback (new, add immediately after the popLastPoint useCallback closing brace):
      - Outer guard: stateRef.current.mode !== 'drawing' → return false
      - Outer guard: stateRef.current.redoPoints.length === 0 → return false
      - setState updater: inner guard (prev.mode !== 'drawing' || prev.redoPoints.length === 0 → return prev); destructure [next, ...remaining] = prev.redoPoints; return spread prev with points appended with next, previewPoint: null, redoPoints: remaining
      - Dependency array: []

    Edit 7 — return object at the bottom: Add repushLastPoint after popLastPoint in the return object.

    CRITICAL — Pitfall 1 (nested setState): The SC3 one-point path MUST call cancel() in the useCallback body directly, reading stateRef.current.points.length BEFORE entering setState. Never call cancel() inside a setState updater.

    CRITICAL — Pitfall 2 (stale redoPoints): recordClick's setState updater must include redoPoints: [] in the return for the linear/area/perimeter/wall branch. The count branch is a separate early-return path and does not need this change.

    CRITICAL — dependency array: popLastPoint's dep array changes from [] to [cancel] because cancel() is now called from the callback body.
  </action>

  <verify>
    <automated>npx vitest run src/tests/markup-tool-point-redo.test.ts src/tests/markup-tool-pop-last-point.test.ts 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - MarkupDrawState interface contains redoPoints: StagePoint[] — grep confirms: grep -c "redoPoints: StagePoint\[\]" src/renderer/src/hooks/useMarkupTool.ts returns 1
    - UseMarkupToolReturn interface contains repushLastPoint: () => boolean — grep -c "repushLastPoint" src/renderer/src/hooks/useMarkupTool.ts returns >= 4 (interface, impl, dep, return)
    - INITIAL_STATE contains redoPoints: [] — grep -c "redoPoints: \[\]" src/renderer/src/hooks/useMarkupTool.ts returns >= 2 (INITIAL_STATE + recordClick)
    - popLastPoint dep array is [cancel] — grep -c "\[cancel\]" src/renderer/src/hooks/useMarkupTool.ts returns 1
    - npx vitest run src/tests/markup-tool-point-redo.test.ts shows all 12 tests passing (excluding the redo-ref dispatch test which requires Wave 1 Task 3+4)
    - npx vitest run src/tests/markup-tool-pop-last-point.test.ts exits 0 — all existing pop tests pass
  </acceptance_criteria>

  <done>useMarkupTool exports repushLastPoint; redoPoints tracked in state; SC3 auto-cancel implemented; recordClick clears redoPoints; all 12 new tests GREEN</done>
</task>

<task type="auto">
  <name>Task 3: Wire redo handler in CanvasViewport and extend Ctrl+Y dispatch in useKeyboardShortcuts</name>
  <files>
    src/renderer/src/components/CanvasViewport.tsx
    src/renderer/src/hooks/useKeyboardShortcuts.ts
  </files>

  <read_first>
    - src/renderer/src/components/CanvasViewport.tsx — lines 1-30 (imports), lines 265-290 (useMarkupTool destructure and undo useEffect wiring at 273-281)
    - src/renderer/src/hooks/useKeyboardShortcuts.ts — lines 1-20 (imports), lines 88-125 (Ctrl+Z and Ctrl+Y dispatch blocks)
    - src/renderer/src/lib/markup-undo-ref.ts — verify setMarkupRedoHandler and getMarkupRedoHandler are now exported (from Task 1)
    - .planning/phases/10-granular-undo-foundation/10-PATTERNS.md — Section "CanvasViewport.tsx" and "useKeyboardShortcuts.ts" for the exact before/after diff
  </read_first>

  <action>
    CanvasViewport.tsx — two edits:

    Edit 1 — Import line: Find the existing import from '../lib/markup-undo-ref' (which imports setMarkupUndoHandler). Add setMarkupRedoHandler to the same import statement so it reads: import { setMarkupUndoHandler, setMarkupRedoHandler } from '../lib/markup-undo-ref'.

    Edit 2 — useMarkupTool destructure (line ~270): Add repushLastPoint to the destructured fields from useMarkupTool(stageRef). Find the line that destructures { state, activate, cancel, recordClick, updatePreview, finishLinear, finishPolygon, commitCountName, commitShape, dismissError, popLastPoint } and add repushLastPoint to the list.

    Edit 3 — New useEffect: Immediately after the existing undo useEffect (lines 277-281), add a parallel redo useEffect with the same structure verbatim, substituting: setMarkupRedoHandler instead of setMarkupUndoHandler, repushLastPoint instead of popLastPoint. The dep array is [repushLastPoint]. The cleanup sets setMarkupRedoHandler(null).

    useKeyboardShortcuts.ts — two edits:

    Edit 4 — Import line: Find the existing import from '../lib/markup-undo-ref' (which imports getMarkupUndoHandler). Add getMarkupRedoHandler to the same import statement.

    Edit 5 — Ctrl+Y branch (lines 114-119): Replace the body of the Ctrl+Y if-block. Keep the isTextInputActive() guard and e.preventDefault() unchanged. Replace the single useMarkupStore.getState().redo() call with: const handledByDraw = getMarkupRedoHandler()?.() ?? false; if (!handledByDraw) { useMarkupStore.getState().redo() }. The return statement stays.

    Structural invariant: In the Ctrl+Y branch, ordering must be: isTextInputActive guard first, e.preventDefault() second, getMarkupRedoHandler check third, markupStore.redo() fallthrough fourth. Mirrors the Ctrl+Z block structure exactly.
  </action>

  <verify>
    <automated>npx vitest run src/tests/markup-tool-point-redo.test.ts src/tests/markup-tool-pop-last-point.test.ts src/tests/markup-shortcuts.test.ts 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - CanvasViewport.tsx imports setMarkupRedoHandler — grep -c "setMarkupRedoHandler" src/renderer/src/components/CanvasViewport.tsx returns 3 (import + useEffect call + cleanup call)
    - CanvasViewport.tsx destructures repushLastPoint from useMarkupTool — grep -c "repushLastPoint" src/renderer/src/components/CanvasViewport.tsx returns >= 2
    - useKeyboardShortcuts.ts imports getMarkupRedoHandler — grep -c "getMarkupRedoHandler" src/renderer/src/hooks/useKeyboardShortcuts.ts returns >= 2 (import + call)
    - useKeyboardShortcuts.ts Ctrl+Y branch calls getMarkupRedoHandler()?.() — grep -c "getMarkupRedoHandler" src/renderer/src/hooks/useKeyboardShortcuts.ts returns >= 1 in the block after "key === 'y'"
    - npx vitest run src/tests/markup-tool-point-redo.test.ts exits 0 — all 12 tests GREEN
    - npx vitest run src/tests/markup-shortcuts.test.ts exits 0 — existing Ctrl+Y post-commit redo tests unaffected
    - npx vitest run exits 0 — all 66+ test files pass (full suite green)
  </acceptance_criteria>

  <done>CanvasViewport registers repushLastPoint via setMarkupRedoHandler; useKeyboardShortcuts Ctrl+Y prefers in-progress redo before falling through to markupStore.redo(); all 12 Phase 10 tests GREEN; full suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard → hook state | Ctrl+Z/Ctrl+Y key events cross from the DOM into the useMarkupTool state machine via the module-level handler ref |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering | useMarkupTool redoPoints | accept | redoPoints is renderer in-memory state only; not persisted; no IPC boundary; cleared on cancel/commit/activate — no attack surface |
| T-10-02-02 | Denial of Service | repushLastPoint / popLastPoint rapid keyboard | accept | Both functions are O(1) array operations with early-exit guards; no loop or allocation that could be exhausted by key-repeat events |
</threat_model>

<verification>
- npx vitest run src/tests/markup-tool-point-redo.test.ts — all 12 tests GREEN
- npx vitest run src/tests/markup-tool-pop-last-point.test.ts — all existing pop tests GREEN
- npx vitest run src/tests/markup-shortcuts.test.ts — existing Ctrl+Z/Ctrl+Y post-commit tests GREEN
- npx vitest run — full 66+ file suite green (phase gate)
</verification>

<success_criteria>
MARK-09 SC1-SC5 all pass:
1. Ctrl+Z during drawing removes only the last placed point, tool stays active
2. Ctrl+Y after Ctrl+Z re-adds the most recently popped point, navigable LIFO
3. Ctrl+Z on first point cancels the markup entirely (mode: 'idle')
4. Post-commit Ctrl+Z / Ctrl+Y unchanged — whole-markup granularity
5. All five multi-point tools share the same pop/repush logic (not duplicated per tool)
Full test suite: 66+ files, all green.
</success_criteria>

<output>
After completion, create .planning/phases/10-granular-undo-foundation/10-02-SUMMARY.md
</output>
