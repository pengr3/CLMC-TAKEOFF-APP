---
phase: "10"
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/tests/markup-tool-point-redo.test.ts
autonomous: true
requirements:
  - MARK-09

must_haves:
  truths:
    - "RED test file exists at src/tests/markup-tool-point-redo.test.ts and fails before any implementation"
    - "Test covers SC1 (pop returns true and removes last vertex — regression)"
    - "Test covers SC2 (repushLastPoint re-adds popped vertex, LIFO order, clears on new click)"
    - "Test covers SC3 (pop on single point cancels to idle)"
    - "Test covers SC4 (repushLastPoint does not touch committed markups)"
    - "Test covers SC5 (repushLastPoint is tool-type-agnostic: area, perimeter, wall)"
    - "The existing 66-file suite continues to pass after Wave 0 (regressions absent)"
  artifacts:
    - path: "src/tests/markup-tool-point-redo.test.ts"
      provides: "RED test stubs for in-progress point redo (MARK-09 SC1-SC5)"
  key_links:
    - from: "src/tests/markup-tool-point-redo.test.ts"
      to: "src/renderer/src/hooks/useMarkupTool.ts"
      via: "HookHost/probe mount pattern (mirrors markup-tool-pop-last-point.test.ts)"
      pattern: "useMarkupTool.*repushLastPoint"
---

<objective>
Write the failing test file for all five Success Criteria of MARK-09 before any production code is written. Tests will fail with "repushLastPoint is not a function" (or similar TypeScript error) because the method does not exist yet. Wave 1 makes them green.

Purpose: Establish the test contract first so Wave 1 implementation is driven by verifiable, explicit requirements.
Output: src/tests/markup-tool-point-redo.test.ts — NEW file, 12 test cases, all RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-granular-undo-foundation/10-RESEARCH.md
@.planning/phases/10-granular-undo-foundation/10-PATTERNS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write RED tests for repushLastPoint and SC3 auto-cancel (Wave 0 TDD)</name>
  <files>src/tests/markup-tool-point-redo.test.ts</files>

  <read_first>
    - src/tests/markup-tool-pop-last-point.test.ts — copy the full scaffolding verbatim (lines 1-78): HookHost, Probe interface, makeFakeStage, mount(), globalThis.IS_REACT_ACT_ENVIRONMENT, beforeEach store reset pattern
    - src/renderer/src/hooks/useMarkupTool.ts — understand MarkupDrawState, UseMarkupToolReturn, and the current popLastPoint (lines 16-81 and 356-379) so tests reference real field names
    - src/renderer/src/lib/markup-undo-ref.ts — understand the module-level ref pattern so the redo-ref test uses the same dynamic import approach as lines 219-231 of the pop-last-point test
  </read_first>

  <behavior>
    The test file must cover all Success Criteria. Each test must FAIL before Wave 1 implementation (RED state). Do not implement any production code in this task.

    Test cases (12 total across two describe blocks):

    DESCRIBE: "useMarkupTool.repushLastPoint (in-progress Ctrl+Y redo, MARK-09 SC1-SC5)"

    - SC1 regression: activate('linear'), click x3, popLastPoint() returns true, points.length===2, last point is {x:20,y:20}. (Ensures pop still works after redoPoints field is added — will pass immediately if pop unchanged, that is expected.)
    - SC2 basic: activate('linear'), click x3, popLastPoint(), repushLastPoint() returns true, points.length===3, last point restored to {x:30,y:30}.
    - SC2 LIFO: activate('linear'), click x3 ({x:10,y:10}, {x:20,y:20}, {x:30,y:30}), pop p3 (redoPoints=[p3]), pop p2 (redoPoints=[p2,p3]), repush → points ends with {x:20,y:20}, repush again → points ends with {x:30,y:30}.
    - SC2 cleared on new click: activate('linear'), click x3, pop → redoPoints.length===1, click new point ({x:99,y:99}), repushLastPoint() returns false (redoPoints cleared).
    - SC3 cancel on first point: activate('linear'), recordClick x1, popLastPoint() returns true, state.mode==='idle', state.points.length===0.
    - SC4 does not touch committed markups: seed a committed markup in markupStore, activate linear, click x2, repushLastPoint() (nothing to repush) returns false, committed markup still present.
    - SC5 area: activate('area'), click x3, pop, repush → points.length===3.
    - SC5 perimeter: activate('perimeter'), click x2, pop, repush → points.length===2.
    - SC5 wall: activate('wall'), click x2, pop, repush → points.length===2.
    - returns false when redoPoints is empty (no prior pop): activate('linear'), click x2, repushLastPoint() returns false.
    - returns false when not in drawing mode: idle state, repushLastPoint() returns false.

    DESCRIBE: "markup-undo-ref module — redo handler (MARK-09 SC2 wiring)"

    - setMarkupRedoHandler / getMarkupRedoHandler module-level ref: dynamic import from '@renderer/lib/markup-undo-ref', getMarkupRedoHandler() is null initially, set a fn, get returns same ref, set null, get returns null again.
  </behavior>

  <action>
    Create src/tests/markup-tool-point-redo.test.ts as a new file. Structure it as follows:

    1. Copy the file header verbatim from markup-tool-pop-last-point.test.ts (lines 1-78): the /** @vitest-environment jsdom */ directive, all imports (describe, it, expect, beforeEach, React, useRef, createRoot, Root, act, Konva, useMarkupTool, useMarkupStore, useViewerStore), the Tool and Probe types, makeFakeStage(), HookHost, mount(), globalThis.IS_REACT_ACT_ENVIRONMENT = true, and the beforeEach reset.

    2. Add a JSDoc header comment at the top (after @vitest-environment) describing Phase 10: "Tests for repushLastPoint (in-progress Ctrl+Y redo) and SC3 auto-cancel on first-point Ctrl+Z."

    3. Write the first describe block ("useMarkupTool.repushLastPoint ...") with the 11 test cases listed in behavior. Each test creates its own probe, mounts, runs act() blocks, asserts, then unmounts. Use the same style as the existing pop-last-point tests.

    4. Write the second describe block ("markup-undo-ref module — redo handler") with 1 test. Use dynamic import: `const { setMarkupRedoHandler, getMarkupRedoHandler } = await import('@renderer/lib/markup-undo-ref')`. This mirrors lines 219-231 of the pop-last-point test exactly.

    Do NOT write fenced code blocks in this action. The test file content is fully specified by the behavior block and analog patterns above.

    Key constraints:
    - Every test that calls repushLastPoint must fail in RED state (the method does not exist until Wave 1)
    - The SC1 regression test and the SC4 committed-markup test may pass immediately — that is acceptable and expected
    - The redo-ref test will fail until Wave 1 adds setMarkupRedoHandler/getMarkupRedoHandler to markup-undo-ref.ts
    - SC3 test (mode==='idle' after single-point pop) will fail until Wave 1 extends popLastPoint with the auto-cancel branch
    - Do NOT add @vitest-environment jsdom on a separate line if the copy already includes it in the first line — keep exactly one @vitest-environment directive
  </action>

  <verify>
    <automated>npx vitest run src/tests/markup-tool-point-redo.test.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - src/tests/markup-tool-point-redo.test.ts exists and contains exactly 12 test cases (11 in first describe + 1 in second describe)
    - Running npx vitest run src/tests/markup-tool-point-redo.test.ts shows failures on tests that call repushLastPoint (TypeScript compile error or runtime "not a function") and on the redo-ref test (getMarkupRedoHandler is not exported) — confirming RED state
    - The SC1 regression test and SC4 committed-markup test may show as passing (green) — acceptable
    - Running npx vitest run src/tests/markup-tool-pop-last-point.test.ts exits 0 — the existing pop-last-point tests are unaffected
    - The full suite (npx vitest run) passes all 66 existing test files at their prior pass count — no regressions introduced by the new test file in isolation (TypeScript errors only affect the new file)
  </acceptance_criteria>

  <done>markup-tool-point-redo.test.ts exists; new tests are RED for the unimplemented repushLastPoint and redo-ref methods; existing 66-file suite remains green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → hook | Unit tests drive the useMarkupTool hook directly via HookHost mount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | Test scaffolding | accept | Test-only file; no production surface; jsdom is isolated per test run |
</threat_model>

<verification>
- npx vitest run src/tests/markup-tool-point-redo.test.ts — fails on repushLastPoint and redo-ref tests (RED confirmed)
- npx vitest run src/tests/markup-tool-pop-last-point.test.ts — exits 0 (no regression)
</verification>

<success_criteria>
New test file written with 12 test cases covering all five MARK-09 Success Criteria. Tests are RED for the unimplemented half. Existing suite remains green.
</success_criteria>

<output>
After completion, create .planning/phases/10-granular-undo-foundation/10-01-SUMMARY.md
</output>
