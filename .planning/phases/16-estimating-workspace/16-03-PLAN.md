---
phase: 16-estimating-workspace
plan: 03
type: execute
wave: 2
depends_on: [16-02]
files_modified:
  - src/renderer/src/types/viewer.ts
  - src/renderer/src/stores/viewerStore.ts
  - src/renderer/src/components/RibbonToolbar.tsx
  - src/renderer/src/components/EstimateRow.tsx
  - src/renderer/src/components/EstimateCategoryBlock.tsx
  - src/renderer/src/components/EstimatePanel.tsx
  - src/renderer/src/App.tsx
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
must_haves:
  truths:
    - "A Plan | Estimate segmented toggle in the Estimating ribbon tab swaps the center area between the PDF canvas and a full-width Estimate sheet"
    - "Toggling Plan⟷Estimate does NOT unmount the Konva CanvasViewport (mount-preserving CSS display swap)"
    - "The Estimate sheet groups rows by category with editable Material/Labor/Markup cells and read-only Cost/Price/Margin"
    - "Each estimate category shows Cost/Price/Margin subtotals; a pinned grand-total bar shows Cost/Price/Margin"
    - "Editing a Material/Labor/Markup cell dispatches setPrice(`name|type`, patch) and recomputes cost/price/margin live"
    - "viewMode lives in viewerStore, is never serialized, and resets to 'plan' on file open/reset"
  artifacts:
    - path: "src/renderer/src/components/EstimateRow.tsx"
      provides: "One estimate grid row: editable material/labor/markup cells (uncontrolled + native listeners + stopPropagation) + read-only ₱ cost/price/margin"
      contains: "estimate-row-material-input"
    - path: "src/renderer/src/components/EstimatePanel.tsx"
      provides: "Full-width estimate view container: grouped column header + category blocks + Cost/Price/Margin grand-total bar off useBoqLive"
      contains: "useBoqLive"
    - path: "src/renderer/src/stores/viewerStore.ts"
      provides: "viewMode:'plan'|'estimate' state + setViewMode; reset to 'plan' in setFile/resetViewer/hydrate"
      contains: "viewMode"
    - path: "src/renderer/src/App.tsx"
      provides: "Center-area mount-preserving display toggle (canvas + EstimatePanel siblings); totals panel gated to Plan view"
      contains: "viewMode"
  key_links:
    - from: "src/renderer/src/components/EstimateRow.tsx"
      to: "projectStore.setPrice"
      via: "native input/blur/Enter → useProjectStore.getState().setPrice(priceKey, { material|labor|markup })"
      pattern: "setPrice\\("
    - from: "src/renderer/src/components/RibbonToolbar.tsx"
      to: "viewerStore.viewMode / setViewMode"
      via: "segmented [Plan|Estimate] buttons read viewMode + dispatch setViewMode"
      pattern: "setViewMode"
    - from: "src/renderer/src/App.tsx"
      to: "CanvasViewport (preserved) + EstimatePanel (sibling)"
      via: "CSS display:(viewMode==='plan'?…) on both center subtrees — no conditional unmount"
      pattern: "viewMode === 'estimate'"
---

<objective>
Wave 2a — the Estimate workspace. Add `viewMode: 'plan' | 'estimate'` to `viewerStore`, a `Plan | Estimate` segmented toggle in the Estimating ribbon tab, a mount-preserving CSS `display` swap in `App.tsx` (the Konva canvas stays mounted — D-01/Pitfall 1), and the full-width Estimate grid (`EstimatePanel` → `EstimateCategoryBlock` → `EstimateRow`) that reads the widened `BoqStructure` from `useBoqLive` and commits per-cell edits via `setPrice`. This turns the Wave-0 RED estimate-grid-edit + view-switch tests GREEN and is the user-facing half of the estimate model built in Wave 1 (16-02).

This plan CONSUMES the Wave 1 data model: `projectStore.setPrice`, `PriceEntry`, the six BoqItemRow money fields + three subtotal/grand fields, and `useBoqLive` (already live for the PriceEntry map). It does NOT touch the aggregator, store math, serialize, writers, or the totals panel (Wave 2b / 16-04 owns the totals-panel revert; the writers are Wave 3 / 16-05).

Purpose: pricing moves OFF the measurement surface into a dedicated spreadsheet the estimator opens from the Estimating tab — the estimator types material/labor/markup on a row and watches Cost/Price/Margin (and category subtotals + grand totals) update instantly, without disturbing the PDF canvas.

Output: a working Estimate view reachable via the ribbon toggle, with editable material/labor/markup cells and live Cost/Price/Margin, over a preserved-canvas view switch. `16-03-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-estimating-workspace/16-CONTEXT.md
@.planning/phases/16-estimating-workspace/16-RESEARCH.md
@.planning/phases/16-estimating-workspace/16-PATTERNS.md
@.planning/phases/16-estimating-workspace/16-VALIDATION.md
@.planning/phases/16-estimating-workspace/16-02-PLAN.md

<decisions_locked>
- VIEW STATE (RESEARCH Pattern 1 / A3): `viewMode: 'plan' | 'estimate'` lives in `viewerStore` beside `activeTool`. It is transient session UI — NEVER serialized to `.clmc` (snapshotProject reads explicit fields only, so a sibling `viewMode` is auto-excluded — do NOT add it to project-serialize). It RESETS to `'plan'` on `setFile`/`resetViewer`/`hydrate` (mirror how those reset `activeTool: 'select'`) so opening a project lands on the canvas. Add `ViewMode` to `types/viewer.ts` next to `ActiveTool`.
- VIEW SWITCH (D-01, Pitfall 1): mount-preserving CSS `display` toggle. Render BOTH the existing canvas center-column AND the new `EstimatePanel` as siblings inside `<main>`'s center region; toggle visibility with `display: viewMode === 'plan' ? … : 'none'`. NEVER conditional-render `{viewMode==='plan' ? <CanvasViewport/> : <EstimatePanel/>}` (that unmounts the Konva Stage → PDF re-rasterize + 0×0 ResizeObserver — the core landmine). The canvas render pipeline stays warm; markups already live in Zustand so no data is ever at risk.
- TOGGLE (D-01, RESEARCH Pattern 3): a `[Plan | Estimate]` segmented control in `renderEstimatingTab()` (keep the Export button). It is a STORE toggle — read `viewMode` via `useViewerStore((s) => s.viewMode)`, dispatch `useViewerStore.getState().setViewMode(...)`. NOT the module-ref pattern (module-refs are for imperative canvas commands with no React state; `viewMode` IS React state). Build the two-button segmented control from the existing ribbon tab-strip `aria-selected` styling (there is no reusable segmented-control component).
- GRID LAYOUT (D-07): columns grouped `Item · Qty · UoM | Material ₱ · Labor ₱ · Cost ₱ | Markup % · Price ₱ · Margin ₱`. Rows grouped by category with per-category Cost/Price/Margin subtotals and a pinned grand-total bar (Cost/Price/Margin). Editable cells: material rate, labor rate, markup %. Computed (read-only): materialCost or cost, price, margin — the grid displays `item.cost`/`item.price`/`item.margin` directly (NO arithmetic in the UI; the aggregator is the single source of truth — mirrors the "no rate×quantity in TotalsRow" rule).
- CELL COMMIT (D-04, Pitfall 3): each editable cell is UNCONTROLLED with NATIVE `input`/`blur`/`keydown` listeners (NOT React onChange/onBlur) — React 19's value-tracker suppresses synthetic onChange under programmatic `.value` set, and delegates blur as focusout. Copy the `TotalsRow.tsx:160-216` recipe PER FIELD. material/labor commit: `parseFloat`, clamp NaN/negative → 0. markup commit (Pitfall 5): blank/NaN → clear so the row falls back to the 30% default (do NOT force 0); a typed value → clamp ≥ 0.
- KEY (Pitfall 4): the price key is `` `${labelToName(item.label)}|${item.type}` `` — category-INDEPENDENT, reuse the exported `labelToName` (strips the `(perimeter)`/`(count)` collision suffix). Same key the aggregator reads. Do NOT include categoryId.
- CURRENCY (SP-4): import `CURRENCY_SYMBOL` from `../lib/currency`; import `DEFAULT_MARKUP_PCT` from `../lib/estimate-defaults` (markup-cell placeholder/seed). No new currency constant.
- TOTALS PANEL VISIBILITY (A4): the right `TotalsPanel` + `Splitter` are Plan-only chrome — gate them on `viewMode === 'plan'` so the Estimate grid is full-width. (In Estimate mode the grid fills the center; the totals panel hides.)
</decisions_locked>

<interfaces>
<!-- SHAPES this plan reads from the Wave 1 (16-02) source. Use these directly — no exploration needed. -->
<!-- From src/renderer/src/lib/boq-types.ts (after 16-02): -->
//   interface BoqItemRow { label; quantity; uom; color; type; categoryId?;
//     material; labor; markup; materialCost; laborCost; cost; price; margin }   // markup PERCENT
//   interface BoqCategoryGroup { name; items; subtotals; costSubtotal; priceSubtotal; marginSubtotal }
//   interface BoqStructure { metadata; categories; grandTotals; grandTotalCost; grandTotalPrice; grandTotalMargin }
//   interface PriceEntry { material; labor; markup }
<!-- From src/renderer/src/stores/projectStore.ts (after 16-02): -->
//   rates: Record<string, PriceEntry>
//   setPrice: (key: string, patch: Partial<PriceEntry>) => void   // merges + markDirty
<!-- From src/renderer/src/lib/estimate-defaults.ts (after 16-02):  export const DEFAULT_MARKUP_PCT = 30 -->
<!-- From src/renderer/src/hooks/useBoqLive.ts:  useBoqLive(): BoqStructure   (live for the PriceEntry map) -->
<!-- From src/renderer/src/components/TotalsRow.tsx:  export function labelToName(label): string -->
<!-- NEW state to add — src/renderer/src/types/viewer.ts:  export type ViewMode = 'plan' | 'estimate' -->
<!-- NEW test-ids the Wave-0 tests assert:  estimate-row-material-input, estimate-row-labor-input, estimate-row-markup-input -->
</interfaces>

<read_first>
- src/renderer/src/types/viewer.ts (`ActiveTool` :103 + `ViewerState` :7-90 — add `ViewMode` next to `ActiveTool`, add `viewMode`/`setViewMode` to the interface next to `activeTool` :34/:51).
- src/renderer/src/stores/viewerStore.ts (whole file — `activeTool: 'select'` initial :15, `setFile` reset :24-35, `resetViewer` reset :77-90, `setActiveTool` :106, `hydrate` reset :136-143 — add `viewMode: 'plan'` beside every `activeTool` reset + a `setViewMode` action).
- src/renderer/src/components/TotalsRow.tsx (THE analog for EstimateRow's editable cells — the `rateInputRef` + seed `useEffect` :160-171, `commitRate` :178-182, the native-listener `useEffect` :189-216, the input JSX :400-434, `labelToName` :74-76, the `formatCost` non-finite guard :68-71, the top-level rate selector :151). This is the exact recipe to replicate PER cell.
- src/renderer/src/components/TotalsCategoryBlock.tsx (THE analog for EstimateCategoryBlock — heading + collapse via `useUiPanels().collapsedCategories` :47/:50/:72, the items map :113-129, the per-category cost-subtotal render :135-157 [triple it for Cost/Price/Margin]; DROP the `matchesForRow`/`onRowHover`/`onRowClick`/`onArmTool` machinery :36-62 — the grid is a spreadsheet, not a navigator).
- src/renderer/src/components/TotalsPanel.tsx (THE analog for EstimatePanel — `const boq = useBoqLive()` :92, the category map :251-263, the grand-total bar :272-294 [triple it for Cost/Price/Margin], the empty-state tree :143-151; the container is FULL-WIDTH center-area, NOT the 320px right rail).
- src/renderer/src/components/RibbonToolbar.tsx (`renderEstimatingTab()` :529-555 — the toggle goes here; the tab-strip `aria-selected` button styling :614-647 as the segmented-control analog; the `handleMarkupToolClick` handler shape :193-202 as the `handleViewToggle` template — read enough of the file top to see the imports + `useViewerStore` usage + COLORS import).
- src/renderer/src/App.tsx (the center-column mount site :325-336 [the `totalPages === 0 ? EmptyState : CanvasViewport` swap to wrap], the `<main>` flex row :311-321, the Splitter :440-448 + TotalsPanel :451-461 to gate on `viewMode === 'plan'`, the toast stack :338-436 which stays inside the Plan center column).
- src/renderer/src/lib/constants.ts (COLORS tokens: dominant, secondary, accent, border, textPrimary, textSecondary, hoverSurface — the dark palette for the grid + toggle).
- src/tests/estimate-row-edit.test.ts + src/tests/estimate-view-switch.test.ts (the RED contracts this plan turns GREEN — read the exact test-ids, the `setPrice` key/patch spies, and the canvas-not-remounted assertion; do NOT modify these test files).
- $HOME/.claude/get-shit-done/references/ui-brand.md (chrome/styling tokens for the grid header, cells, subtotal rows, grand-total bar, segmented toggle).
- 16-PATTERNS.md Pattern Assignments for EstimatePanel/EstimateRow/EstimateCategoryBlock + "View-mode state" + "RibbonToolbar toggle" + "App.tsx mount switch" (options A/B — use A) + "No Analog Found" (grid header + segmented toggle + full-width swap — design from mockup).
- 16-RESEARCH.md Pattern 2 (App.tsx display toggle) + Pattern 3 (ribbon toggle) + Pitfalls 1/3/4/5/6 + "Estimate grid editable cell" Code Example.
</read_first>

<interface_context>
Run `npx vitest run src/tests/estimate-row-edit.test.ts src/tests/estimate-view-switch.test.ts` BEFORE editing to confirm the Wave 0 RED state (EstimateRow doesn't exist; `viewMode` doesn't exist). These two files are the proof this plan turns GREEN. Do NOT edit either test file. `use-boq-live` / totals-panel-quantity-only / writer tests are owned by other plans — do not expect them green here.
</interface_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add viewMode to viewerStore + the Plan|Estimate segmented toggle in the Estimating ribbon tab (proof g setup)</name>
  <files>src/renderer/src/types/viewer.ts, src/renderer/src/stores/viewerStore.ts, src/renderer/src/components/RibbonToolbar.tsx</files>
  <read_first>
    - src/renderer/src/types/viewer.ts (`ActiveTool` :103; `ViewerState` interface `activeTool` :34 + `setActiveTool` :51)
    - src/renderer/src/stores/viewerStore.ts (initial `activeTool` :15; `setFile` :24-35; `resetViewer` :77-90; `setActiveTool` :106; `hydrate` :136-143)
    - src/renderer/src/components/RibbonToolbar.tsx (`renderEstimatingTab` :529-555; tab-strip button styling :614-647; the component's `useViewerStore`/COLORS imports near the top; `handleMarkupToolClick` :193-202)
    - 16-RESEARCH.md Pattern 1 + Pattern 3; 16-PATTERNS.md "View-mode state" + "RibbonToolbar toggle"
  </read_first>
  <action>
    In `types/viewer.ts`: add `export type ViewMode = 'plan' | 'estimate'` next to `ActiveTool` (:103). In the `ViewerState` interface, add `viewMode: ViewMode` next to `activeTool` (:34) and `setViewMode: (mode: ViewMode) => void` next to `setActiveTool` (:51), with a short JSDoc noting it is transient session UI, never serialized (mirrors the `activeTool`/snapping-flags notes).

    In `viewerStore.ts`: import `ViewMode` (extend the existing `../types/viewer` import :3). Add `viewMode: 'plan' as ViewMode` to the initial state (beside `activeTool: 'select'` :15). Add `viewMode: 'plan' as ViewMode` to BOTH the `setFile` reset object (:24-35, next to its `activeTool` line) and the `resetViewer` reset object (:77-90) and the `hydrate` set object (:136-143) — so opening/creating/loading a project always lands on the Plan canvas. Add a `setViewMode: (mode) => set({ viewMode: mode })` action (mirror `setActiveTool` :106). Do NOT add `viewMode` to any subscribeWithSelector subscription or to project-serialize.

    In `RibbonToolbar.tsx`: read `const viewMode = useViewerStore((s) => s.viewMode)` and `const setViewMode = useViewerStore((s) => s.setViewMode)` (top of the component, beside the existing viewer selectors). Rewrite `renderEstimatingTab()` (:529-555) to render, BEFORE the existing Export button, a `[Plan | Estimate]` segmented control: two adjacent `<button role="tab">` (or `role="radio"`) elements, "Plan" and "Estimate", each with `aria-selected={viewMode === '<mode>'}`, the active one styled with `background: COLORS.dominant` + a `2px solid COLORS.accent` bottom (or box) accent and `color: COLORS.textPrimary`, the inactive one `color: COLORS.textSecondary` with a hover color swap — reuse the tab-strip button styling (:614-647) as the visual template. On click, `setViewMode('plan')` / `setViewMode('estimate')`. Give the toggle a `data-testid="view-mode-toggle"` (and the two buttons `data-testid="view-mode-plan"` / `view-mode-estimate`) for the render tests. Keep the existing Export `RibbonButton` (:544-553) after the toggle. Do NOT change any other tab render function.
  </action>
  <verify>
    <automated>npx vitest run src/tests/estimate-view-switch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 16-VALIDATION proof (g) setup (the viewMode state + toggle it drives). `git grep -n "viewMode" -- src/renderer/src/stores/viewerStore.ts src/renderer/src/types/viewer.ts` shows the field + type + resets in setFile/resetViewer/hydrate + the setViewMode action. `git grep -n "setViewMode" -- src/renderer/src/components/RibbonToolbar.tsx` shows the toggle dispatches it. `git grep -n "viewMode" -- src/renderer/src/lib/project-serialize.ts` returns ZERO (never serialized). `npm run typecheck` is clean. (The full proof-g test also needs Task 3's App.tsx swap; it may stay partially RED until Task 3 — that is expected within this plan.)
  </acceptance_criteria>
  <done>viewerStore has `viewMode` + `setViewMode` resetting to 'plan' on file open/reset/hydrate; ViewMode is typed in viewer.ts; the Estimating ribbon tab renders a Plan|Estimate segmented toggle wired to setViewMode (Export button kept); viewMode is not in the serialize path; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the Estimate grid — EstimateRow (editable cells) + EstimateCategoryBlock (subtotals) + EstimatePanel (header + grand total) (proof d)</name>
  <files>src/renderer/src/components/EstimateRow.tsx, src/renderer/src/components/EstimateCategoryBlock.tsx, src/renderer/src/components/EstimatePanel.tsx</files>
  <behavior>
    - Typing into the material cell (data-testid="estimate-row-material-input") then committing (native input+blur, and separately Enter keydown) calls `useProjectStore.getState().setPrice('Outlet|count', { material: <parsed> })` — the category-INDEPENDENT `labelToName(label)|type` key.
    - Labor cell (estimate-row-labor-input) dispatches `setPrice('Outlet|count', { labor: <parsed> })`; markup cell (estimate-row-markup-input) dispatches `setPrice('Outlet|count', { markup: <parsed> })`.
    - material/labor: parseFloat, NaN/negative → 0. markup: blank/NaN → clear (fall back to 30% default), typed value → clamp ≥ 0.
    - Interacting with a cell (click/mousedown/keydown) does NOT bubble to any parent handler (e.stopPropagation on the cell) — the grid is a spreadsheet.
    - The row renders read-only ₱ Cost/Price/Margin from `item.cost`/`item.price`/`item.margin` (non-finite → ₱0.00 guard); markup shown as `<n>%`. No arithmetic in the component.
    - EstimateCategoryBlock renders three per-category subtotals (Cost/Price/Margin) from `category.costSubtotal`/`priceSubtotal`/`marginSubtotal`.
    - EstimatePanel renders a grouped column header + category blocks + a pinned grand-total bar with three totals from `boq.grandTotalCost`/`grandTotalPrice`/`grandTotalMargin`.
  </behavior>
  <read_first>
    - src/renderer/src/components/TotalsRow.tsx (the editable-cell recipe: `rateInputRef`+seed `useEffect` :160-171, `commitRate` :178-182, native-listener `useEffect` :189-216, input JSX :400-434, `labelToName` :74-76, `formatCost` :68-71, top-level selector :151)
    - src/renderer/src/components/TotalsCategoryBlock.tsx (heading+collapse :66-108, items map :113-129, per-category cost-subtotal render :135-157)
    - src/renderer/src/components/TotalsPanel.tsx (`useBoqLive` :92, category map :251-263, grand-total bar :272-294, empty-state tree :143-151)
    - src/renderer/src/lib/estimate-defaults.ts (DEFAULT_MARKUP_PCT) + src/renderer/src/lib/currency.ts (CURRENCY_SYMBOL) + src/renderer/src/lib/constants.ts (COLORS)
    - src/tests/estimate-row-edit.test.ts (the RED render test — the exact test-ids, key string, patch shape, and the not-bubbled spy; do NOT modify it)
    - 16-PATTERNS.md EstimateRow/EstimateCategoryBlock/EstimatePanel Pattern Assignments + SP-5/SP-6 + "No Analog Found" (grid header)
  </read_first>
  <action>
    Create `EstimateRow.tsx` (analog: TotalsRow). Props: `{ item: BoqItemRow }` (a spreadsheet row needs no cycle/hover/arm props — DROP them). Derive `const itemName = labelToName(item.label)` and `const priceKey = `${itemName}|${item.type}`` (memoized). Read the current entry via a top-level selector: `const entry = useProjectStore((s) => s.rates[priceKey])` (may be undefined). For EACH of the three editable cells (material, labor, markup) replicate the TotalsRow SP-6 recipe: a `useRef` input, a seed `useEffect` that re-seeds `el.value` from the store field only when it differs (keyed on `[priceKey, entry?.material]` etc.), and a native-listener `useEffect` (`input`/`blur`/`keydown` commit + `stopPropagation` on click/mousedown/keydown) re-bound on `[priceKey]`. Commit functions:
    - `commitMaterial(text)`: `const p = parseFloat(text); const safe = Number.isNaN(p) || p < 0 ? 0 : p; setPrice(priceKey, { material: safe })`.
    - `commitLabor(text)`: same shape with `{ labor: safe }`.
    - `commitMarkup(text)`: `const p = parseFloat(text)`; if `Number.isNaN(p)` → treat as "use default" (dispatch `setPrice(priceKey, { markup: DEFAULT_MARKUP_PCT })` so a blank field reverts to 30, NOT 0); else `const safe = p < 0 ? 0 : p; setPrice(priceKey, { markup: safe })`. Seed the markup field from `entry?.markup ?? DEFAULT_MARKUP_PCT` (so an unpriced row shows "30"); seed material/labor from `entry?.material`/`entry?.labor` (blank when 0/absent, mirroring TotalsRow's `seedRateText`).
    Give the three inputs `data-testid="estimate-row-material-input"` / `estimate-row-labor-input` / `estimate-row-markup-input`, `type="text"`, `inputMode="decimal"`, dark COLORS styling. Render read-only cells: Item label, Qty (`formatQuantity`-style: count → rounded int, else 2dp), UoM, and three ₱ read-only cells for Cost/Price/Margin using a `formatMoney(n)` = `${CURRENCY_SYMBOL}${(Number.isFinite(n)?n:0).toFixed(2)}` helper (mirror `formatCost` :68-71) reading `item.cost`/`item.price`/`item.margin`; show Markup as `${entry?.markup ?? DEFAULT_MARKUP_PCT}%` (or read `item.markup`). NO `material*qty` arithmetic in the component — the read-only money cells come straight from the aggregator-computed row. Lay the row out as a CSS grid/flex matching the D-07 column groups.

    Create `EstimateCategoryBlock.tsx` (analog: TotalsCategoryBlock). Props: `{ category: BoqCategoryGroup }`. Copy the heading + collapse via `useUiPanels().collapsedCategories` (reuse the SAME collapse state keyed by category name — matches "preferences follow the workstation"). Map `category.items` → `<EstimateRow item={item} key={item.label} />`. DROP `matchesForRow` and all `onRow*`/`onArmTool` props (:36-62 in the analog) — not needed. After the items, render THREE per-category subtotal cells (Cost/Price/Margin) reading `category.costSubtotal`/`priceSubtotal`/`marginSubtotal`, each guarded non-finite → 0, right-aligned tabular-nums, in a bold secondary row (mirror the :135-157 cost-subtotal chrome, tripled). Give them `data-testid="estimate-category-cost-subtotal"` / `…-price-subtotal` / `…-margin-subtotal`.

    Create `EstimatePanel.tsx` (analog: TotalsPanel, but full-width center-area). `const boq = useBoqLive()`. Render: (1) a grouped column HEADER row (net-new — no analog): `Item · Qty · UoM | Material ₱ · Labor ₱ · Cost ₱ | Markup % · Price ₱ · Margin ₱`, styled with COLORS in the dark palette (a two-tier header or grouped labels per the mockup/D-07); (2) the empty-state tree adapted from TotalsPanel :143-151 ("Open a PDF to begin." / "Place markups to see totals." / "Place markups on a calibrated page…"); (3) `boq.categories.map((cat) => <EstimateCategoryBlock category={cat} key={cat.name} />)` inside a scrollable body; (4) a pinned grand-total bar (mirror :272-294, tripled) with three totals from `boq.grandTotalCost`/`grandTotalPrice`/`grandTotalMargin`, each non-finite-guarded, `data-testid="estimate-grand-total-cost"` / `…-price` / `…-margin`. The outer container fills its parent (flex:1, full width) — NOT the 320px rail chrome. Give the root `data-testid="estimate-panel"`.
  </action>
  <verify>
    <automated>npx vitest run src/tests/estimate-row-edit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 16-VALIDATION proof (d) grid-edit dispatch. `estimate-row-edit.test.ts` goes GREEN: committing the material/labor/markup inputs calls `setPrice('Outlet|count', { material|labor|markup: <parsed> })` with the category-independent `name|type` key; interacting with a cell does NOT bubble to the parent spy (stopPropagation). No `* quantity` / `* qty` arithmetic appears in EstimateRow (`git grep -nE "\\* *(quantity|qty|acc\\.)" src/renderer/src/components/EstimateRow.tsx` returns no matches). `git grep -n "estimate-row-material-input\|estimate-row-labor-input\|estimate-row-markup-input" -- src/renderer/src/components/EstimateRow.tsx` shows all three. `git grep -n "useBoqLive" -- src/renderer/src/components/EstimatePanel.tsx` returns ≥1. `npm run typecheck` is clean.
  </acceptance_criteria>
  <done>EstimateRow has editable material/labor/markup cells (uncontrolled + native listeners + stopPropagation; markup blank → 30 default, material/labor NaN/neg → 0) keyed by `name|type`, dispatching setPrice, plus read-only ₱ Cost/Price/Margin from the row; EstimateCategoryBlock renders three per-category subtotals; EstimatePanel renders the grouped header + category blocks + three grand totals off useBoqLive; estimate-row-edit.test.ts GREEN; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Mount-preserving view swap in App.tsx (canvas + EstimatePanel siblings; totals panel gated to Plan) (proof g)</name>
  <files>src/renderer/src/App.tsx</files>
  <read_first>
    - src/renderer/src/App.tsx (imports :1-31; the center-column div :325-337 [the `totalPages === 0 ? EmptyState : CanvasViewport` swap + the toast stack :338-436]; the `<main>` flex row :311-321; the Splitter :440-448 + TotalsPanel :451-461)
    - src/renderer/src/components/EstimatePanel.tsx (the component created in Task 2 — its root testid + that it fills its parent)
    - src/tests/estimate-view-switch.test.ts (the RED contract — the exact canvas container test-id it queries + the not-remounted / both-present-one-display-none assertion; do NOT modify it)
    - 16-RESEARCH.md Pattern 2 (the exact App.tsx display-toggle JSX) + Pitfall 1; 16-PATTERNS.md "App.tsx mount switch" option A
  </read_first>
  <action>
    Import `EstimatePanel` from `./components/EstimatePanel` and read `const viewMode = useViewerStore((s) => s.viewMode)` (beside the existing viewer selectors :34-38).

    Wrap the center region (:325-337) so BOTH subtrees are always mounted and toggled by CSS `display` (mount-preserving — Pitfall 1). Keep the EXISTING center-column div (CanvasHeaderBar + the canvas/EmptyState swap + the toast stack) exactly as-is, but wrap it in an outer container and add a `display` gate: the Plan subtree gets `style={{ display: viewMode === 'plan' ? 'flex' : 'none', flexDirection: 'column', flex: 1, minWidth: 0, minHeight: 0 }}`; add a SIBLING `<div style={{ display: viewMode === 'estimate' ? 'flex' : 'none', flex: 1, minWidth: 0, minHeight: 0 }}><EstimatePanel /></div>`. Do NOT convert this to a conditional `{viewMode==='plan' ? … : …}` — both must stay in the DOM (the canvas Stage must not remount). The existing `totalPages === 0 ? EmptyState : CanvasViewport` swap stays INSIDE the Plan subtree untouched (that swap is the rare open/close-project remount, which is fine).

    Gate the right-side chrome to Plan view: wrap the `<Splitter … />` (:440-448) and the `<TotalsPanel … />` (:451-461) so they render only when `viewMode === 'plan'` (e.g. `{viewMode === 'plan' && (<><Splitter/><TotalsPanel/></>)}`). In Estimate mode the grid is full-width and the totals panel + splitter are hidden (D-01/A4). Leave the TotalsPanel props unchanged — only the render gate is added.

    Confirm the canvas container carries a stable `data-testid` the view-switch test can query for the "not remounted" assertion — if `CanvasViewport`'s root (or the wrapping `<div style={{ flex:1, position:'relative', minHeight:0 }}>` at :327) has no test-id, add a `data-testid="canvas-viewport-container"` to that wrapping div (a non-functional attribute) so the test can assert the same node persists across the toggle. Do NOT modify CanvasViewport's internals.
  </action>
  <verify>
    <automated>npx vitest run src/tests/estimate-view-switch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 16-VALIDATION proof (g) view-switch preserves canvas. `estimate-view-switch.test.ts` goes GREEN: toggling `viewMode` estimate↔plan keeps the CanvasViewport container as the SAME DOM node (not remounted), OR both the canvas container and `estimate-panel` are present in the DOM simultaneously with exactly one carrying `display: none`. `git grep -n "viewMode === 'estimate'\|viewMode === 'plan'" -- src/renderer/src/App.tsx` shows the display gate on both center subtrees AND the Plan-only gate on Splitter/TotalsPanel. `git grep -n "EstimatePanel" -- src/renderer/src/App.tsx` shows it mounted as a sibling (not inside a `viewMode ? … : …` ternary that would unmount the canvas). `npm run typecheck` clean; full suite has no NEW failures attributable to App.tsx (the totals-panel-quantity-only + writer reds are owned by other plans). MANUAL UAT (SC-1, per 16-VALIDATION Manual-Only): toggle Plan|Estimate in the Estimating tab → the center swaps cleanly; returning to Plan shows the PDF + all markups intact with NO re-rasterization flicker; the totals panel is visible in Plan and hidden in Estimate.
  </acceptance_criteria>
  <done>App.tsx renders the canvas center-column and EstimatePanel as always-mounted siblings toggled by CSS display (Konva Stage never remounts); the Splitter + TotalsPanel are gated to Plan view; the canvas container has a stable test-id; estimate-view-switch.test.ts GREEN; view switch confirmed via manual UAT with no canvas flicker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Renderer-only UI. Every task reads existing in-memory store state (`viewerStore.viewMode`, `projectStore.rates`, the `BoqStructure` from `useBoqLive`) and dispatches the already-defined `setPrice`/`setViewMode` actions. No new IPC, no file I/O, no untrusted input crossing a process boundary. Persistence of the PriceEntry map (schema/serialize coercion) is Wave 1 scope, not this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Tampering | an estimate cell parsing a non-numeric / empty string | mitigate | material/labor: `parseFloat` → NaN/negative clamped to 0 (never writes a string into the PriceEntry map). markup: blank/NaN → dispatches DEFAULT_MARKUP_PCT (30), typed → clamp ≥ 0. The map value shape stays `{ material: number; labor: number; markup: number }`. |
| T-16-03-02 | Elevation of Privilege (UX) | estimate-cell events leaking to a parent handler | mitigate | `e.stopPropagation()` on each cell's onClick/onMouseDown/onKeyDown (native listeners, mirrors the TotalsRow lightbulb/rate pattern); asserted by the estimate-row-edit render test's not-bubbled spy. |
| T-16-03-03 | Denial of Service (UX) | conditional-rendering the canvas on view toggle → repeated Konva Stage teardown + PDF re-rasterize (freeze/flicker) | mitigate | Mount-preserving CSS `display` toggle (Pattern 2) keeps the Stage mounted; both subtrees stay in the DOM. Asserted by the view-switch test (canvas not remounted). This is the D-01 landmine, explicitly avoided. |
| T-16-03-04 | Tampering | price scoped to the wrong key (category-dependent, or including a collision suffix) | mitigate | The price key is `` `${labelToName(item.label)}|${item.type}` `` — reuses the exported `labelToName` to strip the suffix; category-INDEPENDENT; DISTINCT from the visibility `name|categoryId` key. Asserted by the estimate-row-edit test's `setPrice` key spy. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs — uses the existing React/Zustand + Vitest infra (16-RESEARCH "Package Legitimacy Audit — Not applicable"). No legitimacy gate needed. |

No network, auth, or secret surface is added or crossed. No blocking-high threat.
</threat_model>

<verification>
- `npx vitest run src/tests/estimate-row-edit.test.ts src/tests/estimate-view-switch.test.ts` — the two Wave 0 RED proofs this plan owns are GREEN (grid-cell setPrice dispatch + stopPropagation; view-switch keeps the canvas mounted).
- `npx vitest run` — full suite: no NEW regressions from this plan. The totals-panel-quantity-only + writer reds remain (owned by 16-04 / 16-05); the aggregator/serialize/schema/use-boq-live greens from 16-02 stay green.
- `npm run typecheck` (or `npx tsc --noEmit`) clean — `viewMode`/`setViewMode`, the widened `BoqStructure`/`PriceEntry`, `setPrice`, and `DEFAULT_MARKUP_PCT` all resolve against the 16-02 types.
- `git grep -n "viewMode" -- src/renderer/src/lib/project-serialize.ts` returns ZERO (never persisted).
- MANUAL (SC-1/SC-2/SC-3/SC-4, deferred to UAT per 16-VALIDATION Manual-Only): in Estimate view, typing Material/Labor/Markup on a row updates Cost/Price/Margin + the category subtotal + grand totals live; interacting with a cell does not arm a tool or switch pages; the Plan⟷Estimate toggle swaps the center cleanly with the canvas + markups intact on return.
</verification>

<success_criteria>
- A Plan | Estimate segmented toggle in the Estimating ribbon tab swaps the center area; `viewMode` lives in viewerStore, is never serialized, and resets to 'plan' on file open/reset.
- The view switch is mount-preserving (CSS display) — the Konva CanvasViewport is never remounted; the totals panel + splitter are hidden in Estimate view.
- The Estimate grid groups rows by category with editable Material/Labor/Markup cells (uncontrolled + native listeners + stopPropagation; markup blank → 30 default) and read-only ₱ Cost/Price/Margin from the aggregator row.
- Each estimate category shows Cost/Price/Margin subtotals; a pinned grand-total bar shows Cost/Price/Margin (all from `useBoqLive`).
- Editing a cell dispatches `setPrice(`${name}|${type}`, patch)` and recomputes cost/price/margin live; no arithmetic in the grid.
- `estimate-row-edit.test.ts` + `estimate-view-switch.test.ts` GREEN; full suite has no new regressions; typecheck clean.
</success_criteria>

<output>
Create `.planning/phases/16-estimating-workspace/16-03-SUMMARY.md` when done.
</output>
</content>
