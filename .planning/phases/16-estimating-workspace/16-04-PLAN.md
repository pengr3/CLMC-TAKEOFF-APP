---
phase: 16-estimating-workspace
plan: 04
type: execute
wave: 2
depends_on: [16-02]
files_modified:
  - src/renderer/src/components/TotalsRow.tsx
  - src/renderer/src/components/TotalsCategoryBlock.tsx
  - src/renderer/src/components/TotalsPanel.tsx
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "The right-side totals panel shows quantities only — no ₱ rate field, no cost, no cost subtotal, no grand-total-cost bar"
    - "TotalsRow retains quantity, lightbulb visibility toggle, color chip, label, UoM, cycle-nav, and arm-tool"
    - "No totals-row-rate-input / totals-row-cost / totals-category-cost-subtotal / totals-grand-total render node remains in the totals components"
    - "No CURRENCY_SYMBOL import or formatCost helper remains in the three totals components"
  artifacts:
    - path: "src/renderer/src/components/TotalsRow.tsx"
      provides: "Quantity-only row: rate input + cost span + rate machinery removed; visibility/cycle/arm kept"
      contains: "toggleHiddenItem"
    - path: "src/renderer/src/components/TotalsCategoryBlock.tsx"
      provides: "Category block with the per-category ₱ cost-subtotal render removed"
      contains: "totals-category-heading"
    - path: "src/renderer/src/components/TotalsPanel.tsx"
      provides: "Panel with the pinned grand-total ₱ bar removed"
      contains: "totals-panel-categories"
  key_links:
    - from: "src/renderer/src/components/TotalsRow.tsx"
      to: "projectStore (visibility only)"
      via: "toggleHiddenItem(itemKey) retained; setPrice/rates NO LONGER referenced"
      pattern: "toggleHiddenItem"
    - from: "src/tests/totals-panel-quantity-only.test.ts"
      to: "TotalsRow / TotalsCategoryBlock / TotalsPanel"
      via: "asserts pricing render nodes ABSENT, quantity/visibility/chip PRESENT"
      pattern: "queryByTestId"
---

<objective>
Wave 2b — revert the totals panel to quantity-only (D-02). Strip Phase-15 Plan 15-03's inline ₱ rate input + row cost span from `TotalsRow`, the per-category ₱ cost-subtotal from `TotalsCategoryBlock`, and the pinned grand-total ₱ bar from `TotalsPanel`. Pricing now lives ONLY in the Estimate workspace (Wave 2a / 16-03); the right measurement panel returns to showing quantities. This turns the Wave-0 RED `totals-panel-quantity-only` test GREEN.

This is a straight reversal of the render additions from Plan 15-03's commits `1da21b0` (inline rate input + cost span) and `380f762` (cost subtotal + grand-total bar). KEEP everything else in these three components: quantity, lightbulb visibility toggle, color chip, label, UoM, cycle navigation, arm-tool, context menu, empty states, header, rail. KEEP the `currency.ts` module itself (the Estimate grid + the writer's own copy use ₱) and the `useBoqLive` `rates` wiring (Wave 1 widened it — the panel still subscribes for quantities/metadata).

This plan is parallel-safe with Wave 2a (16-03): it touches only `TotalsRow.tsx` / `TotalsCategoryBlock.tsx` / `TotalsPanel.tsx`, which 16-03 does not touch (16-03 owns viewerStore / RibbonToolbar / App.tsx / the new Estimate* components). No `files_modified` overlap.

Output: quantity-only totals components; `16-04-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-estimating-workspace/16-CONTEXT.md
@.planning/phases/16-estimating-workspace/16-RESEARCH.md
@.planning/phases/16-estimating-workspace/16-PATTERNS.md
@.planning/phases/16-estimating-workspace/16-VALIDATION.md

<decisions_locked>
- D-02 / SUPERSEDE: remove Plan 15-03's inline ₱ rate field AND the per-row/subtotal/grand-total cost display from the totals panel. The totals panel is quantity-only. KEEP the PerimeterMarkup length-only render from 15-03 (that is a Phase-15 keeper, NOT part of this revert — and PerimeterMarkup is not in this plan's file set anyway).
- KEEP the `src/renderer/src/lib/currency.ts` module (SP-4 — the Estimate grid imports `CURRENCY_SYMBOL`; the writer keeps its own `NUMFMT_PESO`). Only remove the `CURRENCY_SYMBOL` IMPORT from the three totals components (they no longer render ₱).
- KEEP the `useBoqLive` `rates` subscription (Wave 1 widened `rates` to a PriceEntry map; `useBoqLive` still drives the panel's quantities + metadata). Do NOT touch `useBoqLive.ts` in this plan.
- KEEP in TotalsRow: quantity span, lightbulb visibility toggle (`toggleHiddenItem`, `hiddenItemSet`, `itemKey = name|categoryId`), color chip, label, UoM, cycle-dot, cycle navigation (`handleClick`/`setPage`/`onTriggerPulse`), arm-tool (`onArmTool`), Plus affordance, `labelToName`, `rowTypeToMarkupType`, `formatQuantity`.
- REMOVE from TotalsRow: `CURRENCY_SYMBOL` import; `formatCost`; the `rateKey` memo; the `rate` selector; `rateInputRef` + the seed `useEffect`; `commitRate` + the native-listener `useEffect`; the inline ₱ rate input JSX block; the `totals-row-cost` span. Also drop `setPrice`/`rates` usage (the panel no longer edits prices). Trim now-unused `useRef`/`useEffect` imports if nothing else uses them.
- REMOVE from TotalsCategoryBlock: `CURRENCY_SYMBOL` import; the `totals-category-cost-subtotal` block.
- REMOVE from TotalsPanel: `CURRENCY_SYMBOL` import; the pinned `totals-grand-total` ₱ bar block.
</decisions_locked>

<interfaces>
<!-- After the Wave 1 (16-02) widen, projectStore has setPrice (not setRate) and rates:Record<string,PriceEntry>. -->
<!-- This plan REMOVES the totals-panel's price READ/WRITE entirely — it references neither setPrice nor rates after the revert. -->
<!-- The totals panel keeps reading BoqStructure via useBoqLive for QUANTITIES + metadata (unchanged). -->
<!-- The three render nodes the Wave-0 test asserts ABSENT:  totals-row-rate-input, totals-row-cost, totals-category-cost-subtotal, totals-grand-total -->
<!-- The nodes the test asserts PRESENT:  totals-row-quantity, totals-row-lightbulb, totals-row-color-chip -->
</interfaces>

<read_first>
- src/renderer/src/components/TotalsRow.tsx (whole file — the EXACT removal targets: `CURRENCY_SYMBOL` import :5; `formatCost` :64-71; `rateKey` memo :140-147; `rate` selector :151; `rateInputRef` + seed `useEffect` :153-171; `commitRate` :173-182 + native-listener `useEffect` :184-216; the inline ₱ input JSX :394-434; the `totals-row-cost` span :436-449. KEEP: `useProjectStore` import :10 [still used for `toggleHiddenItem` :335 + `hiddenItemSet` :225], `labelToName` :74-76, `rowTypeToMarkupType` :83-85, `formatQuantity` :59-62, `itemKey` :135-138, `isHidden` :225, `handleClick` :246-273, quantity span :369-380, UoM span :382-392, lightbulb :332-342, color chip :344-355, cycle dot :305-326, Plus affordance :451-458).
- src/renderer/src/components/TotalsCategoryBlock.tsx (whole file — `CURRENCY_SYMBOL` import :4; the `totals-category-cost-subtotal` block :131-157 to remove. KEEP the heading :66-108, the items map :111-129, the `useUiPanels` collapse :47/:50/:72.)
- src/renderer/src/components/TotalsPanel.tsx (whole file — `CURRENCY_SYMBOL` import :5; the pinned `totals-grand-total` bar block :267-294 [`{totalPages > 0 && (…)}`] to remove. KEEP the rail :104-141, header :177-218, metadata header :221, empty-state tree :143-151/:224-240, category list :242-265, context menu :296-313.)
- src/tests/totals-panel-quantity-only.test.ts (the RED contract this plan turns GREEN — the exact `queryByTestId`/`querySelector` ABSENT assertions + the PRESENT survivors; do NOT modify it).
- src/tests/totals-row-cycle.test.ts + src/tests/totals-row-context-menu.test.ts + src/tests/totals-row-hover.test.ts + src/tests/totals-row-visibility.test.ts + src/tests/totals-panel-render.test.ts + src/tests/totals-panel-empty-states.test.ts + src/tests/totals-panel-category-collapse.test.ts (the KEEP-GREEN suites — after the revert these must stay green; if any asserts on a removed pricing node, that specific assertion is a Phase-16 update, but per 16-RESEARCH the Removal Map expects these to be quantity/visibility/cycle tests that survive).
- 16-RESEARCH.md "Removal Map — Totals-panel revert to quantity-only (D-02)" (the file-by-file line refs — the authority for exactly what to strip) + "Tests to update/remove/keep".
- 16-PATTERNS.md "Revert / Remove" §(the line-by-line strip map for all three files) — read together with the RESEARCH removal map.
</read_first>

<interface_context>
Run `npx vitest run src/tests/totals-panel-quantity-only.test.ts` BEFORE editing to confirm the Wave 0 RED state (the pricing nodes still render, so the ABSENT assertions fail). This is the proof this plan turns GREEN. Also run `npx vitest run src/tests/totals-row-cycle.test.ts src/tests/totals-row-visibility.test.ts src/tests/totals-panel-render.test.ts` BEFORE to capture the KEEP-GREEN baseline (they must stay green after the revert).
</interface_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip the inline ₱ rate input + row cost span + rate machinery from TotalsRow (quantity-only)</name>
  <files>src/renderer/src/components/TotalsRow.tsx</files>
  <read_first>
    - src/renderer/src/components/TotalsRow.tsx (whole file — removal targets `CURRENCY_SYMBOL` :5, `formatCost` :64-71, `rateKey` :140-147, `rate` selector :151, `rateInputRef`+seed :153-171, `commitRate`+native-listener :173-216, inline input JSX :394-434, cost span :436-449; KEEP list per <decisions_locked>)
    - src/tests/totals-panel-quantity-only.test.ts (the ABSENT/PRESENT assertions for TotalsRow nodes; do NOT modify)
    - src/tests/totals-row-cycle.test.ts + totals-row-visibility.test.ts + totals-row-context-menu.test.ts + totals-row-hover.test.ts (the KEEP-GREEN row suites)
    - 16-RESEARCH.md Removal Map "TotalsRow.tsx" row + 16-PATTERNS.md "Revert / Remove" TotalsRow table
  </read_first>
  <action>
    Remove, per the Removal Map, from `TotalsRow.tsx`:
    - the `import { CURRENCY_SYMBOL } from '../lib/currency'` line (:5).
    - the `formatCost` helper (:64-71).
    - the `rateKey` useMemo (:140-147).
    - the `rate` top-level selector (`const rate = useProjectStore((s) => s.rates[rateKey] ?? 0)`, :151).
    - the `rateInputRef` declaration + `seedRateText` + the seed `useEffect` (:153-171).
    - the `commitRate` function + the native-listener `useEffect` (:173-216).
    - the inline ₱ rate input JSX block — the wrapping `<div>` with the `CURRENCY_SYMBOL` prefix span + the `<input data-testid="totals-row-rate-input">` (:394-434).
    - the row cost `<span data-testid="totals-row-cost">` (:436-449).
    Then trim imports: if `useRef` and/or `useEffect` are no longer used anywhere else in the file after these removals, remove them from the React import (:1) — check the remaining code (cycle state uses `useState`; there may be no other `useEffect`/`useRef` — verify before trimming). KEEP the `useProjectStore` import (:10) — it is still used for `toggleHiddenItem` (:335) and `hiddenItemSet` (:225). Do NOT remove any of: the cycle dot (:305-326), lightbulb (:332-342), color chip (:344-355), label span (:358-367), quantity span (:369-380), UoM span (:382-392), Plus affordance (:451-458), `handleClick`/cycle nav (:246-273), `labelToName`/`rowTypeToMarkupType`/`formatQuantity`, `itemKey`/`isHidden`. Update the component's leading layout docstring (:12-40) if it enumerates a rate/cost slot → describe the quantity-only slot set.
  </action>
  <verify>
    <automated>npx vitest run src/tests/totals-row-cycle.test.ts src/tests/totals-row-visibility.test.ts src/tests/totals-row-context-menu.test.ts src/tests/totals-row-hover.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 16-VALIDATION proof (e) + (i). `git grep -n "totals-row-rate-input\|totals-row-cost\|formatCost\|CURRENCY_SYMBOL\|commitRate\|rateInputRef\|rateKey" -- src/renderer/src/components/TotalsRow.tsx` returns ZERO matches. `git grep -n "setPrice\|s.rates\[" -- src/renderer/src/components/TotalsRow.tsx` returns ZERO (the panel no longer edits/reads prices). `git grep -n "toggleHiddenItem\|totals-row-quantity\|totals-row-lightbulb\|totals-row-color-chip" -- src/renderer/src/components/TotalsRow.tsx` still returns matches (survivors intact). The KEEP-GREEN row suites (cycle/visibility/context-menu/hover) stay GREEN. `npm run typecheck` is clean.
  </acceptance_criteria>
  <done>TotalsRow renders quantity-only (cycle dot + lightbulb + color chip + label + quantity + UoM + Plus); the rate input, cost span, rate machinery, formatCost, and CURRENCY_SYMBOL import are gone; visibility/cycle/arm survive; the row KEEP-GREEN suites pass; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the per-category ₱ cost-subtotal (TotalsCategoryBlock) and the grand-total ₱ bar (TotalsPanel)</name>
  <files>src/renderer/src/components/TotalsCategoryBlock.tsx, src/renderer/src/components/TotalsPanel.tsx</files>
  <read_first>
    - src/renderer/src/components/TotalsCategoryBlock.tsx (whole file — `CURRENCY_SYMBOL` import :4; the `totals-category-cost-subtotal` block :131-157; KEEP heading :66-108, items map :111-129, `useUiPanels` collapse)
    - src/renderer/src/components/TotalsPanel.tsx (whole file — `CURRENCY_SYMBOL` import :5; the `totals-grand-total` bar :267-294; KEEP rail/header/metadata/empty-state/category-list/context-menu)
    - src/tests/totals-panel-quantity-only.test.ts (the ABSENT assertions for `totals-category-cost-subtotal` + `totals-grand-total`; do NOT modify)
    - src/tests/totals-panel-render.test.ts + totals-panel-empty-states.test.ts + totals-panel-category-collapse.test.ts (KEEP-GREEN panel suites)
    - 16-RESEARCH.md Removal Map "TotalsCategoryBlock.tsx" + "TotalsPanel.tsx" rows + 16-PATTERNS.md "Revert / Remove" tables
  </read_first>
  <action>
    In `TotalsCategoryBlock.tsx`: remove the `import { CURRENCY_SYMBOL } from '../lib/currency'` line (:4). Remove the per-category ₱ cost-subtotal render — the `<div data-testid="totals-category-cost-subtotal">…</div>` block (:135-157) including its inner `<span>`. After removal, the expanded body renders heading + items only (the fragment `<>…</>` at :112-158 collapses to just the items map :113-129 — keep the fragment or simplify to the map, whichever type-checks cleanly). Do NOT touch the heading (:66-108), the collapse logic, or the items map. Update the component docstring (:10-29) if it mentions a cost subtotal → "heading + item rows" only.

    In `TotalsPanel.tsx`: remove the `import { CURRENCY_SYMBOL } from '../lib/currency'` line (:5). Remove the pinned grand-total ₱ bar — the entire `{totalPages > 0 && (<div data-testid="totals-grand-total">…</div>)}` block (:267-294). Do NOT touch the rail (:104-141), the header row (:177-218), the metadata header (`<TotalsPanelHeader boq={boq} />` :221), the empty-state message (:224-240), the category list (:242-265), or the context menu (:296-313). `useBoqLive` (:92) stays (drives quantities/metadata). Update the panel docstring (:15-47) if it describes a "pinned grand-total bar" → the panel is quantity-only now.
  </action>
  <verify>
    <automated>npx vitest run src/tests/totals-panel-quantity-only.test.ts src/tests/totals-panel-render.test.ts src/tests/totals-panel-empty-states.test.ts src/tests/totals-panel-category-collapse.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 16-VALIDATION proof (e). `totals-panel-quantity-only.test.ts` goes GREEN (the `totals-category-cost-subtotal` + `totals-grand-total` ABSENT assertions pass, and the `totals-row-rate-input`/`totals-row-cost` ABSENT assertions from Task 1 pass; the quantity/lightbulb/chip survivors are present). `git grep -n "totals-category-cost-subtotal\|CURRENCY_SYMBOL" -- src/renderer/src/components/TotalsCategoryBlock.tsx` returns ZERO. `git grep -n "totals-grand-total\|CURRENCY_SYMBOL" -- src/renderer/src/components/TotalsPanel.tsx` returns ZERO. The KEEP-GREEN panel suites (render/empty-states/category-collapse) stay GREEN. `npm run typecheck` is clean. MANUAL UAT (SC-1): the right totals panel shows quantities only — no ₱ rate field, no cost, no cost subtotal, no grand-total-cost bar.
  </acceptance_criteria>
  <done>TotalsCategoryBlock renders heading + items only (cost subtotal removed); TotalsPanel renders header + categories + context menu only (grand-total ₱ bar removed); no CURRENCY_SYMBOL import in either; totals-panel-quantity-only.test.ts GREEN and the KEEP-GREEN panel suites pass; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Renderer-only UI removal. This plan deletes render nodes + a store read/write from three components. No new IPC, no file I/O, no untrusted input, no new trust boundary. It reduces surface area (removes the price-write path from the measurement panel). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-01 | Tampering | an incomplete strip leaving a dead pricing node or a dangling `rates`/`setRate` reference (compile break or stale UI) | mitigate | The Removal Map enumerates exact line ranges; the grep gates in the acceptance criteria assert ZERO `totals-row-rate-input`/`totals-row-cost`/`totals-category-cost-subtotal`/`totals-grand-total`/`CURRENCY_SYMBOL`/`setPrice` matches remain in the three files; `npm run typecheck` + the KEEP-GREEN suites catch any dangling reference. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure renderer removal against the existing Vitest infra (16-RESEARCH "Package Legitimacy Audit — Not applicable"). No legitimacy gate needed. |

No network, auth, or secret surface is added or crossed. No blocking-high threat.
</threat_model>

<verification>
- `npx vitest run src/tests/totals-panel-quantity-only.test.ts` — GREEN (the pricing nodes are ABSENT; quantity/visibility/chip survivors PRESENT).
- `npx vitest run src/tests/totals-row-cycle.test.ts src/tests/totals-row-visibility.test.ts src/tests/totals-row-context-menu.test.ts src/tests/totals-row-hover.test.ts src/tests/totals-panel-render.test.ts src/tests/totals-panel-empty-states.test.ts src/tests/totals-panel-category-collapse.test.ts` — all stay GREEN (the KEEP suites; no assertion regressed by the strip).
- `npm run typecheck` (or `npx tsc --noEmit`) clean — no dangling `CURRENCY_SYMBOL`/`formatCost`/`rateKey`/`setPrice`/`rates` reference in the three components.
- `! git grep -n "totals-row-rate-input\|totals-row-cost\|totals-category-cost-subtotal\|totals-grand-total" -- src/renderer/src/components/TotalsRow.tsx src/renderer/src/components/TotalsCategoryBlock.tsx src/renderer/src/components/TotalsPanel.tsx` — returns zero matches (gate FAILS if any pricing node remains).
- MANUAL (SC-1, deferred to UAT per 16-VALIDATION Manual-Only): the right totals panel is quantity-only — no ₱ anywhere.
</verification>

<success_criteria>
- The right-side totals panel shows quantities only — no ₱ rate field, no cost, no cost subtotal, no grand-total-cost bar.
- TotalsRow keeps quantity, lightbulb visibility, color chip, label, UoM, cycle-nav, arm-tool, Plus; the rate machinery + cost span + formatCost + CURRENCY_SYMBOL import are gone; the row no longer reads or writes prices.
- TotalsCategoryBlock renders heading + items only; TotalsPanel renders header + categories + context menu only.
- No `totals-row-rate-input`/`totals-row-cost`/`totals-category-cost-subtotal`/`totals-grand-total` node remains; no CURRENCY_SYMBOL import in the three components.
- `totals-panel-quantity-only.test.ts` GREEN; the seven KEEP-GREEN totals suites stay green; typecheck clean.
</success_criteria>

<output>
Create `.planning/phases/16-estimating-workspace/16-04-SUMMARY.md` when done.
</output>
</content>
