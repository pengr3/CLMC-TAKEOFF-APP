---
phase: 16-estimating-workspace
plan: 05
type: execute
wave: 3
depends_on: [16-02, 16-03]
files_modified:
  - src/main/boq-writers.ts
  - src/renderer/src/components/RibbonToolbar.tsx
  - .planning/PROJECT.md
  - .planning/phases/16-estimating-workspace/16-MANUAL-NOTES.md
autonomous: true
requirements: [SC-5, SC-6]
must_haves:
  truths:
    - "Both writers emit the 9-column BOQ in the locked order: Item · Quantity · UoM · Material · Labor · Cost · Markup · Price · Margin"
    - "xlsx Material/Labor/Cost/Price/Margin cells are NATIVE numbers with a ₱ numFmt (SUM-safe); Markup is a native number with a percent numFmt"
    - "xlsx has per-category Cost/Price/Margin subtotal rows and grand-total Cost/Price/Margin rows; the category heading merge spans A:I"
    - "csv Material/Labor/Cost/Markup/Price/Margin are plain numeric values and the UTF-8 BOM still leads line 1"
    - "The Estimating Settings surface exposes an editable project-wide default markup % (D-05), seeded from DEFAULT_MARKUP_PCT"
    - "PROJECT.md records the estimate model as delivered; 16-MANUAL-NOTES.md documents back-compat, the ₱ constant, and priced-estimate UAT"
  artifacts:
    - path: "src/main/boq-writers.ts"
      provides: "buildBoqXlsx + buildBoqCsv 9-column layout with ₱ numFmt, percent markup, Cost/Price/Margin subtotals + grand totals, A:I merge"
      contains: "Margin"
    - path: ".planning/phases/16-estimating-workspace/16-MANUAL-NOTES.md"
      provides: "Phase-15→16 back-compat + hardcoded ₱ + Estimate-workspace/priced-export UAT documentation"
      contains: "Estimate"
  key_links:
    - from: "src/main/boq-writers.ts (appendItemRow)"
      to: "BoqItemRow.material/labor/cost/markup/price/margin"
      via: "native-number cells + NUMFMT_PESO (money) / NUMFMT_PERCENT (markup)"
      pattern: "NUMFMT_PESO"
    - from: "src/main/boq-writers.ts (subtotal/grand rows)"
      to: "BoqCategoryGroup.{cost,price,margin}Subtotal / BoqStructure.grandTotal{Cost,Price,Margin}"
      via: "triple cost/price/margin subtotal + grand rows"
      pattern: "grandTotalMargin"
---

<objective>
Wave 3 — the writer-boundary + docs. Widen both `buildBoqXlsx` and `buildBoqCsv` from the Phase-15 5-column priced layout to the full 9-column estimate layout (Item · Quantity · UoM · Material · Labor · Cost · Markup · Price · Margin, D-07/D-08), with per-category Cost/Price/Margin subtotals and grand-total Cost/Price/Margin rows; add a minimal editable project-wide default-markup control to the Estimating Settings surface (D-05); then flip PROJECT.md to record the estimate model as delivered and write the phase manual notes. This turns the Wave-0 RED writer tests GREEN and closes the last source seam.

This is the Phase-16 analog of Plan 15-04 (which added the 5-column Rate/Cost export + scope flip + manual notes). The aggregator/types/store (Wave 1) and the Estimate grid (Wave 2a) already populate + display the six money fields + three subtotal/grand kinds — this plan renders them at the file-I/O boundary, adds the Settings default control, and records the closure. It also completes the 4-way type lock by widening the `boq-writers.ts` inline mirror (the 4th copy Wave 1 deliberately left for this plan).

Output: a 9-column priced xlsx + csv (SUM-safe ₱ cells, percent markup, Cost/Price/Margin subtotals + grand totals), an editable Settings default-markup input, a PROJECT.md recording the estimate model as delivered, and a NEW `16-MANUAL-NOTES.md`. Turns GREEN: `boq-writers-xlsx.test.ts`, `boq-writers-csv.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-estimating-workspace/16-CONTEXT.md
@.planning/phases/16-estimating-workspace/16-RESEARCH.md
@.planning/phases/16-estimating-workspace/16-PATTERNS.md
@.planning/phases/16-estimating-workspace/16-VALIDATION.md
@.planning/phases/16-estimating-workspace/16-02-PLAN.md

<decisions_locked>
- COLUMN ORDER (D-07/D-08): **Item · Quantity · UoM · Material · Labor · Cost · Markup · Price · Margin** — exactly this order in both writers' title/header rows and every data row. Material(4)/Labor(5)/Cost(6) are the internal group; Markup(7)/Price(8)/Margin(9) are the client group.
- MONEY CELLS (D-08): xlsx Material/Labor/Cost/Price/Margin cells are NATIVE numbers with `numFmt = NUMFMT_PESO ('₱#,##0.00')`. Markup is a NATIVE number with a percent numFmt (add `NUMFMT_PERCENT = '0"%"'` — a display-only suffix; the stored value is the percent number like 30, NOT 0.30, so do NOT use Excel's `0%` format which multiplies by 100). NEVER a pre-formatted `"₱123.45"` string (breaks SUM). Guard every value with `money()` (non-finite → 0).
- CSV (D-08): Material/Labor/Cost/Markup/Price/Margin are plain numeric values (`Number(x.toFixed(2))`), NO ₱ glyph — same discipline as the existing quantity column. The UTF-8 BOM on line 1 (boq-writers.ts:342) MUST remain. Markup emitted as the plain percent number (e.g. 30).
- SUBTOTALS/GRAND (D-07): Cost/Price/Margin are unit-agnostic single ₱ numbers per category (`costSubtotal`/`priceSubtotal`/`marginSubtotal`) and one project total each (`grandTotalCost`/`grandTotalPrice`/`grandTotalMargin`) — NOT bucketed per-UoM. The existing per-UoM quantity subtotals + grand totals stay (parallel).
- HEADING MERGE (D-08): category-heading merge widens `A:E` → `A:I`. Pad every heading/subtotal/grand `addRow` to 9 cells; metadata single-column rows are exempt.
- 4-WAY TYPE LOCK (SP-9): Wave 1 (16-02) widened `boq-types.ts` + the two preload mirrors to the six money fields + three subtotal/grand kinds but LEFT `boq-writers.ts` on the old `rate`/`cost` shape (it compiles independently; the lock uses an empty STUB_STRUCT). THIS plan widens the `boq-writers.ts` inline `BoqItemRow`/`BoqCategoryGroup`/`BoqStructure` (:18-40) to match — replacing `rate`/`cost` on BoqItemRow with material/labor/markup/materialCost/laborCost/cost/price/margin, `costSubtotal` on BoqCategoryGroup with the three, and `grandTotalCost` on BoqStructure with the three — field-for-field with boq-types.ts (the writer mirror also omits `categoryId` today; do not add it).
- ₱ CONSTANT (SP-4): `boq-writers.ts` keeps its OWN local `NUMFMT_PESO` (:55) — the main process CANNOT import the renderer `CURRENCY_SYMBOL`. Do NOT add a renderer import; do NOT create a third currency seam. Add `NUMFMT_PERCENT` as a sibling local constant.
- SETTINGS DEFAULT MARKUP (D-05/D-09, RESEARCH A5): ship a MINIMAL editable project-wide default-markup % control in the Settings ribbon tab (currently a "Coming soon" stub, RibbonToolbar.tsx:557-569 / renderStubTab). v1 scope: an input seeded from `DEFAULT_MARKUP_PCT` that, when a mutable default is wired, updates the project-wide default. If wiring a persisted mutable default is out of budget, the acceptable v1 minimum is the input present + functional against a renderer-held value — but do NOT claim more than shipped in the docs. Do NOT deep-scope a full Settings framework (D-09 defers it).
- PROJECT.md: record the estimate model (material+labor cost, markup→price+margin, Estimate workspace, 9-column export) as delivered by Phase 16, extending the Phase-15 pricing entry. Keep the Item Library + currency picker explicitly deferred (do NOT claim a currency picker shipped).
</decisions_locked>

<currency_constant_import_path>
`boq-writers.ts` runs in the MAIN process and CANNOT import a renderer constant across the process boundary — the codebase convention is duplication-with-test-lock (like the inline BoqRowType/BoqItemRow mirrors and the local `NUMFMT_DECIMAL`/`NUMFMT_PESO`). Wave 2a's Estimate grid imports `CURRENCY_SYMBOL` renderer-side; the writer keeps its own `NUMFMT_PESO` (already present at :55). ADD a local `NUMFMT_PERCENT` next to it. Do NOT import from renderer; do NOT define a third copy of ₱.
</currency_constant_import_path>

<interfaces>
<!-- Shapes already created by Wave 1 (16-02) — this plan READS them in the writer + widens the writer's own inline mirror to match. -->
<!-- BoqItemRow (boq-types.ts + the 4-way mirror; writer's inline copy is boq-writers.ts:19-27 — WIDEN it here): -->
//   material: number; labor: number; markup: number   // markup PERCENT (30 = 30%)
//   materialCost: number; laborCost: number; cost: number; price: number; margin: number
<!-- BoqCategoryGroup += costSubtotal, priceSubtotal, marginSubtotal   (writer copy boq-writers.ts:29-34) -->
<!-- BoqStructure += grandTotalCost, grandTotalPrice, grandTotalMargin   (writer copy boq-writers.ts:35-40) -->

From src/main/boq-writers.ts (existing local format constants — :45-63; ADD the percent sibling + money col widths):
//   const NUMFMT_DECIMAL = '0.00'
//   const NUMFMT_PESO = '₱#,##0.00'
//   const COL_WIDTH_RATE = 14  /  const COL_WIDTH_COST = 14

From src/main/boq-writers.ts (existing native-number + numFmt discipline — appendItemRow :216-232; each money column mirrors this):
//   const rateCell = r.getCell(4);  rateCell.value = rate;  rateCell.numFmt = NUMFMT_PESO
</interfaces>

<read_first>
- src/main/boq-writers.ts (whole file — already in context). The inline type mirror :18-40 (WIDEN BoqItemRow :19-27 / BoqCategoryGroup :29-34 / BoqStructure :35-40 to match boq-types.ts). Local constants :45-63 (ADD NUMFMT_PERCENT + Material/Labor/Markup/Price/Margin col widths near COL_WIDTH_RATE :59). The xlsx touch-points: column defs :140-146, title row :158, `appendItemRow` :216-240, `appendCostSubtotalRow` :257-267 + the per-UoM `appendSubtotalRow` :243-250, the grand-total-cost block :186-196 + the per-UoM grand loop :175-182, `appendCategoryHeading` A:E merge :208-213. The csv touch-points: header :294, heading push :297, item push :299-308, quantity-subtotal push :310-318, cost-subtotal push :320, grand-quantity push :323-331, grand-cost push :334, BOM return :342.
- src/tests/boq-writers-xlsx.test.ts — the RED contract this plan turns GREEN (9-col title row, Material/Labor/Cost/Price/Margin cells are Numbers with ₱ numFmt, Markup Number with percent numFmt, three subtotal + three grand rows, A:I merge). Read to confirm the exact cell indices/assertions. Do NOT edit it.
- src/tests/boq-writers-csv.test.ts — the RED csv contract (9-col header, the six money columns numeric not "₱.." strings, BOM on line 1). Do NOT edit it.
- src/renderer/src/components/RibbonToolbar.tsx (`renderStubTab('Settings')` usage :584 + the stub renderer :557-569; the `renderActivePanel` switch :571-590; the `renderEstimatingTab` toggle from 16-03 lands here too — DO NOT touch the Estimating tab render fn; only replace/extend the Settings stub; read `useProjectStore`/COLORS imports at the top). NOTE: 16-03 (Wave 2a) also edits RibbonToolbar (the Estimating-tab toggle) but runs in an EARLIER wave — by the time this Wave-3 plan runs, that edit has landed; make the Settings edit additive and do NOT disturb the toggle.
- src/renderer/src/lib/estimate-defaults.ts (DEFAULT_MARKUP_PCT — the seed for the Settings input).
- .planning/PROJECT.md (the Validated block + the Key Decisions table + the Phase-15 pricing entry to extend — read to find the exact current pricing wording added by 15-04).
- .planning/phases/15-boq-pricing-perimeter-simplification/15-MANUAL-NOTES.md (the prior manual-notes shape to mirror for document quality).
- 16-PATTERNS.md "boq-writers.ts" Pattern Assignment (the per-touch-point map) + "default-markup Settings control" (No Analog Found — minimal) + 16-RESEARCH.md "State of the Art" (5-col → 9-col) + Pitfall 2 (mirror lock) + "Runtime State Inventory" (the only migration surface = on-disk Phase-15 scalar rates).
</read_first>

<interface_context>
Run `npx vitest run src/tests/boq-writers-xlsx.test.ts src/tests/boq-writers-csv.test.ts` BEFORE editing to confirm the Wave 0 RED state (the 16-01 assertions reference the 9-column title/header + Material/Labor/Cost/Markup/Price/Margin cells that the writers do not emit yet). This plan turns them GREEN.
</interface_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen both writers to the 9-column estimate layout (₱ money + percent markup + Cost/Price/Margin subtotals + grand totals + A:I merge) + complete the 4-way type lock (proof f, h / SC-6)</name>
  <files>src/main/boq-writers.ts</files>
  <read_first>
    - src/main/boq-writers.ts (the inline mirror :18-40 to widen; constants :45-63; the six xlsx + eight csv touch-points listed in `<read_first>` above; `appendItemRow` :216-240 is the analog for the six native-number money cells; `appendCostSubtotalRow` :257-267 + the grand-cost block :186-196 are the analogs for the Cost/Price/Margin triples)
    - src/tests/boq-writers-xlsx.test.ts + src/tests/boq-writers-csv.test.ts (the exact RED assertions — cell indices, the ₱-in-numFmt + percent-numFmt checks, the BOM check — to satisfy precisely)
    - 16-PATTERNS.md "boq-writers.ts" Pattern Assignment + 16-RESEARCH.md Pitfall 2
  </read_first>
  <action>
    First, complete the 4-way type lock: widen the writer's inline `BoqItemRow` (:19-27) — REPLACE `rate`/`cost` (:25-26) with `material`/`labor`/`markup`/`materialCost`/`laborCost`/`cost`/`price`/`margin` (field-for-field with boq-types.ts; keep the `categoryId` omission). Widen `BoqCategoryGroup` (:29-34) — REPLACE `costSubtotal` (:33) with `costSubtotal`/`priceSubtotal`/`marginSubtotal`. Widen `BoqStructure` (:35-40) — REPLACE `grandTotalCost` (:39) with `grandTotalCost`/`grandTotalPrice`/`grandTotalMargin`. This makes the writer compile against the widened `BoqStructure` the aggregator now produces and closes the structural lock (boq-export-ipc.test.ts).

    Add constants near :55-63: `const NUMFMT_PERCENT = '0"%"'` (a display suffix — the stored value is the percent number 30, NOT 0.30, so do NOT use Excel's `0%` which ×100). Add `COL_WIDTH_MATERIAL`/`COL_WIDTH_LABOR`/`COL_WIDTH_MARKUP`/`COL_WIDTH_PRICE`/`COL_WIDTH_MARGIN` (~14 each) — you may reuse `COL_WIDTH_RATE`/`COL_WIDTH_COST` (:59-60) as the money width or rename; keep it consistent.

    XLSX (buildBoqXlsx) — apply ALL together (move atomically or columns misalign):
    (1) Column defs (:140-146): replace the `rate`/`cost` columns with SIX: `{ key: 'material' }`, `{ key: 'labor' }`, `{ key: 'cost' }`, `{ key: 'markup' }`, `{ key: 'price' }`, `{ key: 'margin' }` (each with its width), after `uom`. Final column count = 9.
    (2) Title row (:158): change to `['Item', 'Quantity', 'UoM', 'Material', 'Labor', 'Cost', 'Markup', 'Price', 'Margin']` (locked order).
    (3) `appendItemRow` (:216-240): push all 9 cells into `addRow([label, quantity, uom, material, labor, cost, markup, price, margin])` (guard each money value with `money()`). Keep the qty cell native-number + `numFmtForUom` (:221-223). Set NATIVE-number cells with numFmt: Material=getCell(4) ₱, Labor=getCell(5) ₱, Cost=getCell(6) ₱, Markup=getCell(7) NUMFMT_PERCENT, Price=getCell(8) ₱, Margin=getCell(9) ₱ (mirror the existing rateCell/costCell pattern :227-232). NEVER pass money through `safeText`. Keep the item-cell color fill on cell 1 (:233-239).
    (4) `appendCategoryHeading` (:208-213): widen the merge from `A${r.number}:E${r.number}` to `A${r.number}:I${r.number}`; pad the heading `addRow` to 9 cells (`[safeText(name), null, null, null, null, null, null, null, null]`).
    (5) Per-category Cost/Price/Margin subtotals: replace the single `appendCostSubtotalRow` call (:171) + helper (:257-267) with THREE rows — Cost, Price, Margin — placing the ₱ value in the matching group column (Cost→col 6, Price→col 8, Margin→col 9). Bold + SUBTOTAL_FILL_ARGB + NUMFMT_PESO on the value cell; pad each row to 9 cells. Read `cat.costSubtotal`/`cat.priceSubtotal`/`cat.marginSubtotal`. Keep the per-UoM quantity `appendSubtotalRow` loop (:168) unchanged (parallel).
    (6) Grand-total Cost/Price/Margin: replace the single grand-total-cost block (:186-196) with THREE grand rows (Cost→col 6, Price→col 8, Margin→col 9) mirroring its shape (bold, GRAND_TOTAL_FILL_ARGB, NUMFMT_PESO, 9 cells) reading `b.grandTotalCost`/`b.grandTotalPrice`/`b.grandTotalMargin`. Keep the per-UoM quantity grand loop (:175-182) unchanged.

    CSV (buildBoqCsv) — mirror the xlsx column changes as raw numerics (no ₱ glyph):
    (7) Header (:294): change to `['Item', 'Quantity', 'UoM', 'Material', 'Labor', 'Cost', 'Markup', 'Price', 'Margin']`.
    (8) Item push (:299-308): replace the rate/cost cells with `Number(money(item.material).toFixed(2))`, `Number(money(item.labor).toFixed(2))`, `Number(money(item.cost).toFixed(2))`, `Number(money(item.markup).toFixed(2))` (plain percent number), `Number(money(item.price).toFixed(2))`, `Number(money(item.margin).toFixed(2))` — 9 cells total (label, quantity, uom, then the six money numbers).
    (9) Category heading push (:297): pad to 9 cells.
    (10) Quantity-subtotal push (:310-318): pad to 9 cells (quantity subtotal in cols 1-3, `''` in cols 4-9); add THREE cost/price/margin subtotal rows (each a label + the value in its money column, `''` elsewhere) reading the three category subtotals.
    (11) Grand push (:323-334): pad the per-UoM grand rows to 9 cells; add THREE grand cost/price/margin rows reading `b.grandTotalCost`/`Price`/`Margin`.
    (12) BOM (:342): leave `return '﻿' + stringify(...)` EXACTLY as-is — the UTF-8 BOM on line 1 keeps ₱/`m²` safe; do NOT remove or move it.
  </action>
  <verify>
    <automated>npx vitest run src/tests/boq-writers-xlsx.test.ts src/tests/boq-writers-csv.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 16-VALIDATION.md proof (f) writer columns + (h) type-lock (SC-6). Both RED writer test files are now GREEN. Specifically:
    - xlsx: the title row is `['Item','Quantity','UoM','Material','Labor','Cost','Markup','Price','Margin']`; Material/Labor/Cost/Price/Margin item cells are NATIVE numbers (typeof === 'number') with a `.numFmt` containing the ₱ glyph; the Markup cell is a native number with the percent numFmt (NOT ₱); per-category Cost/Price/Margin subtotal rows and grand-total Cost/Price/Margin rows exist; the category-heading merge spans `A:I` (not `A:E`).
    - csv: the header row is the 9-column set; the six money columns are numeric values (Number, not `"₱…"`-prefixed strings); line 1 starts with the UTF-8 BOM `﻿` (U+FEFF).
    - `git grep -nE "^\s*rate:|^\s*cost:" -- src/main/boq-writers.ts` shows the writer's inline BoqItemRow no longer has the OLD single `rate` field (`cost:` still appears as one of the new fields, which is correct). `npm run typecheck` is clean (the 4-way lock is complete — boq-export-ipc.test.ts compiles).
  </acceptance_criteria>
  <done>Both writers emit the locked 9-column priced layout; xlsx money cells are native numbers with ₱ numFmt (SUM-safe) + markup native with percent numFmt; Cost/Price/Margin subtotals + grand totals render; A:I merge applied; csv money columns numeric with the BOM intact; the writer inline mirror is widened (4-way lock complete); both writer tests GREEN; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add a minimal editable default-markup % control to the Settings ribbon tab (D-05)</name>
  <files>src/renderer/src/components/RibbonToolbar.tsx</files>
  <read_first>
    - src/renderer/src/components/RibbonToolbar.tsx (`renderStubTab('Settings')` :584 + `renderStubTab` :557-569; the `renderActivePanel` switch :571-590; the component's `useProjectStore`/`useViewerStore`/COLORS imports at the top; the Estimating-tab toggle added by 16-03 — do NOT touch it; the RibbonButton/input styling used elsewhere as the control analog)
    - src/renderer/src/lib/estimate-defaults.ts (DEFAULT_MARKUP_PCT — the seed value)
    - 16-PATTERNS.md "default-markup Settings control" (No Analog Found — minimal) + 16-CONTEXT.md D-05/D-09
  </read_first>
  <action>
    Replace the Settings tab's `renderStubTab('Settings')` (:584 case) with a small `renderSettingsTab()` render function that shows a labeled numeric input for the project-wide default markup %, seeded from `DEFAULT_MARKUP_PCT` (import from `../lib/estimate-defaults`). Give the input `data-testid="settings-default-markup-input"`, `type="text"`, `inputMode="decimal"`, a `%` affordance, and dark COLORS styling (reuse the styling shape of the Estimate/ScalePopup numeric inputs). Label it "Default markup %" with a short helper caption noting it applies to rows with no explicit markup.

    Scope (D-05/D-09 — minimal): the required v1 deliverable is that the control EXISTS, is functional (accepts a numeric value; parseFloat NaN/negative → clamp), and is wired to the project-wide default seam. If a persisted mutable default (surviving save/reload) is within budget, wire it (e.g. a `defaultMarkup` field alongside `rates` in projectStore + a `setDefaultMarkup` action + serialize it additively like `rates` — NO formatVersion bump — and have the aggregator read `entry?.markup ?? state.defaultMarkup ?? DEFAULT_MARKUP_PCT`). If that is out of budget for this plan, ship the input seeded from and displaying `DEFAULT_MARKUP_PCT` with an in-session-only setter, and record the persistence deferral honestly in Task 3's manual notes. Do NOT deep-scope a full Settings framework; do NOT touch the Estimating-tab toggle; do NOT change any other tab.

    Add the new case to the `renderActivePanel` switch: change `case 'settings': return renderStubTab('Settings')` → `return renderSettingsTab()`. Keep the `help` stub unchanged.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    `git grep -n "settings-default-markup-input" -- src/renderer/src/components/RibbonToolbar.tsx` returns ≥1 (the control exists). `git grep -n "DEFAULT_MARKUP_PCT" -- src/renderer/src/components/RibbonToolbar.tsx` shows the seed import is used. The Settings tab no longer renders the "Settings — Coming soon" stub (`git grep -n "renderStubTab('Settings')" -- src/renderer/src/components/RibbonToolbar.tsx` returns ZERO). `npm run typecheck` is clean. The full Vitest suite has no new failures from this change (the Estimating-tab toggle tests from 16-03 stay green). MANUAL UAT (D-05): the Settings tab shows an editable default markup % field seeded at 30; typing a value updates the project-wide default (or, if persistence deferred, the in-session default) used for rows with no explicit markup.
  </acceptance_criteria>
  <done>The Settings ribbon tab exposes an editable default-markup % input (data-testid=settings-default-markup-input) seeded from DEFAULT_MARKUP_PCT, replacing the Coming-soon stub, without disturbing the Estimating-tab toggle; typecheck clean; no suite regression; the persistence scope (wired vs in-session) is recorded for the manual notes.</done>
</task>

<task type="auto">
  <name>Task 3: Flip PROJECT.md to record the estimate model as delivered + write 16-MANUAL-NOTES.md (SC-5/SC-6)</name>
  <files>.planning/PROJECT.md, .planning/phases/16-estimating-workspace/16-MANUAL-NOTES.md</files>
  <read_first>
    - .planning/PROJECT.md (the `### Validated` block + the Key Decisions table + the exact Phase-15 pricing entry added by 15-04 — the entry to extend, not duplicate)
    - .planning/phases/16-estimating-workspace/16-CONTEXT.md (D-01..D-09 — the delivered scope; D-06 back-compat; D-09 deferrals: price-book/item library/equipment/overhead/separate unit-price column)
    - .planning/phases/15-boq-pricing-perimeter-simplification/15-MANUAL-NOTES.md (the prior manual-notes shape to mirror for document quality)
    - 16-RESEARCH.md "Runtime State Inventory" (the ONLY migration surface = on-disk Phase-15 `.clmc` scalar rates → coerced on load) + "State of the Art"
  </read_first>
  <action>
    Edit PROJECT.md (extend, do NOT duplicate the Phase-15 entry):
    (1) Update the `### Validated` pricing entry to note the Phase-16 expansion: material + labor internal cost, a default-30% markup → client price + margin, a dedicated Estimate workspace (Plan | Estimate toggle), the right totals panel reverted to quantity-only, and the 9-column xlsx/csv export (Item · Qty · UoM · Material · Labor · Cost · Markup · Price · Margin) — "Validated in Phase 16: Estimating Workspace" (keep the Phase-15 reference intact; add Phase 16 as the superseding/expanding milestone).
    (2) Add a Key Decisions row: Decision = "Estimating workspace (internal cost + client price/margin)"; Rationale = "Pricing separated from measurement per industry practice (PlanSwift/STACK); single rate → material+labor+markup"; Outcome = "Delivered in Phase 16 (dedicated Estimate sheet + 9-column export; price-book / item library / equipment / overhead deferred per D-09)".
    Keep the ₱ currency picker + Item Library explicitly deferred (do NOT claim a currency picker shipped). Do NOT modify unrelated scope bullets or the Active list.

    Create `.planning/phases/16-estimating-workspace/16-MANUAL-NOTES.md` (document-quality — the project has a standing how-to-manual requirement). Include these sections:
    Section 1 — Phase-15 → Phase-16 pricing back-compat (no data loss). State: a `.clmc` saved by Phase 15 carries a single rate per `name|type` (`rates: Record<string, number>`). On reload in Phase 16 it coerces to `{ material: <that rate>, labor: 0, markup: 30 }` — the old rate becomes the MATERIAL rate, labor starts at 0, markup defaults to 30%. No data is lost, no `formatVersion` bump, no migration prompt (additive field, rides the validateV2 cast; a missing `rates` key loads as `{}`). Older pre-Phase-15 files (no rates) load with every material/labor 0 and markup 30.
    Section 2 — The Estimate workspace + how pricing moved off the Plan. State: pricing now lives ONLY in the Estimating tab's `Plan | Estimate` toggle → a full-width Estimate sheet; the Plan canvas and the right totals panel are quantity-only (the Phase-15 inline rate field is removed). Each estimate row has editable Material/Labor/Markup cells → live Cost (= material×qty + labor×qty), Price (= cost × (1 + markup/100)), Margin (= price − cost); category subtotals + a grand-total bar show Cost/Price/Margin. Note markup is a PERCENT (30 = 30%). Note the view switch is mount-preserving (the PDF/markups stay intact when toggling).
    Section 3 — ₱ currency is a hardcoded constant + the default-markup Settings control's scope. State: ₱ (Philippine Peso) is hardcoded (renderer `src/renderer/src/lib/currency.ts` + the writer's own `NUMFMT_PESO` in `src/main/boq-writers.ts`) — no currency picker yet (deferred). The project-wide default markup (30%) lives in `src/renderer/src/lib/estimate-defaults.ts` (`DEFAULT_MARKUP_PCT`) and is surfaced as an editable field in the Settings tab — RECORD HONESTLY whether Task 2 wired it to persist across save/reload or shipped it in-session-only. Also note the D-09 deferrals: price-book / cost catalog, reusable items & assemblies, equipment cost, per-project overhead, and a separate client-facing unit-price column.
    Section 4 — Manual UAT checklist for the estimate + 9-column export. Numbered steps: (a) load a PDF, calibrate, place a few markups; (b) open the Estimating tab → click Estimate → confirm the full-width grid with the grouped columns; (c) type Material + Labor + Markup on a row → confirm Cost/Price/Margin + the category subtotal + grand totals update live, and that a blank markup falls back to 30%; (d) toggle back to Plan → confirm the PDF + markups are intact with no flicker and the totals panel is quantity-only; (e) export xlsx → open in Excel → confirm the 9 columns, ₱ on Material/Labor/Cost/Price/Margin, Markup as a percent, and that `SUM()` works on a money column (native numbers), plus per-category Cost/Price/Margin subtotals + grand totals; (f) export csv → confirm the six money columns are plain numbers and ₱ text isn't mojibaked (BOM). Cross-reference 16-VALIDATION.md "Manual-Only Verifications".
  </action>
  <verify>
    <automated>test -f .planning/phases/16-estimating-workspace/16-MANUAL-NOTES.md && grep -qi "Estimate" .planning/phases/16-estimating-workspace/16-MANUAL-NOTES.md && grep -q "Material" .planning/phases/16-estimating-workspace/16-MANUAL-NOTES.md && grep -q "Margin" .planning/phases/16-estimating-workspace/16-MANUAL-NOTES.md && grep -qi "Phase 15" .planning/phases/16-estimating-workspace/16-MANUAL-NOTES.md && ! grep -q "pricing happens in separate tools" .planning/PROJECT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    `.planning/phases/16-estimating-workspace/16-MANUAL-NOTES.md` exists and contains all four sections: (1) Phase-15→16 back-compat explicitly stating the legacy rate → material / labor 0 / markup 30 coercion with no data loss; (2) the Estimate workspace + pricing-moved-off-the-Plan description; (3) the hardcoded ₱ constant + the Settings default-markup scope (wired vs in-session, recorded honestly); (4) a numbered estimate + 9-column-export UAT checklist mentioning Material/Labor/Markup/Margin, ₱, and Excel/SUM. PROJECT.md's `### Validated` pricing entry references Phase 16 (the estimate model), a Key-Decision row for the estimating workspace exists, and no currency-picker claim is added. The automated guard passes (`OK`).
  </acceptance_criteria>
  <done>A manual-ready notes file documents the Phase-15→16 back-compat (no data loss), the Estimate workspace, the hardcoded ₱ + Settings default-markup scope, and a numbered estimate/9-column-export UAT checklist; PROJECT.md records the estimate model as delivered by Phase 16 with a Key-Decision row; the content guard passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds columns to existing export writers, a Settings input, and edits planning docs. No new trust boundary is added or crossed; the writers already run in the trusted main process on a `BoqStructure` produced in-process. The Settings input writes a parsed number into a renderer store field (or in-session state) — the same class as the Estimate-grid cell inputs (Wave 2a). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05-01 | Tampering | ₱/percent money written into a spreadsheet cell | accept | Money/markup are written as cell DATA (a native number + a `numFmt`), never as a leading `=`/`+`/`-`/`@` formula. `safeText` (boq-writers.ts) is applied ONLY to label strings; numbers bypass it correctly (numbers can't trigger formula execution). No CSV-injection / formula-evaluation risk introduced. |
| T-16-05-02 | Tampering | xlsx money cell stored as a pre-formatted string (breaks SUM) | mitigate | Material/Labor/Cost/Price/Margin cells are kept NATIVE numbers with a display `numFmt` (₱); Markup is a native number with a percent numFmt; enforced by the Wave-0 xlsx test asserting `.value` is a Number + the numFmt content. `money()` guards non-finite → 0. |
| T-16-05-03 | Information disclosure | csv ₱ mojibake leaking a wrong currency/value | accept | The UTF-8 BOM already on line 1 (boq-writers.ts:342) is preserved unchanged; ₱ (U+20B1) stays correctly encoded for Excel-on-Windows. No change to the BOM, no new disclosure surface. |
| T-16-05-04 | Tampering | Settings default-markup input parsing a non-numeric string | mitigate | parseFloat → NaN/negative clamped (mirrors the Estimate-grid cell commit); the input never writes a string into the default-markup value. If wired to a persisted `defaultMarkup`, it rides the same additive serialize path (no formatVersion bump) as `rates`. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — uses the existing `exceljs`/`csv-stringify` + React/Zustand already in the repo (16-RESEARCH "Package Legitimacy Audit — Not applicable"). No legitimacy gate needed. |

**Block-on-high:** none. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages and no high-severity unmitigated threat — no blocking human checkpoint required.
</threat_model>

<verification>
- `npx vitest run src/tests/boq-writers-xlsx.test.ts src/tests/boq-writers-csv.test.ts` — both GREEN (9-col order, ₱ numFmt on native-number money cells, percent numFmt on Markup, Cost/Price/Margin subtotal + grand rows, A:I merge; csv 9-col numeric + BOM on line 1).
- `npx vitest run` — FULL suite green (this is the last source plan — the aggregator/serialize/schema/use-boq-live greens from 16-02, the estimate-grid/view-switch greens from 16-03, and the totals-panel-quantity-only green from 16-04 must all coexist with the writer greens here). No RED should remain after this plan.
- `npm run typecheck` (or `npx tsc --noEmit`) clean — the writer-side `BoqItemRow`/`BoqCategoryGroup`/`BoqStructure` mirror carries the six money fields + three subtotal/grand kinds (4-way lock complete); `DEFAULT_MARKUP_PCT` resolves in RibbonToolbar.
- `npm run build` — succeeds (main + renderer bundle).
- `git grep -n "pricing happens in separate tools" -- .planning/PROJECT.md` returns ZERO; a Phase-16 Validated pricing entry + a Key-Decision estimating-workspace row exist.
- `16-MANUAL-NOTES.md` exists with the four required sections and passes the content guard.
- MANUAL (SC-6, deferred to UAT per 16-VALIDATION Manual-Only): xlsx opens in Excel with the 9 columns, ₱ money + percent markup, working SUM on a money column, and Cost/Price/Margin subtotals + grand totals; csv opens clean with numeric money columns.
</verification>

<success_criteria>
- Both writers emit the locked 9-column priced BOQ (Item · Quantity · UoM · Material · Labor · Cost · Markup · Price · Margin) with per-category Cost/Price/Margin subtotals and grand-total Cost/Price/Margin rows.
- xlsx Material/Labor/Cost/Price/Margin are native numbers with the ₱ numFmt (SUM-safe); Markup is a native number with a percent numFmt; the category heading merges A:I.
- csv money columns are plain numeric values and the UTF-8 BOM still leads line 1.
- The Estimating Settings surface exposes an editable project-wide default markup % (seeded from DEFAULT_MARKUP_PCT), replacing the Coming-soon stub.
- `boq-writers-xlsx.test.ts` + `boq-writers-csv.test.ts` GREEN; the FULL suite is green; typecheck clean; build succeeds.
- PROJECT.md records the estimate model as delivered by Phase 16 (with a Key-Decision row); `16-MANUAL-NOTES.md` documents back-compat, the ₱ constant + Settings default scope, and an estimate/9-column-export UAT checklist.
</success_criteria>

<output>
Create `.planning/phases/16-estimating-workspace/16-05-SUMMARY.md` when done.
</output>
</content>
