---
phase: 16-estimating-workspace
plan: 02
type: execute
wave: 1
depends_on: [16-01]
files_modified:
  - src/renderer/src/lib/estimate-defaults.ts
  - src/renderer/src/stores/projectStore.ts
  - src/renderer/src/lib/project-schema.ts
  - src/renderer/src/lib/project-serialize.ts
  - src/renderer/src/lib/boq-types.ts
  - src/renderer/src/lib/boq-aggregator.ts
  - src/renderer/src/hooks/useBoqLive.ts
  - src/preload/index.ts
  - src/preload/index.d.ts
autonomous: true
requirements: [SC-2, SC-3, SC-4, SC-5, SC-6]
must_haves:
  truths:
    - "Pricing is stored per name|type as PriceEntry {material,labor,markup}; markup is a percent (30 = 30%)"
    - "A material-only edit preserves labor and markup (setPrice merges); a brand-new entry seeds markup 30"
    - "Each BOQ row carries material, labor, markup, materialCost, laborCost, cost, price, margin"
    - "cost = material×qty + labor×qty; price = cost×(1+markup/100); margin = price−cost; absent entry → material/labor 0, markup 30"
    - "Each category exposes costSubtotal/priceSubtotal/marginSubtotal; the structure exposes grandTotalCost/grandTotalPrice/grandTotalMargin"
    - "A Phase-15 scalar rates map loads (coerced material=n, labor=0, markup=30) with no formatVersion bump and no error"
    - "useBoqLive recomputes cost/price/margin live when the PriceEntry map changes"
  artifacts:
    - path: "src/renderer/src/lib/estimate-defaults.ts"
      provides: "DEFAULT_MARKUP_PCT = 30 constant seam consumed by aggregator + hydrate + (Wave 2) grid"
      contains: "DEFAULT_MARKUP_PCT"
    - path: "src/renderer/src/stores/projectStore.ts"
      provides: "rates: Record<string, PriceEntry> + setPrice(key, patch) merging + markDirty; reset to {}"
      contains: "setPrice"
    - path: "src/renderer/src/lib/boq-aggregator.ts"
      provides: "material/labor/materialCost/laborCost/cost/price/margin per row; three subtotal kinds; three grand totals"
      contains: "grandTotalMargin"
    - path: "src/renderer/src/lib/boq-types.ts"
      provides: "PriceEntry type; widened BoqItemRow/BoqCategoryGroup/BoqStructure/AggregateOptions"
      contains: "PriceEntry"
    - path: "src/renderer/src/lib/project-serialize.ts"
      provides: "snapshot emits PriceEntry map; hydrate coerces legacy scalar → PriceEntry + per-field finite-≥0 guard + missing-markup→30"
      contains: "DEFAULT_MARKUP_PCT"
  key_links:
    - from: "src/renderer/src/lib/boq-aggregator.ts"
      to: "useProjectStore rates PriceEntry map"
      via: "opts.rates ?? useProjectStore.getState().rates, lookup by `${name}|${type}`, markup ?? DEFAULT_MARKUP_PCT"
      pattern: "material \\* acc.quantity"
    - from: "src/renderer/src/lib/project-serialize.ts"
      to: "projectStore.rates"
      via: "snapshot read + hydrate coercion (legacy scalar branch + object per-field branch)"
      pattern: "DEFAULT_MARKUP_PCT"
    - from: "src/renderer/src/hooks/useBoqLive.ts"
      to: "aggregateBoq rates option"
      via: "rates selector already threaded — value type widens automatically; deps unchanged"
      pattern: "rates"
---

<objective>
Wave 1 — the estimate data-model spine. Widen the stored per-`name|type` value from a scalar `number` to `PriceEntry = { material, labor, markup }`, replace `setRate` with a merging `setPrice`, extend `boq-aggregator` to compute material/labor/cost/price/margin per row plus Cost/Price/Margin subtotals and grand totals, coerce legacy Phase-15 scalar rates on load, and mirror the widened `BoqItemRow`/`BoqCategoryGroup`/`BoqStructure` into the two preload files (the 4-way type lock). This turns the aggregator / serialize / schema RED tests from Plan 16-01 GREEN.

Purpose: every downstream wave depends on these shapes. The writers (Wave 3) read the six money fields + three subtotals + three grand totals; the Estimate grid (Wave 2a) reads them via `useBoqLive` and commits edits via `setPrice`. This plan defines the contracts so later waves implement against them — no codebase scavenger hunt. This is the Phase-16 analog of Plan 15-02 (which added the single-rate version end-to-end).

Output: NEW `estimate-defaults.ts`; updated `projectStore.ts`, `project-schema.ts`, `project-serialize.ts`, `boq-types.ts`, `boq-aggregator.ts`, `useBoqLive.ts`, and the two preload mirrors. After this plan: `boq-aggregator.test.ts`, `project-serialize.test.ts`, `project-schema.test.ts`, and the `use-boq-live` "recomputes when rates change" case are GREEN; the estimate-grid / totals-panel-quantity-only / view-switch / writer tests stay RED until Waves 2/3 (expected).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-estimating-workspace/16-CONTEXT.md
@.planning/phases/16-estimating-workspace/16-RESEARCH.md
@.planning/phases/16-estimating-workspace/16-PATTERNS.md
@.planning/phases/16-estimating-workspace/16-VALIDATION.md
@.planning/phases/16-estimating-workspace/16-01-PLAN.md

<locked_contract>
<!-- Fixed by 16-01 + CONTEXT + RESEARCH. Every wave obeys this verbatim. -->
- `PriceEntry = { material: number; labor: number; markup: number }` — `markup` is a PERCENT (30 = 30%; A1 locked). `rates?: Record<string, PriceEntry>` additive on `ProjectFileV2`, key `` `${name}|${type}` `` (category-INDEPENDENT). NO formatVersion bump. Mirror the Phase-15 `rates` additive precedent exactly (rides `return raw as ProjectFileV2`).
- projectStore: `rates: Record<string, PriceEntry>` state (the field keeps the name `rates`). Replace `setRate(key, rate)` with `setPrice(key, patch: Partial<PriceEntry>)` that MERGES into the existing entry (`{ ...cur, ...patch }`), seeding a brand-new entry as `{ material: 0, labor: 0, markup: DEFAULT_MARKUP_PCT }`, then calls `get().markDirty()` (load-bearing — mirrors `toggleHiddenItem`). `reset()` and initial state keep `rates: {}` (empty map is shape-agnostic).
- `DEFAULT_MARKUP_PCT = 30` lives in a NEW `src/renderer/src/lib/estimate-defaults.ts` (parallel to `currency.ts`). Consumed in exactly: (a) `setPrice` new-entry seed, (b) the aggregator's absent-markup read, (c) the hydrate coercion missing-markup default. (Wave 2a's grid markup-cell seed also imports it.)
- BoqItemRow: replace `rate`/`cost` with `material`, `labor`, `markup`, `materialCost`, `laborCost`, `cost`, `price`, `margin`. BoqCategoryGroup: replace `costSubtotal` with `costSubtotal`/`priceSubtotal`/`marginSubtotal`. BoqStructure: replace `grandTotalCost` with `grandTotalCost`/`grandTotalPrice`/`grandTotalMargin`. AggregateOptions: widen `rates?: Record<string, number>` → `Record<string, PriceEntry>`. Keep `categoryId?` on BoqItemRow.
- MONEY MATH: `const entry = rates[`${name}|${type}`]`; `material = entry?.material ?? 0`; `labor = entry?.labor ?? 0`; `markup = entry?.markup ?? DEFAULT_MARKUP_PCT` (absent entry → default; an entry WITH `markup: 0` uses 0); `materialCost = material × qty`; `laborCost = labor × qty`; `cost = materialCost + laborCost`; `price = cost × (1 + markup/100)`; `margin = price − cost`. `costSubtotal`/`priceSubtotal`/`marginSubtotal` = Σ over category rows; `grandTotalCost`/`grandTotalPrice`/`grandTotalMargin` = Σ over categories. All unit-agnostic single numbers, PARALLEL to (not bucketed by) the per-UoM quantity subtotals.
- 4 type-duplication files: `boq-types.ts` (canonical), `preload/index.ts`, `preload/index.d.ts`, `boq-writers.ts`. **boq-writers.ts is NOT in this plan's scope — Wave 3 (16-05) owns it; do NOT touch it.** This plan touches the other three. Preload mirrors OMIT `categoryId` (preserve that) and `preload/index.ts` keeps its pre-existing shape drift — do not "fix" drift, only add the new fields. Apply the field widening to all three in-scope files atomically.
- Perimeter behavior is UNCHANGED this phase — do NOT touch the perimeter length path (:148-161), the collision/suffix logic, `uomFor`, or `typeWord` in the aggregator. Phase-14 arc-aware length and Phase-15 one-row perimeter must NOT regress.
</locked_contract>

<interfaces>
<!-- The SHAPES this plan creates. Downstream waves assert/build against these. -->

estimate-defaults.ts (NEW):
//   export const DEFAULT_MARKUP_PCT = 30   // percent; a row with no PriceEntry uses this

projectStore (src/renderer/src/stores/projectStore.ts) — widen the existing rates field:
//   rates: Record<string, PriceEntry>              // was Record<string, number>
//   setPrice: (key, patch: Partial<PriceEntry>) => void
//     set((s) => { const cur = s.rates[key] ?? { material:0, labor:0, markup: DEFAULT_MARKUP_PCT }
//                  return { rates: { ...s.rates, [key]: { ...cur, ...patch } } } }); get().markDirty()

boq-types.ts:
//   interface PriceEntry { material: number; labor: number; markup: number }   // markup PERCENT
//   BoqItemRow += material, labor, markup, materialCost, laborCost, cost, price, margin   (rate/cost REMOVED)
//   BoqCategoryGroup += costSubtotal, priceSubtotal, marginSubtotal   (single costSubtotal REPLACED)
//   BoqStructure += grandTotalCost, grandTotalPrice, grandTotalMargin   (single grandTotalCost REPLACED)
//   AggregateOptions.rates?: Record<string, PriceEntry>   // default useProjectStore.getState().rates

aggregator row build (src/renderer/src/lib/boq-aggregator.ts:219-231 replacement):
//   const entry = rates[`${name}|${type}`]
//   const material = entry?.material ?? 0; const labor = entry?.labor ?? 0
//   const markup = entry?.markup ?? DEFAULT_MARKUP_PCT
//   const materialCost = material * acc.quantity; const laborCost = labor * acc.quantity
//   const cost = materialCost + laborCost; const price = cost * (1 + markup/100); const margin = price - cost
</interfaces>

<read_first>
- src/renderer/src/stores/projectStore.ts (whole file — the `rates: Record<string, number>` field + docstring :15-20/:20, initial `rates: {}` :68, `reset()` rates :93, `setRate` :111-114 incl. `get().markDirty()`, the `setRate` interface decl :36-41; `attachDirtyTracking` :135-161 does NOT subscribe to projectStore — do not add a rates subscription).
- src/renderer/src/lib/currency.ts (the one-line constant-module shape to mirror for estimate-defaults.ts — a single `export const`).
- src/renderer/src/lib/project-schema.ts (`rates?: Record<string, number>` on ProjectFileV2 :88-95 with its JSDoc; `validateV2` :100-127 ends `return raw as ProjectFileV2` :126 with NO rates branch — keep it that way, no formatVersion bump).
- src/renderer/src/lib/project-serialize.ts (snapshot `rates` read :61; hydrate `safeRates` scalar guard :119-124 — THE function to widen; the `setState` :129-133 inside the `suspendDirtyTracking`/`resumeDirtyTracking` bracket :75-139 with the final `isDirty:false` reset :138).
- src/renderer/src/lib/boq-types.ts (whole file — `BoqRowType` :23-28 UNCHANGED; `BoqItemRow` :36-59 with `rate` :44 / `cost` :46 to replace + `categoryId?` :58 to keep; `BoqCategoryGroup` :68-77 with `costSubtotal` :76 to replace; `BoqStructure` :84-92 with `grandTotalCost` :91 to replace; `AggregateOptions.rates?` :110 to widen).
- src/renderer/src/lib/boq-aggregator.ts (whole file — the `rates` default :95, the row-build push :219-231 [`rate`/`cost` at :219-220, the `items.push` :221-230], the per-category accumulator `catCost` :239-245 + category push `costSubtotal: catCost` :255, the `grandCost` accumulator :185/:245 + return `grandTotalCost: grandCost` :284; DO NOT touch `uomFor` :35-40, `typeWord` :42-48, the perimeter path :148-161, the collision build :195-204, the label loop :210-216).
- src/renderer/src/hooks/useBoqLive.ts (whole file — the `rates` selector :42 already exists + is already in the deps :58 + already passed to aggregateBoq :55; the value type widens automatically. Update ONLY the leading docstring if it describes the scalar shape.)
- src/preload/index.ts (the inline `BoqRowType` :16, `BoqItemRow` :24-32 with `rate` :28 / `cost` :29 to replace [omits `categoryId` — keep omitted], `BoqCategoryGroup` :34-39 with `costSubtotal` :38, `BoqStructure` :40-45 with `grandTotalCost` :44).
- src/preload/index.d.ts (the inline `BoqRowType` :6, `BoqItemRow` :14-22 with `rate` :19 / `cost` :20 [omits `categoryId` — keep omitted], `BoqCategoryGroup` :24-29 with `costSubtotal` :28, `BoqStructure` :30-35 with `grandTotalCost` :34).
- 16-PATTERNS.md SP-1 (additive field, no bump), SP-2 (hydrate coercion — the legacy-scalar + object per-field branches), SP-3 (setPrice mirrors setRate incl. markDirty), SP-8 (aggregator six-field expansion), SP-9 (4-way type lock) — the line-cited excerpts for each edit.
- 16-RESEARCH.md Code Examples ("PriceEntry type + widened map", "Legacy scalar → PriceEntry coercion", "Aggregator money math") + Pitfalls 2 (mirror drift), 4 (name|type key), 5 (markup default).
</read_first>

<interface_context>
Run `npx vitest run src/tests/boq-aggregator.test.ts src/tests/project-serialize.test.ts src/tests/project-schema.test.ts src/tests/use-boq-live.test.ts` BEFORE editing to confirm these are currently RED (the 16-01 assertions reference `material`/`labor`/`markup`/`price`/`margin`/PriceEntry and the three-kind subtotals that do not exist yet). This plan turns them GREEN. Do NOT run a global suite expecting all-green — estimate-grid / totals-panel-quantity-only / view-switch / writer tests stay RED until Waves 2/3.
</interface_context>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DEFAULT_MARKUP_PCT seam + widen rates to PriceEntry in projectStore + schema + serialize (setPrice merge, legacy coercion)</name>
  <files>src/renderer/src/lib/estimate-defaults.ts, src/renderer/src/stores/projectStore.ts, src/renderer/src/lib/project-schema.ts, src/renderer/src/lib/project-serialize.ts</files>
  <read_first>
    - src/renderer/src/lib/currency.ts (the single-`export const` module shape to mirror)
    - src/renderer/src/stores/projectStore.ts (rates field + docstring :15-20, initial :68, reset :93, setRate :111-114, interface decl :36-41)
    - src/renderer/src/lib/project-schema.ts (rates? :88-95 + JSDoc; validateV2 :100-127)
    - src/renderer/src/lib/project-serialize.ts (snapshot :61; hydrate safeRates guard :119-124; setState :129-133; suspend/resume bracket :75-139)
    - 16-PATTERNS.md SP-1/SP-2/SP-3 + 16-RESEARCH.md "PriceEntry type + widened map" + "Legacy scalar → PriceEntry coercion" Code Examples
  </read_first>
  <behavior>
    - Round-trip: snapshot reads `useProjectStore.getState().rates`; hydrate restores it; a `rates: { 'Skirting|perimeter': { material: 12, labor: 4, markup: 25 } }` survives snapshot→JSON→hydrate deep-equal (project-serialize.test.ts).
    - Legacy scalar: hydrate of `{ rates: { 'X|count': 50 } }` → `rates['X|count']` deep-equals `{ material: 50, labor: 0, markup: 30 }` (project-serialize.test.ts).
    - Missing markup: hydrate of `{ rates: { 'Y|count': { material: 3, labor: 1 } } }` → `rates['Y|count'].markup === 30` (project-serialize.test.ts).
    - Per-field coercion: hydrate of `{ rates: { 'A|count': { material: -5, labor: 2, markup: 30 }, 'B|count': { material: 'x', labor: 1, markup: 30 }, 'C|count': { material: 10, labor: 3, markup: 30 } } }` → invalid material coerced to 0 (`A.material===0`, `B.material===0`), valid survives (`C.material===10`), no throw (project-serialize.test.ts).
    - Merge semantics: `setPrice('K', { material: 5 })` on an empty map creates `{ material:5, labor:0, markup:30 }`; a subsequent `setPrice('K', { labor: 2 })` yields `{ material:5, labor:2, markup:30 }` (material + markup preserved). (Covered indirectly by the estimate-grid test in Wave 2a; the merge logic is asserted there — here it is a code contract.)
    - validateV2 tolerance: accepts an object with the PriceEntry `rates` field and one without it, no throw, no formatVersion bump (project-schema.test.ts).
  </behavior>
  <action>
    Create `src/renderer/src/lib/estimate-defaults.ts` exporting exactly one constant: `export const DEFAULT_MARKUP_PCT = 30` with a JSDoc noting it is a PERCENT (30 = 30%), is the project-wide default applied when a row has no PriceEntry, and is the single seam a future Settings control will change (D-05/D-09). Export nothing else.

    In `projectStore.ts`: import `DEFAULT_MARKUP_PCT` from `../lib/estimate-defaults` and add/export a `PriceEntry` type (or import it from `../lib/boq-types` once Task 2 lands — to keep this task self-contained and avoid a circular import, declare `PriceEntry` in `boq-types.ts` in Task 2 and import it here; if Task 2 runs after, temporarily widen the field type inline — but prefer ordering Task 2's type addition first, then import). Widen the state field `rates: Record<string, number>` → `Record<string, PriceEntry>` (interface decl + docstring at :15-20 — update the docstring to describe the PriceEntry value + percent markup). Keep initial `rates: {}` (:68) and `reset()` `rates: {}` (:93) unchanged. Replace the `setRate` interface decl (:36-41) and action (:111-114) with `setPrice: (key: string, patch: Partial<PriceEntry>) => void`: `set((s) => { const cur = s.rates[key] ?? { material: 0, labor: 0, markup: DEFAULT_MARKUP_PCT }; return { rates: { ...s.rates, [key]: { ...cur, ...patch } } } })` then `get().markDirty()`. The trailing `markDirty()` is load-bearing (without it an edited price won't persist on Save). Do NOT add any subscription in `attachDirtyTracking`.

    In `project-schema.ts`: widen the `rates?` value type on `ProjectFileV2` (:88-95) from `Record<string, number>` → `Record<string, PriceEntry>` (import `PriceEntry` from `./boq-types`). Update the JSDoc to note the Phase-16 shape `{ material, labor, markup }` (markup percent) + the legacy-scalar back-compat handled at hydrate. Add NO validation branch in `validateV2` — it tolerates `rates` exactly as before (rides `return raw as ProjectFileV2`). Keep `formatVersion === 2`; do NOT bump.

    In `project-serialize.ts`: `snapshotProject` already emits `rates` (:61) — no change beyond the wider value type flowing through. In `hydrateStores`, widen the `safeRates` coercion (:119-124) to handle BOTH forms: import `DEFAULT_MARKUP_PCT`; build `safeRates: Record<string, PriceEntry>`; iterate `data.rates` only when it is a non-null object; for each entry — if the value is a `number` (legacy Phase-15 scalar), map it to `{ material: (Number.isFinite(v) && v >= 0 ? v : 0), labor: 0, markup: DEFAULT_MARKUP_PCT }`; else if the value is a non-null object, per-field coerce (material = finite-≥0 else 0; labor = finite-≥0 else 0; markup = finite-≥0 else DEFAULT_MARKUP_PCT); drop entries whose value is neither number nor object. Default `safeRates` to `{}` when `data.rates` is absent/non-object. Keep the whole block inside the suspend/resume bracket and pass `rates: safeRates` in the existing `setState` (:129-133).
  </action>
  <verify>
    <automated>npx vitest run src/tests/project-serialize.test.ts src/tests/project-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 16-VALIDATION proof (b) persistence/round-trip + back-compat. `npx vitest run src/tests/project-serialize.test.ts src/tests/project-schema.test.ts` is GREEN (the 16-01 PriceEntry round-trip, legacy-scalar→`{material:50,labor:0,markup:30}`, missing-markup→30, and per-field-coercion assertions pass). `git grep -n "setPrice" -- src/renderer/src/stores/projectStore.ts` returns ≥1; `git grep -n "setRate" -- src/renderer/src/stores/projectStore.ts` returns ZERO (renamed). `git grep -n "DEFAULT_MARKUP_PCT" -- src/renderer/src/lib/estimate-defaults.ts src/renderer/src/lib/project-serialize.ts` shows the constant defined + consumed in hydrate. `npm run typecheck` is clean.
  </acceptance_criteria>
  <done>estimate-defaults.ts exports DEFAULT_MARKUP_PCT=30; projectStore has `rates: Record<string, PriceEntry>` + a merging `setPrice` (with markDirty) + reset-to-{}; schema widens `rates?` with no new throw and no formatVersion bump; serialize hydrate coerces legacy scalar → PriceEntry + per-field guards + missing-markup→30; the two serialize/schema test files are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PriceEntry type + widen BoqItemRow/BoqCategoryGroup/BoqStructure/AggregateOptions in boq-types + the two preload mirrors</name>
  <files>src/renderer/src/lib/boq-types.ts, src/preload/index.ts, src/preload/index.d.ts</files>
  <read_first>
    - src/renderer/src/lib/boq-types.ts (`BoqItemRow` :36-59 [`rate` :44 / `cost` :46 to replace; `categoryId?` :58 keep], `BoqCategoryGroup` :68-77 [`costSubtotal` :76], `BoqStructure` :84-92 [`grandTotalCost` :91], `AggregateOptions.rates?` :110)
    - src/preload/index.ts (`BoqItemRow` :24-32 [`rate` :28 / `cost` :29; omits `categoryId`], `BoqCategoryGroup` :34-39 [`costSubtotal` :38], `BoqStructure` :40-45 [`grandTotalCost` :44])
    - src/preload/index.d.ts (`BoqItemRow` :14-22 [`rate` :19 / `cost` :20; omits `categoryId`], `BoqCategoryGroup` :24-29 [`costSubtotal` :28], `BoqStructure` :30-35 [`grandTotalCost` :34])
    - 16-PATTERNS.md SP-9 (4-way lock) + 16-RESEARCH.md Pitfall 2 (mirror drift) + "boq-types.ts" Pattern Assignment
  </read_first>
  <behavior>
    - Type-only change: this task adds no runtime behavior; correctness is proven by `npm run typecheck` (clean) plus the boq-export-ipc structural-lock test (`src/tests/boq-export-ipc.test.ts`) still compiling. No `rate`/`cost`-only shape remains on BoqItemRow.
  </behavior>
  <action>
    In `boq-types.ts`: add `export interface PriceEntry { material: number; labor: number; markup: number }` with a JSDoc noting `markup` is a PERCENT (30 = 30%) and `material`/`labor` are ₱ per unit. On `BoqItemRow` (:36-59) REMOVE `rate` (:44) and `cost` (:46) and ADD, matching the JSDoc-per-field style: `material: number` ("₱ unit material rate"), `labor: number` ("₱ unit labor rate"), `markup: number` ("percent, e.g. 30"), `materialCost: number` ("material × qty, ₱"), `laborCost: number` ("labor × qty, ₱"), `cost: number` ("materialCost + laborCost, ₱"), `price: number` ("cost × (1 + markup/100), ₱"), `margin: number` ("price − cost, ₱"). Keep `categoryId?` (:58). On `BoqCategoryGroup` (:68-77) REMOVE the single `costSubtotal` (:76) and ADD `costSubtotal: number` / `priceSubtotal: number` / `marginSubtotal: number` (JSDoc "Σ row {cost|price|margin} in the category, ₱; unit-agnostic."). On `BoqStructure` (:84-92) REMOVE the single `grandTotalCost` (:91) and ADD `grandTotalCost: number` / `grandTotalPrice: number` / `grandTotalMargin: number` (JSDoc "Σ all category {cost|price|margin} subtotals, ₱."). On `AggregateOptions` (:110) widen `rates?: Record<string, number>` → `Record<string, PriceEntry>` (JSDoc "Per-(name|type) PriceEntry map; default useProjectStore.getState().rates.").

    In `preload/index.ts`: apply the SAME field replacements to the duplicated `BoqItemRow` (:24-32 — replace `rate`/`cost` with the eight fields; do NOT add `categoryId`, preserve the omission), `BoqCategoryGroup` (:34-39 — replace `costSubtotal` with the three), and `BoqStructure` (:40-45 — replace `grandTotalCost` with the three). `BoqRowType` (:16) is UNCHANGED. Do NOT add a `PriceEntry` type to preload (the preload mirror only needs the BoqStructure shape, which carries resolved numbers, not the source map).

    In `preload/index.d.ts`: apply the SAME field replacements to its duplicated `BoqItemRow` (:14-22 — no `categoryId`), `BoqCategoryGroup` (:24-29), and `BoqStructure` (:30-35). `BoqRowType` (:6) UNCHANGED.

    Do NOT touch `src/main/boq-writers.ts` — its `BoqItemRow`/`BoqCategoryGroup`/`BoqStructure` duplicates are Wave 3's (16-05) scope. Leaving it on the old `rate`/`cost` shape is intentional: it compiles independently, and the boq-export-ipc structural lock uses `STUB_STRUCT` (empty categories) so it will not break at compile time. If `npm run typecheck` surfaces a boq-writers error from this change, STOP — that means the writer references a removed field and must be handled by Wave 3, not here; report it rather than editing boq-writers.
  </action>
  <verify>
    <automated>npx vitest run src/tests/project-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 16-VALIDATION proof (a) row/category/grand shape + (h) type-lock. `git grep -n "PriceEntry" -- src/renderer/src/lib/boq-types.ts` returns the interface. `git grep -nE "\brate\b|^\s*cost:" -- src/renderer/src/lib/boq-types.ts` shows the OLD single `rate`/`cost` fields are gone from BoqItemRow (only the new `materialCost`/`laborCost`/`cost` remain — `cost:` still appears as one of the new fields, which is correct). `git grep -n "grandTotalMargin\|marginSubtotal" -- src/renderer/src/lib/boq-types.ts src/preload/index.ts src/preload/index.d.ts` shows all three in-scope files carry the new fields. `git grep -n "categoryId" -- src/preload` returns ZERO (the omission is preserved). `npm run typecheck` is clean OR the ONLY errors are inside `src/main/boq-writers.ts` (which Wave 3 fixes) — if boq-writers errors appear, they are expected and this task's own files typecheck clean. The verify command is a smoke check that the schema suite still compiles against the new types.
  </acceptance_criteria>
  <done>boq-types.ts defines PriceEntry and the widened BoqItemRow (material/labor/markup/materialCost/laborCost/cost/price/margin), BoqCategoryGroup (cost/price/marginSubtotal), BoqStructure (grand cost/price/margin), and PriceEntry-typed AggregateOptions.rates; both preload mirrors match (no categoryId, no PriceEntry export); boq-writers.ts untouched; the in-scope files typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Thread material/labor/cost/price/margin + three subtotals + three grand totals through boq-aggregator; confirm useBoqLive liveness</name>
  <files>src/renderer/src/lib/boq-aggregator.ts, src/renderer/src/hooks/useBoqLive.ts</files>
  <read_first>
    - src/renderer/src/lib/boq-aggregator.ts (whole file — `rates` default :95, row-build push :219-231 [`rate`/`cost` :219-220, `items.push` :221-230], per-category `catCost` accumulator :239-245 + category push `costSubtotal: catCost` :255, `grandCost` :185/:245 + return :284; the perimeter path :148-161, collision build :195-204, label loop :210-216, `uomFor` :35-40, `typeWord` :42-48 all UNCHANGED)
    - src/renderer/src/hooks/useBoqLive.ts (whole file — `rates` selector :42, deps :58, aggregateBoq call :55 — already wired; only docstring may need a wording tweak)
    - src/renderer/src/lib/estimate-defaults.ts (DEFAULT_MARKUP_PCT — created in Task 1)
    - src/tests/boq-aggregator.test.ts + src/tests/use-boq-live.test.ts (the GREEN targets — the 16-01 money-math + recompute cases)
    - 16-PATTERNS.md SP-8 (aggregator expansion) + SP-7 (useBoqLive liveness) + 16-RESEARCH.md "Aggregator money math" Code Example
  </read_first>
  <behavior>
    - Per-row money: `rates: { 'Outlet|count': { material: 5, labor: 3, markup: 30 } }` + 3 'Outlet' counts → row `{ quantity: 3, material: 5, labor: 3, markup: 30, materialCost: 15, laborCost: 9, cost: 24, price: 31.2, margin: 7.2 }` (boq-aggregator.test.ts).
    - Absent entry → default: no `rates` entry → `{ material: 0, labor: 0, markup: 30, materialCost: 0, laborCost: 0, cost: 0, price: 0, margin: 0 }`, no throw (boq-aggregator.test.ts).
    - Explicit markup 0 honored: entry `{ material: 10, labor: 0, markup: 0 }` qty 2 → `markup: 0, cost: 20, price: 20, margin: 0` (boq-aggregator.test.ts).
    - Subtotals/grand: `category.costSubtotal === Σ row costs`; `priceSubtotal === Σ prices`; `marginSubtotal === Σ margins`; `grandTotalCost === Σ category costSubtotals` (+ price/margin analogs) (boq-aggregator.test.ts).
    - Live recompute: setting a new PriceEntry map via `useProjectStore.setState` recomputes the hook's cost/price/margin (use-boq-live.test.ts).
    - No regression: perimeter one-row + WR-07 arc length + collision-suffix cases stay GREEN.
  </behavior>
  <action>
    In `boq-aggregator.ts`: import `DEFAULT_MARKUP_PCT` from `./estimate-defaults`. The `rates` default (:95) stays `opts.rates ?? useProjectStore.getState().rates` (value type now PriceEntry — no code change). At the row-build push (:219-231), REPLACE the `const rate = rates[...] ?? 0` / `const cost = rate * acc.quantity` lines (:219-220) with: `const entry = rates[`${name}|${type}`]`; `const material = entry?.material ?? 0`; `const labor = entry?.labor ?? 0`; `const markup = entry?.markup ?? DEFAULT_MARKUP_PCT` (absent entry → default; an entry with an explicit `markup: 0` uses 0 because `?? ` only fires on `undefined`); `const materialCost = material * acc.quantity`; `const laborCost = labor * acc.quantity`; `const cost = materialCost + laborCost`; `const price = cost * (1 + markup / 100)`; `const margin = price - cost`. Replace the `items.push({... rate, cost ...})` object (:221-230) so it pushes `{ label, quantity: acc.quantity, uom: uomFor(type, globalUnit), material, labor, markup, materialCost, laborCost, cost, price, margin, color: acc.color, type, categoryId: groupCategoryId }`. Keep the `${name}|${type}` key shape; do NOT add material/labor to `RawAccumulator` (they are not summed — they are per-unit rates read at row build).

    Per-category subtotals: at the accumulator block (:239-245), replace the single `let catCost = 0` / `catCost += it.cost` with three accumulators — `let catCost = 0; let catPrice = 0; let catMargin = 0;` and inside the loop `catCost += it.cost; catPrice += it.price; catMargin += it.margin`. In the category push (:255), replace `costSubtotal: catCost` with `costSubtotal: catCost, priceSubtotal: catPrice, marginSubtotal: catMargin`.

    Grand totals: replace the single `let grandCost = 0` (:185) with `let grandCost = 0; let grandPrice = 0; let grandMargin = 0;`; after computing the per-category sums, add `grandCost += catCost; grandPrice += catPrice; grandMargin += catMargin` (extend the existing `grandCost += catCost` at :245). In the return (:284), replace `grandTotalCost: grandCost` with `grandTotalCost: grandCost, grandTotalPrice: grandPrice, grandTotalMargin: grandMargin`.

    DO NOT touch: the perimeter length path (:148-161), `uomFor` (:35-40), `typeWord` (:42-48), the collision-map build (:195-204), the label loop (:210-216), or the per-UoM quantity subtotal/grandByUom logic (:238/242/246/279) — the three money accumulators are PARALLEL to the per-UoM quantity subtotals, not a replacement.

    In `useBoqLive.ts`: the `rates` selector (:42), deps (:58), and aggregateBoq call (:55) are already correct (the value type widens transparently). Update ONLY the leading docstring (:20-22) if it describes `rates` as a scalar/single-rate map — reword to "the per-(name|type) PriceEntry map ({material,labor,markup})" so the doc matches. No functional change.
  </action>
  <verify>
    <automated>npx vitest run src/tests/boq-aggregator.test.ts src/tests/use-boq-live.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 16-VALIDATION proofs (a) money math at row/category/grand + (c) live recompute. `npx vitest run src/tests/boq-aggregator.test.ts src/tests/use-boq-live.test.ts` is GREEN (all 16-01 material/labor/cost/price/margin, markup-default-30, explicit-markup-0, three-kind subtotal/grand, and the rates-recompute cases pass; the perimeter one-row + WR-07 length + collision cases stay green). `git grep -n "material \\* acc.quantity\|price = cost \\* (1 + markup" -- src/renderer/src/lib/boq-aggregator.ts` shows the money math present. `git grep -n "grandTotalMargin\|marginSubtotal" -- src/renderer/src/lib/boq-aggregator.ts` shows both. `git grep -n "perimeter-area\|polygonArea(pts" -- src/renderer/src/lib/boq-aggregator.ts` returns ZERO new matches beyond the existing area-markup line (perimeter untouched). `npm run typecheck` is clean.
  </acceptance_criteria>
  <done>aggregator emits material/labor/markup/materialCost/laborCost/cost/price/margin per row (absent entry → 0/0/30, explicit markup 0 honored), costSubtotal/priceSubtotal/marginSubtotal per category, and grandTotalCost/grandTotalPrice/grandTotalMargin on the structure; perimeter/uomFor/typeWord/label logic untouched; useBoqLive stays live for the PriceEntry map; boq-aggregator.test.ts + use-boq-live.test.ts GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk(.clmc) → renderer hydrate | A crafted/corrupt `.clmc` project file is parsed and its `rates` map (now PriceEntry-shaped, possibly still a legacy scalar) is loaded into `projectStore` via `hydrateStores`. The values flow into cost/price/margin math (× quantity) and later into ₱ export cells. This is the only untrusted-input surface this plan widens (the boundary itself is unchanged from Phase 15). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | Tampering | malformed PriceEntry / legacy scalar in a crafted `.clmc` (negative, NaN/Infinity, non-number field, non-object entry) reaching money math + export cells | mitigate | `hydrateStores` coerces per-field: legacy `number` → `{ material: finite-≥0 else 0, labor: 0, markup: DEFAULT_MARKUP_PCT }`; object form validates each of material/labor (finite-≥0 else 0) and markup (finite-≥0 else DEFAULT_MARKUP_PCT); non-number/non-object entries dropped; absent/non-object `data.rates` → `{}`. This extends the Phase-15 finite-≥0 guard to the widened shape. Proven by the Task-1 legacy-scalar + per-field-coercion + missing-markup tests. |
| T-16-02-02 | Tampering | type-mirror divergence — the 3 in-scope `BoqItemRow`/`BoqCategoryGroup`/`BoqStructure` duplicates drifting on the six-field widen | mitigate | All three in-scope mirrors (boq-types.ts, preload/index.ts, preload/index.d.ts) widened in lockstep in Task 2; `npm run typecheck` + the `boq-export-ipc.test.ts` structural lock fail on divergence. (boq-writers.ts is Wave 3's mirror — flagged, not touched here; its old shape compiles because the lock uses an empty STUB_STRUCT.) |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — modifies existing source against the existing Vitest infra. No new dependency (16-RESEARCH "Package Legitimacy Audit — Not applicable"). No legitimacy gate needed. |

No network, auth, or secret surface is added or crossed. No blocking-high threat — the single relevant threat (malformed PriceEntry) is fully mitigated by the serialize/hydrate coercion.
</threat_model>

<verification>
- `npx vitest run src/tests/boq-aggregator.test.ts src/tests/project-serialize.test.ts src/tests/project-schema.test.ts src/tests/use-boq-live.test.ts` — all GREEN (this plan's four target suites). estimate-grid / totals-panel-quantity-only / view-switch / writer tests remain RED until Waves 2/3 — expected, NOT a failure of this plan.
- `npm run typecheck` (or `npx tsc --noEmit`) — clean for the in-scope files (the boq-export-ipc structural lock compiles with STUB_STRUCT; boq-writers.ts still compiles on the old shape because the lock's fixture has empty categories). If boq-writers surfaces a type error, STOP and report — it is Wave 3's scope.
- `git grep -n "setRate" -- src/renderer/src` returns ZERO (renamed to setPrice; Wave 2b will confirm no totals-panel caller remains).
- `git grep -n "DEFAULT_MARKUP_PCT" -- src/renderer/src` shows the constant defined once + consumed in the aggregator + hydrate.
</verification>

<success_criteria>
- Pricing is stored per `name|type` as `PriceEntry { material, labor, markup }` (markup percent); `setPrice` merges a single-field patch and marks dirty; a brand-new entry seeds markup 30.
- Every BOQ row carries material/labor/markup/materialCost/laborCost/cost/price/margin; each category has costSubtotal/priceSubtotal/marginSubtotal; the structure has grandTotalCost/grandTotalPrice/grandTotalMargin.
- A Phase-15 scalar `.clmc` loads (coerced material=n, labor=0, markup=30) with no formatVersion bump and no error; malformed values are sanitized; missing markup defaults to 30.
- The three in-scope type-duplication files stay in lockstep; typecheck clean for in-scope files; boq-writers.ts left for Wave 3.
- useBoqLive recomputes cost/price/margin live on a PriceEntry-map change.
- Perimeter behavior and the per-UoM quantity subtotals are unchanged (Phase-14/15 no-regression).
- `boq-aggregator.test.ts`, `project-serialize.test.ts`, `project-schema.test.ts`, and the `use-boq-live` recompute case are GREEN.
</success_criteria>

<output>
Create `.planning/phases/16-estimating-workspace/16-02-SUMMARY.md` when done.
</output>
</content>
