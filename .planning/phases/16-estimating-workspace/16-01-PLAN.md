---
phase: 16-estimating-workspace
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/tests/boq-aggregator.test.ts
  - src/tests/project-serialize.test.ts
  - src/tests/project-schema.test.ts
  - src/tests/use-boq-live.test.ts
  - src/tests/boq-writers-xlsx.test.ts
  - src/tests/boq-writers-csv.test.ts
  - src/tests/estimate-row-edit.test.ts
  - src/tests/totals-panel-quantity-only.test.ts
  - src/tests/estimate-view-switch.test.ts
  - src/tests/totals-row-rate-edit.test.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5, SC-6]
must_haves:
  truths:
    - "Every Wave 1-3 source task has a failing test it must turn green"
    - "The single-rate scalar contract is replaced by the {material,labor,markup} PriceEntry contract across aggregator/serialize/schema/writer tests"
    - "The totals-panel pricing render nodes are asserted ABSENT (quantity-only), and the estimate-grid render nodes are asserted PRESENT"
    - "The Konva canvas subtree is asserted NOT remounted across a viewMode toggle"
    - "totals-row-rate-edit.test.ts is deleted (its inline-panel-rate contract is removed by D-02)"
  artifacts:
    - path: "src/tests/estimate-row-edit.test.ts"
      provides: "RED render test for the estimate-grid material/labor/markup cell edit → setPrice dispatch + stopPropagation"
      contains: "setPrice"
    - path: "src/tests/totals-panel-quantity-only.test.ts"
      provides: "RED render test asserting the totals panel renders NO rate-input/cost/cost-subtotal/grand-total-cost nodes"
      contains: "totals-row-rate-input"
    - path: "src/tests/estimate-view-switch.test.ts"
      provides: "RED render test asserting a viewMode toggle keeps the CanvasViewport container mounted (not remounted)"
      contains: "viewMode"
    - path: "src/tests/boq-aggregator.test.ts"
      provides: "material/labor/cost/price/margin per-row cases + Cost/Price/Margin subtotal + grand cases + markup-default-30"
      contains: "margin"
  key_links:
    - from: "src/tests/boq-aggregator.test.ts"
      to: "aggregateBoq rates option (PriceEntry map)"
      via: "rates: { 'Outlet|count': { material, labor, markup } } injection"
      pattern: "material"
    - from: "src/tests/estimate-row-edit.test.ts"
      to: "projectStore.setPrice"
      via: "native input+blur+Enter dispatch of setPrice('name|type', { material|labor|markup })"
      pattern: "setPrice"
---

<objective>
Wave 0 — establish the Nyquist test surface BEFORE any source changes. Extend / rewrite / create the test files so every downstream task in Waves 1-3 has a concrete failing test that proves it on completion. No production source is touched in this plan.

Purpose: GSD Nyquist sampling — each later task maps to one of proofs a–i in 16-VALIDATION.md. Writing them first prevents "test-after" drift and makes the single-rate → `{material,labor,markup}` widening AND the totals-panel-revert / estimate-grid-add contracts explicit before any source is edited. This mirrors the proven Phase-15 Wave-0 approach (15-01).

Output: 3 NEW test files (`estimate-row-edit.test.ts`, `totals-panel-quantity-only.test.ts`, `estimate-view-switch.test.ts`); EXTENDED assertions in `boq-aggregator.test.ts`, `project-serialize.test.ts`, `project-schema.test.ts`, `boq-writers-xlsx.test.ts`, `boq-writers-csv.test.ts`; UPDATED `use-boq-live.test.ts`; DELETED `totals-row-rate-edit.test.ts`. All new/changed assertions are RED (fail at runtime) but COMPILE (`npm run typecheck` clean via `as any` casts where the new shapes don't exist yet), until their Wave 1-3 task lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-estimating-workspace/16-CONTEXT.md
@.planning/phases/16-estimating-workspace/16-RESEARCH.md
@.planning/phases/16-estimating-workspace/16-VALIDATION.md

<decisions_locked>
<!-- From 16-CONTEXT.md (D-01..D-09) + 16-RESEARCH.md (A1..A5). Every wave obeys these verbatim. -->
- STORAGE SHAPE (D-06): the per-`name|type` value widens from a scalar `number` to `PriceEntry = { material: number; labor: number; markup: number }`. `rates: Record<string, PriceEntry>` (the store field keeps the name `rates`; the aggregator option keeps the name `rates`). Additive on `ProjectFileV2` — NO formatVersion bump.
- MARKUP UNIT (A1, locked): `markup` is stored as a PERCENT number (e.g. `30` = 30%). `price = cost × (1 + markup / 100)`. NOT a fraction.
- MARKUP DEFAULT (D-05, Pitfall 5): absent-ENTRY markup defaults to `30`; absent-material/labor default to `0`. Distinguish "no entry" (markup → 30) from "entry with explicit markup 0" (use 0). Constant `DEFAULT_MARKUP_PCT = 30` (Wave 1 seam `src/renderer/src/lib/estimate-defaults.ts`).
- LEGACY MIGRATION (D-06 / A2): a Phase-15 scalar `rates: { 'X|count': 50 }` coerces on hydrate → `{ material: 50, labor: 0, markup: 30 }`.
- MONEY MATH (D-03/D-04/D-05): `materialCost = material × qty`; `laborCost = labor × qty`; `cost = materialCost + laborCost`; `price = cost × (1 + markup/100)`; `margin = price − cost`. Per category: `costSubtotal`/`priceSubtotal`/`marginSubtotal` (Σ rows). Per structure: `grandTotalCost`/`grandTotalPrice`/`grandTotalMargin` (Σ categories).
- KEY (Pitfall 4): rate/price key is `` `${name}|${type}` `` — category-INDEPENDENT, unchanged from Phase 15. DISTINCT from the visibility key `` `${name}|${categoryId}` ``.
- TOTALS-PANEL REVERT (D-02): the totals panel becomes quantity-only. The Phase-15 `totals-row-rate-input`, `totals-row-cost`, `totals-category-cost-subtotal`, and `totals-grand-total` render nodes are REMOVED. Keep quantity, lightbulb, color chip, cycle/arm.
- ESTIMATE GRID (D-01/D-07): a NEW full-width Estimate sheet with per-row editable Material/Labor/Markup cells and read-only Cost/Price/Margin, category subtotals (Cost/Price/Margin), and a grand-total bar (Cost/Price/Margin).
- VIEW SWITCH (D-01, Pitfall 1): a `viewMode: 'plan' | 'estimate'` toggle swaps the center area via CSS `display` (mount-preserving) — the Konva `CanvasViewport` must NOT be unmounted/remounted.
- EXPORT (D-08): xlsx + csv gain `Material · Labor · Cost · Markup · Price · Margin` (full column set `Item · Qty · UoM · Material · Labor · Cost · Markup · Price · Margin`), per-category Cost/Price/Margin subtotals, grand Cost/Price/Margin; ₱ money cells stay SUM-safe native numbers; csv numeric + UTF-8 BOM preserved.
</decisions_locked>

<interfaces>
<!-- These are the SHAPES the Wave 1-3 source tasks will create. Tests assert against them (cast `as any` where the field/shape does not exist yet, mirroring how project-schema-hidden.test.ts casts before types land). -->
<!-- PriceEntry (src/renderer/src/lib/boq-types.ts — NEW): -->
//   interface PriceEntry { material: number; labor: number; markup: number }   // markup is PERCENT (30 = 30%)
<!-- projectStore (src/renderer/src/stores/projectStore.ts): -->
//   rates: Record<string, PriceEntry>                       // widened from Record<string, number>
//   setPrice: (key: string, patch: Partial<PriceEntry>) => void  // merges into existing entry (seeds markup: 30 on create) then get().markDirty()
<!-- BoqItemRow (per row) gains: material, labor, markup, materialCost, laborCost, cost, price, margin -->
<!-- BoqCategoryGroup gains: costSubtotal, priceSubtotal, marginSubtotal -->
<!-- BoqStructure gains: grandTotalCost, grandTotalPrice, grandTotalMargin -->
<!-- AggregateOptions.rates? widens to Record<string, PriceEntry> -->
<!-- ProjectFileV2.rates? widens to Record<string, PriceEntry> (additive, NO formatVersion bump) -->
<!-- viewerStore gains: viewMode: 'plan' | 'estimate' + setViewMode; resets to 'plan' on setFile/resetViewer/hydrate -->
<!-- New estimate-grid cell test-ids (Wave 2a): estimate-row-material-input, estimate-row-labor-input, estimate-row-markup-input -->
</interfaces>

<read_first>
- src/tests/boq-aggregator.test.ts (whole file — the current rate/cost cases from 15-01/15-02 that widen to material/labor/markup; the `rates:` injection fixtures; the `perimeter` one-row cases which STAY green and must NOT regress).
- src/tests/project-schema-hidden.test.ts (THE analog for the PriceEntry round-trip + absent-field-default + coercion tests — clone its localStorage-polyfill + MINIMAL_V2 fixture shape + `as any` casting discipline).
- src/tests/project-serialize.test.ts (the current scalar `rates` round-trip + crafted-coercion tests from 15-02 that widen to PriceEntry + gain the legacy-scalar→PriceEntry back-compat case).
- src/tests/use-boq-live.test.ts (the `callHook()` render harness + the "recomputes when rates change" case from 15-03 — update the fixture from a scalar map to a PriceEntry map and assert cost/price/margin recompute).
- src/tests/totals-row-rate-edit.test.ts (the RED render harness created in 15-01 — READ it in full: its React.createElement + jsdom + localStorage-polyfill + native-event-dispatch + IS_REACT_ACT_ENVIRONMENT pattern is the exact template for the NEW estimate-row-edit.test.ts. This file is DELETED at the end of this plan.)
- src/tests/totals-row-cycle.test.ts (the totals-row render harness — the template for totals-panel-quantity-only.test.ts; note how it mounts TotalsRow via React.createElement).
- src/tests/boq-writers-xlsx.test.ts + src/tests/boq-writers-csv.test.ts (whole files — the current 5-column `['Item','Quantity','UoM','Rate','Cost']` title/header assertions to WIDEN to 9 columns; the fixture builders that omit the new fields — the do-not-throw `money()` guard depends on this, so keep fixtures minimal + cast).
- src/renderer/src/lib/boq-aggregator.ts:83-285 (source of truth for the aggregator contract the tests assert against — the `rates` default at :95, the per-row build at :219-231, the category push at :255, the return at :284).
- 16-VALIDATION.md Proof → Test Map (proofs a–i; every source task's `<verify>` cites one).
- 16-RESEARCH.md "Common Pitfalls" (Pitfalls 1-6) + "Removal Map" + "Code Examples" (the PriceEntry/aggregator/estimate-cell shapes to encode).
</read_first>

<interface_context>
Run `npx vitest run src/tests/boq-aggregator.test.ts src/tests/project-serialize.test.ts src/tests/use-boq-live.test.ts src/tests/boq-writers-xlsx.test.ts src/tests/boq-writers-csv.test.ts` BEFORE editing to confirm the current suite is GREEN (Phase-15 baseline). This plan intentionally turns the widened/rewritten assertions RED until Waves 1-3 land. Do NOT expect a green suite after this plan — RED is the deliverable.
</interface_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen aggregator money-math tests to material/labor/cost/price/margin + subtotals/grand + markup-default-30 (proof a)</name>
  <files>src/tests/boq-aggregator.test.ts</files>
  <read_first>
    - src/tests/boq-aggregator.test.ts (whole file — the existing rate/cost cases that widen; the `rates:` opts injection shape; the perimeter one-row cases that must stay green)
    - src/renderer/src/lib/boq-aggregator.ts:83-285 (the `rates` default :95, per-row row-build :219-231, category push :255, return :284 — the contract the assertions target)
    - 16-RESEARCH.md "Aggregator money math" Code Example (the exact material/labor/cost/price/margin block) + 16-VALIDATION.md proof a rows
  </read_first>
  <action>
    Update the existing per-row rate/cost test(s) to the PriceEntry shape: inject `rates: { 'Outlet|count': { material: 5, labor: 3, markup: 30 } }` alongside 3 'Outlet' count markups and assert the row has `quantity === 3`, `material === 5`, `labor === 3`, `markup === 30`, `materialCost === 15`, `laborCost === 9`, `cost === 24`, `price === 24 * 1.3` (i.e. `31.2`), `margin === price - cost` (i.e. `7.2`). Use `toBeCloseTo` for the price/margin float assertions. Cast the injected `rates` and the read fields `as any` where TypeScript complains (the PriceEntry type + the new BoqItemRow fields do not exist until Wave 1).

    Add a NEW markup-default test (proof a, Pitfall 5): a count markup whose `name|type` key is ABSENT from `rates` (or pass no `rates`) → assert `material === 0`, `labor === 0`, `cost === 0`, `markup === 30` (the absent-entry default), `price === 0`, `margin === 0`, and the call does not throw. Add a SECOND case: an entry present with `markup: 0` explicitly (e.g. `{ material: 10, labor: 0, markup: 0 }`, qty 2) → assert `markup === 0` (explicit 0 is honored, NOT overridden to 30), `cost === 20`, `price === 20`, `margin === 0` — proving "no entry → 30" is distinct from "entry with markup 0 → 0".

    Update/replace the category cost-subtotal + grand-total test to the three-kind shape (proof a, SC-4): two priced rows in one category → assert `categories[0].costSubtotal === Σ row costs`, `categories[0].priceSubtotal === Σ row prices`, `categories[0].marginSubtotal === Σ row margins`. Add a second category with its own priced row and assert `result.grandTotalCost === Σ category costSubtotals`, `result.grandTotalPrice === Σ category priceSubtotals`, `result.grandTotalMargin === Σ category marginSubtotals`. Cast `as any` as needed.

    Update the category-independence test to the PriceEntry shape: the same `'Outlet|count'` appearing in TWO categories with one `rates` entry → both rows read the same `material`/`labor`/`markup` (proof b support; keeps the `name|type` key contract explicit).

    Do NOT touch the perimeter one-row / WR-07 arc-length / collision-suffix cases — they are Phase-15 keepers and must stay GREEN (this phase does not change perimeter behavior).
  </action>
  <verify>
    <automated>npx vitest run src/tests/boq-aggregator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 16-VALIDATION.md proof (a). The new/updated material/labor/cost/price/margin, markup-default-30, explicit-markup-0, and three-kind subtotal/grand assertions FAIL at runtime now (the aggregator still emits scalar `rate`/`cost` and single `costSubtotal`/`grandTotalCost`, has no `material`/`labor`/`markup`/`price`/`margin`) — this is the expected RED state. The file MUST still COMPILE (`as any` casts on `rates` PriceEntry values + the new read fields). The perimeter one-row and WR-07 length cases remain GREEN. `npm run typecheck` is clean.
  </acceptance_criteria>
  <done>Aggregator tests assert the five money fields per row, markup default 30 (distinct from explicit 0), and Cost/Price/Margin subtotals + grand totals; perimeter cases untouched and green; file compiles; new assertions RED.</done>
</task>

<task type="auto">
  <name>Task 2: Widen PriceEntry round-trip + legacy-scalar coercion + writer 9-column tests (proofs b, f)</name>
  <files>src/tests/project-serialize.test.ts, src/tests/project-schema.test.ts, src/tests/boq-writers-xlsx.test.ts, src/tests/boq-writers-csv.test.ts</files>
  <read_first>
    - src/tests/project-schema-hidden.test.ts (the three-test shape to clone for the PriceEntry round-trip; MINIMAL_V2 fixture + localStorage polyfill)
    - src/tests/project-serialize.test.ts (the existing scalar `rates` round-trip + crafted-coercion tests to widen)
    - src/tests/boq-writers-xlsx.test.ts (whole file — the current `['Item','Quantity','UoM','Rate','Cost']` title assertion + the fixtureStructure builder omitting new fields) + src/tests/boq-writers-csv.test.ts (the current header + BOM assertions)
    - src/main/boq-writers.ts (the xlsx column defs :140-146, title row :158, appendItemRow :216-240, cost-subtotal :257-267, grand-total :186-196, A:E merge :211; the csv header :294, item push :299-308, cost-subtotal push :320, grand push :334, BOM return :342) — the contract for the new 9-column layout
    - 16-VALIDATION.md proofs b + f
  </read_first>
  <action>
    In `project-serialize.test.ts`: update the snapshot test so `useProjectStore.setState({ rates: { 'Skirting|perimeter': { material: 12, labor: 4, markup: 25 } } })` then `expect((snap as any).rates)` deep-equals that PriceEntry map. Update the hydrate round-trip test: `hydrateStores({ ...MINIMAL_V2, rates: { 'Outlet|count': { material: 7, labor: 2, markup: 40 } } })` → `useProjectStore.getState().rates` deep-equals that map. Add a NEW legacy-scalar back-compat test (proof b): `hydrateStores({ ...MINIMAL_V2, rates: { 'X|count': 50 } })` (a Phase-15 scalar) → expect `rates['X|count']` deep-equals `{ material: 50, labor: 0, markup: 30 }`. Add a NEW missing-markup test: hydrate `{ rates: { 'Y|count': { material: 3, labor: 1 } } }` (object without markup) → expect `rates['Y|count'].markup === 30`. Update the crafted-coercion test to the object shape: hydrate `{ rates: { 'A|count': { material: -5, labor: 2, markup: 30 }, 'B|count': { material: 'x', labor: 1, markup: 30 }, 'C|count': { material: 10, labor: 3, markup: 30 } } }` → assert negative/non-number material fields are coerced to 0 (e.g. `A.material === 0`, `B.material === 0`) while valid fields survive (`C.material === 10`), and the call does not throw. Cast `as any` where PriceEntry does not exist yet.

    In `project-schema.test.ts`: keep the additive-tolerance tests — `validateV2` ACCEPTS an object with a `rates` field (now a PriceEntry map) and one WITHOUT it, no throw, no formatVersion bump. These tests PASS today (validateV2 does not inspect `rates`) and must STAY passing after the type widens — assert acceptance, not a thrown error.

    In `boq-writers-xlsx.test.ts`: change the title-row assertion from `['Item','Quantity','UoM','Rate','Cost']` to `['Item','Quantity','UoM','Material','Labor','Cost','Markup','Price','Margin']` (9 columns, locked order). Add assertions that Material/Labor/Cost/Price/Margin item cells are NATIVE numbers (`typeof === 'number'`) with a ₱ `.numFmt` (the `NUMFMT_PESO` ₱ glyph), and that the Markup cell is a native number with a percent-style numFmt (NOT the ₱ numFmt). Assert per-category Cost/Price/Margin subtotal rows and grand-total Cost/Price/Margin rows exist. Assert the category-heading merge spans the new last column (A:I, not A:E). Widen the `fixtureStructure()` builder to include `material`/`labor`/`markup`/`materialCost`/`laborCost`/`cost`/`price`/`margin` on items and `costSubtotal`/`priceSubtotal`/`marginSubtotal` on the category + `grandTotalCost`/`grandTotalPrice`/`grandTotalMargin` on the structure (cast `as any` / `as unknown as BoqStructure` as needed).

    In `boq-writers-csv.test.ts`: change the header assertion to the 9-column set `['Item','Quantity','UoM','Material','Labor','Cost','Markup','Price','Margin']`; assert Material/Labor/Cost/Markup/Price/Margin are emitted as numeric values (Number, not a `"₱…"`-prefixed string); keep the UTF-8 BOM assertion (output line 1 starts with `'﻿'`).
  </action>
  <verify>
    <automated>npx vitest run src/tests/project-serialize.test.ts src/tests/project-schema.test.ts src/tests/boq-writers-xlsx.test.ts src/tests/boq-writers-csv.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 16-VALIDATION.md proofs (b) persistence/back-compat + (f) 9-column export. The PriceEntry round-trip, legacy-scalar→`{material:50,labor:0,markup:30}`, missing-markup→30, and per-field coercion assertions FAIL at runtime now (serialize hydrate still coerces a scalar) — expected RED. The writer 9-column/native-number/₱-numFmt/percent-numFmt/three-subtotal/A:I-merge assertions FAIL at runtime now (writers still emit 5 columns) — expected RED. The `project-schema.test.ts` additive-tolerance tests PASS (validateV2 unchanged). All four files COMPILE (`as any` / `as unknown as BoqStructure`). `npm run typecheck` is clean.
  </acceptance_criteria>
  <done>PriceEntry round-trip + legacy-scalar coercion + missing-markup-30 + per-field coercion tests exist (cloned from the hiddenItemNames analog); xlsx 9-column + native-number + ₱/percent numFmt + three-subtotal + A:I merge assertions exist; csv 9-column numeric + BOM assertions exist; schema tolerance tests still pass; all compile; new assertions RED.</done>
</task>

<task type="auto">
  <name>Task 3: New estimate-grid edit test + live-recompute update + totals-panel-quantity-only test + view-switch test; delete totals-row-rate-edit (proofs c, d, e, g)</name>
  <files>src/tests/estimate-row-edit.test.ts, src/tests/use-boq-live.test.ts, src/tests/totals-panel-quantity-only.test.ts, src/tests/estimate-view-switch.test.ts, src/tests/totals-row-rate-edit.test.ts</files>
  <read_first>
    - src/tests/totals-row-rate-edit.test.ts (the FULL render harness: React.createElement + `/** @vitest-environment jsdom */` + `installLocalStoragePolyfill()` in beforeEach + native `input`/`blur`/`keydown` dispatch + `IS_REACT_ACT_ENVIRONMENT = true` at module scope + the setRate/onArmTool/setPage spies — this is the verbatim template for estimate-row-edit.test.ts. This file is DELETED at task end.)
    - src/tests/totals-row-cycle.test.ts (the TotalsRow render mount pattern — template for totals-panel-quantity-only.test.ts)
    - src/tests/use-boq-live.test.ts (the callHook harness + the "recomputes when rates change" case — the fixture widens from scalar to PriceEntry)
    - src/renderer/src/components/TotalsRow.tsx (the render nodes to assert ABSENT after D-02: `totals-row-rate-input` :413, `totals-row-cost` :439; the nodes to assert PRESENT: `totals-row-quantity` :371, `totals-row-lightbulb` :333, `totals-row-color-chip` :346)
    - 16-RESEARCH.md "Estimate grid editable cell" Code Example + Pitfalls 3 (React-19 uncontrolled commit) + 6 (test parallel-safety) + 1 (view-switch keeps canvas) + "Removal Map"
    - 16-VALIDATION.md proofs c, d, e, g
  </read_first>
  <action>
    Create `src/tests/estimate-row-edit.test.ts` (proof d) — a jsdom render test modeling `totals-row-rate-edit.test.ts` verbatim (React.createElement, localStorage polyfill in beforeEach, `IS_REACT_ACT_ENVIRONMENT = true` at module scope, `@vitest-environment jsdom`). It renders the NEW `EstimateRow` (import from `../renderer/src/components/EstimateRow` — the component does not exist yet; the import + render is what fails, which is the intended RED). Assertions: (1) typing into the material cell (find via `data-testid="estimate-row-material-input"`) and committing via a native `input` event then `blur`, and separately Enter `keydown`, calls `useProjectStore.getState().setPrice('Outlet|count', { material: <parsed> })` — the category-INDEPENDENT `name|type` key (spy on the real `setPrice` or seed a `vi.fn()`); (2) the labor cell (`estimate-row-labor-input`) dispatches `setPrice('Outlet|count', { labor: <parsed> })`; (3) the markup cell (`estimate-row-markup-input`) dispatches `setPrice('Outlet|count', { markup: <parsed> })`; (4) interacting with a cell does NOT bubble to any row-level handler passed as a prop (assert a spy passed as e.g. `onRowClick`/`onArmTool` is NOT called — proving `e.stopPropagation()`). Since the grid is a spreadsheet (not a navigator), if EstimateRow has no row-level click handler, assert instead that a `keydown` inside a cell does not propagate to a parent listener attached in the test harness. Cast component props `as any` where the EstimateRow prop type does not exist yet.

    In `use-boq-live.test.ts` (proof c): update the "recomputes when rates change" case fixture from a scalar map to a PriceEntry map — seed one priced count markup + `rates: { 'Outlet|count': { material: 5, labor: 0, markup: 30 } }`, capture the live `cost`/`price`/`margin` via the harness `current()`, then `act(() => useProjectStore.setState({ rates: { 'Outlet|count': { material: 10, labor: 0, markup: 30 } } }))` and assert `cost`/`price`/`margin` updated (cost 5→10 per unit; price recomputes). This FAILS until Wave 1 widens the aggregator to emit price/margin. Keep the existing VIEW-01 snapshot + recompute-on-pageMarkups cases green (only the rates fixture shape changes). Cast `as any` where the PriceEntry map / new read fields don't exist yet.

    Create `src/tests/totals-panel-quantity-only.test.ts` (proof e) — a jsdom render test (mirror the totals-row-cycle harness) that mounts `TotalsRow` (and, if convenient, `TotalsCategoryBlock`/`TotalsPanel`) with a priced item fixture and asserts the pricing render nodes are ABSENT: query for `totals-row-rate-input`, `totals-row-cost`, `totals-category-cost-subtotal`, `totals-grand-total` and assert each is NOT in the DOM (`queryByTestId(...) === null` / `container.querySelector('[data-testid="..."]') === null`). Assert the survivors ARE present: `totals-row-quantity`, `totals-row-lightbulb`, `totals-row-color-chip`. This FAILS until Wave 2b (16-04) strips those nodes.

    Create `src/tests/estimate-view-switch.test.ts` (proof g) — a jsdom render test that mounts a minimal harness exercising the `viewMode` toggle (either mount `App`, or a small harness that renders the App-shell center region). Set `useViewerStore.setState({ viewMode: 'estimate' } as any)`, capture the CanvasViewport container DOM node (via a stable `data-testid` on the canvas container, e.g. `canvas-viewport` — confirm the actual testid on CanvasViewport's root and use it), then `act(() => useViewerStore.setState({ viewMode: 'plan' } as any))` and assert the SAME DOM node instance is still present (not remounted) — OR, simpler and equally valid per 16-RESEARCH Pitfall 1: assert that in either mode BOTH the canvas container and the estimate-grid container exist in the DOM with exactly one carrying `display: none`. Cast `viewMode` `as any` (the field does not exist on ViewerState until Wave 2a). This FAILS until Wave 2a adds `viewMode` + the mount-preserving toggle.

    DELETE `src/tests/totals-row-rate-edit.test.ts` — its inline-panel-rate contract is removed by D-02 (its behavior moves to `estimate-row-edit.test.ts`). Remove the file entirely (`git rm` or delete + stage).
  </action>
  <verify>
    <automated>npx vitest run src/tests/estimate-row-edit.test.ts src/tests/use-boq-live.test.ts src/tests/totals-panel-quantity-only.test.ts src/tests/estimate-view-switch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Maps to 16-VALIDATION.md proofs (c) live recompute, (d) grid-edit dispatch, (e) panel quantity-only, (g) view-switch. `estimate-row-edit.test.ts`, `totals-panel-quantity-only.test.ts`, and `estimate-view-switch.test.ts` FAIL at runtime (EstimateRow doesn't exist; the panel still renders pricing nodes; `viewMode` doesn't exist) but COMPILE. `use-boq-live.test.ts` "recomputes when rates change" FAILS at runtime (aggregator has no price/margin) but compiles; its other cases pass. `totals-row-rate-edit.test.ts` no longer exists (`git ls-files src/tests/totals-row-rate-edit.test.ts` returns nothing). `npm run typecheck` is clean.
  </acceptance_criteria>
  <done>A RED estimate-grid edit test asserts setPrice dispatch (material/labor/markup) + stopPropagation; use-boq-live recompute case drives a PriceEntry map + asserts cost/price/margin; a RED totals-panel-quantity-only test asserts the pricing nodes ABSENT + survivors PRESENT; a RED view-switch test asserts the canvas is not remounted; totals-row-rate-edit.test.ts is deleted; all compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only edits/creates/deletes test files; no runtime trust boundary is added or crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Tampering | test fixtures encoding the wrong money-math or storage contract | mitigate | Tests assert the LOCKED `{material,labor,markup}` + `price = cost × (1 + markup/100)` + markup-default-30 contract directly, so a regression (e.g. treating markup as a fraction, or defaulting markup to 0) fails CI. |
| T-16-01-02 | Tampering | new render tests modifying vitest.config.ts or relying on jsdom persistent localStorage (breaks parallel executor) | mitigate | New tests are `*.test.ts` (not `.tsx`) using React.createElement + `@vitest-environment jsdom` + the in-memory `installLocalStoragePolyfill()` + `IS_REACT_ACT_ENVIRONMENT = true` (16-RESEARCH Pitfall 6 / CLAUDE.md "no config change mid-wave"). No `vitest.config.ts` edit. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — Wave 0 uses the existing Vitest 4.1.x infra (16-RESEARCH "Package Legitimacy Audit — Not applicable"). No legitimacy gate needed. |
</threat_model>

<verification>
- `npx vitest run src/tests/boq-aggregator.test.ts src/tests/project-serialize.test.ts src/tests/project-schema.test.ts src/tests/use-boq-live.test.ts src/tests/boq-writers-xlsx.test.ts src/tests/boq-writers-csv.test.ts src/tests/estimate-row-edit.test.ts src/tests/totals-panel-quantity-only.test.ts src/tests/estimate-view-switch.test.ts` — RED assertions are present and the suite COMPILES (TypeScript errors === 0; runtime failures are the expected RED).
- `npm run typecheck` (or `npx tsc --noEmit`) is clean — the `as any` / `as unknown as BoqStructure` casts absorb every not-yet-existent shape.
- `git ls-files src/tests/totals-row-rate-edit.test.ts` returns nothing (the file is deleted).
</verification>

<success_criteria>
- Every Wave 1-3 source task has ≥1 failing test pointing at it (proofs a–i mapped in 16-VALIDATION.md).
- Aggregator tests assert the five money fields + markup default 30 (distinct from explicit 0) + Cost/Price/Margin subtotals/grand totals; perimeter cases stay green.
- Serialize tests assert PriceEntry round-trip + legacy-scalar→`{material,labor,markup}` coercion + missing-markup→30 + per-field coercion.
- Writer tests assert the 9-column layout, native-number ₱ cells, percent markup numFmt, three subtotal kinds, and A:I merge.
- New estimate-grid edit / totals-panel-quantity-only / view-switch RED tests exist; use-boq-live drives a PriceEntry map.
- `totals-row-rate-edit.test.ts` is deleted. Type-check clean.
</success_criteria>

<output>
Create `.planning/phases/16-estimating-workspace/16-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
