---
phase: 04.1-zip-embedded-clmc
plan: 06
type: execute
wave: 6
depends_on:
  - "04.1-05"
files_modified:
  - .planning/phases/04.1-zip-embedded-clmc/04.1-UAT.md
autonomous: false
requirements:
  - PERS-01
  - PERS-02
must_haves:
  truths:
    - "User has executed end-to-end save → close → reopen test on a real PDF and confirmed all markups + scale + viewport state survive"
    - "User has executed portability test: copied .clmc to a folder containing no PDFs and reopened on the same machine — file opens without prompts"
    - "User has executed Replace Plan PDF: opened a v2 .clmc, used Replace, picked a same-page-count PDF, confirmed markups preserved, saved (Ctrl+S) and confirmed new sha256 written"
    - "User has confirmed the 'Saving...' indicator + button-disable behavior occurs visibly (even briefly) during save of a real ~50 MB construction PDF"
    - "User has confirmed Phase 04 v1 plain-JSON .clmc files open with silent migration (dirty asterisk appears on title bar after open)"
    - "Phase 04.1 closure recorded: REQUIREMENTS.md PERS-01 + PERS-02 annotated with v2 format note; ROADMAP.md Phase 4.1 status updated"
  artifacts:
    - path: ".planning/phases/04.1-zip-embedded-clmc/04.1-UAT.md"
      provides: "User Acceptance Test record with pass/fail + observations for each scenario"
      contains: "## UAT Results"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/04.1-zip-embedded-clmc/"
      via: "PERS-01/PERS-02 annotation referencing v2 phase"
      pattern: "v2.*ZIP"
---

<threat_model>
| Threat ID | Vector | Severity | Mitigation |
|-----------|--------|----------|------------|
| T-4.1-06-01 | UAT passes locally but fails on user's actual workflow (e.g., 200 MB scanned PDF causes OOM) | Med | UAT script includes one realistic-size PDF (≥30 MB) test case; user reports any memory or speed concerns in UAT.md observations |
| T-4.1-06-02 | UAT skips Replace Plan PDF flow because user doesn't have a "revised" PDF on hand | Low | UAT script provides a fallback: replace with the same PDF (page-count + dim match guaranteed) just to verify the flow runs; user can repeat with a real revision later |
| T-4.1-06-03 | v1 silent-migration test cannot be performed because user has no Phase 04 .clmc artifacts | Low | UAT step explicitly notes this is OK to skip if no v1 file exists; flag as "no-test" rather than "fail" |
| T-4.1-06-04 | Save UI flicker too fast to observe on small PDFs (sub-50ms) | Low | UAT script explicitly says "use the largest PDF available" for the saving-indicator step |
</threat_model>

<objective>
Wave 6 phase gate. Manual UAT covering the 4 success criteria from ROADMAP Phase 4.1:
1. Save → reopen round-trip preserves all state
2. Old v1 file opens with silent migration (dirty asterisk)
3. Replace Plan PDF works with page-count validation + markup preservation
4. "Saving..." indicator + button disable visible during write

Purpose: Final human-verification checkpoint before declaring Phase 4.1 done. Captures any UX issues that automated tests cannot catch (timing, visual feel, error messages, real-PDF behavior).

Output: UAT.md with checked-off scenarios; updates to REQUIREMENTS.md and ROADMAP.md to reflect v2 format adoption.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-zip-embedded-clmc/04.1-CONTEXT.md
@.planning/phases/04.1-zip-embedded-clmc/04.1-RESEARCH.md
@.planning/phases/04.1-zip-embedded-clmc/04.1-05-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-project-persistence/04-06-UAT.md

<interfaces>
<!-- Functional surface that UAT exercises. -->

User-facing actions covered:
- Open PDF (new project)
- Place markups + set scale (existing Phase 03 behavior)
- Ctrl+S → save dialog → write .clmc v2 ZIP
- Close window
- Open v2 .clmc → restore state
- Use "Replace Plan PDF" button → file picker → swap PDF
- Open Phase 04 .clmc → silent migration → dirty asterisk → Ctrl+S → write v2

UAT prerequisites:
- A real construction PDF (>= 5 MB; ideally one with multiple pages)
- A "revised" PDF with the same page count (could be a copy of the same PDF for the smoke test)
- A Phase 04 .clmc file from prior testing (optional — silent-upgrade test is skippable if absent)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 04.1-UAT.md scaffold with the 5 test scenarios</name>
  <files>.planning/phases/04.1-zip-embedded-clmc/04.1-UAT.md</files>
  <read_first>
    - .planning/phases/04-project-persistence/04-06-UAT.md (Phase 04 UAT format — copy structure)
    - .planning/phases/04.1-zip-embedded-clmc/04.1-CONTEXT.md (decisions to verify against)
    - .planning/ROADMAP.md (Phase 4.1 success criteria block to mirror)
  </read_first>
  <action>
    Create `.planning/phases/04.1-zip-embedded-clmc/04.1-UAT.md` with this exact content (replace `{...}` placeholders with empty fields the user fills during the actual test):

    ```markdown
    # Phase 04.1: ZIP-Embedded .clmc Format — User Acceptance Test

    **Status:** awaiting-user-test
    **Started:** {ISO date when user begins UAT}
    **Completed:** {ISO date when user finishes UAT}
    **Tester:** pengr.clmc.3@gmail.com

    ---

    ## Pre-flight Checklist

    Before starting, run:
    ```
    npm run build
    npm run dev   # OR: launch the packaged .exe from a previous build
    ```

    Confirm:
    - [ ] Application window opens without errors
    - [ ] DevTools console shows no errors on startup
    - [ ] At least one real construction PDF (≥ 5 MB) is on disk for testing
    - [ ] [Optional] A Phase 04 .clmc file (plain JSON) is on disk for the silent-upgrade test

    ---

    ## Test 1 — End-to-end save & reopen (PERS-01 + PERS-02)

    **Goal:** A v2 .clmc file written by the app and re-opened later restores every detail of the project state.

    **Steps:**
    1. Open a real PDF: Toolbar → Open → pick `your-test.pdf`
    2. Set scale on page 1 (draw calibration line, enter known distance)
    3. Place at least 5 markups across at least 2 pages: 1 count, 1 linear, 1 area, 1 perimeter, 1 more count
    4. Press Ctrl+S → Save dialog → choose path `your-test.clmc` → click Save
    5. Observe title bar shows "Saving..." (briefly), Save/Save As/Replace buttons disabled
    6. After save: dirty asterisk gone, save toast appears "Saved"
    7. Close the window (X button) — should close immediately (no dirty modal)
    8. Re-launch the app
    9. Toolbar → Open → pick `your-test.clmc`
    10. Verify EVERY markup appears on the SAME page at the SAME position with the SAME name + color
    11. Verify per-page scale is preserved (StatusBar shows the calibrated 1:N ratio)
    12. Verify zoom + pan state restored on the page that was current at save time
    13. Verify dirty asterisk is NOT shown after open

    **Pass criteria:**
    - [ ] Save completed without console errors
    - [ ] "Saving..." indicator was visible (even briefly)
    - [ ] Save/Save As/Replace buttons were disabled during save
    - [ ] All markups restored to exact positions
    - [ ] Per-page scales restored correctly
    - [ ] Title bar after open: `your-test.clmc — CLMC Takeoff` (no asterisk)

    **Result:** {pass | fail | partial}
    **Observations:** {notes, console errors, timing}

    ---

    ## Test 2 — Portability (Send-to-other-folder test)

    **Goal:** A .clmc v2 file is fully self-contained — opens on any folder without the original PDF nearby.

    **Steps:**
    1. From Test 1, take the saved `your-test.clmc` file
    2. Create a NEW folder (e.g. `C:\portability-test\`) that contains NO PDFs
    3. Copy `your-test.clmc` to that folder (move or copy — both work)
    4. From the app: Toolbar → Open → navigate to the new folder → pick the .clmc
    5. Verify the project opens normally, all markups visible, no "PDF not found" prompt appears

    **Pass criteria:**
    - [ ] No prompt to locate a PDF
    - [ ] Project opened cleanly with PDF rendering correctly
    - [ ] All markups visible on the original positions

    **Result:** {pass | fail | partial}
    **Observations:** {notes}

    ---

    ## Test 3 — Replace Plan PDF (D-08, D-09, D-10)

    **Goal:** The Replace Plan PDF button swaps the embedded PDF while preserving markups; page-count validation prevents catastrophe.

    **Steps:**
    1. With the project from Test 1 open: Toolbar → Replace Plan PDF
    2. **Smoke test (same file):** pick the SAME PDF used originally → expect: project markups remain visible, dirty asterisk appears, no modal
    3. Press Ctrl+S → save → confirm new sha256 written (overwrite same .clmc)
    4. **Page-count abort test:** Toolbar → Replace Plan PDF → pick a PDF with a DIFFERENT page count → expect modal "Wrong PDF — Selected PDF has X pages, but project expects Y" with [Pick again] / [Cancel]
    5. Click [Cancel] — verify project state unchanged (markups still there, no dirty marker change)
    6. **Dimension warn test (optional):** find a PDF with same page count but different page sizes; click Replace Plan PDF → pick it → expect modal "PDF dimensions changed — markup positions may look wrong" with [Open anyway] / [Cancel]
    7. Click [Cancel] — verify state unchanged
    8. (Optional) Click [Open anyway] on the dimension warning — verify markups appear (possibly drifting on the new PDF)

    **Pass criteria:**
    - [ ] Smoke test (same file) succeeds without modals; markups preserved
    - [ ] Save after smoke test successful (sha256 updated; project saves cleanly)
    - [ ] Page-count abort modal appears with correct numbers
    - [ ] Cancel from page-count modal leaves project state unchanged
    - [ ] [Optional] Dimension warning modal appears for size mismatch
    - [ ] [Optional] Cancel from dimension modal leaves state unchanged

    **Result:** {pass | fail | partial | skipped}
    **Observations:** {notes}

    ---

    ## Test 4 — Saving indicator UX (D-11, D-12)

    **Goal:** During a real-size PDF save, the title bar visibly shows "Saving..." and Save/SaveAs/Replace buttons are disabled.

    **Steps:**
    1. Open the LARGEST construction PDF available (ideally 30 MB+)
    2. Add a markup or two (so isDirty=true)
    3. Press Ctrl+S
    4. WATCH the title bar carefully during save
    5. WATCH the Toolbar Save/Save As/Replace buttons during save

    **Pass criteria:**
    - [ ] Title bar showed "Saving..." text during the write (even if brief)
    - [ ] Save/Save As/Replace buttons all visibly dimmed/disabled during the write
    - [ ] After save: title bar reverts to filename, buttons re-enable
    - [ ] No console errors

    **Result:** {pass | fail | partial}
    **Observations:** {timing notes — was it visible? estimated duration}

    ---

    ## Test 5 — v1 silent migration (D-06)

    **Goal:** A Phase 04 plain-JSON .clmc file opens with silent migration; user re-saves to get v2.

    **Skip condition:** if no v1 .clmc file exists on disk, mark this test "skipped" and proceed.

    **Steps:**
    1. Locate a Phase 04 .clmc (plain JSON, before this phase's deploy). If unavailable, this test is skipped.
    2. Confirm the file is plain JSON: `node -e "console.log(require('fs').readFileSync('PATH', 'utf-8').slice(0, 50))"` should show `{"formatVersion":1,...`
    3. Toolbar → Open → pick the v1 .clmc
    4. Verify NO migration prompt appears
    5. Verify project loads with all markups + scale visible
    6. Verify title bar shows the filename WITH a dirty asterisk: `name.clmc * — CLMC Takeoff`
    7. Press Ctrl+S → save dialog → choose path → click Save
    8. After save: dirty asterisk gone, file overwritten in v2 format
    9. Verify file is now ZIP: `node -e "console.log(require('fs').readFileSync('PATH').slice(0, 4).toString('hex'))"` should print `504b0304`

    **Pass criteria:**
    - [ ] Open succeeded without prompt
    - [ ] All markups + scale loaded
    - [ ] Title bar showed dirty asterisk after open
    - [ ] After Ctrl+S the file became a ZIP archive (hex starts 504b0304)

    **Result:** {pass | fail | partial | skipped}
    **Observations:** {if skipped, note: no v1 file available}

    ---

    ## UAT Results Summary

    | Test | Result | Notes |
    |------|--------|-------|
    | 1 — Save & reopen | {} |  |
    | 2 — Portability | {} |  |
    | 3 — Replace Plan PDF | {} |  |
    | 4 — Saving indicator | {} |  |
    | 5 — v1 silent migration | {} |  |

    **Overall verdict:** {pass | fail with gaps | needs revision}

    **Gaps to address (if any):** {list each gap as a candidate for /gsd:plan-phase --gaps}

    ---

    *UAT script: 2026-04-30*
    ```

    The user fills in {placeholders} during the actual test session. The file's `status: awaiting-user-test` frontmatter signals to gsd:execute-phase that this checkpoint is unresolved.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const exists = fs.existsSync('.planning/phases/04.1-zip-embedded-clmc/04.1-UAT.md'); process.exit(exists ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04.1-zip-embedded-clmc/04.1-UAT.md` exists
    - File contains `## Test 1 — End-to-end save & reopen`
    - File contains `## Test 2 — Portability`
    - File contains `## Test 3 — Replace Plan PDF`
    - File contains `## Test 4 — Saving indicator UX`
    - File contains `## Test 5 — v1 silent migration`
    - File contains `504b0304` (the ZIP magic-bytes hex check for Test 5)
    - File contains `awaiting-user-test` (status flag)
  </acceptance_criteria>
  <done>UAT scaffold ready for the user to walk through during the human-verify checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — execute the 5 UAT scenarios</name>
  <files>.planning/phases/04.1-zip-embedded-clmc/04.1-UAT.md</files>
  <read_first>
    - .planning/phases/04.1-zip-embedded-clmc/04.1-UAT.md (Test 1 scaffold from Task 1; user fills in results during this checkpoint)
  </read_first>
  <what-built>
    The complete Phase 4.1 v2 ZIP-embedded .clmc format. Save now writes a self-contained ZIP archive containing project.json + plan.pdf. Open auto-detects v1/v2 and silently migrates v1 files. Replace Plan PDF button swaps the embedded PDF while preserving markups. Title bar + Toolbar reflect saving-in-flight state.
  </what-built>
  <action>
    The user (estimator / developer) executes the 5 UAT scenarios from 04.1-UAT.md in a real running build of the app. Claude pauses execution at this checkpoint until the user confirms via the resume-signal.

    For each test, the user:
    1. Follows the step-by-step instructions in 04.1-UAT.md
    2. Ticks each pass-criteria checkbox if observed
    3. Fills the Result line: pass | fail | partial | skipped
    4. Captures console errors, timing concerns, or surprising behaviors in Observations

    After all 5 tests, the user fills the UAT Results Summary table and Overall verdict, then types one of:
    - "approved" — overall verdict is pass; phase ready for closure (Task 3 runs)
    - description of failures — overall verdict is fail/partial; phase needs gap closure (Task 3 SKIPS)
  </action>
  <how-to-verify>
    Open `.planning/phases/04.1-zip-embedded-clmc/04.1-UAT.md` and walk through Tests 1–5 in order.

    Test 5 (v1 silent migration) is OPTIONAL — mark it `skipped` if no v1 .clmc exists on disk.

    Save the populated UAT.md before resuming. Tip: use the dev tools console (Ctrl+Shift+I) to copy any error messages verbatim into the Observations field.

    Estimated time: 15–30 minutes for all 5 tests with a real construction PDF.

    If gaps surface (any `fail` or `partial` results), the next step is `/gsd:plan-phase 04.1 --gaps` to create a gap-closure plan against the captured failures.
  </how-to-verify>
  <verify>
    <automated>node -e "const c = require('fs').readFileSync('.planning/phases/04.1-zip-embedded-clmc/04.1-UAT.md', 'utf-8'); process.exit(c.includes('Overall verdict') && !c.includes('{pass | fail | partial}') ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - 04.1-UAT.md has all 5 Result lines filled in (no `{pass | fail | partial}` placeholders remaining)
    - 04.1-UAT.md has "Overall verdict" line filled with one of: pass, fail with gaps, needs revision
    - User has provided the resume-signal (either "approved" or a failure description)
  </acceptance_criteria>
  <done>User has executed all 5 UAT scenarios (or marked Test 5 skipped) and recorded results in 04.1-UAT.md. The resume-signal text dictates whether Task 3 (roadmap closure) runs or whether the phase routes to gap closure.</done>
  <resume-signal>Type "approved" if all tests pass (or all marked skipped/pass), or describe failures by referencing the test number (e.g. "Test 3 partial — page-count modal didn't appear")</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md + ROADMAP.md to record Phase 4.1 closure</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current — find PERS-01 + PERS-02 lines)
    - .planning/ROADMAP.md (current — find Phase 4.1 block)
    - .planning/phases/04.1-zip-embedded-clmc/04.1-UAT.md (after Task 2 — for status reference)
  </read_first>
  <action>
    **Trigger condition:** only execute this task if Task 2 (UAT human-verify) returned `approved` (i.e., overall UAT verdict is `pass`). If verdict is `fail` or `needs revision`, STOP — do not modify REQUIREMENTS or ROADMAP, and prompt the user to run `/gsd:plan-phase 04.1 --gaps`.

    **File 1: `.planning/REQUIREMENTS.md`**

    Find the lines:
    ```
    - [x] **PERS-01**: User can save the current project (PDF file reference + all markup positions + per-page scale) to a .clmc project file
    - [x] **PERS-02**: User can reopen a .clmc project file and continue marking up where they left off
    ```

    Replace with annotated versions:
    ```
    - [x] **PERS-01**: User can save the current project (PDF file reference + all markup positions + per-page scale) to a .clmc project file — **revised by Phase 04.1 (2026-04-30): the .clmc file is now a ZIP archive embedding the PDF, making it fully self-contained. See `.planning/phases/04.1-zip-embedded-clmc/04.1-CONTEXT.md`.**
    - [x] **PERS-02**: User can reopen a .clmc project file and continue marking up where they left off — **revised by Phase 04.1 (2026-04-30): self-contained v2 archive eliminates path-resolution and missing-PDF recovery. v1 plain-JSON files migrate silently to v2 on first save.**
    ```

    Also update the Traceability table — change the rows for PERS-01 + PERS-02 to:
    ```
    | PERS-01 | Phase 4 (revised by 04.1) | Complete |
    | PERS-02 | Phase 4 (revised by 04.1) | Complete |
    ```

    **File 2: `.planning/ROADMAP.md`**

    Find the Phase 4.1 block (line 111-121):
    ```
    ### Phase 4.1: ZIP-Embedded .clmc Format (INSERTED)

    **Goal**: Upgrade the `.clmc` file format from plain UTF-8 JSON ...
    ...
    **Plans**: TBD
    ```

    Update the Plans line to reflect the actual plan structure:
    ```
    **Plans**: 7 plans
    Plans:
    - [x] 04.1-00-PLAN.md — Wave 0 RED tests + jszip install
    - [x] 04.1-01-PLAN.md — project-io v2 primitives + project-schema v2 + serialize
    - [x] 04.1-02-PLAN.md — IPC handlers + preload (readProject discriminated union, writeProject with PDF bytes)
    - [x] 04.1-03-PLAN.md — projectStore.isSaving + viewerStore.pdfBytes
    - [x] 04.1-04-PLAN.md — useProject rewrite (v2 save/open + replacePlanPdf) + sha256-renderer + ArchiveCorruptedModal
    - [x] 04.1-05-PLAN.md — TitleBar Saving... + Toolbar Replace Plan PDF + App.tsx wiring
    - [x] 04.1-06-PLAN.md — Manual UAT + roadmap closure
    ```

    Also update the **Progress** table — change the Phase 4.1 row (or ADD it if not yet listed) to:
    ```
    | 4.1. ZIP-Embedded .clmc Format | 7/7 | Complete | 2026-04-30 |
    ```

    Update the high-level checklist near the top of ROADMAP (around line 11):
    ```
    - [x] **Phase 4.1: ZIP-Embedded .clmc Format** - Upgrade .clmc to a self-contained ZIP archive that embeds the PDF, eliminating path-dependency and enabling true portability (completed 2026-04-30)
    ```

    Run `git diff .planning/REQUIREMENTS.md .planning/ROADMAP.md` to verify changes look right before committing (executor handles commit).
  </action>
  <verify>
    <automated>grep -c "Phase 04.1\|Phase 4.1" .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` PERS-01 line contains `revised by Phase 04.1` annotation
    - `.planning/REQUIREMENTS.md` PERS-02 line contains `revised by Phase 04.1` annotation
    - `.planning/REQUIREMENTS.md` Traceability table shows `Phase 4 (revised by 04.1)` for PERS-01 + PERS-02
    - `.planning/ROADMAP.md` Phase 4.1 block has 7-plan list (each marked `[x]`)
    - `.planning/ROADMAP.md` Progress table includes the Phase 4.1 row marked Complete
    - `.planning/ROADMAP.md` top-level checklist marks Phase 4.1 with `[x]`
    - This task is SKIPPED (and these criteria not enforced) if Task 2 UAT verdict is `fail` or `needs revision`
  </acceptance_criteria>
  <done>Roadmap + requirements reflect Phase 4.1 closure; v2 format is the new normal for PERS-01/PERS-02.</done>
</task>

</tasks>

<verification>
- UAT.md scaffold exists and contains all 5 tests
- After human-verify: UAT.md has populated Results + Observations + Overall verdict
- If verdict=pass: REQUIREMENTS.md + ROADMAP.md updated; commit made
- If verdict=fail: STOP — gap-closure phase needed
</verification>

<success_criteria>
- 5 UAT scenarios documented with explicit pass criteria
- User has executed all 5 tests (or marked Test 5 skipped)
- Overall verdict captured in UAT.md
- If pass: REQUIREMENTS.md PERS-01/PERS-02 annotated; ROADMAP.md Phase 4.1 marked complete with all 7 plans listed
- If fail: gaps captured for follow-up `/gsd:plan-phase 04.1 --gaps`
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-zip-embedded-clmc/04.1-06-SUMMARY.md`: UAT verdict, list of any captured gaps, links to updated REQUIREMENTS.md / ROADMAP.md commits, and a final "Phase 4.1 done" or "Phase 4.1 needs gap closure" recommendation.
</output>
