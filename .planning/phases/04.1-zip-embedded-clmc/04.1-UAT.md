---
status: diagnosed
phase: 04.1-zip-embedded-clmc
source: 04.1-00-SUMMARY.md, 04.1-01-SUMMARY.md, 04.1-02-SUMMARY.md, 04.1-03-SUMMARY.md, 04.1-04-SUMMARY.md, 04.1-05-SUMMARY.md
started: 2026-05-02T03:02:47Z
updated: 2026-05-02T03:21:00Z
tester: pengr.clmc.3@gmail.com
---

## Current Test

[testing complete]

## Tests

### 1. End-to-end save & reopen (PERS-01 + PERS-02)
expected: |
  Pre-flight: `npm run build` + `npm run dev` (or packaged .exe). App opens, no console errors. Real construction PDF >=5 MB on disk.

  Open a real PDF, set scale, place 5+ markups across 2+ pages (count/linear/area/perimeter), Ctrl+S to .clmc. During save: title bar "Saving...", Save buttons disabled. After save: dirty asterisk gone, "Saved" toast. Close cleanly (no dirty modal). Re-launch app, open the .clmc — every markup at same page/position/name/color, per-page scale preserved, zoom/pan state restored, no dirty asterisk.
result: pass

### 2. Portability — open .clmc from a folder with no PDFs
expected: |
  Take the saved .clmc from Test 1, copy to a NEW folder (e.g. `C:\portability-test\`) that contains NO PDFs. Open from that folder. Project opens normally, all markups visible, NO "PDF not found" prompt. (Validates v2 file is fully self-contained.)
result: pass

### 3. Replace Plan PDF + DimensionMismatchModal scale-warning copy
expected: |
  With the Test 1 project open: Toolbar -> Replace Plan PDF.

  (a) Smoke (same file): pick the SAME PDF -> markups remain visible, dirty asterisk appears, no modal. Ctrl+S overwrites .clmc with new sha256.
  (b) Page-count abort: pick a PDF with DIFFERENT page count -> modal "Wrong PDF — Selected PDF has X pages, but project expects Y" with [Pick again]/[Cancel]. Cancel leaves project unchanged.
  (c) Dimension warn (optional, needs PDF with same page count but different size): modal "PDF dimensions changed — markup positions may look wrong".
  (d) CRITICAL — review-concern check: dimension-warning modal body MUST also contain text about scale calibration (e.g. "your scale calibration may need to be re-verified or reset"). This is the MEDIUM consensus review concern.
  Cancel from dimension modal leaves state unchanged.
result: issue
reported: |
  error detected when selecting a pdf:
  pdf-setup.ts:144 [PDF] workerSrc set to blob wrapper; real worker: http://localhost:5173/@fs/.../node_modules/pdfjs-dist/build/pdf.worker.mjs
  usePdfDocument.ts:18 [PDF] loadPdf called, byteLength: 2897118 filePath: CMC_ME Layout_03-31-2026 rev.8.pdf
  usePdfDocument.ts:18 [PDF] loadPdf called, byteLength: 0 filePath: CMC_ME Layout_03-16-2026 rev.7.pdf
  useProject.ts:322 [useProject] replacePlanPdf failed: Cannot perform Construct on a detached ArrayBuffer
severity: blocker

### 4. Saving indicator UX on a large PDF (D-11, D-12)
expected: |
  Open the LARGEST construction PDF available (ideally 30 MB+). Add a markup so isDirty=true. Press Ctrl+S and WATCH the title bar and toolbar during the write.

  Title bar shows "Saving..." text during the write (even if brief). Save/Save As/Replace buttons all visibly dimmed/disabled during the write. After save: title reverts to filename, buttons re-enable, no console errors.
result: pass

### 5. v1 silent migration + first-save-after-open (HIGH review concern)
expected: |
  SKIP if no Phase 04 v1 .clmc (plain JSON) is on disk.

  Confirm file is plain JSON (`{"formatVersion":1,...` at start). Toolbar -> Open the v1 .clmc. NO migration prompt. Project loads with all markups + scale. Title bar shows filename WITH dirty asterisk: `name.clmc * — CLMC Takeoff`.

  CRITICAL — HIGH review-concern check: Press Ctrl+S, choose path, Save. Save COMPLETES SUCCESSFULLY (no error toast, no console error). Without explicit pdfBytes population in openV1Silent, this would fail with "no pdfBytes". After save: dirty asterisk gone, file is now ZIP (first 4 bytes hex `504b0304`). Reopen the now-v2 file — markups + scale identical to before.
result: pass

### 6. Atomic write safety (MEDIUM review concern, optional)
expected: |
  SKIP if not feasible to time the interrupt (a primary automated test in `atomic-write.test.ts` already covers this; this is defense-in-depth).

  Open a project, save it once (valid baseline). Add markups -> dirty. Use a large PDF (50 MB+) so save takes long enough to interrupt. Press Ctrl+S then IMMEDIATELY kill the Electron process via Task Manager. Reopen the app, open the original .clmc — it still opens with the LAST-SAVED state (not corrupted). Note in observations whether any orphaned `.clmc.tmp` exists.
result: pass

## Summary

total: 6
passed: 5
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Replace Plan PDF swaps the embedded PDF and either succeeds (smoke / same file) or aborts cleanly (page-count / dimension warning modals)"
  status: failed
  reason: |
    User reported: "error detected when selecting a pdf"
    Console excerpt:
      usePdfDocument.ts:18 [PDF] loadPdf called, byteLength: 2897118 filePath: CMC_ME Layout_03-31-2026 rev.8.pdf
      usePdfDocument.ts:18 [PDF] loadPdf called, byteLength: 0 filePath: CMC_ME Layout_03-16-2026 rev.7.pdf
      useProject.ts:322 [useProject] replacePlanPdf failed: Cannot perform Construct on a detached ArrayBuffer
    Smoking gun: the second loadPdf shows byteLength: 0 — the source ArrayBuffer was detached by pdfjs-dist before replacePlanPdf could re-read it. Recent commit 1dca433 ("defensive copy before pdfjs-dist transfers ArrayBuffer ownership") may have only patched the initial load path; the replace path is still unprotected.
  severity: blocker
  test: 3
  root_cause: |
    `replacePlanPdf` (src/renderer/src/hooks/useProject.ts:286) calls `pdfjsLib.getDocument({ data: newBytes })` directly with no defensive copy as a pre-validation probe. PDF.js postMessage-transfers the underlying ArrayBuffer to its worker, leaving `newBytes` detached (byteLength === 0). The next branch (`applyReplacePlanPdf` → `loadPdfFromBytes` → `loadPdf`) then receives the same now-detached `newBytes`; in usePdfDocument.ts:23 the `data instanceof Uint8Array` branch is taken, and line 29's `new Uint8Array(u8)` throws "Cannot perform Construct on a detached ArrayBuffer". The 1dca433 fix lived INSIDE `loadPdf` and only protected its own getDocument call — it does nothing for callers that hand getDocument their bytes first.
  artifacts:
    - path: "src/renderer/src/hooks/useProject.ts:286"
      issue: "Unprotected pdfjsLib.getDocument({ data: newBytes }) — primary failure site (pre-validation probe)"
    - path: "src/renderer/src/hooks/useProject.ts:310"
      issue: "Dimension-mismatch path returns pendingBytes: newBytes — but newBytes is already detached by line 286, so Use-anyway flow is also broken (latent symptom of same root cause)"
    - path: "src/renderer/src/hooks/usePdfDocument.ts:29"
      issue: "Already-fixed inline `new Uint8Array(u8)` defensive copy from commit 1dca433 — refactor to share helper, no behavior change"
  missing:
    - "Add cloneForPdfWorker(bytes: Uint8Array): Uint8Array helper in src/renderer/src/lib/pdf-setup.ts returning new Uint8Array(bytes)"
    - "Update useProject.ts:286 to wrap newBytes with cloneForPdfWorker before getDocument"
    - "Update useProject.ts:310 so pendingBytes is a live copy (cleanest: take the clone before line 286 probe and use it for both probe and pendingBytes)"
    - "Refactor usePdfDocument.ts:29 to use the helper instead of inline new Uint8Array(u8) (consistency)"
    - "Add regression test (mirror src/tests/pdf-loader-detached-buffer.test.ts) exercising replacePlanPdf end-to-end with a getDocument mock that detaches its input"
  debug_session: ".planning/debug/replace-plan-pdf-detached-buffer.md"
