---
phase: 04.1-zip-embedded-clmc
plan: "05"
subsystem: ui-wiring
tags: [TitleBar, Toolbar, App, isSaving, Replace-Plan-PDF, ArchiveCorruptedModal, modal-cleanup, tdd-green]

requires:
  - 04.1-03 (projectStore.isSaving, viewerStore.pdfBytes)
  - 04.1-04 (useProject v2: replacePlanPdf, applyReplacePlanPdf, applyArchiveCorruptedProceed; ArchiveCorruptedModal; deleted HashMismatchModal/MissingPdfModal)

provides:
  - TitleBar shows "Saving..." prefix when projectStore.isSaving=true (D-11)
  - Toolbar Replace Plan PDF button (lucide Replace icon, ariaLabel="Replace Plan PDF") in save cluster (D-08)
  - Toolbar onReplaceClick prop wired from App.tsx handleReplaceClick
  - Save / Save As / Replace Plan PDF buttons all aria-disabled=true when isSaving=true (D-11)
  - App.tsx handleReplaceClick: openPdf dialog -> replacePlanPdf -> routes page-count-mismatch/dimension-mismatch/error
  - App.tsx mounts ArchiveCorruptedModal on ProjectOpenResult.kind === 'archive-corrupted' (D-07)
  - App.tsx all deleted-modal references purged (MissingPdfModal, HashMismatchModal, relinkPdf, applyHashMismatchProceed, applyDimensionMismatchProceed)
  - IconButton ariaLabel override prop (prevents verbose title from clobbering aria-label test selector)
  - IconButton pointerEvents: none when disabled (T-4.1-05-05 mitigation)

affects:
  - Runtime now functionally complete for ZIP-embedded .clmc: end-to-end save+reopen, Replace Plan PDF, v1 silent upgrade all operational via GUI

tech-stack:
  added: []
  patterns:
    - ariaLabel override pattern: IconButton accepts ariaLabel prop; uses ariaLabel ?? title for aria-label attribute — decouples verbose tooltip text from aria selector
    - Replace-flow modal routing: App.tsx owns replace state (replacePageAbort, replaceDimMiss) symmetrically with open-flow state (archiveCorrupted)
    - D-11 title override: isSaving branch takes priority over dirtyMark — Saving... prefix replaces entire normal title computation

key-files:
  created: []
  modified:
    - src/renderer/src/components/TitleBar.tsx
    - src/renderer/src/components/Toolbar.tsx
    - src/renderer/src/App.tsx

key-decisions:
  - "ariaLabel override prop added to IconButton — Replace Plan PDF button has verbose tooltip but test selector requires exact 'Replace Plan PDF' string; ariaLabel prop decouples the two"
  - "pointerEvents: none added to disabled IconButton — T-4.1-05-05: pointer-events still fire on aria-disabled without this; no behaviour change for other buttons as onClick is already guarded"
  - "npm install run during execution — jszip was in package.json but not hydrated in worktree; pre-existing issue (same as Wave 3); installed to enable build verification"

metrics:
  duration: 6min
  completed: 2026-04-30
  tasks: 3
  files_modified: 3
---

# Phase 4.1 Plan 05: UI Wiring — TitleBar isSaving + Toolbar Replace + App.tsx v2 Rewrite Summary

**Wave 5 UI wiring complete: TitleBar shows Saving... prefix (D-11); Toolbar has Replace Plan PDF button with correct aria-label (D-08) and isSaving-driven disable on all three save buttons; App.tsx fully rewritten for v2 — ArchiveCorruptedModal mounts on archive-corrupted, handleReplaceClick wired, all deleted-modal references purged. 284 tests GREEN, web typecheck clean, build succeeds.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-04-30T09:22:06Z
- **Completed:** 2026-04-30T09:28:21Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

### Task 1 — TitleBar.tsx: isSaving branch

Added `isSaving` selector from `useProjectStore` and a conditional title computation:

- `isSaving=true` + displayName present: `"Saving... {filename} — CLMC Takeoff"`
- `isSaving=true` + no displayName: `"Saving... — CLMC Takeoff"`
- `isSaving=false`: existing dirty-asterisk behaviour preserved unchanged

Tests: `title-bar-saving.test.ts` (2 tests) GREEN + `title-bar-dirty.test.ts` (3 tests) GREEN (no regression).

### Task 2 — Toolbar.tsx: Replace Plan PDF button + isSaving disable

Changes made:

1. Added `Replace` to lucide-react imports
2. Added `useProjectStore` import
3. Added `ariaLabel?: string` override prop to `IconButton` — `aria-label={ariaLabel ?? title}` prevents verbose tooltip from breaking test selector
4. Added `pointerEvents: disabled ? 'none' : 'auto'` to button style (T-4.1-05-05)
5. Added `onReplaceClick: () => void | Promise<void>` to `ToolbarProps` (D-08)
6. Updated component signature to destructure `onReplaceClick`
7. Added `isSaving` selector from `useProjectStore`
8. Updated `saveDisabled = totalPages === 0 || isSaving`; added `replaceDisabled = totalPages === 0 || isSaving`
9. Added `IconButton` with `icon={Replace}`, `label="Replace Plan PDF"`, `ariaLabel="Replace Plan PDF"`, `disabled={replaceDisabled}`, `onClick={() => void onReplaceClick()}`

Tests: `toolbar-replace-pdf.test.ts` (2) + `toolbar-saving-disabled.test.ts` (2) + `toolbar-open-prop.test.ts` (2) all GREEN.

### Task 3 — App.tsx: v2 rewrite

**Removed:**
- `import { MissingPdfModal }` — module deleted in Wave 4
- `import { HashMismatchModal }` — module deleted in Wave 4
- `import type { ProjectFileV1 }` — v1 type gone from public API
- Destructured `relinkPdf`, `applyHashMismatchProceed`, `applyDimensionMismatchProceed` from `useProject`
- State: `missing`, `hashMiss`, `dimMiss`, `pageAbort` (open-flow modals)
- JSX mounts: `MissingPdfModal`, `HashMismatchModal`, open-flow `DimensionMismatchModal` and `PageCountAbortModal`
- `handleOpenResult` v1 kind comparisons: `missing-pdf`, `hash-mismatch`, `dimension-mismatch`, `page-count-mismatch`

**Added:**
- `import { ArchiveCorruptedModal }` + `import type { ProjectFileV2 }`
- `replacePlanPdf`, `applyReplacePlanPdf`, `applyArchiveCorruptedProceed` from `useProject`
- State: `archiveCorrupted` (open-flow, D-07), `replacePageAbort`, `replaceDimMiss` (replace-flow, D-08)
- `handleReplaceClick`: `window.api.openPdf()` → `new Uint8Array(pick.data)` → `replacePlanPdf(newBytes, pick.filePath)` → routes `page-count-mismatch` / `dimension-mismatch` / `error`
- Slim `handleOpenResult`: handles only `ok` / `archive-corrupted` / `canceled` / `error`
- JSX mounts: `ArchiveCorruptedModal` (archive-corrupted), `PageCountAbortModal` (replace-flow), `DimensionMismatchModal` (replace-flow)
- `onReplaceClick={handleReplaceClick}` on `<Toolbar>`

## Test Results

| Test File | Tests | Status | Notes |
|-----------|-------|--------|-------|
| src/tests/title-bar-saving.test.ts | 2 | GREEN | Task 1 verification |
| src/tests/title-bar-dirty.test.ts | 3 | GREEN | No regression |
| src/tests/toolbar-replace-pdf.test.ts | 2 | GREEN | Task 2 verification |
| src/tests/toolbar-saving-disabled.test.ts | 2 | GREEN | Task 2 verification |
| src/tests/toolbar-open-prop.test.ts | 2 | GREEN | No regression |
| All other tests | 273 | GREEN | Full suite — no regressions |
| **Total** | **284** | **GREEN** | **35 test files** |

## Typecheck Status

| Check | Status | Notes |
|-------|--------|-------|
| `npm run typecheck:web` | PASS (0 errors) | Renderer process |
| `npm run typecheck:node` | PASS (0 errors, after npm install) | Main + preload process |
| `npm run build` | PASS | Full electron-vite build succeeds |

## Task Commits

1. **Task 1: Add isSaving branch to TitleBar** — `a00d9b7` (feat)
2. **Task 2: Add Replace Plan PDF button + isSaving disable to Toolbar** — `07980cc` (feat)
3. **Task 3: Rewrite App.tsx** — `30e4eae` (feat)

## User-Facing Decision Confirmation

| Decision | Status |
|----------|--------|
| D-07: ArchiveCorruptedModal on sha256 mismatch | DONE — App.tsx mounts on `kind === 'archive-corrupted'` |
| D-08: Replace Plan PDF button in Toolbar | DONE — lucide Replace icon, ariaLabel="Replace Plan PDF", wired via prop |
| D-09: Page count validation in Replace flow | DONE — handleReplaceClick routes page-count-mismatch to PageCountAbortModal |
| D-10: Dimension mismatch validation in Replace flow | DONE — handleReplaceClick routes dimension-mismatch to DimensionMismatchModal |
| D-11: Saving... title bar prefix while isSaving=true | DONE — TitleBar conditional title computation |
| D-12: isSaving-driven save button disable | DONE — Toolbar saveDisabled + replaceDisabled both include isSaving |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] npm install required for worktree**
- **Found during:** Task 3 verification (build step)
- **Issue:** jszip in package.json but not in node_modules — same pre-existing issue as Wave 3 (worktree reset to base commit)
- **Fix:** Ran `npm install --prefer-offline`; jszip hydrated from package-lock.json
- **Files modified:** None (package.json already correct)
- **Verification:** `npm run build` succeeded; `npm run typecheck:node` passed

---

**Total deviations:** 1 auto-fixed (Rule 3 — same pattern as Wave 3).
**Impact on plan:** Zero scope change; all acceptance criteria met.

## Known Stubs

None — all three components are fully implemented with real logic. No placeholder values or partial wiring.

## Self-Check: PASSED

Files verified:
- `src/renderer/src/components/TitleBar.tsx` — FOUND, contains `useProjectStore((s) => s.isSaving)`, `Saving...`
- `src/renderer/src/components/Toolbar.tsx` — FOUND, contains `Replace` (in lucide import), `import { useProjectStore }`, `onReplaceClick`, `useProjectStore((s) => s.isSaving)`, `saveDisabled = totalPages === 0 || isSaving`, `label="Replace Plan PDF"`, `ariaLabel="Replace Plan PDF"`, `pointerEvents: disabled ? 'none' : 'auto'`, `aria-label={ariaLabel ?? title}`
- `src/renderer/src/App.tsx` — FOUND, contains `import { ArchiveCorruptedModal }`, `handleReplaceClick`, `applyReplacePlanPdf`, `applyArchiveCorruptedProceed`, `onReplaceClick={handleReplaceClick}`, `setArchiveCorrupted`, `setReplacePageAbort`, `setReplaceDimMiss`; does NOT contain `MissingPdfModal`, `HashMismatchModal`, `relinkPdf`, `applyHashMismatchProceed`, `applyDimensionMismatchProceed`

Commits verified:
- `a00d9b7` — FOUND in git log (Task 1: TitleBar)
- `07980cc` — FOUND in git log (Task 2: Toolbar)
- `30e4eae` — FOUND in git log (Task 3: App.tsx)

---
*Phase: 04.1-zip-embedded-clmc*
*Completed: 2026-04-30*
