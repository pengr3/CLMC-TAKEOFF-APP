---
phase: 04.1-zip-embedded-clmc
plan: "03"
subsystem: state-layer
tags: [zustand, projectStore, viewerStore, isSaving, pdfBytes, dirty-tracking, jsdoc, tdd-green]

requires:
  - 04.1-01 (project-io.ts ZIP primitives + ProjectFileV2 schema)
  - 04.1-02 (IPC layer with file:writeProject 3-arg signature)

provides:
  - isSaving: boolean in projectStore — drives title-bar/button-disable UX in Wave 5
  - setSaving(v: boolean) action in projectStore
  - reset() clears isSaving to false even from mid-save
  - pdfBytes: Uint8Array | null in viewerStore — cache for save flow (avoids re-reading disk)
  - setPdfBytes(bytes) action in viewerStore
  - resetViewer() clears pdfBytes alongside pdfDocument
  - ViewerState.pdfBytes with full lifecycle JSDoc (review-driven documentation)
  - isSaving and pdfBytes both excluded from dirty-tracking subscriptions

affects:
  - 04.1-04 (Wave 4 useProject.ts: reads pdfBytes from viewerStore for writeProject; reads isSaving to gate concurrent saves)
  - 04.1-05 (Wave 5 Toolbar/TitleBar: subscribes to isSaving to disable Save button + show saving indicator)

tech-stack:
  added: []
  patterns:
    - Transient UX flag pattern: isSaving in projectStore is orthogonal to isDirty — setting it never triggers dirty-tracking
    - PDF bytes cache pattern: viewerStore.pdfBytes holds Uint8Array while project open; null on close/reset
    - JSDoc lifecycle pattern: explicit null/non-null contract documented on ViewerState.pdfBytes with memory cost acknowledgment

key-files:
  created: []
  modified:
    - src/renderer/src/stores/projectStore.ts
    - src/renderer/src/stores/viewerStore.ts
    - src/renderer/src/types/viewer.ts

key-decisions:
  - "isSaving excluded from attachDirtyTracking by construction — subscriber selects [s.pageViewports, s.currentPage] from viewerStore and markup/scale fields from their stores; isSaving is projectStore-internal"
  - "pdfBytes lifecycle JSDoc added to ViewerState per review feedback — documents null/non-null contract, memory cost (100MB+ PDFs), and dirty-tracking exclusion warning"
  - "setSaving uses simple set({ isSaving: v }) — no guard needed since Wave 4 save handler checks isSaving at entry and Wave 5 disables the button while saving (T-4.1-03-04 mitigation is UI-layer)"

metrics:
  duration: 3min
  completed: 2026-04-30
  tasks: 2
  files_modified: 3
---

# Phase 4.1 Plan 03: State Layer — isSaving + pdfBytes Pure State Additions Summary

**Wave 3 state-layer delivered: isSaving boolean in projectStore (D-12) and pdfBytes Uint8Array cache in viewerStore — both excluded from dirty-tracking; ViewerState lifecycle JSDoc added per review feedback. All 24 tests GREEN.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-30T09:08:39Z
- **Completed:** 2026-04-30T09:11:34Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Added `isSaving: boolean` (defaults false) and `setSaving: (v: boolean) => void` to `projectStore.ts` — interface + initial state + action + reset body all updated
- Confirmed `attachDirtyTracking` subscribers in `projectStore.ts` still select only `[s.pageMarkups, s.categories, s.categoryOrder]` (markupStore), `[s.pageScales, s.globalUnit]` (scaleStore), and `[s.pageViewports, s.currentPage]` (viewerStore) — `isSaving` is excluded by construction
- Added `pdfBytes: Uint8Array | null` to `ViewerState` interface in `viewer.ts` with full lifecycle JSDoc (null before open or after reset; Uint8Array while project open; memory cost warning for 100MB+ PDFs; dirty-tracking exclusion warning)
- Added `setPdfBytes: (bytes: Uint8Array | null) => void` action declaration to `ViewerState` interface
- Added `pdfBytes: null` to initial state in `viewerStore.ts`
- Added `setPdfBytes` action implementation in `viewerStore.ts`
- Updated `resetViewer()` in `viewerStore.ts` to clear `pdfBytes: null` alongside `pdfDocument: null`

## Final API Surface

### projectStore.ts — additions

| Export | Type | Notes |
|--------|------|-------|
| `isSaving` | `boolean` (in ProjectStoreState) | Defaults false; transient UX flag |
| `setSaving` | `(v: boolean) => void` (in ProjectStoreState) | Sets isSaving; does NOT trigger isDirty |
| `reset()` | updated | Now clears `isSaving: false` alongside currentFilePath/isDirty/lastSavedAt |

### viewerStore.ts — additions

| Export | Notes |
|--------|-------|
| `pdfBytes: null` | Initial state; null before any PDF loads |
| `setPdfBytes(bytes)` | Sets pdfBytes Uint8Array or null |
| `resetViewer()` | Now includes `pdfBytes: null` in reset set |

### types/viewer.ts — additions

| Export | Notes |
|--------|-------|
| `pdfBytes: Uint8Array | null` | In ViewerState interface with lifecycle JSDoc |
| `setPdfBytes: (bytes: Uint8Array | null) => void` | Action declaration in ViewerState interface |

## Test Results

| Test File | Tests | Status | Note |
|-----------|-------|--------|------|
| src/tests/project-store.test.ts | 10 | GREEN | 6 existing + 4 isSaving (D-12) tests |
| src/tests/viewer-store.test.ts | 14 | GREEN | All existing tests still pass |

## Task Commits

1. **Task 1: Add isSaving + setSaving to projectStore.ts** — `ed2c361` (feat)
2. **Task 2: Add pdfBytes cache to viewerStore + ViewerState lifecycle JSDoc** — `d03312a` (feat)

## Dirty-Tracking Isolation Verification

`attachDirtyTracking` in `projectStore.ts` (lines 80-106) wires three subscriptions:

```
useMarkupStore.subscribe((s) => [s.pageMarkups, s.categories, s.categoryOrder])
useScaleStore.subscribe((s) => [s.pageScales, s.globalUnit])
useViewerStore.subscribe((s) => [s.pageViewports, s.currentPage])
```

`isSaving` is not selected by any subscription. `pdfBytes` is not selected by the viewerStore subscription. Both fields are orthogonal to user-data dirty-tracking by construction.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] npm install required for worktree**
- **Found during:** Pre-execution check
- **Issue:** Worktree was reset to base commit with no node_modules directory; vitest not available
- **Fix:** Ran `npm install --prefer-offline` to hydrate node_modules from package-lock.json
- **Files modified:** None (package.json already correct)
- **Verification:** `node_modules/.bin/vitest` confirmed present; all tests passed

---

**Total deviations:** 1 auto-fixed (Rule 3 — blocking dependency issue; same pattern as Wave 2)
**Impact on plan:** Zero scope change.

## Known Stubs

None — both fields are fully implemented primitives. No placeholder values or partial wiring.

## Self-Check: PASSED

Files verified:
- src/renderer/src/stores/projectStore.ts — FOUND, contains isSaving: boolean (interface), isSaving: false (x2: initial + reset), setSaving action
- src/renderer/src/stores/viewerStore.ts — FOUND, contains pdfBytes: null (x2: initial + resetViewer), setPdfBytes action
- src/renderer/src/types/viewer.ts — FOUND, contains pdfBytes: Uint8Array | null, setPdfBytes action declaration, lifecycle JSDoc with Lifecycle: heading and dirty-tracking warning

Commits verified:
- ed2c361 (feat: add isSaving + setSaving to projectStore) FOUND
- d03312a (feat: add pdfBytes cache to viewerStore + ViewerState lifecycle JSDoc) FOUND

---
*Phase: 04.1-zip-embedded-clmc*
*Completed: 2026-04-30*
