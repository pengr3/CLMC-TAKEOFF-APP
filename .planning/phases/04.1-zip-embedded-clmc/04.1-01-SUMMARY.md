---
phase: 04.1-zip-embedded-clmc
plan: "01"
subsystem: persistence-logic
tags: [jszip, project-io, project-schema, project-serialize, tdd-green, v2-format, verifySha256]

requires:
  - 04.1-00 (jszip installed, RED tests written)

provides:
  - sha256Buffer — sync Buffer/Uint8Array hash, no I/O
  - verifySha256 — boolean hash-verification helper (review-suggested; used by Wave 2 + Wave 4)
  - detectClmcFormat — magic-byte ZIP/JSON/unknown sniffer
  - assembleClmcZip — jszip STORE-mode ZIP builder (project.json + plan.pdf)
  - extractClmcZip — exact-match ZIP extractor with descriptive error throws
  - ProjectFileV2 interface (originalFilename replaces absolutePath/relativePath)
  - validateV2 function (rejects v1 path fields)
  - migrate() new signature — returns { data: ProjectFileV2; wasMigrated: boolean }
  - migrateV1ToV2 internal helper (basenameAny handles Windows + POSIX paths)
  - SnapshotParams.pdfOriginalFilename (replaces pdfAbsolutePath + pdfRelativePath)
  - snapshotProject returns ProjectFileV2 with formatVersion: 2

affects:
  - 04.1-02 (Wave 2 IPC: file:readProject/writeProject now have correct helper surface)
  - 04.1-04 (Wave 4 useProject.ts will TypeScript-error until it adopts v2 SnapshotParams)

tech-stack:
  added: []
  patterns:
    - jszip@3.10.1 STORE-mode ZIP assembly/extraction in main process
    - verifySha256 boolean wrapper pattern for clean Wave 2 + 4 callsites
    - basenameAny helper handles both POSIX / and Windows \\ path separators in migration
    - migrate() returns discriminated { data, wasMigrated } — caller marks dirty on wasMigrated=true

key-files:
  created: []
  modified:
    - src/main/project-io.ts
    - src/renderer/src/lib/project-schema.ts
    - src/renderer/src/lib/project-serialize.ts
    - src/tests/project-io.test.ts
    - src/tests/project-schema.test.ts
    - src/tests/project-store.test.ts

key-decisions:
  - "verifySha256 added per cross-AI review suggestion — boolean wrapper around sha256Buffer(buf) === expectedHex for cleaner Wave 2 + 4 callsites"
  - "basenameAny handles both forward-slash (POSIX) and back-slash (Windows) separators in v1 absolutePath migration"
  - "ProjectFileV1 + validateV1 kept in project-schema.ts — still needed as the migration source for v1 files"
  - "migrate() return type widened to { data: ProjectFileV2; wasMigrated: boolean } — caller uses wasMigrated to set isDirty (D-06)"
  - "useProject.ts TypeScript errors are deliberate cross-wave handoff — Wave 4 fixes"

metrics:
  duration: 6min
  completed: 2026-04-30
  tasks: 3
  files_modified: 6
---

# Phase 4.1 Plan 01: Logic Layer — v2 ZIP Primitives + Schema + Serialize Summary

**Wave 1 logic layer delivered: 5 new exports in project-io.ts (including verifySha256 per review), ProjectFileV2 with silent v1 migration in project-schema.ts, and SnapshotParams updated to v2 shape in project-serialize.ts. 31 Wave 0 RED tests now GREEN.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-04-30T08:52:42Z
- **Completed:** 2026-04-30T08:59:01Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- Rewrote `project-io.ts`: deleted 2 path-resolution functions (resolvePdfPath, computeRelativePath) + ResolvedPdfPath interface; added sha256Buffer, verifySha256, detectClmcFormat, assembleClmcZip, extractClmcZip
- Added `ProjectFileV2` interface, `validateV2`, `migrateV1ToV2`, updated `migrate()` signature, updated `ProjectFile` type alias in `project-schema.ts`; kept ProjectFileV1 + validateV1 intact as migration sources
- Updated `SnapshotParams` (pdfOriginalFilename replacing pdfAbsolutePath/pdfRelativePath), `snapshotProject` (returns ProjectFileV2 with formatVersion 2), and `hydrateStores` (accepts ProjectFileV2) in `project-serialize.ts`
- Purged 4 stale path-resolution tests from `project-io.test.ts`; fixed FIXME migration test in `project-schema.test.ts`; updated snap fixture from v1 to v2 in `project-store.test.ts`

## Final API Surface

### project-io.ts — exports

| Function | Signature | Notes |
|----------|-----------|-------|
| `sha256File` | `(filePath: string): Promise<string>` | KEPT — stream-based, file on disk |
| `sha256Buffer` | `(buf: Buffer \| Uint8Array): string` | NEW — sync, in-memory |
| `verifySha256` | `(buf: Buffer \| Uint8Array, expectedHex: string): boolean` | NEW — review-suggested helper |
| `enforceClmcExtension` | `(filePath: string): string` | KEPT |
| `detectClmcFormat` | `(buf: Buffer): ClmcFormat` | NEW — magic-byte sniffer |
| `assembleClmcZip` | `(projectJson: string, pdfBytes: Buffer): Promise<Buffer>` | NEW — jszip STORE mode |
| `extractClmcZip` | `(zipBytes: Buffer): Promise<ExtractedClmc>` | NEW — exact-match lookup |
| `ClmcFormat` | `type = 'v2-zip' \| 'v1-json' \| 'unknown'` | NEW |
| `ExtractedClmc` | `interface { projectJson: string; pdfBytes: Buffer }` | NEW |

**Deleted:** `resolvePdfPath`, `computeRelativePath`, `ResolvedPdfPath` (D-05)

### project-schema.ts — exports

| Export | Type | Notes |
|--------|------|-------|
| `ProjectFileV1` | interface | KEPT — migration source; has absolutePath/relativePath |
| `validateV1` | function | KEPT — migration source |
| `ProjectFileV2` | interface | NEW — has originalFilename, no path fields |
| `validateV2` | function | NEW — rejects v1 path fields, requires originalFilename |
| `migrate` | `(raw, fromVersion) => { data: ProjectFileV2; wasMigrated: boolean }` | CHANGED — now returns { data, wasMigrated } |
| `ProjectFile` | `type = ProjectFileV2` | CHANGED — bumped from V1 to V2 |
| `migrateV1ToV2` | function (internal) | NEW — basenameAny handles Win + POSIX paths |

### project-serialize.ts — exports

| Export | Change | Notes |
|--------|--------|-------|
| `SnapshotParams` | CHANGED | pdfOriginalFilename replaces pdfAbsolutePath + pdfRelativePath |
| `snapshotProject` | CHANGED | Returns ProjectFileV2 with formatVersion: 2 |
| `hydrateStores` | CHANGED | Accepts ProjectFileV2 (body unchanged) |

## Test Results

| Test File | Tests | Status | Note |
|-----------|-------|--------|------|
| src/tests/project-io.test.ts | 15 | GREEN | 13 new v2 tests + 2 kept |
| src/tests/project-schema.test.ts | 11 | GREEN | 6 new v2 tests + 5 existing |
| src/tests/project-serialize.test.ts | 5 | GREEN | All 5 updated for v2 shape |

## Task Commits

1. **Task 1: Rewrite project-io.ts** — `b2b9135` (feat)
2. **Task 2: Add ProjectFileV2 + validateV2 + migrateV1ToV2 to project-schema.ts** — `7a694d2` (feat)
3. **Task 3: Update project-serialize.ts** — `28a3909` (feat)

## Known Cross-Wave Breakage (Expected)

| File | Breakage | Resolved in |
|------|----------|-------------|
| `src/renderer/src/hooks/useProject.ts` | TypeScript errors: snapshotProject call passes pdfAbsolutePath to a function that no longer accepts it | Wave 4 (Plan 04.1-04) |
| `src/main/ipc-handlers.ts` | References resolvePdfPath + computeRelativePath which no longer exist in project-io.ts | Wave 2 (Plan 04.1-02) |

These TypeScript errors are the deliberate cross-wave handoff documented in the plan. They do NOT affect test execution (tests run in the specific file's compilation scope).

## Pre-existing RED Tests (Wave 0 — Not Regressed)

The following 18 tests remain RED from Wave 0 — they are contracts for future waves and were NOT caused by Wave 1 changes:

- `atomic-write.test.ts` (2) — Wave 2 IPC handler atomic write pattern
- `project-store.test.ts` isSaving describe (4) — Wave 3 projectStore.isSaving field
- `project-open-flow.test.ts` archive-corrupted (1) — Wave 2 IPC routing
- `title-bar-saving.test.ts` (1) — Wave 5 TitleBar
- `v1-migration-pdfbytes.test.ts` (3) — Wave 4 useProject v1 flow
- `replace-plan-pdf.test.ts` (3) — Wave 4 replacePlanPdf hook
- `toolbar-replace-pdf.test.ts` (2) — Wave 5 Toolbar
- `toolbar-saving-disabled.test.ts` (2) — Wave 5 Toolbar

## Deviations from Plan

None — plan executed exactly as written. The 3 tasks completed in order with all acceptance criteria met.

## Known Stubs

None — this plan delivers complete pure-function implementations, not stubs.

## Self-Check: PASSED

Files verified:
- src/main/project-io.ts FOUND
- src/renderer/src/lib/project-schema.ts FOUND
- src/renderer/src/lib/project-serialize.ts FOUND
- src/tests/project-io.test.ts FOUND
- src/tests/project-schema.test.ts FOUND
- src/tests/project-store.test.ts FOUND

Commits verified:
- b2b9135 (feat: rewrite project-io.ts) FOUND
- 7a694d2 (feat: add ProjectFileV2 + validateV2) FOUND
- 28a3909 (feat: update project-serialize.ts) FOUND

---
*Phase: 04.1-zip-embedded-clmc*
*Completed: 2026-04-30*
