---
phase: 07.1
plan: 06
type: execute
wave: 3
depends_on:
  - 07.1-04
  - 07.1-05
files_modified:
  - src/renderer/src/App.tsx
  - src/renderer/src/components/RibbonToolbar.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "App.tsx has a handleArmTool(item, categoryName) useCallback that calls setChainArmedFromTotals BEFORE setActiveTool (critical ordering)"
    - "handleArmTool resolves itemName via labelToName(item.label) and toolType via rowTypeToMarkupType(item.type)"
    - "handleArmTool passes color: item.color ?? MARKUP_PALETTE[0] to setChainArmedFromTotals payload"
    - "handleArmTool scans markup store for most-recent WallMarkup with matching name when toolType === 'wall', defaults to 2400"
    - "TotalsPanel in App.tsx JSX has onArmTool={handleArmTool} wired"
    - "RibbonToolbar handleMarkupToolClick calls setChainArmedFromTotals(null) in the disarm branch (activeTool === tool)"
    - "Esc handler in CanvasViewport (or equivalent cancel path) sets _chainArmedItem = null via setChainArmedFromTotals(null)"
  artifacts:
    - path: "src/renderer/src/App.tsx"
      provides: "handleArmTool handler + TotalsPanel onArmTool wire"
      contains: "handleArmTool"
    - path: "src/renderer/src/components/RibbonToolbar.tsx"
      provides: "setChainArmedFromTotals(null) in disarm branch"
      contains: "setChainArmedFromTotals"
  key_links:
    - from: "App.tsx handleArmTool"
      to: "setChainArmedFromTotals(payload)"
      via: "import from CanvasViewport"
      pattern: "setChainArmedFromTotals\\("
    - from: "App.tsx handleArmTool"
      to: "useViewerStore.getState().setActiveTool(toolType)"
      via: "called AFTER setChainArmedFromTotals (ordering critical)"
      pattern: "setActiveTool\\(toolType\\)"
    - from: "RibbonToolbar handleMarkupToolClick disarm branch"
      to: "setChainArmedFromTotals(null)"
      via: "import from CanvasViewport"
      pattern: "setChainArmedFromTotals\\(null\\)"
---

<objective>
Wire handleArmTool into App.tsx (reading from Plans 01–05's contracts) and add setChainArmedFromTotals(null) to RibbonToolbar's disarm path. This is the final integration plan that makes the end-to-end arm flow functional.

Purpose: App.tsx is the orchestration layer. RibbonToolbar needs an explicit null-clear on disarm to synchronously update _chainArmedItem for any imperative callers.

Output: App.tsx with handleArmTool + TotalsPanel wire. RibbonToolbar with setChainArmedFromTotals(null) in handleMarkupToolClick disarm branch.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\PROJECT.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\STATE.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\phases\07.1-resume-markup-group-from-totals\07.1-RESEARCH.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\phases\07.1-resume-markup-group-from-totals\07.1-PATTERNS.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\phases\07.1-resume-markup-group-from-totals\07.1-UI-SPEC.md

<interfaces>
<!-- From App.tsx — existing patterns to replicate -->

Existing imports near line 24:
  import { getCanvasControls } from './components/CanvasViewport'
  — add: setChainArmedFromTotals (same import line or new)
  — add: labelToName, rowTypeToMarkupType (from './components/TotalsRow')

Existing handler pattern — handleExportClick (lines 204–228):
  const handleExportClick = useCallback(async (): Promise<void> => {
    const { pageMarkups, categories, ... } = useMarkupStore.getState()
    ...
    useProjectStore.getState().setIsExporting(true)
    ...
  }, [])

useMarkupStore.getState() pattern: already used in multiple handlers — same pattern for wall scan

TotalsPanel JSX (lines ~362–371):
  <TotalsPanel
    open={totals.open}
    width={effectiveTotalsWidth}
    onSetOpen={setTotalsOpen}
    onSetWidth={setTotalsWidth}
    onRowHover={setHoverMatches}
    onTriggerPulse={triggerPulse}
    onCopy={(msg) => setCopyToast(msg)}
    onCopyError={() => setCopyToast('Copy failed.')}
  />
  — add: onArmTool={handleArmTool}

useViewerStore.getState().setActiveTool: already used in App.tsx via store import

MARKUP_PALETTE import: src/renderer/src/lib/markup-palette.ts — check if already imported in App.tsx; if not, add it

WallMarkup type: src/renderer/src/types/markup.ts — may need import

<!-- From RibbonToolbar.tsx — handleMarkupToolClick -->

handleMarkupToolClick (lines 193–201):
  const handleMarkupToolClick = (tool: 'count' | 'linear' | 'area' | 'perimeter' | 'wall'): void => {
    if (activeTool === tool) {
      setActiveTool('select')   // disarm — add setChainArmedFromTotals(null) here
    } else {
      setActiveTool(tool)
    }
  }

Existing CanvasViewport import in RibbonToolbar: check if already imported; if not, add:
  import { getChainArmedItem, setChainArmedFromTotals } from './CanvasViewport'
  (getChainArmedItem is already used in render — check the import)

<!-- Wall height scan pattern from markupStore.ts getColorForName (lines 225–237) -->
Scan all pages in pageMarkups, filter m.type === 'wall' && m.name === itemName,
find latest by createdAt, return wallHeight ?? 2400
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add handleArmTool to App.tsx and wire to TotalsPanel</name>
  <files>src/renderer/src/App.tsx</files>
  <read_first>
    src/renderer/src/App.tsx — read fully; locate: existing imports block (especially CanvasViewport import ~line 24), handleExportClick useCallback (lines ~204–228) as handler pattern reference, TotalsPanel JSX (~line 362), existing useMarkupStore.getState() usage to confirm import, existing useViewerStore import
    src/renderer/src/lib/markup-palette.ts — confirm MARKUP_PALETTE[0] value (likely '#EF4444' or similar — needed as color fallback)
    src/renderer/src/types/markup.ts — confirm WallMarkup interface has wallHeight: number field
  </read_first>
  <action>
    In App.tsx:

    1. Imports: add setChainArmedFromTotals to the existing CanvasViewport import (same line as getCanvasControls). Add labelToName and rowTypeToMarkupType to a new import from './components/TotalsRow'. Add MARKUP_PALETTE import from './lib/markup-palette' if not present. Add WallMarkup type import from './types/markup' if not present.

    2. Add handleArmTool useCallback. Place it after other useCallback handlers (e.g., after handleExportClick). The CRITICAL ordering rule: setChainArmedFromTotals(payload) MUST be called BEFORE useViewerStore.getState().setActiveTool(toolType). Do not reverse this.

    handleArmTool body:
    - const toolType = rowTypeToMarkupType(item.type)
    - const itemName = labelToName(item.label)
    - For wall height: if (toolType === 'wall') { const { pageMarkups } = useMarkupStore.getState(); let latestWall: WallMarkup | null = null; iterate Object.values(pageMarkups), for each markup in the list check m.type === 'wall' && m.name === itemName; track latest by createdAt; wallHeight = latestWall?.wallHeight ?? 2400 }
    - setChainArmedFromTotals({ name: itemName, categoryName, color: item.color ?? MARKUP_PALETTE[0], toolType, wallHeight })
    - useViewerStore.getState().setActiveTool(toolType)
    - Deps array: []

    3. TotalsPanel JSX: add `onArmTool={handleArmTool}` to the existing TotalsPanel props.

    The categoryName argument to handleArmTool is the second argument (string) — use it directly (per RESEARCH Pitfall 4: do NOT look up from item.categoryId; the categoryName is already the display string curried from TotalsCategoryBlock).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - handleArmTool useCallback is defined in App.tsx
    - setChainArmedFromTotals is called BEFORE setActiveTool in handleArmTool body
    - Wall height scan uses the getColorForName pattern (Object.values(pageMarkups) iteration)
    - item.color ?? MARKUP_PALETTE[0] used as color fallback
    - TotalsPanel JSX has onArmTool={handleArmTool}
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add setChainArmedFromTotals(null) to RibbonToolbar disarm branch</name>
  <files>src/renderer/src/components/RibbonToolbar.tsx</files>
  <read_first>
    src/renderer/src/components/RibbonToolbar.tsx — read lines 1–30 (imports) and lines 185–210 (handleMarkupToolClick) to find: (a) whether setChainArmedFromTotals is already imported (getChainArmedItem is already used in render, so CanvasViewport is likely imported), (b) exact current body of handleMarkupToolClick
  </read_first>
  <action>
    In RibbonToolbar.tsx:

    1. Imports: add setChainArmedFromTotals to the existing CanvasViewport import (alongside getChainArmedItem which is already used). If CanvasViewport is not yet imported, add: `import { getChainArmedItem, setChainArmedFromTotals } from './CanvasViewport'`.

    2. handleMarkupToolClick: in the disarm branch (where activeTool === tool), add `setChainArmedFromTotals(null)` before or alongside `setActiveTool('select')`:
       `if (activeTool === tool) {`
         `setChainArmedFromTotals(null)`
         `setActiveTool('select')`
       `}`

    The null call synchronously clears _chainArmedItem so that even if something reads getChainArmedItem() between the setActiveTool call and the next React render, it gets null. This is defensive per RESEARCH Focus Area 8 recommendation.

    Do not modify the else branch or any other handler.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - setChainArmedFromTotals is imported in RibbonToolbar.tsx
    - handleMarkupToolClick disarm branch contains setChainArmedFromTotals(null) before setActiveTool('select')
    - TypeScript compiles without errors
    - No other handler or render logic changed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TotalsRow onClick → App.tsx handleArmTool | item.label, item.type, item.color, categoryName from in-memory store |
| App.tsx → useMarkupTool via setChainArmedFromTotals | name/category/color/toolType from store; no external user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.1-06-01 | Tampering | item.color null fallback | accept | MARKUP_PALETTE[0] is a safe compile-time constant fallback; null item.color means no prior markup placed, palette default is correct |
| T-7.1-06-02 | Spoofing | categoryName arg | accept | categoryName is curried from category.name in TotalsCategoryBlock — source is the markup store, not user input at this call |
| T-7.1-06-03 | Denial of Service | wall scan O(n) | accept | Scan runs once per row click; markup store is bounded by project size (no streaming); acceptable cost |
</threat_model>

<verification>
1. TypeScript compilation: `npx tsc --noEmit` exits 0
2. handleArmTool in App.tsx: `grep -n "handleArmTool" src/renderer/src/App.tsx` — defined as useCallback and wired to TotalsPanel
3. Ordering check: in App.tsx grep output, setChainArmedFromTotals line appears before setActiveTool line within handleArmTool body
4. Disarm in RibbonToolbar: `grep -n "setChainArmedFromTotals" src/renderer/src/components/RibbonToolbar.tsx` — one match in handleMarkupToolClick
5. Full test run: `npx vitest run src/tests/` — all tests pass
</verification>

<success_criteria>
End-to-end arm flow is wired: TotalsRow click → TotalsCategoryBlock curry → TotalsPanel pass-through → App.tsx handleArmTool → setChainArmedFromTotals → activatePreset → setActiveTool. Disarm works via RibbonToolbar re-click. TypeScript clean. All existing tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-resume-markup-group-from-totals/07.1-06-SUMMARY.md`
</output>
