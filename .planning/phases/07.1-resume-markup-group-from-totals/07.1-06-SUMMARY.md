---
phase: "07.1"
plan: "06"
subsystem: "arm-from-totals integration"
tags: [arm-tool, totals-panel, ribbon-toolbar, chain-armed, App.tsx]
dependency_graph:
  requires: [07.1-04, 07.1-05]
  provides: [handleArmTool, setChainArmedFromTotals-disarm]
  affects: [App.tsx, RibbonToolbar.tsx]
tech_stack:
  added: []
  patterns: [module-level-ref-arm-pattern, useMarkupStore.getState-wall-scan]
key_files:
  created: []
  modified:
    - src/renderer/src/App.tsx
    - src/renderer/src/components/RibbonToolbar.tsx
decisions:
  - "handleArmTool calls setChainArmedFromTotals BEFORE setActiveTool — ordering is critical per RESEARCH Focus Area 8"
  - "Wall height scan uses getColorForName pattern (Object.values(pageMarkups) iteration) with max(createdAt) to find most-recent wall markup"
  - "setChainArmedFromTotals(null) in disarm branch fires before setActiveTool('select') to ensure synchronous _chainArmedItem clear"
metrics:
  duration: "~8min"
  completed_date: "2026-05-19"
  tasks_completed: 2
  files_modified: 2
---

# Phase 7.1 Plan 06: App.tsx Integration + RibbonToolbar Disarm Summary

End-to-end arm flow wired: TotalsRow click curried via TotalsCategoryBlock through TotalsPanel onArmTool prop to App.tsx handleArmTool which fires setChainArmedFromTotals (arming _chainArmedItem + activatePreset) then setActiveTool; RibbonToolbar disarm re-click also clears _chainArmedItem via setChainArmedFromTotals(null).

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add handleArmTool to App.tsx and wire to TotalsPanel | d5c9440 | src/renderer/src/App.tsx |
| 2 | Add setChainArmedFromTotals(null) to RibbonToolbar disarm branch | 319cd3e | src/renderer/src/components/RibbonToolbar.tsx |

## What Was Built

### Task 1 — App.tsx handleArmTool

Added `handleArmTool` useCallback to App.tsx. It:
1. Calls `rowTypeToMarkupType(item.type)` to get the MarkupType from the BoqRowType
2. Calls `labelToName(item.label)` to strip disambiguation suffixes ("Outlet (count)" → "Outlet")
3. For `wall` tool type: scans `useMarkupStore.getState().pageMarkups` for the most-recent `WallMarkup` with matching name, defaults wallHeight to 2400 if none found
4. Calls `setChainArmedFromTotals(payload)` FIRST (critical ordering — fires _activatePresetRef in CanvasViewport before tool mode change)
5. Calls `useViewerStore.getState().setActiveTool(toolType)` AFTER

New imports added to App.tsx:
- `setChainArmedFromTotals` from `./components/CanvasViewport` (added to existing import)
- `labelToName, rowTypeToMarkupType` from `./components/TotalsRow` (new import)
- `MARKUP_PALETTE` from `./lib/markup-palette` (new import)
- `useMarkupStore` from `./stores/markupStore` (new import)
- `BoqItemRow` type from `./lib/boq-types` (added to existing import)
- `WallMarkup` type from `./types/markup` (new import)

TotalsPanel JSX gained `onArmTool={handleArmTool}`.

### Task 2 — RibbonToolbar disarm clear

Added `setChainArmedFromTotals` to the existing `CanvasViewport` import (alongside `getChainArmedItem`). In `handleMarkupToolClick`, the disarm branch now calls `setChainArmedFromTotals(null)` before `setActiveTool('select')`, synchronously clearing `_chainArmedItem` so any imperative caller reading `getChainArmedItem()` between the setActiveTool call and the next React render sees null.

## Verification Results

- TypeScript: `npx tsc --noEmit` exits 0 (clean)
- Tests: `npx vitest run src/tests/` — 493/493 passed, 68 test files
- handleArmTool defined as useCallback and wired to TotalsPanel: confirmed
- setChainArmedFromTotals called before setActiveTool in handleArmTool: confirmed (lines 260 then 267)
- RibbonToolbar disarm contains setChainArmedFromTotals(null): confirmed (line 197)

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes introduced. Both trust boundaries (TotalsRow → handleArmTool, handleArmTool → setChainArmedFromTotals) operate purely in-memory using data from the Zustand markup store.

## Self-Check: PASSED

- [x] src/renderer/src/App.tsx modified — verified handleArmTool at line 238, onArmTool wire at line 411
- [x] src/renderer/src/components/RibbonToolbar.tsx modified — verified setChainArmedFromTotals(null) at line 197
- [x] Commit d5c9440 exists — Task 1
- [x] Commit 319cd3e exists — Task 2
- [x] TypeScript clean
- [x] All 493 tests pass
