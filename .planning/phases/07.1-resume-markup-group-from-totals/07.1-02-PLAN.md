---
phase: 07.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/TotalsRow.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "TotalsRow renders a 20px fixed-width slot after the UoM column; a Plus icon (size=14) appears inside it only while the row is hovered"
    - "TotalsRow accepts onArmTool?: (item: BoqItemRow) => void and fires it inside handleClick after cycle navigation"
    - "labelToName and rowTypeToMarkupType are exported from TotalsRow.tsx so App.tsx can import them"
    - "The Plus icon tooltip reads 'Add more to this group'"
    - "The slot is always 20px wide regardless of hover state — no reflow of UoM column"
  artifacts:
    - path: "src/renderer/src/components/TotalsRow.tsx"
      provides: "onArmTool prop + Plus slot + exported helpers"
      exports: ["labelToName", "rowTypeToMarkupType"]
  key_links:
    - from: "TotalsRow.handleClick"
      to: "onArmTool?.(item)"
      via: "optional-chain call after cycle navigation block"
      pattern: "onArmTool\\?\\."
---

<objective>
Extend TotalsRow with three changes: (1) export labelToName and rowTypeToMarkupType as named exports; (2) add the optional onArmTool prop and fire it inside handleClick after cycle navigation; (3) add the 20px "+" hover affordance slot after the UoM column.

Purpose: TotalsRow is the leaf component where the arm interaction happens. All three changes are independent of each other and of the other Wave 1 plans.

Output: TotalsRow.tsx with exported helpers, new prop, "+" slot, and updated handleClick.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\PROJECT.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\STATE.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\phases\07.1-resume-markup-group-from-totals\07.1-RESEARCH.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\phases\07.1-resume-markup-group-from-totals\07.1-PATTERNS.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\phases\07.1-resume-markup-group-from-totals\07.1-UI-SPEC.md

<interfaces>
<!-- From TotalsRow.tsx — current structure the executor must extend -->

labelToName (lines 62–64): private function — strips '\s*\((count|linear|area|perimeter|wall)\)\s*$' suffix from label
rowTypeToMarkupType (lines 67–70): private function — maps BoqRowType → MarkupType
  'perimeter-length' | 'perimeter-area' → 'perimeter'; others pass through unchanged

TotalsRowProps interface (lines 40–54):
  item: BoqItemRow
  cycleIndex: number
  currentPageMatches: Markup[]
  onHover: (matches: Markup[]) => void
  onClick: (item: BoqItemRow) => void
  onContextMenu: (x: number, y: number) => void
  onTriggerPulse?: (matches: Markup[], color: string) => void

handleClick (lines 153–173):
  - calls onClick(item) always
  - if pagesWithMatches.length === 0: returns early
  - else: cycle navigation (setPage, onTriggerPulse, setInternalCycleIndex)

Lightbulb slot analog (lines 232–243):
  <div style={{ width: 16, flexShrink: 0, display: 'flex', alignItems: 'center', cursor: 'pointer' }} ... >
    {isHidden ? <LightbulbOff size={12}/> : <Lightbulb size={12}/>}
  </div>

bg hover state (line 105): useState('transparent')
  onMouseEnter → setBg(COLORS.hoverSurface)
  onMouseLeave → setBg('transparent')

Existing lucide-react import (line ~7): import { Lightbulb, LightbulbOff } from 'lucide-react'

COLORS tokens from src/renderer/src/lib/constants.ts — all styling uses these tokens, no raw hex literals, no Tailwind.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export labelToName and rowTypeToMarkupType</name>
  <files>src/renderer/src/components/TotalsRow.tsx</files>
  <read_first>
    src/renderer/src/components/TotalsRow.tsx — read fully; locate labelToName (line ~62) and rowTypeToMarkupType (line ~67) exact current signatures before editing
  </read_first>
  <action>
    Add the export keyword to both function declarations:
    - Change `function labelToName(label: string): string` to `export function labelToName(label: string): string`
    - Change `function rowTypeToMarkupType(t: BoqRowType): MarkupType` to `export function rowTypeToMarkupType(t: BoqRowType): MarkupType`

    Do not modify the function bodies. Do not add any other changes in this task step (they follow in Task 2).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - TotalsRow.tsx line for labelToName starts with `export function labelToName`
    - TotalsRow.tsx line for rowTypeToMarkupType starts with `export function rowTypeToMarkupType`
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add onArmTool prop, extend handleClick, and add Plus slot</name>
  <files>src/renderer/src/components/TotalsRow.tsx</files>
  <read_first>
    src/renderer/src/components/TotalsRow.tsx — after Task 1 edits; locate TotalsRowProps interface, handleClick body, lightbulb slot in JSX, UoM span in JSX, existing lucide-react import
  </read_first>
  <action>
    Three targeted edits to TotalsRow.tsx:

    1. TotalsRowProps interface: add `onArmTool?: (item: BoqItemRow) => void` as the last optional prop, after `onTriggerPulse?`. Note: onArmTool takes only item (not categoryName) — TotalsCategoryBlock curries the category name before passing down.

    2. Destructure onArmTool from props alongside the other props.

    3. handleClick: append `onArmTool?.(item)` as the final statement in handleClick — fires after the early-return block AND after the cycle navigation block. Concretely: add `onArmTool?.(item)` once at the very end of handleClick, after the setInternalCycleIndex call. The early-return branch for pagesWithMatches.length===0 should also call onArmTool before returning — insert `onArmTool?.(item)` before the `return` statement in that branch.

    4. Lucide-react import: add `Plus` to the existing import alongside Lightbulb and LightbulbOff.

    5. JSX: after the UoM `<span>` (the 40px-wide quantity/unit column), append a new 20px fixed-width slot:
       `<div style={{ width: 20, flexShrink: 0, display: 'flex', alignItems: 'center', justifyContent: 'center' }} title="Add more to this group">`
       Inside: `{bg !== 'transparent' && <Plus size={14} color={COLORS.textSecondary} />}`
       Close `</div>`.
       The slot has no onClick handler of its own — it is a passive visual affordance only.

    COLORS.textSecondary is the icon color per the UI spec (unobtrusive, not accent).
    Do not add any Tailwind classes. Use inline style only.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - TotalsRowProps contains `onArmTool?: (item: BoqItemRow) => void`
    - handleClick fires `onArmTool?.(item)` in both the early-return path and the normal path
    - The 20px Plus slot exists in JSX after the UoM span
    - Plus icon renders only when bg !== 'transparent'
    - Slot title attribute is "Add more to this group"
    - Plus is in the lucide-react import
    - TypeScript compiles without errors
    - No Tailwind classes introduced
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TotalsRow onClick → onArmTool | Row click passes BoqItemRow (from markup store) to parent callback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.1-02-01 | Tampering | onArmTool item payload | accept | BoqItemRow is derived from the in-memory markup store; no external user-typed data crosses this prop boundary |
| T-7.1-02-02 | Spoofing | Plus slot click interception | accept | The Plus slot has no click handler; clicks propagate to row onClick naturally — no additional attack surface |
</threat_model>

<verification>
1. TypeScript compilation: `npx tsc --noEmit` exits 0
2. Export check: `grep -n "^export function labelToName\|^export function rowTypeToMarkupType" src/renderer/src/components/TotalsRow.tsx` — both lines present
3. onArmTool in props interface: `grep -n "onArmTool" src/renderer/src/components/TotalsRow.tsx` — appears in TotalsRowProps, destructure, and handleClick
4. Plus slot: `grep -n "Plus" src/renderer/src/components/TotalsRow.tsx` — appears in import and JSX
</verification>

<success_criteria>
TotalsRow.tsx exports labelToName and rowTypeToMarkupType. TotalsRowProps includes onArmTool optional callback. handleClick fires onArmTool in both code paths. Plus icon appears in the 20px hover slot. TypeScript clean.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-resume-markup-group-from-totals/07.1-02-SUMMARY.md`
</output>
