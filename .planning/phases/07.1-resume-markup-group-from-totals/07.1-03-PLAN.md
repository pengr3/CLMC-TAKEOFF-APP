---
phase: 07.1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/TotalsCategoryBlock.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "TotalsCategoryBlock accepts onArmTool?: (item: BoqItemRow, categoryName: string) => void"
    - "TotalsCategoryBlock passes onArmTool={(it) => onArmTool?.(it, category.name)} to each TotalsRow render"
    - "The curry pattern matches the existing onRowClick curry at TotalsCategoryBlock line 142"
  artifacts:
    - path: "src/renderer/src/components/TotalsCategoryBlock.tsx"
      provides: "onArmTool prop thread with category name curry"
      contains: "onArmTool"
  key_links:
    - from: "TotalsCategoryBlock"
      to: "TotalsRow.onArmTool"
      via: "curry: (it) => onArmTool?.(it, category.name)"
      pattern: "onArmTool.*category\\.name"
---

<objective>
Add the onArmTool prop to TotalsCategoryBlock and wire it through to TotalsRow with a category.name curry, following the identical pattern already used for onRowClick at line 142.

Purpose: TotalsCategoryBlock is the middle layer that knows category.name. It curries the name into the callback so TotalsRow only needs to pass item, and App.tsx receives (item, categoryName) without needing to look up the category separately.

Output: TotalsCategoryBlock.tsx with new prop, destructure, and curry in TotalsRow render.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\PROJECT.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\phases\07.1-resume-markup-group-from-totals\07.1-RESEARCH.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\phases\07.1-resume-markup-group-from-totals\07.1-PATTERNS.md

<interfaces>
<!-- From TotalsCategoryBlock.tsx — current structure the executor must extend -->

TotalsCategoryBlockProps interface (lines 29–40):
  category: BoqCategoryGroup
  pageMarkups: Markup[]
  cycleIndexByKey?: Record<string, number>
  onRowHover: (matches: Markup[]) => void
  onRowClick: (item: BoqItemRow, categoryName: string) => void
  onRowContextMenu: (item: BoqItemRow, x: number, y: number) => void
  onTriggerPulse?: (matches: Markup[], color: string) => void

TotalsRow render (lines 136–145) — existing curry pattern to replicate:
  onClick={(it) => onRowClick(it, category.name)}

Destructure (line ~65):
  const { category, pageMarkups, cycleIndexByKey, onRowHover, onRowClick, onRowContextMenu, onTriggerPulse } = props

TotalsRow import: already imported from './TotalsRow'
  — after Plan 02, TotalsRow accepts onArmTool?: (item: BoqItemRow) => void (no categoryName — curry happens here)

BoqItemRow import: already in scope from '../lib/boq-types'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add onArmTool prop and curry it into TotalsRow render</name>
  <files>src/renderer/src/components/TotalsCategoryBlock.tsx</files>
  <read_first>
    src/renderer/src/components/TotalsCategoryBlock.tsx — read fully; locate TotalsCategoryBlockProps interface, the props destructure line, and the TotalsRow render JSX block (especially the onClick curry pattern at line 142)
  </read_first>
  <action>
    Three targeted edits to TotalsCategoryBlock.tsx:

    1. TotalsCategoryBlockProps interface: add `onArmTool?: (item: BoqItemRow, categoryName: string) => void` after the `onTriggerPulse?` line.

    2. Props destructure: add `onArmTool` to the destructuring alongside onTriggerPulse.

    3. TotalsRow render: add the onArmTool prop using the same curry pattern as onClick:
       `onArmTool={onArmTool ? (it) => onArmTool(it, category.name) : undefined}`
       Place this prop on the TotalsRow JSX element alongside onTriggerPulse.

    Do not modify any existing prop or behavior. The curry pattern exactly matches the onClick pattern at line 142 — do not deviate from it.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - TotalsCategoryBlockProps contains `onArmTool?: (item: BoqItemRow, categoryName: string) => void`
    - onArmTool is destructured from props
    - TotalsRow render includes `onArmTool={onArmTool ? (it) => onArmTool(it, category.name) : undefined}`
    - TypeScript compiles without errors
    - No existing props or behavior changed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TotalsCategoryBlock → TotalsRow | Curries category.name (from store) into onArmTool callback; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.1-03-01 | Tampering | category.name in curry | accept | category.name comes from the in-memory BOQ aggregation; not user-typed at this layer |
</threat_model>

<verification>
1. TypeScript compilation: `npx tsc --noEmit` exits 0
2. Prop in interface: `grep -n "onArmTool" src/renderer/src/components/TotalsCategoryBlock.tsx` — appears in interface, destructure, and TotalsRow JSX
3. Curry pattern: grep output includes `(it) => onArmTool(it, category.name)`
</verification>

<success_criteria>
TotalsCategoryBlock.tsx accepts onArmTool and passes it to TotalsRow with category.name curried in. TypeScript clean. No existing behavior changed.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-resume-markup-group-from-totals/07.1-03-SUMMARY.md`
</output>
