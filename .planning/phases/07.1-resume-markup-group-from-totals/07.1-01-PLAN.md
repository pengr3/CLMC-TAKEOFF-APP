---
phase: 07.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/hooks/useMarkupTool.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "useMarkupTool exposes activatePreset() which skips the naming popup and lands count in mode:'placing' with chainArmed:true"
    - "activatePreset() lands linear/area/perimeter/wall in mode:'drawing' with chainArmed:true and pre-filled pendingName/pendingCategoryName/pendingColor"
    - "activatePreset() for wall sets pendingWallHeight from the preset.wallHeight argument (fallback 2400)"
    - "activatePreset() sets toolType in state so the CanvasViewport sync effect sees toolType===activeTool and skips activateMarkup() override"
  artifacts:
    - path: "src/renderer/src/hooks/useMarkupTool.ts"
      provides: "activatePreset useCallback + UseMarkupToolReturn export"
      contains: "activatePreset"
  key_links:
    - from: "CanvasViewport.tsx _activatePresetRef"
      to: "useMarkupTool.activatePreset"
      via: "useEffect populator in CanvasViewport (Plan 04)"
      pattern: "_activatePresetRef = activatePreset"
---

<objective>
Extend the useMarkupTool state machine with a new `activatePreset()` method that accepts a tool type and a pre-filled preset payload, bypassing the naming popup and landing directly in the correct placement/drawing mode with chainArmed:true.

Purpose: The arm-from-totals flow must skip the naming popup entirely — the user already chose an existing named item. activatePreset() is the single new state-machine entry point that enables this.

Output: useMarkupTool.ts with activatePreset useCallback defined and exported in UseMarkupToolReturn.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\PROJECT.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\STATE.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\phases\07.1-resume-markup-group-from-totals\07.1-RESEARCH.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\phases\07.1-resume-markup-group-from-totals\07.1-PATTERNS.md

<interfaces>
<!-- From useMarkupTool.ts — current structure the executor must extend -->

MarkupDrawState interface (lines 16–33):
  mode: 'idle' | 'naming' | 'drawing' | 'confirming' | 'placing'
  toolType: 'count' | 'linear' | 'area' | 'perimeter' | 'wall' | null
  pendingName: string
  pendingCategoryName: string
  pendingColor: string
  chainArmed: boolean   (default false in INITIAL_STATE)
  pendingWallHeight: number  (default 2400 in INITIAL_STATE)

UseMarkupToolReturn interface (lines 67–85):
  state: MarkupDrawState
  activate: (tool: ...) => void
  cancel: () => void
  repushLastPoint: () => void
  // + other existing callbacks

UNCATEGORIZED constant: already imported at line 10

activate() analog (lines 100–113):
  if (tool === 'count') setState({ ...INITIAL_STATE, mode: 'naming', toolType: 'count', popupScreenPos: { x:16, y:16 } })
  else setState({ ...INITIAL_STATE, mode: 'drawing', toolType: tool })

Return object (line ~411): { state, activate, cancel, ..., repushLastPoint }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add activatePreset useCallback and export in UseMarkupToolReturn</name>
  <files>src/renderer/src/hooks/useMarkupTool.ts</files>
  <read_first>
    src/renderer/src/hooks/useMarkupTool.ts — read fully before editing; locate: INITIAL_STATE (line ~40–50), activate() useCallback (lines 100–113), UseMarkupToolReturn interface (lines 67–85), return object (line ~411)
    src/renderer/src/lib/markup-palette.ts — confirm MARKUP_PALETTE export name (already imported in the hook or needs importing)
  </read_first>
  <behavior>
    - activatePreset('count', { name:'X', categoryName:'Y', color:'#fff' }) → state becomes { ...INITIAL_STATE, mode:'placing', toolType:'count', pendingName:'X', pendingCategoryName:'Y', pendingColor:'#fff', chainArmed:true }
    - activatePreset('linear', { name:'X', categoryName:'Y', color:'#fff' }) → state becomes { ...INITIAL_STATE, mode:'drawing', toolType:'linear', pendingName:'X', pendingCategoryName:'Y', pendingColor:'#fff', chainArmed:true }
    - activatePreset('wall', { name:'X', categoryName:'Y', color:'#fff', wallHeight:3000 }) → state includes pendingWallHeight:3000
    - activatePreset('wall', { name:'X', categoryName:'Y', color:'#fff' }) → state includes pendingWallHeight:2400 (default)
    - activatePreset with empty categoryName → uses UNCATEGORIZED constant
    - activatePreset('area', ...) and activatePreset('perimeter', ...) → mode:'drawing', toolType matches
  </behavior>
  <action>
    In useMarkupTool.ts:

    1. Add activatePreset as a new useCallback after activate() (around line 115). Copy the activate() structure exactly. Signature: (tool: 'count' | 'linear' | 'area' | 'perimeter' | 'wall', preset: { name: string; categoryName: string; color: string; wallHeight?: number }) => void. For count: setState({ ...INITIAL_STATE, mode: 'placing', toolType: 'count', pendingName: preset.name, pendingCategoryName: preset.categoryName || UNCATEGORIZED, pendingColor: preset.color, chainArmed: true }). For all others: setState({ ...INITIAL_STATE, mode: 'drawing', toolType: tool, pendingName: preset.name, pendingCategoryName: preset.categoryName || UNCATEGORIZED, pendingColor: preset.color, chainArmed: true, ...(tool === 'wall' ? { pendingWallHeight: preset.wallHeight ?? 2400 } : {}) }). Deps array: [].

    2. Extend UseMarkupToolReturn interface: add activatePreset: (tool: 'count' | 'linear' | 'area' | 'perimeter' | 'wall', preset: { name: string; categoryName: string; color: string; wallHeight?: number }) => void after the repushLastPoint line.

    3. Add activatePreset to the return object alongside repushLastPoint.

    No changes to activate(), cancel(), or any other existing method. No new imports needed (UNCATEGORIZED and INITIAL_STATE already in scope).
  </action>
  <verify>
    <automated>npx vitest run src/tests/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - UseMarkupToolReturn interface contains activatePreset with exact signature above
    - activatePreset useCallback is defined in the hook body with [] deps
    - activatePreset appears in the return object
    - All existing tests continue to pass (no regressions)
    - For count tool: state.mode === 'placing' and state.chainArmed === true after call
    - For non-count tools: state.mode === 'drawing' and state.toolType matches tool arg
    - For wall: state.pendingWallHeight === preset.wallHeight ?? 2400
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TotalsRow → useMarkupTool state | Row click data (name, category, color) flows into hook state; all values come from the in-memory markup store (no external input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.1-01-01 | Tampering | activatePreset preset.categoryName | accept | Values sourced from existing markup store categories; no user-typed string crosses this boundary in this plan |
| T-7.1-01-02 | Denial of Service | activatePreset called with empty name | accept | TotalsRow only fires onArmTool for items that exist in the store and have non-empty names; empty name cannot reach this path in normal flow |
</threat_model>

<verification>
1. TypeScript compilation passes: `npx tsc --noEmit` exits 0
2. activatePreset is exported in UseMarkupToolReturn (grep: `activatePreset` appears in interface block)
3. Existing vitest tests pass: `npx vitest run src/tests/`
</verification>

<success_criteria>
useMarkupTool.ts compiles cleanly and UseMarkupToolReturn.activatePreset is callable with (tool, preset) signature. All existing tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-resume-markup-group-from-totals/07.1-01-SUMMARY.md`
</output>
