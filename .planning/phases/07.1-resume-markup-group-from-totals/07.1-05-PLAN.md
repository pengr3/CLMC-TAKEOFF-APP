---
phase: 07.1
plan: 05
type: execute
wave: 2
depends_on:
  - 07.1-02
  - 07.1-03
files_modified:
  - src/renderer/src/components/TotalsPanel.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "TotalsPanel accepts onArmTool?: (item: BoqItemRow, categoryName: string) => void"
    - "TotalsPanel passes onArmTool through to each TotalsCategoryBlock render without transformation"
    - "No other panel behavior is changed"
  artifacts:
    - path: "src/renderer/src/components/TotalsPanel.tsx"
      provides: "onArmTool pass-through prop"
      contains: "onArmTool"
  key_links:
    - from: "TotalsPanel.onArmTool"
      to: "TotalsCategoryBlock.onArmTool"
      via: "direct prop pass-through (no curry at this level)"
      pattern: "onArmTool=\\{onArmTool\\}"
---

<objective>
Add onArmTool as a pass-through prop on TotalsPanel, following the identical pattern used by onTriggerPulse (accepted in TotalsPanelProps, destructured, forwarded to TotalsCategoryBlock unchanged).

Purpose: TotalsPanel is a pure relay at this level — it does not transform the callback because TotalsCategoryBlock handles the category.name curry.

Output: TotalsPanel.tsx with new optional prop and pass-through to TotalsCategoryBlock.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\PROJECT.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\ROADMAP.md
@C:\Users\Admin\Roaming\CLMC TAKEOFF APP\.planning\phases\07.1-resume-markup-group-from-totals\07.1-PATTERNS.md

<interfaces>
<!-- From TotalsPanel.tsx — current structure the executor must extend -->

TotalsPanelProps interface (lines 47–64):
  open: boolean
  width: number
  onSetOpen: (open: boolean) => void
  onSetWidth?: (width: number) => void
  onRowHover?: (matches: Markup[]) => void
  onRowClick?: (item: BoqItemRow, categoryName: string) => void
  onRowContextMenu?: (item: BoqItemRow, x: number, y: number) => void
  cycleIndexByKey?: Record<string, number>
  onTriggerPulse?: (matches: Markup[], color: string) => void
  onCopy?: (msg: string) => void
  onCopyError?: () => void

Props destructure (line ~79):
  const { open, width, onSetOpen, onRowHover, onRowClick, onRowContextMenu, cycleIndexByKey, onTriggerPulse, onCopy, onCopyError } = props

TotalsCategoryBlock render (line ~255) — existing pass-through pattern for onTriggerPulse:
  <TotalsCategoryBlock
    key={cat.name}
    category={cat}
    pageMarkups={pageMarkups}
    cycleIndexByKey={cycleIndexByKey}
    onRowHover={handleRowHover}
    onRowClick={handleRowClick}
    onRowContextMenu={handleRowContextMenu}
    onTriggerPulse={onTriggerPulse}
  />

BoqItemRow import: already in scope from '../lib/boq-types'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add onArmTool prop and pass through to TotalsCategoryBlock</name>
  <files>src/renderer/src/components/TotalsPanel.tsx</files>
  <read_first>
    src/renderer/src/components/TotalsPanel.tsx — read fully; locate TotalsPanelProps interface, the props destructure line, and the TotalsCategoryBlock render in the JSX to find the exact insertion points
  </read_first>
  <action>
    Three targeted edits to TotalsPanel.tsx:

    1. TotalsPanelProps interface: add `onArmTool?: (item: BoqItemRow, categoryName: string) => void` after `onCopyError?`.

    2. Props destructure: add `onArmTool` alongside the other destructured props.

    3. TotalsCategoryBlock render: add `onArmTool={onArmTool}` as a prop — direct pass-through, no transformation. Position it after `onTriggerPulse={onTriggerPulse}`.

    Do not modify any other props, handlers, or JSX. This is a pure pass-through at this level.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - TotalsPanelProps contains `onArmTool?: (item: BoqItemRow, categoryName: string) => void`
    - onArmTool is destructured from props
    - TotalsCategoryBlock render includes `onArmTool={onArmTool}`
    - TypeScript compiles without errors
    - No existing props or handlers changed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TotalsPanel → TotalsCategoryBlock | Pure prop relay; no data transformation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.1-05-01 | Tampering | onArmTool relay | accept | TotalsPanel does not transform the callback; trust boundary is at App.tsx handler entry |
</threat_model>

<verification>
1. TypeScript compilation: `npx tsc --noEmit` exits 0
2. Prop in interface: `grep -n "onArmTool" src/renderer/src/components/TotalsPanel.tsx` — appears in TotalsPanelProps, destructure, and TotalsCategoryBlock JSX
</verification>

<success_criteria>
TotalsPanel.tsx accepts onArmTool and passes it to TotalsCategoryBlock unchanged. TypeScript clean. No existing behavior changed.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-resume-markup-group-from-totals/07.1-05-SUMMARY.md`
</output>
