---
phase: 04-project-persistence
plan: 06
type: execute
wave: 5
depends_on:
  - "04-00"
  - "04-01"
  - "04-02"
  - "04-03"
  - "04-04"
  - "04-05"
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - PERS-01
  - PERS-02

must_haves:
  truths:
    - "All five PERS manual-only verifications from 04-VALIDATION.md pass: missing-PDF modal, page-count abort, dimension-mismatch warn, hash-mismatch warn, close-window-while-dirty modal"
    - "PERS-01 and PERS-02 success criteria (both ROADMAP Phase 4 bullets) are observably satisfied end-to-end"
    - "REQUIREMENTS.md PERS-01 and PERS-02 flipped to [x] on user approval"
    - "ROADMAP.md Phase 4 entry flips to [x] (completed {today}) and the 7 plan entries flip to [x] on user approval"
    - "STATE.md is NOT edited by this plan — the /gsd:transition command owns STATE.md after phase completion"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "PERS-01 and PERS-02 flipped to [x] + Traceability table entries updated to Complete"
      contains: "[x] **PERS-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 checkbox [x] with completion date + all 7 plan checkboxes [x]"
      contains: "[x] **Phase 4:"
  key_links:
    - from: ".planning/REQUIREMENTS.md PERS-01/PERS-02"
      to: ".planning/phases/04-project-persistence/"
      via: "checkbox status + Traceability 'Complete' notation"
      pattern: "\\[x\\] \\*\\*PERS-0[12]"
---

<objective>
Final human-verification checkpoint for Phase 4. Walk the user through a manual protocol that exercises every PERS-01 / PERS-02 observable truth AND every D-12/D-16/D-19/D-23/D-26/D-27 recovery path. On approval, annotate REQUIREMENTS.md and ROADMAP.md to mark the phase complete.

Purpose: Vitest scaffolds cover pure logic + hook contracts + store flow. They cannot cover: Electron close-window signal, OS file-picker interaction, visual modal rendering, cross-session save/reload, cross-drive path portability. Those require a real user at a real keyboard.

Output: Verified observable truths for all PERS manual-only items + docs annotations committed (on approval) or gap list captured (on failure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-project-persistence/04-CONTEXT.md
@.planning/phases/04-project-persistence/04-VALIDATION.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-verification — typecheck, vitest, dev server boot</name>
  <read_first>
    - package.json (scripts: typecheck, dev, build)
    - .planning/phases/04-project-persistence/04-VALIDATION.md (Manual-Only Verifications — 5 items)
    - .planning/phases/04-project-persistence/04-CONTEXT.md (D-12, D-16, D-19, D-22, D-23, D-26, D-27 — authoritative decision list for the checker)
  </read_first>
  <action>
Before the user starts verification, confirm the build is healthy:

1. Run `npm run typecheck`. MUST exit 0. If it fails, STOP and surface errors verbatim — do not proceed to checkpoint.
2. Run `npx vitest run`. MUST exit 0. If it fails, STOP and surface failures verbatim.
3. Start the dev server in the background: `npm run dev` (run_in_background=true). Wait for the "ready" log line or 15s.
4. Confirm the Electron window launches. If it fails to appear within 30s of `npm run dev`, STOP and report — do not hand off to the user.
5. If all green: announce "Build is healthy; ready for Phase 4 human verification."
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0
    - `npx vitest run` exits 0 (all scaffolds now GREEN — Wave 0 through Wave 5 have flipped every test)
    - Dev server starts without error in background
    - Announcement text surfaced: "Build is healthy; ready for Phase 4 human verification." (or explicit failure report)
  </acceptance_criteria>
  <done>Dev environment running + automated tests green; user can proceed to the checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Phase 4 end-to-end human verification protocol</name>
  <what-built>
Project persistence end-to-end: `.clmc` save/load with full state round-trip (markups, scale, viewport, categories), per-session dirty-state tracking (title bar asterisk D-15), Ctrl+S / Ctrl+Shift+S / Ctrl+O shortcuts with text-input guards (D-13, D-20), extension-sniffing Open button (D-19), close-window guard with Save/Discard/Cancel modal (D-16), open-another-project guard using the same modal (D-21), and all recovery modals: Missing PDF with Browse/Cancel (D-23), PDF hash-mismatch warn with Open Anyway/Cancel (D-12, D-28), PDF dimension-mismatch warn (D-27), page-count hard-abort with no Open Anyway (D-26), plus an in-memory pdf-path update on re-link that requires Ctrl+S to persist (D-25).
  </what-built>
  <how-to-verify>
Run through the protocol below. Mark each step PASS/FAIL. If everything passes, reply "approved". If any step fails, list the step numbers with what you saw — Claude will scope gap-closure tasks.

**Preparation before you start:**
- Have two PDFs ready. One 3-page (call it `planA-3p.pdf`), one 5-page (`planB-5p.pdf`). Both in the same folder.
- Have a second small 3-page PDF that is NOT the same bytes as planA-3p.pdf (to test hash-mismatch). Call it `planA-3p-modified.pdf` — same page count as A, different bytes.
- (Optional for cross-drive test) If you have a second drive, note a `.clmc` save path on it.

### Section A — Fresh project, save for the first time (PERS-01)

1. App window opens. Title bar shows `CLMC Takeoff` (no filename).
2. Click the blue **Open** button. Windows file picker shows filters for "Supported (PDF, CLMC Project)" / "PDF Files" / "CLMC Project".
3. Pick `planA-3p.pdf`. The PDF loads on page 1. Title bar now reads `planA-3p.pdf — CLMC Takeoff` (no asterisk — clean state).
4. Set Scale on page 1 (draw a calibration line, enter a real-world distance, Confirm). Title bar now shows `planA-3p.pdf * — CLMC Takeoff` (asterisk present — **D-15 PASS**).
5. Place 3 Count pins with name "Outlet". Title still dirty.
6. Press **Ctrl+S**. Because this project was never saved, the Save As dialog appears (**D-13 PASS**). The default filename is `planA-3p.clmc` in `planA-3p.pdf`'s folder (**D-14 PASS**).
7. Accept the default path. Save completes. A toast appears reading "Saved" and disappears after ~2s (**D-18 PASS**). Title bar now reads `planA-3p.clmc — CLMC Takeoff` (**no asterisk — D-15 clean** — PASS).
8. Verify on disk: `planA-3p.clmc` exists next to `planA-3p.pdf`. Open the `.clmc` in a text editor — confirm it's JSON, starts with `{` and contains `"formatVersion": 1` and `"pdf"` and `"pages"` and `"markups"` (**D-01/D-02 PASS**).
9. In the JSON, confirm `"undoStack"` and `"redoStack"` do NOT appear anywhere (**D-09 PASS**). Confirm `"activeTool"` does NOT appear.

### Section B — Save As a second time, overwrite

10. Place 2 more Outlet pins. Title shows asterisk.
11. Press **Ctrl+S** (not Shift). NO dialog appears — file silently overwrites (existing path is known; **D-13 fast-path PASS**). Toast reads "Saved". Title clean again.
12. Press **Ctrl+Shift+S**. Save As dialog appears. Cancel it. Title still clean (no regression from cancel).
13. Press **Ctrl+Shift+S** again, pick a new filename `planA-3p-v2.clmc`. Save succeeds. Title now reads `planA-3p-v2.clmc — CLMC Takeoff`.

### Section C — Reopen and continue (PERS-02)

14. Click the app's close button (X). With no unsaved changes, the app closes immediately (no modal — because isDirty=false).
15. Relaunch the app. Title is back to `CLMC Takeoff`, empty state visible.
16. Click **Open**. Select `planA-3p-v2.clmc`. App loads the .clmc: PDF renders (page 1), scale on page 1 is restored, 5 Outlet pins are on page 1 in their original positions, the title reads `planA-3p-v2.clmc — CLMC Takeoff` (clean — **D-15 after load PASS**).
17. Right-click an Outlet pin and recolor it. Title flips to `planA-3p-v2.clmc * — CLMC Takeoff`.
18. Press Ctrl+Z. The recolor reverts. Title remains dirty (the undo itself is a further state change).
19. Press Ctrl+S. Save succeeds. Title clean.

### Section D — Close-window-while-dirty guard (D-16)

20. Make a change (place one more pin). Title dirty.
21. Click the window X. **SaveCloseModal appears** with copy "Save changes to planA-3p-v2.clmc?" and three buttons [Save] [Discard] [Cancel] (**D-16 PASS**).
22. Click Cancel. Modal closes, app stays open, title still dirty.
23. Click X again. Modal reappears. Click Discard. App closes immediately (changes lost — expected).
24. Relaunch, open `planA-3p-v2.clmc`. The last pin from step 20 is NOT present (discard worked). Correct.
25. Make a change. Click X. In the modal, click Save. Save completes. App closes. (A canceled Save-As inside the save flow keeps the modal open for retry — **Pitfall 5 PASS**.)

### Section E — Open-another-while-dirty (D-21)

26. Relaunch, open `planA-3p-v2.clmc`. Place one more pin (title dirty).
27. Click **Open**. Pick `planA-3p.pdf` (a completely different file — fresh PDF path). **SaveCloseModal appears FIRST** (same three-button modal, referencing the dirty file — **D-21 PASS**).
28. Click Cancel. Modal closes, app stays on `planA-3p-v2.clmc` with the pin still there, title still dirty.
29. Click Open → pick `planA-3p.pdf` again → Save in the modal. File saves, then `planA-3p.pdf` loads as a FRESH project. Title shows `planA-3p.pdf — CLMC Takeoff` (no asterisk, no markups — fresh).

### Section F — Missing PDF recovery (D-23, D-25)

30. Open `planA-3p-v2.clmc` to restore state. Close the app cleanly.
31. Rename `planA-3p.pdf` in the folder to `MOVED-planA-3p.pdf`.
32. Relaunch. Click Open, pick `planA-3p-v2.clmc`. **MissingPdfModal appears** with message showing filename `planA-3p.pdf` and the original path. Two buttons: [Cancel] and [Browse for PDF] (**D-23 PASS**).
33. Confirm NO "Open without PDF" option is visible anywhere on the modal (**D-24 PASS**).
34. Click Cancel. App returns to empty state (no project loaded). Title back to `CLMC Takeoff`.
35. Click Open, pick the .clmc again. Modal reappears. Click Browse for PDF. A PDF-only file picker appears. Pick `MOVED-planA-3p.pdf` (the renamed original). Project loads. All markups appear on the correct pages. **Title shows `planA-3p-v2.clmc * — CLMC Takeoff`** (dirty, because the PDF path was updated in memory — **D-25 PASS**).
36. Press Ctrl+S. Confirm the file saves. Close and reopen. Confirm no missing-PDF prompt (the new path persisted).

### Section G — Page-count hard abort (D-26)

37. Rename `MOVED-planA-3p.pdf` back to `planA-3p.pdf`. Delete it temporarily (or move it to another folder the picker can't reach easily).
38. Open `planA-3p-v2.clmc`. MissingPdfModal appears. Click Browse. Pick `planB-5p.pdf` (WRONG file — different page count).
39. **PageCountAbortModal appears** with message "Selected PDF has 5 pages, but project expects 3. This is probably a different file." Two buttons: [Cancel] and [Pick again] (**D-26 PASS**).
40. Confirm NO [Open anyway] button exists on this modal (**D-26 PASS — no escape hatch**).
41. Click Pick again. PDF-only picker reopens. Cancel it. App returns to pre-open state. Relaunch if needed — move `planA-3p.pdf` back to its folder. Open `planA-3p-v2.clmc`. MissingPdfModal → Browse → pick `planA-3p.pdf`. Project loads cleanly.

### Section H — Hash-mismatch warn (D-12, D-28)

42. Close the app. Make `planA-3p-modified.pdf` the file at the expected path: rename `planA-3p.pdf` to `planA-3p-original.pdf`, then rename `planA-3p-modified.pdf` to `planA-3p.pdf`. Now opening the .clmc will find a PDF at the expected path but with different bytes.
43. Relaunch. Open `planA-3p-v2.clmc`. **HashMismatchModal appears** with copy about "PDF has changed since your last save — markup positions may no longer align" and two buttons: [Cancel] and [Open anyway] (**D-12 PASS**).
44. Click Cancel. App returns to pre-open state.
45. Open the .clmc again. Modal reappears. Click Open anyway. Project loads. Markups render (possibly misaligned — that's the user's risk). Title reads `planA-3p-v2.clmc — CLMC Takeoff` (clean — Open Anyway does NOT dirty the project; the .clmc already has the old hash which is still on disk).

### Section I — Dimension-mismatch warn (D-27)

46. (Optional — requires two same-page-count PDFs with different dimensions, e.g. A4 vs A3 3-pagers. If unavailable, mark as N/A and note.)
47. Save a project for an A4 3-page PDF. Close. Rename the A4 file away. Open the .clmc → Missing-PDF → Browse → pick the A3 3-page PDF. **DimensionMismatchModal appears** with [Cancel] and [Open anyway]. Click Open anyway. Project loads; markup positions may look offset (markups are in normalized 0-1 space so they scale, but the plan proportions differ). Title dirty (because PDF-reference changed via Browse — D-25).

### Section J — Sanity: text-input shortcut guard

48. Click Count tool. In the name popup, type "Save Me" (literally including the word "Save"). Space must work. Press Ctrl+S INSIDE the input field — the shortcut must NOT fire (no save dialog, no toast). The input field may or may not react natively, that's fine — we only care that our global handler did not consume it. Press Enter in the popup to submit. Start Count. Place one pin. Title is now dirty. Outside any input, Ctrl+S saves normally.

### Section K — Out-of-scope confirmations (should NOT exist)

49. Confirm there is NO "Recent files" menu anywhere (D-22 — deferred).
50. Confirm there is NO welcome/empty-state screen showing recent files (deferred).
51. Confirm there is NO auto-save indicator or background save activity (D-17).

### Result

- If ALL sections A–J pass (section K shows the expected absences): reply "approved" — Claude will flip PERS-01 and PERS-02 to [x] in REQUIREMENTS.md and mark Phase 4 complete in ROADMAP.md.
- If any FAIL: list the section + step numbers with a short description of what you saw. Claude will scope a targeted gap-closure plan.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 4 and mark PERS-01 + PERS-02 complete, or list failing step numbers with a short description.</resume-signal>
  <action>
Hand control to the user with the protocol above. Do NOT proceed until the user replies with "approved" or a concrete failure list. If Claude Code auto-executes this task, it MUST pause and wait for human input — do not fabricate approval.
  </action>
  <verify>
User reply contains the literal string "approved" (case-insensitive) OR a list of failing step numbers.
  </verify>
  <acceptance_criteria>
    - User personally walked the protocol and supplied a definitive yes/no response
    - If failure: step numbers + descriptions captured for the next planning cycle
    - No fabricated approval — if Claude did not receive an explicit user reply, this task stays incomplete
  </acceptance_criteria>
  <done>Approval received (close phase) or failure list captured (follow-up plan pending). Either outcome is a successful resolution of the checkpoint — the distinction drives the next task.</done>
</task>

<task type="auto">
  <name>Task 2: On approval, flip PERS-01 + PERS-02 to [x] in REQUIREMENTS.md and close Phase 4 in ROADMAP.md</name>
  <read_first>
    - .planning/REQUIREMENTS.md (find PERS-01 and PERS-02 entries + Traceability table rows)
    - .planning/ROADMAP.md (find Phase 4 entry + Progress table row)
    - .planning/STATE.md (REFERENCE ONLY — do NOT edit; /gsd:transition owns STATE.md)
  </read_first>
  <behavior>
    - Only run if the preceding checkpoint returned "approved". If gaps were reported, DO NOT run. Instead print:
      "Checkpoint gaps reported; skipping docs annotation until the next checkpoint passes."
    - REQUIREMENTS.md edits:
        - `- [ ] **PERS-01**: ...` → `- [x] **PERS-01**: ...`
        - `- [ ] **PERS-02**: ...` → `- [x] **PERS-02**: ...`
        - Traceability table: `| PERS-01 | Phase 4 | Pending |` → `| PERS-01 | Phase 4 | Complete |`
        - Traceability table: `| PERS-02 | Phase 4 | Pending |` → `| PERS-02 | Phase 4 | Complete |`
    - ROADMAP.md edits:
        - `- [ ] **Phase 4: Project Persistence** ...` → `- [x] **Phase 4: Project Persistence** ... (completed {TODAY})`
        - All 7 plan checkboxes in Phase 4 plan list flip from `[ ]` → `[x]`
        - Progress table row for Phase 4: update Plans Complete to `7/7`, Status to `Complete`, Completed to `{TODAY}`
  </behavior>
  <action>
Only proceed if the preceding checkpoint resolved with "approved". Otherwise print `Checkpoint not approved — docs will be updated after the next approval.` and exit cleanly.

Use today's date (you can compute it via `new Date().toISOString().slice(0, 10)` — format `YYYY-MM-DD`).

**1. Update `.planning/REQUIREMENTS.md`:**

   a. Find the line `- [ ] **PERS-01**: User can save the current project...` and change `[ ]` to `[x]`.

   b. Find the line `- [ ] **PERS-02**: User can reopen a .clmc project file...` and change `[ ]` to `[x]`.

   c. In the Traceability table, replace the two PERS rows:
      - Before: `| PERS-01 | Phase 4 | Pending |`
      - After: `| PERS-01 | Phase 4 | Complete |`
      - Before: `| PERS-02 | Phase 4 | Pending |`
      - After: `| PERS-02 | Phase 4 | Complete |`

**2. Update `.planning/ROADMAP.md`:**

   a. Find the Phase 4 checkbox line. Replace `- [ ] **Phase 4:` with `- [x] **Phase 4:`. Append ` (completed {TODAY})` at the end.

   b. In the Phase 4 "Plans:" list, flip all 7 entries from `[ ]` to `[x]`:
      - 04-00-PLAN.md
      - 04-01-PLAN.md
      - 04-02-PLAN.md
      - 04-03-PLAN.md
      - 04-04-PLAN.md
      - 04-05-PLAN.md
      - 04-06-PLAN.md

   c. Replace `**Plans**: TBD` under Phase 4 section with `**Plans:** 7 plans (completed {TODAY})` if that placeholder is still present after Plan 04-00 planning ran.

   d. In the Progress table, update the Phase 4 row:
      - Before: `| 4. Project Persistence | 0/0 | Not started | - |`
      - After:  `| 4. Project Persistence | 7/7 | Complete | {TODAY} |`

**3. Commit:**
```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(04): close Phase 4; mark PERS-01 and PERS-02 complete" --files .planning/REQUIREMENTS.md .planning/ROADMAP.md
```

If the gsd-tools commit fails, fall back to a plain `git commit` restricted to the two files listed above.
  </action>
  <verify>
    <automated>grep -q "\\[x\\] \\*\\*PERS-01" .planning/REQUIREMENTS.md</automated>
    <automated>grep -q "\\[x\\] \\*\\*PERS-02" .planning/REQUIREMENTS.md</automated>
    <automated>grep -q "| PERS-01 | Phase 4 | Complete |" .planning/REQUIREMENTS.md</automated>
    <automated>grep -q "| PERS-02 | Phase 4 | Complete |" .planning/REQUIREMENTS.md</automated>
    <automated>grep -q "\\[x\\] \\*\\*Phase 4:" .planning/ROADMAP.md</automated>
    <automated>grep -q "\\[x\\] 04-00-PLAN.md" .planning/ROADMAP.md</automated>
    <automated>grep -q "\\[x\\] 04-01-PLAN.md" .planning/ROADMAP.md</automated>
    <automated>grep -q "\\[x\\] 04-02-PLAN.md" .planning/ROADMAP.md</automated>
    <automated>grep -q "\\[x\\] 04-03-PLAN.md" .planning/ROADMAP.md</automated>
    <automated>grep -q "\\[x\\] 04-04-PLAN.md" .planning/ROADMAP.md</automated>
    <automated>grep -q "\\[x\\] 04-05-PLAN.md" .planning/ROADMAP.md</automated>
    <automated>grep -q "\\[x\\] 04-06-PLAN.md" .planning/ROADMAP.md</automated>
    <automated>grep -q "| 4. Project Persistence | 7/7 | Complete" .planning/ROADMAP.md</automated>
    <automated>git log -1 --pretty=%B | grep -q "docs(04)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` PERS-01 and PERS-02 both have `[x]` checkboxes
    - `.planning/REQUIREMENTS.md` Traceability table rows for PERS-01 and PERS-02 both read `Complete`
    - `.planning/ROADMAP.md` Phase 4 checkbox is `[x]` with `(completed YYYY-MM-DD)` suffix
    - `.planning/ROADMAP.md` all 7 plan checkboxes are `[x]`
    - `.planning/ROADMAP.md` Progress table row for Phase 4 reads `7/7 Complete YYYY-MM-DD`
    - A git commit exists with message starting `docs(04)` touching only the two docs files
    - STATE.md is NOT modified in this task
  </acceptance_criteria>
  <done>REQUIREMENTS.md and ROADMAP.md reflect Phase 4 closure; commit recorded; /gsd:transition can now advance the project state.</done>
</task>

</tasks>

<verification>
- Checkpoint resolved with "approved" OR with a concrete gap list
- If approved: REQUIREMENTS.md + ROADMAP.md updated and committed
- If gaps: no docs edits made; failing step list captured for next plan
</verification>

<success_criteria>
- User personally walked the 51-step protocol and either approved or listed gaps
- On approval, Phase 4 is marked complete in ROADMAP.md and PERS-01 + PERS-02 are both [x] in REQUIREMENTS.md
- Next work item (Phase 5 BOQ Export) is unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/04-project-persistence/04-06-SUMMARY.md` containing: (a) the full approved/failed status of all protocol sections (A–K), (b) the user's verbatim response, (c) a one-line summary "Phase 4 closed" or "Gaps captured for follow-up".
</output>
