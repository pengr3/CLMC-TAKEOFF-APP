import { describe, it } from 'vitest'
// Wave 0 RED stub
import { TotalsPanel } from '../renderer/src/components/TotalsPanel'

void TotalsPanel

describe('TotalsPanel empty states', () => {
  it.todo('shows "Open a PDF to begin." when totalPages === 0 (D-09)')
  it.todo('shows "Place markups to see totals." when totalPages > 0 and zero markups (D-09)')
  it.todo('shows "Place markups on a calibrated page to see length and area totals." when markups exist but only non-count on uncalibrated pages (D-09)')
})
