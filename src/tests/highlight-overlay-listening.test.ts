import { describe, it } from 'vitest'
// Wave 0 RED stub — Konva node tree inspection
import { HoverRing } from '../renderer/src/components/HoverRing'
import { PulseHighlight } from '../renderer/src/components/PulseHighlight'

void HoverRing
void PulseHighlight

describe('Overlay listening={false} regression guard', () => {
  it.todo('every Konva shape inside HoverRing has listening={false} (D-11)')
  it.todo('every Konva shape inside PulseHighlight has listening={false} (D-12)')
})
