import { describe, it } from 'vitest'
// Wave 0 RED stub — useBoqLive does not exist yet
import { useBoqLive } from '../renderer/src/hooks/useBoqLive'

void useBoqLive

describe('useBoqLive', () => {
  it.todo('returns BoqStructure matching aggregateBoq() snapshot (VIEW-01)')
  it.todo('recomputes when pageMarkups changes')
  it.todo('returns empty categories when no markups exist')
})
