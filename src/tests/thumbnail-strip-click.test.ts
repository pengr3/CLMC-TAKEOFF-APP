import { describe, it } from 'vitest'
// Wave 0 RED stub
import { ThumbnailStrip } from '../renderer/src/components/ThumbnailStrip'

void ThumbnailStrip

describe('ThumbnailStrip', () => {
  it.todo('renders one Thumbnail tile per page (PDF-05)')
  it.todo('clicking a tile calls viewerStore.setPage(N) (PDF-05)')
  it.todo('active page tile has 2px accent outline (D-16)')
  it.todo('idle page tile has 1px border outline (D-16)')
})
